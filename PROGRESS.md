# 進捗（PROGRESS） — ドミニオン Webアプリ

---

## 0-44. **古い拡張の敵対レビュー②＝基本・陰謀・海辺・錬金術・収穫祭・ギルド156枚で確定54件 → 全件対応**（2026-08-30〜31・`sw.js` v94→v96・**push済**）

§0-43（繁栄・異郷・暗黒時代125枚）と同じ軸＝**engine の実挙動が公式の裁定（Official FAQ・Other rules
clarifications・エラッタ・カード間の相互作用）と一致しているか**を、残る古い6拡張に当てた。
**候補59件 → 別の4体が敵対検証（公式を自分で引き直し、報告の再現コードを自分で走らせる）＝確定54・棄却5。**
§0-43 と違い、**基本セットの最も基本的なカードに穴があった**のが今回の特徴。

### 🛑 系統的な塊（1コミット目・v95）＝1箇所の直しが数十枚に効いたもの
| 塊 | 公式 | 本アプリ（修正前） |
|---|---|---|
| **捨て札トリガーの呼び忘れ 約25経路** | 坑道／進路／織工／村有緑地／忠犬 は `When you **discard** this other than during Clean-up` | **共通ヘルパ `discardFromHand` 自体が呼んでいなかった**（倉庫・潮溜り・村落・馬商人・魔女娘が一斉に空振り）＋個別に **地下貯蔵庫**（出荷5セットに入る最も基本的なカード）・家臣・書庫・衛兵・冒険者・道化師・収穫・見張り・貢物・風車・破壊工作員・秘密の部屋・郎党・ゴーレム・念視の泉・助言者・熟練工・医者 |
| **アタック免疫の席にリアクション窓が開かない 65箇所** | Lighthouse / Official FAQ＝免疫でもリアクションは使える | 灯台/チャンピオンで免疫の席を**被害者キューから丸ごと除外**していたため、**物乞い・馬商人・番犬・隊商の護衛・外交官が一度も使えなかった**。共通述語 **`attackTargets`** に寄せ、免疫の席には新窓 `immune_react` を（攻撃の後に）開く |
| 🛑 **コルセア(corsair) が `ATTACKS` 未登録** [high] | Action-**Attack**-Duration | **堀でも灯台でもチャンピオンでも防げなかった**（出荷 `seaside` で踏む） |
| **山賊／役人** | 獲得を完全に解決してからアタック | 金貨/銀貨の獲得で開いた**望楼の窓を直後のアタックが `state.pending` ごと上書き**して握りつぶしていた |
| **寵臣(minion)** | Official FAQ が Minion を名指し＝**二択を選ぶ前**に反応する | 選んだ後にしか窓が開かず、+2コインを選ぶと一度も開かなかった |
ほか 鉄工所×混合山の種別／書庫のオンライン情報漏洩／貧民街の `reveal()` 漏れ／停泊所（手札0枚で持続にしない）／
前哨地（追加ターン中に使うと次の手札が5枚）／引揚水夫（手札があれば廃棄は強制）。

### 🛑 設計判断＝**闇市場デッキに「準備つきカード」を入れない**（2コミット目・v96）
公式（Black Market / Other rules clarifications）は2つのことを言っている：
1. `Any setup instructions or "in games using this" rules that apply to cards in the Black Market deck are in
   effect, even if nobody ever gains (or even reveals) the relevant cards.`（例＝魔女娘が居れば災いカードを出す／
   山師が居れば呪いはゲーム中ずっと財宝）
2. `It is not necessary to include every unused Kingdom card; **you may decide how many cards to include in the
   Black Market deck, and which ones, in any way.**` ／ `**Dominion Online** creates a deck of up to 60 cards.
   **It does not include cards with an obvious setup that would give away what's in the Black Market deck,
   such as Joust or Baker.**`

本アプリのデッキは「サプライに無い王国カードを1枚ずつ全部」（489枚）なので、①をそのまま適用すると
**闇市場のある対局が毎回「災いカード＋廃墟＋馬＋祝福＋呪詛＋予言＋同盟＋戦利品＋賞品…全機構入り」**になる。
→ ②が明示的に許す構成の自由を使い、**公式のデジタル実装と同じく「準備を持つ札をデッキに入れない」**ことで
①を構造的に満たした（デッキに入らないので適用すべき準備が存在しない）。
- **`BM_SETUP_EXCLUDE`（74枚）** を新設＝パン屋/一騎討ち/交易路/魔女娘/渡し守/山師/野盗/シャーマン/馬上槍試合/
  隠遁者/浮浪児/ネクロマンサー/川船/略奪者3種/略奪品2種/ページ/農民/アーティファクト4種/馬8種/幸運8種/不運6種/
  家宝4種/インプ・精霊2種/連携10種/戦利品6種/前兆6種。デッキは 489→**411枚**。
- あわせて**分割山の下段**も除外（公式＝`For piles with differently named cards in them (e.g. Knights or split
  piles), **only one card from each pile** can be added`）。
- 🛑 **恒久検査を `test/integrity.test.js` に新設**＝**準備つきカードを機械的に導出**（王国に1枚入れて
  `createInitialState` の痕跡＝追加の山/災いカード/渡し守の山/祝福/呪詛/予言/同盟/アーティファクト/常設ルール/
  交易路/川船/廃棄置き場/廃墟/戦利品/財源/好意/開始デッキ を見る）し、その全部が除外集合に入っていることを検査する。
  **新しい準備つきカードを足して除外し忘れたら必ず落ちる**（バグ注入 2/2 検出）。
  ポーション／白金貨・植民地は「サプライの内容で決まるランダマイザ規則」＝闇市場デッキとは無関係なので除外しない。
- これで **渡し守・野盗・山師・魔女娘**（reviewer が確定した4件）が**到達不能**になった。
  engine 側の「闇市場にパン屋/一騎討ち/交易路が居たら準備する」ブロックは**安全網として残してある**。

### [medium] 個別
- 🛑 **支配(possession)＝2023年12月エラッタ「2回連続の追加ターンにはならない」が未実装**。カード文にも engine にも
  無く、1ターンに支配を2枚使うと被支配者が**3連続の手番**を取っていた（公式FAQ＝`all Possessions after the first
  will fail.`）。→ 消化時に判定し、失敗した予約は**その場で捨てる**（後の手番まで残すと本来存在しない支配ターンが湧く）。
  カード文も現行へ（`Any cards **or [D]** they would gain` の負債も欠落していた）＝**webp 焼き直し**。
- 🛑 **追加ターンが同点時のタイブレークに数えられていた**。公式は Outpost と Possession の**両ページが明記**＝
  `Extra turns do not count towards the tiebreaker of which tied player had fewer turns.` ／
  `Possession turns (and other extra turns) do not count for the tiebreaker.`
  以前は `seizeTurn`（今を生きる／島民／航海／旅行）だけを除いており、**前哨地・使節団・支配・艦隊**が数えられて
  **支配された側が一方的に不利**になっていた。→ `state.turn.isExtraTurn` の1箇所に一般化。
- **廷臣(courtier)** が公開したカードの「種類の数」を静的な `types` で数えていた。公式＝`**Capitalism, Inheritance,
  Charlatan, and Enlightenment** can change the number of types a card has, and Courtier takes that into account.`
  → `typeCountFor`（同じ engine の置き去り(Maroon) は既に正しく数えていた＝内部で不整合だった）。
- **サル(monkey)** が boolean で管理されており、**2枚使っても獲得1回につき1枚しか引けなかった**（ターン開始時の
  +1カードは正しく2回発火していた＝壊れていたのは獲得窓だけ）。→ 枚数カウンタ（**旧スナップショット互換**あり）。
- **鉱山の村(mining_village)**＝場に**別の鉱山の村**があると、玉座の間の2回目も「これを廃棄」でき **+$4 ＋2枚廃棄**
  になった。公式の「これ」は**その1枚**。→ 1プレイ単位の印 `t.mvTrashed`（再演の2回目だけ止める）。
- **船乗り(sailor)** の窓が `!state.pending` で捨てられ、**工房/封鎖/密輸人など「対話中の獲得」では一度も開かなかった**
  （公式FAQ が Workshop と Blockade を名指し）。→ `onGainQueue` に積んで解決時に再検査（権利が残っているか／
  その札がまだ獲得先のゾーンに居るか）。**権利は実際に使ったときだけ消費**する（辞退では減らさない）。
- **策士(tactician)** の「手札を全て捨てる」が**任意**になっていた（公式のカード文にも FAQ にも "you may" が無い）。
  金貨を抱えた手札のまま策士を使えていた＝**公式では絶対にできない選択肢**。→ 使用時にその場で強制解決。
- 🛑 **身代わり(replace)** はアタックカードなのに、**獲得したのが勝利点のときしかリアクション窓が開かなかった**。
  公式（Moat/Diplomat 逐語）＝`depends on **another player playing an Attack card**, not on whether the Attack card
  itself has any effect on you`。→ **使用した時点**で `attackWindowEnter(..., 'replace_trash')`。
  堀/盾を公開した席は `t.replaceImmune` に記録し、後の呪いから免除する（`attack_window` に `immune` を持たせた）。
- **拷問人(torturer)・民兵(militia) のモーダルに番犬(guard_dog)のボタンが無かった**＝engine は受理し CPU も必ず使うのに
  **人間だけが +2〜4カードを取り逃していた**（§P1b で盾を直したのと同じクラスで番犬が取り残されていた）。

### [low] / [nit]
- **風車(mill)**＝手札1枚のとき窓自体が開かなかった（公式FAQ＝`You can choose to discard 2 cards even if you only
  have one card in hand, but you only get +$2 if you actually discarded 2 cards.`）。
- **外交官(diplomat)**＝1回のアタックにつき1回しか公開できなかった（公式＝手札5枚以上なら何度でも。
  1回で手札は差し引き1枚減るので終端する）。
- **海賊(pirate)**＝手札に2枚あっても1回の財宝獲得につき1枚しか使えなかった（公式＝同じ事象に持っている枚数ぶん）。
- **封鎖(blockade)**＝獲得した札が脇に残らなくても（ヴィラ/ゴーストタウン/遊牧民の野営地/望楼）呪いを配り続け、
  封鎖自身も持続として場に残った。公式＝`If the gained card somehow doesn't end up set aside ... nothing further
  happens` `you'll discard the Blockade from play during Clean-up.`
- **灯台(lighthouse)**＝**自分の次のターン中も免疫**になっていた。公式＝`**Lighthouse offers no protection during
  your next turn.** ... you can still get attacked by another player's **Black Cat**.` → 守護者と同型の
  `p.lighthouseActive`（**旧スナップショット互換**のフォールバックあり）。回帰テストは公式が名指しする黒猫で書いた。
- **薬草商(herbalist)** の自動選択が `potion/賢者の石/金貨/銀貨` の4種決め打ちで、**白金貨・銀行・隠し財産などを
  1枚も置けなかった**（mix で薬草商が何もしない札になる）。→ 場の財宝全般（銅貨と価値0は置かない＝許容簡略化）。
- **錬金術師(alchemist)**＝習性(Way)や女魔術師で「記載効果を使わずに」プレイしても山札の上に戻っていた
  （公式 Unofficial FAQ 2022＝`If you play Alchemist but don't activate its abilities ... you don't get to top-deck`）。
  → 隣の薬草商と同じく**このターンの使用回数** `t.alchemists` で持つ。
- **カード名を宣言するモーダル**（願いの井戸／秘術師／建て直し／医者／熟練工）が `SUPPLY_ORDER`（基本＋王国10種）
  しか候補に出さず、**ポーション・トラベラー成長先・略奪品/狂人/傭兵・賞品・馬 を宣言できなかった**
  （engine はどのカード名でも受理する）＝§0-24 で `modalGainSupply` に入れたのと同じ対処。
- **名馬(trusty_steed)** が長老(Elder)の対象になっていなかった（公式 Elder の対象一覧＝`Cornucopia & Guilds:
  **Courser, Trusty Steed**`。§0-40 で駿馬だけ対応して取り残されていた）。engine/CPU/UI の3面を同時に。
- **石工(stonemason) の過払いをポーションで行えなかった**（公式FAQ＝`Overpaying with a [P] will let you gain cards
  with [P] in the cost.`＝ポーションだけが例外）。→ 過払いを2段（コイン→ポーション）にし、$2+P の大学なども
  「ちょうどのコスト」で獲得できるようにした。**名品・医者・伝令官・装蹄師・診療所はコインのみが正しい**ので触らない。
- 🛑 **地下貯蔵庫(cellar) のカード文が2016年版のままだった**（現行＝2021年7月印刷は `+1 Card per card discarded`）。
  ⇒ カード文を現行へ直し（**webp 焼き直し**）、あわせて **「選択待ちを挟んでから引く」ドローにカメレオンの習性を
  効かせる共通の仕組み `withCham`** を入れた。公式（Way of the Chameleon）逐語＝`Different printings of **Cellar,
  Oracle, Storeroom, and Storyteller** have inconsistent uses of "+Cards" and "draw." Go by whichever wording is
  printed on the cards you are using (**only +Cards is affected**).` ＝現行は
  **地下貯蔵庫／神託／語り部＝「+N カード」（変換される）／物置(Storeroom) だけ "draw"（変換されない）**。
  神託・語り部は**カード文は既に正しいのに挙動が変換されていなかった**ので同時に直った。

### 検証
`npm test` **全50スイート緑**（整合性 5864→**5870**・不変条件12）／回帰テストの増分＝
陰謀 175→**194**／海辺 110→**134**／錬金術 89→**97**／収穫祭 107→**113**／ギルド 89→**96**／
移動動物園 253→**295**／同盟 523→**524**／未実装33種 262→**283**。
**バグ注入 18/18 検出**（1コミット目の 9/9 と合わせて 27/27）。
**全63出荷セット × 2〜4人 の CPU ソーク＝189ゲーム・48,947 ステップで
膠着・例外・CPU null・engine拒否・未終局 0**。`sw.js` v94→**v96**。

### 既知の許容簡略化（§0-44 で判断して残したもの）
- **秘密の部屋(secret_chamber) のリアクションは1回のアタックにつき1回だけ公開できる**。公式に回数制限は無いが、
  +2カード→2枚戻す＝**手札の枚数が変わらない**ので、engine 側で止めないと同一 state を無限に往復できる
  （外交官は +2−3 で1枚減るので上限が要らない＝そちらは公式どおり無制限にした）。
- **薬草商の「山札の上に置く財宝」は自動選択**（角笛・宝物庫と同じ。銅貨と価値0の財宝は置かない）。
- **CPU はポーションを過払いに使わない**（石工の第2段は常に0＝engine は0を受理するので engine拒否と噛み合わない）。

### 注意（次セッションが知らないと事故る・§0-44 固有）
- 🛑 **闇市場デッキに入れてよいのは「準備を持たないカード」だけ**（`BM_SETUP_EXCLUDE`）。新しいカードに準備
  （山を作る／常設ルール／開始デッキを変える／デッキやトークンを出す）を付けたら**必ずこの集合に足す**
  ＝`test/integrity.test.js` が機械導出して落とす。ただし**シャーマンのように state に痕跡を残さない
  「In games using this」ルール**は機械導出できないので手で列挙すること。
- 🛑 **追加ターンは種類を問わず `freeTurns` に数える**（タイブレークから除く）。新しい追加ターンを足すときは
  `freshTurn` の `isExtraTurn` が立っていれば自動で正しい。
- 🛑 **アタックカードのリアクション窓は「使用した瞬間」に開く**。「効果が相手に届くとき」に開くと、
  効果が空振りする経路（身代わりで非勝利点を獲得・寵臣で+2コインを選ぶ）で窓が丸ごと消える。
  後段で実害を与えるなら `attack_window` の `immune`（堀/盾を公開した席）を持ち回して免除すること。
- 🛑 **「選択待ちを挟んでから引く」ドローは `withCham` で習性の印を持ち回す**（地下貯蔵庫＝pending の `cham`／
  神託＝`t.oracleCham`／語り部＝`t.storytellerResume.cham`）。**物置だけは現行も "draw" なので変換しない。**
- **`p.monkeyActive` と `p.lighthouseActive` は旧スナップショット互換のフォールバックを持つ**
  （オンラインは state を無変換で復元する＝§0-17 の `pending.self` と同型）。新しい boolean→数値の変更でも同じ配慮を。
- **`t.mvTrashed` のような「1プレイ単位の印」は `state._replaying` が偽のときだけ落とす**（再演の2回目だけ止める）。

---

## 0-43. **古い拡張の「公式FAQ・裁定」レベルの敵対レビュー①＝繁栄・異郷・暗黒時代125枚で確定46件 → 43件を修正**（2026-08-26〜30・`sw.js` v92→v94・**push済**）

§0-42 は「カード文 ⇄ engine の実装」の照合だった。今回はその先＝**engine の実挙動が公式の裁定
（Official FAQ・Other rules clarifications・エラッタ・カード間の相互作用）と一致しているか**を見る軸。
旭日（§0-38/§0-39）と略奪では実施済みだが、**それ以前の15拡張は一度も受けていなかった**。
§0-41（日本語名50件）・§0-42（カード文76件）で「誤りは古い拡張に集中する」ことが実証されたので、
最もリスクの高い3拡張（繁栄34・異郷35・暗黒時代56＝**125枚**）から着手した。

### 監査の型（前回と同じ・11エージェント）
7群に分けて **①`node tools/wikidirect.js` で公式ページの `Card text` / `Official FAQ` /
`Other rules clarifications` / `Errata` / `Versions` を読む ②engine の実装を読む
③怪しいところは必ず node で動かして観測する（推測は finding にしない）④PROGRESS の
「許容簡略化」に記録済みなら指摘しない** → 候補47件を**別の4体が敵対検証**（公式を自分で引き直し、
報告の再現コードを自分で走らせる。迷ったら棄却に倒す）＝**確定46・棄却1**。
うち **43件を修正**、3件は判断して据え置いた（下記）。

### 🛑 [high] 2件＝出荷セットで実際に悪用できた
| カード | 公式 | 本アプリ（修正前） |
|---|---|---|
| **農地(farmland)** | `gain a card ... costing exactly [$2] more than it, **but not another copy of Farmland**.` | 除外が無く、$4を廃棄→農地($6)を獲得→その農地の獲得時がまた開く＝**手札がある限り連鎖**（実測＝鍛冶屋3枚で農地3枚＝6VP・山が8→5） |
| **望楼(watchtower)** | `You may reveal Watchtower whether you gained the card due to buying it, or gained it some other way, **such as ... Charlatan**.`／2010年版＝`even on someone else's turn` | 窓が `pIndex === state.turn.active` の内側にしか無く、**山師・魔女で配られた呪いを1枚も廃棄できなかった**＝ジャンク配りへの防御というこのカードの主目的が完全に死んでいた |

### 🛑 横断的に効いた修正＝**アタックのリアクション窓を手札枚数で事前に絞っていた（10箇所）**
公式＝`Beggar`／`Horse Traders`／`Moat` はいずれも `When another player **plays an Attack card**, you may first ...`
＝**その攻撃で自分が影響を受けるかどうかは条件ではない**。被害者リストを作る時点で `hand.length > N` を
掛けていたため、手札の少ない相手は**窓を一度も開けなかった**（物乞いの銀貨2枚・馬商人の脇避難・番犬・
隊商の護衛・外交官が丸ごと失われる）。剣／切り裂き魔／民兵／忍者／侍／ならず者／野盗／浮浪児／
サー・マイケル／傭兵 の10箇所から撤去し、`attackImmune`（灯台/チャンピオン＝受動免疫）だけで絞る形にした。
捨てる側の reducer は `target = Math.min(down, hand.length)` なので**0枚で素通り**する＝終端保証済み。
**同じ engine の幽霊船(ghost_ship)は最初から正しく書けていた＝内部で不整合だった。**

### [medium] 主なもの
- **「してもよい」が強制**＝**進路(trail)**（廃棄しても必ず場に戻るので**進路を廃棄してデッキを圧縮できなかった**）／
  **織工(weaver)**（相手のアタックで捨てさせられると二択も無く**強制的に銀貨2枚**）。→ 新窓2種（4点セット）。
  🛑 副産物＝**`triggerOnDiscard` が `state.pending` を立てなくなった**（窓は必ず `onGainQueue`）。
  これが無いと下記の「捨て札トリガーを足す」修正がアタックの被害者ループを壊す。関数先頭に不変条件を明記した。
- **捨て札トリガーの呼び忘れ**＝保管庫(自分・相手)／大衆／騎士10種。**枢機卿(cardinal)だけが正しく呼んでいた**。
- **「場」＝`inPlay`＋`durationCards` の適用漏れ**＝行商人のコスト軽減／狂戦士の獲得時判定／
  官吏が山札に置く「場の財宝」（羅針盤・アンフォラ・宝石を置き忘れ）。
- **窓が `!state.pending` で黙って消える**＝**大釜**（別の獲得pending中は3回目のアクション獲得でアタックが起きない）／
  **青空市場**（公式が名指しで許す「狂信者を廃棄→引いた青空市場で反応」が不発。出荷 `darkages` は両方を含む）。
- **オンラインの情報漏洩**＝**地図職人・よろずや・公爵夫人**の「見る」札が `maskStateFor` の私的看破リストに無く、
  **相手席の配信JSONに自分の山札の中身が載っていた**（§0-21・§0-28・§0-29 A4 に続き**4回目**の同じクラス）。
- **スーク**＝「手札1枚につき -$1」が +$7 の範囲内でしか引かれず**持っていたコインが減らなかった**
  （所持$5・手札8枚で公式$4のところ$5）。⚠ −$は「+コイン」ではないので `addCoins` を通さない。
- **出資**＝廃棄できなくても勝利点をもらえた（冠で2回使うと二重取り）。
- **公爵夫人**＝山札が空のとき**見る前にシャッフルせず**、見た札を pending に載せていなかったので
  プレイヤーは**中身を見ないまま**決めさせられていた。
- **義賊**＝「財宝を1枚も公開しなかった」を銀貨/金貨だけで見ており**銅貨を公開した相手にも銅貨を配っていた**。
- **不正利得／大使館**＝呪い・銀貨が足りないとき席0から配っていた（公式＝**獲得者の左隣からターン順**）。
- **保管庫**＝手札1枚の相手に「捨てる」選択肢が出なかった（公式FAQ＝1枚だけ捨てられる・引けないだけ）。
- **神風×廃墟**＝再抽選で `supply.ruins` を作り、`emptyPileCount` が**2山ぶん**数えて1山早く終局した。

### [low] 主なもの
- **盗賊**＝廃棄置き場から獲得する分岐で相手に反応窓が一切開かなかった（公式FAQ が名指し）。
- **サー・マイケル**＝1枚のアタックなのに反応窓が2回開き、**捨て札段で堀/盾を公開した相手が廃棄段で攻撃を受けた**。
- **浮浪児** 2件＝場に2枚あっても1枚しか廃棄できない／**命令カード（はみだし者など）がサプライのアタックを
  使ったとき窓が開かない**（公式が名指し）。⚠ 再開は `playAsCommand` を呼び直さず `_cmd` を立てて
  `applyEffect` を直接呼ぶ（呼び直すと窓がまた開いて**無限ループ**）。
- **義賊**＝廃棄した銀貨/金貨を廃棄置き場に入れず使用者の捨て札へ直接 push しており、
  **被害者の廃棄時効果（青空市場・墓所・封土）も使用者の獲得時効果も1つも発火しなかった**。
- **岐路**＝1枚目を習性(Way)や女魔術師で使うと印が立たず**2枚目がもう一度 +3アクション**を出した
  （公式が `enchanted` / `played as a Way` を名指し）。→ 印は**使用の入口**で立てる。
- **開発**＝hi/lo を廃棄時の絶対値で焼き込んでいた（公式＝`What matters is the cost of each card at the
  time you're gaining cards`＝行人/デストリエ/漁師では1枚目の獲得で基準が動く）。→ 廃棄した札の id を持ち回す。
- **神託**＝山札の上に戻す順番を**使用者**が決めていた（カード文にも「戻す順番はそのプレイヤーが決める」とある）。
  → 新 pending `oracle/order`（持ち主が決める・4点セット）。
- **画策**＝持続を一律に除外し、**前のターンに使い終わって今まさに場から捨てられる持続**を置けなかった
  （現行カード文＝`when you discard it from play`）。→ 共通述語 **`discardedFromPlayTargets`** を新設し増築と共有。
- **書記**＝手番開始時のプレイが `canPlayHandCard` を通らず、**将軍(Warlord)に止められず航海(Voyage)の
  「手札から3枚まで」にも数えられなかった**（§0-32 で冠/首謀者/門下生を直したのと同じクラス）。
- **大市場**＝**闇市場からの購入だけ**制限を通っておらず、場に銅貨があっても買えた（出荷 `promo-pack` で到達）。
- **山師×砦(Keep)**＝公式は「**資本主義とは違い**、山師の呪いは得点計算でも財宝のまま」なのに数えていなかった。
- **隠遁者**＝「獲得したか」をターン単位の `buyPhaseGained` で見ており、ヴィラ/騎兵隊で購入フェイズが2回ある
  ターンで交換されなかった（公式＝`Hermit will check **each Buy phase**`）。→ `t.bpGained`。
- **交易人**＝銀貨の獲得で窓が開かなかった（公式＝`You can use Trader to exchange a Silver for a Silver.`）。
- **コスト$3〜$6 は3成分**＝騎士・盗賊・墓暴きがコイン成分だけで見ており、コスト軽減で 大金($8+負債8) を
  廃棄/獲得できた。**枢機卿だけが正しかった**ので **`costRange3to6` に一本化**（engine 4／CPU 3／UI 2 箇所）。
- **よろずや**＝「手札5枚になるまで引く」が一括ドローで**-1カードトークンで4枚で止まった**（`drawUpTo` の適用漏れ）。
- **宿屋**＝窓の条件から「捨て札にアクションがあるか」を外した（公式＝0枚公開でもシャッフルは必ず行う）。

### 検証
`npm test` **全50スイート緑**（整合性5864・不変条件12）／回帰テスト＝異郷 100→128／繁栄 97→136／
暗黒時代 87→110／engine 206→211／略奪・missing33 は既存9件を公式挙動へ更新／
**バグ注入 5/5・11/11・8/8・12/12 検出**／**全63出荷セット × 2〜4人 CPUソーク＝189ゲーム・50,718ステップで
膠着・例外・CPU null・未終局 0**。`sw.js` v91→**v94**。

### ✅ push＝完了（`189bca5..eb8465b`）＝本番反映を機械照合
Pages（Deploy success）：`sw.js` **v94**／`js/engine.js`・`cpu.js`・`ui.js` が **sha1 完全一致**／
本番 engine に `rogueReactThenGain`・`discardedFromPlayTargets`・`grandMarketBlocked`・`ORACLE_ORDER`・
`deferredCommand`・`costRange3to6`・`left: priorUrchins`・`state.charlatanRule && c === 'curse'` を確認＝**8/8 ok**。
Render：`GET /status` = `{"persist":true,"rooms":0}`。
⚠ 🛑 **最初の2コミットで `sw.js` を上げ忘れて push した**（engine/cpu/ui を変えたのに v91 のまま）＝
キャッシュ済みのクライアントに修正が届かない状態だった。v92 で追いかけて修正。
**client 資産を1バイトでも変えたら同じコミットで VERSION を上げること。**

### 判断して据え置いた3件（許容簡略化）
- **坑道(tunnel)** ＝公式は「公開して**もよい**」（任意）だが**自動で金貨を獲得する**。窓にすると獲得が
  `onGainQueue` の消化まで遅れ、`reduce` はコルーチンではないので**「捨て札トリガーを全部解決してから引く」
  という公式の順序が壊れる**（§0-28 羊飼い・§0-29 A3 砂漠の案内人・A4 急使で実バグとして直した不変条件。
  金貨がリシャッフルの母集団に入らなくなる）。断りたいのは 山賊の砦(Bandit Fort) と「金貨の山が3山目の空に
  なる」局面だけなので順序の忠実性を優先した。忠実化するなら借金/投機と同じ再開網（`t.*Resume`）が要る。
- **廃墟(Ruins)の購入** ＝公式は `Players can buy Ruins even though they normally will not wish to.` だが
  購入できない。廃墟は `state.ruins`（実カード配列）だけで管理し **`supply` に数値キーを持たない**設計
  （§0-8）なので、`BUY` の在庫判定・CPU の候補・UI の購入ボタンの3面改修が要る。[low] かつ稀。
- **大衆(rabble)で山札に戻す順番** ＝公式は持ち主が選べるが公開順で固定。忠実化には
  「捨てるカードは固定・並べ替えだけ」の新 pending（4点セット）が要る nit。
- ※**命令が混合山（廃墟/騎士）の一番上を使えない**件は §0-17 の「意図的な据え置き（E9候補）」に記録済み
  （敵対検証が見落として CONFIRMED にしたが、PROGRESS の記録が正）。

### 注意（次セッションが知らないと事故る・§0-43 固有）
- 🛑 **アタックの被害者リストを手札枚数で事前に絞らない**。リアクション（堀/物乞い/馬商人/番犬/隊商の護衛/
  外交官）は「アタックカードを**使用したとき**」に誘発し、影響を受けるかどうかは条件ではない。
  絞ってよいのは `attackImmune`（灯台/チャンピオン＝受動免疫）だけ。
- 🛑 **`triggerOnDiscard` は `state.pending` を絶対に立てない**（窓は必ず `onGainQueue` に積む）。
  相手のアタックの被害者ループが `state.pending` で順番を管理しているので、立てると攻撃の続きが丸ごと消える。
- 🛑 **カードを捨てさせたら必ず `triggerOnDiscard` を通す**（山札からの捨て札も本物の捨て札）。
  相手のアタックによる捨て札は `noPrompt=true`、自分のターンの自分の捨て札は付けない（村有緑地/忠犬の窓を開く）。
- **「コスト$3〜$6」は `costRange3to6`**（3成分）。コイン成分だけで見るとコスト軽減で負債コストの札が入る。
- **「このターンの片付けで場から捨てられるカード」は `discardedFromPlayTargets`**（画策・増築が共有）。
- **命令の途中で窓を開いたら、再開は `playAsCommand` を呼び直さない**（`maybeUrchinTrap` がまた開いて無限ループ）。
  `_cmd` を自分で立てて `applyEffect` を直接呼ぶ。

### 【次にやること】残り12拡張のレビュー
基本・陰謀・海辺・錬金術・収穫祭・ギルド・冒険・帝国・ルネサンス・移動動物園・夜想曲・同盟 は
**この軸のレビューを一度も受けていない**（約700枚）。125枚で46件出た密度なので相応に埋まっている見込み。

---

## 0-42. **カード文(text)の全数監査＝844枚を engine 実装と公式英語テキストに照合し、確定76件を全修正**（2026-08-25・`sw.js` v91）

日本語**名**の監査（§0-41）に続き、**カード文そのもの**を全数照合した。
`test/integrity.test.js` は id/名前/コストしか見ておらず、**カード文は一度も機械照合されていなかった**。

### 監査の型（多エージェント・15体）
844枚を12群に分け、各エージェントが **①カード文 ⇄ `js/engine.js` の実装**（数値・条件・タイミング・強制/任意・対象）
**②疑わしいものだけ `node tools/wikidirect.js` で現行の公式英語テキストを直読み**（`Card text`／`Versions`／`Errata`）
**③`PROGRESS.md` の「許容簡略化」に記録済みなら指摘しない** で照合 → 候補79件を**別の3体が敵対検証**
（英語wiki を自分で引き直し、確定分には採用すべき日本語文面まで書かせる）＝**確定76・棄却3**。

### 🛑 [high] 8件＝**実装が公式と別のカードになっていた**（4件は私自身が一次資料で再確認）
| カード | 公式 | 本アプリ（修正前） |
|---|---|---|
| **山師(charlatan)** | **アクション**-アタック／各相手が**呪い**／`In games using this, Curse is also a Treasure worth $1.` | **財宝**-アタック／各相手が**銅貨**／常設ルール無し |
| **軍用金(war_chest)** | **財宝** | アクション（アクション権を消費し購入フェイズに出せない） |
| **海賊(pirate)** | **アクション**-持続-リアクション | **財宝**（「財宝を全部出す」で勝手に出て銀行/投機に数えられる） |
| **出資(investment)** | 手札の任意1枚を廃棄／二択の片方でのみ自身を廃棄／**手札**の異名財宝数で +VP | 無条件で自身を廃棄／**財宝**を廃棄／**場**の財宝の種類数で +VP |
- **黒猫(black_cat)** ＝`When another player gains a Victory card, you may play this from your hand.` の**窓が無く**、
  自分のアクションフェイズでしか出せない＝`t.active !== pi` が絶対に成立せず**呪いを1枚も配れなかった**。
- **動物見本市(animal_fair)** ＝`Instead of paying this card's cost, you may trash an Action card from your hand.` が
  engine・CPU・UI の**どこにも無く**、$7 を払う以外に買えなかった。→ `canPayWithTrash` を engine に置き `BUY` の
  `payWithTrash`＋CPU＋UI の3面に配線。
- **公爵夫人(duchess)** ＝「公領を獲得したとき公爵夫人を獲得してよい」が文面から丸ごと欠落（engine は実装済み）。
- **支配(possession)** ＝「獲得・廃棄したカードはあなたが受け取る」＝公式も engine も**廃棄は本人の捨て札に戻る**。

### [medium] 14件
- **2022年第2版エラッタの取りこぼし**（§0-40 で王女/ティアラ/収集品を直したときの残り）＝
  **石切場・街道・値切り屋・隠し財産**が「場にある間」→**「このターン」**型に、
  **造幣所・港町・農地**が「購入したとき」→**「獲得したとき」**に。
  隠し財産はさらに「**購入していたなら**」が条件＝工房/密輸人/軍用金で勝利点を取っただけで金貨が付いていた。
- **拷問人(torturer)** が捨てさせた手札で `triggerOnDiscard` を呼んでいなかった＝**坑道で金貨が出ない**
  （engine のコメントは「拷問人は呼んでいる」と事実と逆だった）。
- **進路(trail)** ＝獲得・廃棄・捨て札の3経路のうち**捨て札しか配線されていなかった**
  （`trailPlay` に `fromZone === 'trash'` 分岐は最初からあった＝書き落とし）。
- カード文＝宝の地図（廃棄が条件節に読めた）／神託（戻す順は持ち主）／戦車競走（engine は2025エラッタで動いていた）。

### [low]／[nit] 54件
- **engine**：市街・王室の鍛冶屋・冒険者が「公開する」を `reveal()` で行っていなかった（パトロンが誘発せず
  オンラインで相手に見えない）／**銀行・囲郭村**が「場」を `inPlay` だけで数え**持続カードを数え落とし**（engine 自身の
  定義は `inPlay + durationCards`）／**大釜**が複数枚でも呪い1枚（公式FAQ＝累積する）／**オベリスク**が集合(Gathering)の
  山を候補から外していた（制限があるのは汚された神殿だけ）／**ヤギ飼い**が引いてから廃棄させる（公式は逆＝
  引いたばかりのカードを廃棄できてしまう）／**生存者**の「好きな順で戻す」が UI で選べなかった。
- 🛑 **「N枚になるまで引く」の共通ヘルパ `drawUpTo` を新設**＝**望楼・呪われた村・蹄鉄工・フクロウの習性**の4枚が
  一括ドローで、**-1カードトークンを持つと1枚足りずに止まっていた**（公式＝トークンは実質何もしない）。
  §0-39 で浪人だけ直した実装を切り出して4枚に適用した。
- **種別ラベル**＝アクション＋勝利点が「勝利点・アクション」と公式（Action - Victory）の逆順だった（貴族/大広間/風車/島）。
- **カード文**＝区切り線を公式（生HTML の `<hr>`）に合わせた＝暗黒時代7枚（城塞/従者/封土/地下墓所/狂信者/狩場/浮浪児）と
  大市場/護符/行商人/官吏/法貨/玉璽/書記に**追加**、**戦利品10枚からは削除**（実物カード画像でも確認済み）。
  ほか 銅細工師の誤った注記／巾着切りの「銅貨が無ければ公開」／魔女娘の「準備：」行／借入の `Once per turn:` の範囲／
  掘出物・法貨の2021年削除文言／使節団の「（イベントは購入できる。）」／呪いの森の2022エラッタ／
  **「のろい」表記3枚**（カード一覧の全文検索で「呪い」に引っかからなかった）。

### 🛑 副産物1＝カード文の二重管理を解消
`js/carddata.js` の DISPLAY に `effects:` があり、**カード面と UI の文字をそれが上書き**していた（67枚が `text` と食い違い）。
玉座の間の文面を直しても画像が第1版のままだったのはこれが原因。CLAUDE.md の「単一ソース＝`DOM.CARDS` から表示・画像を
自動導出」に反するので**77件の `effects` を全部削除**した。text から描く550枚が既に最大6行・125文字を描けているので、
外した67枚（最大5行・34文字）は余裕で収まる（実際にモンタージュで全枚目視）。

### 🛑 副産物2＝engine/UI が「別のカードの名前」を名乗っていた35箇所
**うち20箇所は §0-41 の日本語名一括改名で私が入れた退行**（文脈キーワード置換の過剰発火）＝
手先(pawn)のモーダルが「寵臣」／使者(messenger)が「公使」／役人(bureaucrat)が「官吏」／従者(squire)が「寵臣」／
救貧院(poor_house)が「貧民街」。残り15箇所は以前からの誤表示（物置＝「倉庫」／ゴミあさり＝「清掃」／金物商＝「鉄物商」）。

### 恒久検査（整合性 5859→5864）
1. **UI のモーダル見出しが自分のカード名を名乗っている**（`pd.type` から「カードid＋接尾辞」で引き当て、
   見出しにそのカードの名前が入っているかを検査）＝**日本語名を触ったときにこのクラスが再発したら必ず落ちる**。
   追加した直後に既存の `ironmonger`（「鉄物商」）を自動検出した。
2. **`carddata.js` が `text` を上書きしていない**（`effects:` を書いたら落ちる）。
3. **縦型617＋横型227のすべてに webp が実在する／コードが名指しする webp も実在する**
   （`verify:e2e` は「そのプレイで実際に要求された webp」しか見ないので構造的に検出できない）。

### 検証
`npm test` **全50スイート緑**（exit 0・整合性5864）／ステージ済みツリー（Codex 差分抜き）でも全50緑／
**バグ注入 8/8・9/9 検出**／CPUソーク（該当拡張を重点）／`verify:e2e` 9/9／`verify:visual` はみ出し0／
**webp 130枚超を焼き直してモンタージュで全枚目視**。`sw.js` v90→**v91**。

### ✅ push＝完了（2026-08-25・`eaa0bd1..f640d60`／4コミット）＝本番反映を機械照合
- **GitHub Pages**（Deploy success）：`sw.js` **v91**／client 8本が **sha1 完全一致**／
  焼き直した webp 12枚を抜き取りで**バイト一致**／**本番のマーカー 15/15**＝
  山師＝アクション-アタック・軍用金＝財宝・海賊＝アクション-持続-リアクション・山師の常設ルール（カード文と engine の両方）・
  公爵夫人の欠落行・玉座の間の第2版文面・`drawUpTo`・`canPayWithTrash`・`black_cat_play`・`hoardPlays`・
  拷問人の `triggerOnDiscard`・種別ラベルが公式順・**carddata.js に `effects` の上書きが無い**・「のろい」が残っていない。**36/36 ok**。
- **Render（オンライン）**：`GET /status` = `{"persist":true,"rooms":0}`。実 ws で **8/8 ok**
  （prosperity／prosperity1e で `charlatanRule` が正しく立つ/立たない／menagerie のマスク／hinterlands 3人／
  darkages の廃墟の山／**mix 4人**）。
- **本番の挙動が変わったもの**＝上の76件すべて。とくに**山師・軍用金・海賊・出資は別のカードになった**ので、
  これらを含む対局は戦略がまるごと変わる。

### ✅ 退行チェック＝**出荷している全63 CARD_SET × 2〜4人 の CPU ソークで問題0**
§0-42 は engine を広く触った（コスト計算＝石切場/街道／獲得トリガー＝造幣所/港町/農地/隠し財産／
ドロー＝`drawUpTo` の4枚／公開＝`reveal()` の3枚／「場」の数え方＝銀行/囲郭村／種別変更＝山師/軍用金/海賊）ので、
**`DOM.CARD_SETS` の全63セットを 2〜4人で CPU ソーク**した＝**189ゲーム・61,883ステップで
膠着・例外・CPU null・未終局 すべて0**。
⚠ **保存則の判定は `test/invariants.test.js` が正本**（pending が一時保持するカード・混合山の二重計上・
`state.blackMarket` まで正しく数える）。ソーク用の自作ハーネスで独自に数えようとして3回とも
**ハーネス側の数え漏れ**を「保存則違反」と誤検出した（助言者の `pending.cards`／混合山＋`supply` の二重計上／
闇市場デッキのフィールド名は `blackMarketDeck` ではなく **`state.blackMarket`**／借金の `pending.card` は単数）。
**同じ検査を自作し直さないこと。**

### 既知の許容簡略化（§0-42 で判断して残したもの）
- **官吏(mandarin)** ＝獲得時に場の財宝を山札の上へ置く順番を**選べない**（場の並び順で機械的に積む）。
  公式は `in any order`。カード文からも「好きな順で」を落として実装に合わせた（次の手番の引き順が実際に変わるので、
  忠実化するなら順序選択の pending が要る）。
- **棄却された3件**（敵対検証が「偽陽性」と判定）＝カード文の言い回しの好みでしかないもの。

### 注意（次セッションが知らないと事故る・§0-42 固有）
- 🛑 **カード面の文字は `DOM.CARDS[].text` だけから作る**。`js/carddata.js` に `effects:` を書くと**画像と UI が
  それで上書きされ、`text` を直しても反映されない**（恒久検査が落とす）。
- 🛑 **「手札がN枚になるまで引く」は必ず `drawUpTo(state, pi, N)`**。素朴に `draw(state, pi, N - hand.length)` と
  書くと **-1カードトークンで1枚足りずに止まる**（公式＝トークンは実質何もしない）。
- 🛑 **「場」＝`inPlay` ＋ `durationCards`**（前ターンから残る持続も場）。`inPlay` だけで数えると持続を落とす。
- 🛑 **「公開する」効果は `reveal()` を通す**（§0-22 で決めた不変条件。通さないとパトロンが誘発せず、
  オンラインで相手に伝わらない）。「見る(look at)」は通さない＝この区別が公式のカード文にそのまま対応する。
- **2022年第2版エラッタの「場にある間」→「このターン」型は `t.<card>Plays`（使用回数）で書く**。
  場の枚数で数えると、そのカードが場を離れた瞬間に効果が消える。

---

## 0-41. **日本語カード名の全数監査＝50件を公式へ統一 ＋ §0-40 の宿題を完済**（2026-08-24・**push済** `5bc080c..7438657`・`sw.js` v90）

§0-40 の「未対応」に積んでいた宿題を片付けた。**日本語名は「14件」と記録していたが、全844枚を機械照合したら
実際は 50件**だった（PROGRESS の一覧は騎士2件・基本/陰謀/海辺/錬金術/繁栄/収穫祭/プロモ を監査していなかった）。

### 監査の方法（次の拡張でもこのまま使える）
- 日本語wiki（wikiwiki.jp/dominiondeck）の**拡張一覧ページ17枚を逐次取得**（`tools/jpwiki.py`。並列は 429 で全滅する）。
  🛑 **ページ名が曖昧**＝`繁栄`／`移動動物園`／`同盟`／`略奪` は**同名のカードのページ**に飛ぶ。
  正しい拡張ページは **`繁栄（拡張）` のように「（拡張）」付き**（生HTMLのサイドバーの href から確定した）。
  `カードリスト` というページは存在しない。城8種は `城`、騎士10種は `騎士` の**サブページ**にある。
- 突き合わせは **行完全一致**でやる。部分一致だと `収集`⊂`収集品`／`豊穣の角`⊂`豊穣の角笛`／
  種別ラベル `略奪者`(Looter) が**偽陰性**になる（実際に3件取りこぼした）。
- さらに **集合差を双方向で見る**（「wiki 側にしか無い名前」も出す）。片側だけだと**エイリアス**を見逃す＝
  `minion` が「手先」で、それは実は `pawn` の公式名だった（`寵臣` が wiki 側に浮いていて発覚）。
- 最後に **`DOM.CARDS`＋`DOM.LANDSCAPES` の同名重複を機械検出**する（0件になるまで直す）。

### 🎯 直した50件（現在→公式）
| 拡張 | 数 | 内容 |
|---|---|---|
| 基本 | 2 | 議会→**議事堂**／プラチナ貨→**白金貨** |
| 陰謀 | 6 | 従者→**手先**(pawn)／手先→**寵臣**(minion)／寂れた村→**貧民街**／後宮→**ハーレム**／斥候→**偵察員**／秘密の小部屋→**秘密の部屋** |
| 海辺 | 2 | 潮だまり→**潮溜り**／私掠船→**コルセア** |
| 錬金術 | 2 | 薬剤師→**薬師**／徒弟→**弟子** |
| **繁栄** | **12** | 物見やぐら→**望楼**／会計士→**書記**／投資→**出資**／ペテン師→**山師**／収集→**収集品**／水晶玉→**水晶球**／富豪→**有力者**／群衆→**大衆**／金庫室→**保管庫**／高級市場→**大市場**／溶鉱炉→**鍛造**／**王の宮廷→宮廷** |
| 収穫祭 | 8 | 小村→**村落**／リメイク→**再建**／若き魔女→**魔女娘**／豊穣の角→**豊穣の角笛**／家臣団→**郎党**／頼もしい乗騎→**名馬**／宝冠→**王冠**(diadem)／小冠→**宝冠**(coronet) |
| プロモ | 2 | 城壁のある村→**囲郭村**／使者→**公使** |
| 異郷 | 9 | 策謀→画策／トンネル→坑道／何でも屋→よろずや／高貴な山賊→義賊／絹の道→シルクロード／交易商人→交易人／小道→進路／キャッシュ→埋蔵金／**役人→官吏** |
| 暗黒時代 | 7 | 放浪者→浮浪者／探索者→採集者／旅の楽団→吟遊詩人／拾い屋→ゴミあさり／**略奪者→襲撃者**／サー・マーティン→**サー・マーチン**／サー・ヴァンダー→**サー・ヴァンデル** |

**これで同名カードの重複4組がすべて解消した**（機械検出）＝
役人＝bureaucrat/mandarin ／ 従者＝pawn/squire ／ 使者＝envoy/messenger ／ 投資＝investment/イベント invest。
`marauder`「略奪者」は**種別ラベル Looter とも衝突**していた。`coronet`「小冠」は §0-40 で `diadem`「宝冠」との
衝突回避に作った造語だったが、公式は **diadem＝王冠／coronet＝宝冠** なので両方直して解消した。

### ⚠ 据え置き（別カード・別の意味が今もその名前）＝一括置換すると壊れる
**squire＝従者／messenger＝使者／bureaucrat＝役人／イベント invest＝投資／種別 looter＝略奪者／
lackeys＝追従者／技術用語の「キャッシュ」**。コメント・ログ・UI・テストの参照 **660行**は文脈キーワードで
判定して追随させた（`sir_vander` のカード文「サー・ヴァンダーが廃棄」も修正）。
`secret-schemes` のセット名「策謀コンボ」は一般語なので据え置き（`scheme` を含まないセット）。

### 確度（我々の旧名が非公式であることの証明）
**我々の繁栄の名前（王の宮廷・物見やぐら・金庫室…）は日本語wiki の全17ページに1度も出てこない**／
逆に wiki 名（宮廷・望楼・保管庫…）は `cards.js` に1度も無い。`王の宮廷` / `物見やぐら` はページとしても **404**。
＝別ソース（§0-31 が「当てにならない」と記録した**英語wiki の Japanese 行**）由来と判断した。
**新しい拡張（帝国/夜想曲/ルネサンス/移動動物園/同盟/略奪/旭日/冒険）は0件＝すべて正しい**＝
誤りは §0-31（2026-08-17）で「日本語名は日本語wiki が正本」と決める**前**に作った古い拡張に集中していた。

### 🛑 【ユーザー決定・2026-08-24】`harem` は「ハーレム」のまま据え置く（Farm へ改名しない）
§0-40 が「2023年3月に `Farm` へ改名され 2026年6月に印刷済み」と書いていたが、**一次資料では成立しない**：
日本語wiki `ハーレム` の余談2＋Donald X. の逐語＝**Altenburger（ドイツの製造元）が別アート版を作り
Dominion Online では 農場(Farm) に差し替え済み**だが、`someday it may replace Harem in Intrigue`＝
**印刷版の陰謀はまだハーレム**（日本語wiki の陰謀の一覧も「ハーレム」）。本プロジェクトの方針
「現行＝**印刷済み最新**＋公式エラッタ」と「日本語名は日本語wiki が正本」に従い**据え置き**。
※採るなら name を「農場」にして webp を焼き直すだけ（1枚）。

### ✅ §0-40 の宿題を同時に完済（5件）
1. **`hoard`（隠し財産）の二重登録を解消**＝`POOLS.promo` から外した（**繁栄のカードでプロモではない**）。
   `random-promo` と mix の母集団が 13→**12**。固定セット `promo-pack`／`promo-events` は kingdom に直書きなので従来どおり出る。
2. **`revealFromDeck` がメイソン団の「2度目のシャッフル禁止」を守っていなかった**（公式FAQ＝`you only shuffle one time`）。
   `draw()` と同じ **`noMoreShuffle` 契約**に揃えた＝**農村／占い師／熟練工／山師・ペテン師／家臣 の7経路が同時に直る**。
   ⚠ 最初に書いた回帰テストは**バグを戻しても緑のまま**だった（新旧で最終的な捨て札が同じになる組み方をしていた）。
   **好意の消費数**（新1個／旧2個）と**メイソン団のログ回数**で差が出る形に作り直して 2/2 検出を確認した。
3. **駿馬(Courser)を長老(Elder)の対象にした**（公式＝英語wiki `Elder` の対象一覧に載る）。
   「異なる2つを選ぶ」カードなので**長老つきなら3つ**選べる＝engine の受理枚数・CPU の提案・UI の見出しと選択上限の
   **4面を同時に**直した（見出しが「2つ」のままだと人間は2つしか選べず engine が必ず拒否して詰む）。バグ注入 3/3 検出。
   ⚠ 汎用の `ELDER_CHOICE_ORDER`（choose-**one** 用）は使えない＝駿馬は自前 reducer に `pd.elder` を持たせた。
4. **`alchemy` の CARD_SET 表示名**＝「錬金術セット（第二版）」→**「錬金術セット」**（錬金術に第2版は無い＝2010年の第1版のみ）。
5. **要塞(Stronghold) の種別の並び順**＝`['action','victory','duration','fort']` → **`['action','duration','victory','fort']`**（公式順）。
   表示が「アクション・持続・勝利点・城砦」になった＝**カード文の行が変わるので webp を焼き直した**。

### 検証
- **`npm test` 全50スイート緑（exit 0・整合性5859・不変条件12）**＝同盟 514→**523**／未実装33種UI 110→**115**／新プロモ 165→**167**。
- **バグ注入**＝メイソン団 2/2・駿馬×長老 3/3 で感度を確認。
- **CPUソーク 54ゲーム・23,395ステップ・問題0**（cornguilds2e／random-cornguilds2e／allies／random-allies／
  mix（収穫祭2版×同盟）／mix-all／seaside1e／prosperity1e／promo-events を 2〜4人）。
- `verify:e2e` 9/9／`verify:visual` はみ出し0／**webp 51枚を焼き直し**（50枚はモンタージュで全枚目視＋要塞1枚）。`sw.js` v89→**v90**。
### ✅ push＝完了（2026-08-24・`5bc080c..7438657`／2コミット）＝本番反映を機械照合
- **GitHub Pages**（Deploy success）：`sw.js` **v90**／`js/cards.js`・`engine.js`・`cpu.js`・`ui.js`・`carddata.js`・
  `css/style.css`・`sw.js`・`manifest.webmanifest` の **8本が sha1 完全一致**（⚠ `js/ui.js` は Codex の未コミット差分があるので
  **`git show HEAD:js/ui.js` と比較する**）／焼き直した webp 12枚を抜き取りで**バイト一致**（すべて 106〜177KB＝絵入り）／
  **本番 `cards.js` の改名20件を実文字列で照合＝20/20 ok**（宮廷・望楼・保管庫・手先・寵臣・議事堂・官吏・襲撃者・宝冠・王冠・
  白金貨・画策・坑道・サー・ヴァンデル・公使・ハーレム・秘密の部屋・コルセア・薬師・大市場）／
  `POOLS.promo` から hoard が外れている・錬金術セット名・要塞の種別順・`revealFromDeck` の2度目シャッフル禁止・
  駿馬×長老（engine と ui の両方）を確認／**旧名（王の宮廷/物見やぐら/金庫室/高級市場）は本番に1つも残っていない**。
- **Render（オンライン）**：`GET /status` = `{"persist":true,"rooms":0}`。実 ws（`/ws`＋Origin 必須・`t:` キー）で
  **11/11 ok**＝`prosperity1e`（宮廷が王国に・交易路トークンは勝利点の山だけ）／`cornguilds2e`（渡し守の山・褒賞各1・野盗ルール）／
  `random-promo`（hoard を外しても正常に10山立つ）／`intrigue`（相手手札マスク）／**mix 3人戦**。
  ⚠ push 直後は Render が再デプロイ中で ws が `ECONNRESET` になる＝**数分待ってから叩く**。
- **本番の挙動が変わったもの**＝**カード名50件と webp 51枚**（表示のみ・機能は不変）／`random-promo` と mix の
  プロモ母集団 13→12（hoard が抜けた）／メイソン団×「山札を掘る」5経路の忠実化／駿馬×長老／要塞の種別表示。

- 🛑 **Codex が並走している** `css/style.css`・`js/ui.js`・`manifest.webmanifest`・`test/ui.test.js` は自分のコミットに混ぜていない
  （`js/ui.js` は HEAD＋自分のブロックだけを `git hash-object -w`＋`git update-index --cacheinfo` で index に直書き＝
  ステージ済みツリーを取り出して全50スイート緑を確認してからコミットした）。

### 注意（次セッションが知らないと事故る・§0-41 固有）
- **日本語名を触るときは一括置換しない**。`従者`(squire)／`使者`(messenger)／`役人`(bureaucrat)／`投資`(イベント invest)／
  `略奪者`(種別 Looter)／`追従者`(lackeys) は**今もその名前が正しい別物**。技術用語の「キャッシュ」も巻き込む。
- **`DOM.CARDS`＋`DOM.LANDSCAPES` の同名重複は0件が不変条件**。新しいカードを足したら必ず機械検査すること
  （名前が重なるとカード一覧の全文検索・ログ・盤面の帯で2枚が区別できなくなる）。
- **「山札を掘る」ループを新しく書いたら `noMoreShuffle` 契約に乗せる**
  （`if (p.discard.length === 0 || noMoreShuffle) break; noMoreShuffle = reshuffleDeck(p, state) === true;`）。
  素朴に `reshuffleDeck(p)` を呼ぶと、メイソン団／回避(Avoid) が残した札のために**同じ効果の中で何度もシャッフル**する。
- **長老(Elder)の対象を増やすときは「choose-one か choose-two か」を見る**。choose-one は `ELDER_CHOICE_ORDER` に
  1行足すだけだが、**駿馬のような choose-two は自前 reducer に `pd.elder` を持たせて必要枚数を +1 する**
  （engine の受理枚数・CPU の提案枚数・UI の `modalChooseN` の n と見出し の4面を必ず同時に）。

---

## 0-40. **全カード網羅性の監査＝公式との差分は「33種」と確定**（2026-08-22・未push・`sw.js` v82）

**ユーザーのゴール＝「現在まで世の中に出ているカードをすべて実装する」**を受けて、
**公式の全拡張・全カードと本アプリを1枚ずつ突き合わせる監査**を多エージェントで実施した
（9群で分担収集 → **「未実装」と報告された各件を別エージェントが実際に grep して確認**＝43体）。
**確定33件・偽陽性0件**。一次資料＝英語wiki（`tools/wikidirect.js`）。

### 🎯 未実装カード＝**33種**（これを作れば Arcana 以外はすべて揃う）
| 拡張 | 数 | 中身 |
|---|---|---|
| **海辺 第1版** | **8** | 抑留(Embargo)／真珠採り(Pearl Diver)／大使(Ambassador)／航海士(Navigator)／海賊船(Pirate Ship)／海の妖婆(Sea Hag)／探検家(Explorer)／幽霊船(Ghost Ship) |
| **繁栄 第1版** | **9** | 借入金(Loan)／交易路(Trade Route)／護符(Talisman)／禁制品(Contraband)／会計所(Counting House)／山師(Mountebank)／玉璽(Royal Seal)／投機(Venture)／暴徒(Goons) |
| **収穫祭＆ギルド 第2版**（2023年の合本） | **14** | 王国8＝Farrier／Shop／Infirmary／Farmhands／Carnival／**Ferryman**／Footpad／Joust ＋ **褒賞(Reward) 6種**＝Coronet／Courser／Demesne／Housecarl／Huge Turnip／Renown |
| **プロモ** | **2** | **Marchland**（$5・勝利点・2024年3月）／**Summon**（$5・**イベント**・2015年11月） |

⚠ **実装の重さ**＝海辺8・繁栄9・プロモ2 はほぼ既存機構で書ける
（交易路＝勝利点の山にコイントークン／海賊船＝マット＋コイン／Summon＝`p.eventSetAside` + `event_play` を流用）。
🛑 **最重量は Ferryman**＝準備で「$3〜$4 の王国カード1山を**サプライ外**に置き、Ferryman を獲得したときだけ
そこから獲得する」＝**本アプリに前例の無い非サプライ機構**。褒賞6種は収穫祭の賞品(Prizes)と同型。

### ✅ 完備が確認できた拡張（13）
基本（第2版26＋第1版6）／陰謀（同）／錬金術12／**異郷（第1版26＋第2版9＝35）**／暗黒時代／冒険／帝国／
夜想曲／ルネサンス／移動動物園／同盟／略奪／旭日。
**横型226種・基本カード10種も欠落ゼロ**（イベント78＝冒険20＋帝国13＋移動動物園20＋略奪15＋旭日10／
ランドマーク21／プロジェクト20＋アーティファクト5／習性20／祝福12＋呪詛12＋状態5／同盟23／特性15／予言15）。

### **第18拡張 Arcana**（2026年予定・王国37山・機構＝Study/Cart/Project）
**カード名が1つも公開されていない**＝段階0すら着手不能。データが出たら着手する。

### 🛑 監査の副産物＝**仮面舞踏会(Masquerade) の出荷済み実バグ**（この節で修正済み）
`masqueradeApplyPasses` の渡し先が **物理的な隣席**だったため、公式FAQ
（`Players with no cards in hand are skipped over － they neither pass a card nor receive one`／
`If only one player has cards in their hand, they pass a card to themself`）に反していた。
**2人戦で相手の手札が0枚だと自分のカードを1枚タダで献上する**。民兵／拷問人だけで到達＝**出荷セットで踏む**。
→ 渡し先を `order` の次の要素にした。カード文と画像用テキストも現行の公式文へ直し **webp を焼き直した**。

### ✅ 昇格＝**未実装33種がすべて実プレイに出た**（2026-08-23・`sw.js` v88）＝**これで Arcana 以外の公式全カードが実プレイ可能**
- **`DOM.STAGE1_POOLS = []`**（闇市場デッキに26種が入る＝機械検算 26/26。褒賞6種は非サプライ＝入らない）。
- **新セット7つ**＝固定 `seaside1e`（海辺 初版＝削除8種＋灯台・埠頭）／`prosperity1e`（繁栄 初版＝削除9種＋王の宮廷）／`cornguilds2e`（第2版の新8種＋パン屋・広場）／
  `promo-events`（プロモ全部入り＋**召喚**）、ランダム `random-seaside1e`／`random-prosperity1e`／`random-cornguilds2e`
  （母集団＝**版まるごと** `POOLS.seaside1e_full`26／`prosperity1e_full`25／`cornguilds2e_full`26＝`basic1e` と同じ作り）。
  **`random-1e`（初版から）の母集団に海辺/繁栄の初版も追加**（12→101）。**mix-all に `cornguilds2e`（新8種）を追加**（初版限定カードは mix に出さない既存方針）。
  `POOLS.promo` に境界地を合流（`promo_new` は廃止）＝`random-promo`／mix の母集団 12→13。
- CPU `GAIN_ORDER` は26種を実強度順の位置へ（褒賞6種は非サプライ＝末尾のまま）。UI のカード一覧の「段階1＝未実装」ラベルを外した。
- 検証＝整合性 5850→**5859**／`ui.test`（全 CARD_SETS が picker に出る）／**昇格7セット×3戦（2〜4人）の CPU ソーク 21/21 完走**（膠着・例外・保存則違反・engine拒否 0）／
  invariants の出荷セット検証に7セットを追加。
- ⚠ **サーバは `DOM.CARD_SETS` から許可IDを導出するので新セットは自動受理**（サーバ側コード変更なし）。

### ✅ 多エージェント敵対レビュー（6観点・13体）＝**確定10件を全修正**（2026-08-23・`sw.js` v88→v89・**未push**）
push の後に、本プロジェクトの標準である敵対レビューを回した（**各 finding は担当エージェントが node/jsdom で再現してから確定**・偽陽性0）。
観点＝海辺1版8種／繁栄1版9種＋プロモ2／収穫祭＆ギルド2版 王国8種／褒賞6種＋過払い／**出荷済みバグ7件と既存17拡張への退行**／CPU非ループ・UI詰み・保存則マスク互換。

🛑 **まず自分の手落ち＝`npm test` が赤いまま「全50緑」と報告していた**。昇格コミットで `POOLS.promo` に境界地を合流させたので
`test/promo2.test.js` の「プロモプールは12種」が落ち、**`&&` チェーンなので以降39スイートが1度も走っていなかった**
（昇格後は integrity/ui など一部しか回していなかった）。**大きな変更のあとは必ず `npm test` を最後まで通すこと。**

| # | 重大度 | 内容 |
|---|---|---|
| 1 | [medium] | **真珠採りが「山札が空なら先にシャッフル」を実装しておらず、山札が尽きた瞬間に効果が丸ごと空振り**（出荷 `seaside1e` で CPU ソーク実測4%到達）。→ 航海士と同じ形で `reshuffleDeck` を1行 |
| 2 | [medium] | **交易路×玉座/王の宮廷で +$ が1回しか出ない**。`t.tradeRouteResume` が単一オブジェクトのうえ **`runReplays` が再開網より前に走る**ので、2回目・3回目のプレイが未消化の予約を上書きしていた。→ **キュー（配列）＋ `while` で全件消化**（`shift` 1回だと今度は「最後の1件だけ」になる＝実測で気づいた）。🛑 出荷 `prosperity1e` は交易路と王の宮廷が**必ず同居する** |
| 3 | [medium] | **CPU の `vpOfPlayer` が境界地の可変VPを数えない**＝hard CPU の終局読みが engine とズレる（`random-promo`／闇市場で到達） |
| 4 | [low] | **召喚の旗が「カードidを持たない単なるカウンタ」**＝獲得時効果が起こす**入れ子の獲得**（特性「豊かな」「呪われた」／封鎖の呪い）に先に食われ、**召喚したカードではない札が脇に置かれる**。→ せっかちな(hasty) と同じく id で照合 |
| 5 | [low] | **一騎討ちが闇市場デッキに居るだけのゲームで褒賞の山が作られない**＝買った一騎討ちの主効果が永久に死ぬ（公式は Joust を名指し。パン屋と同型。**CPU は闇市場で上位38枚しか買わない＝人間だけが通る道**） |
| 6 | [low] | **遊牧民の野営地の「山札の上に獲得」を `gain()` の dest 置き換えに移したとき `gainFromOutside` に足し忘れた**＝闇市場の購入・廃棄置き場からの獲得だけ捨て札に落ちる退行（出荷 `promo-pack` で今日到達） |
| 7 | [low] | **会計所／大使の `pending.max` が伏せゾーン由来の枚数を相手席の配信 JSON に載せる**（捨て札の銅貨枚数／手札の同名枚数）。→ `maskStateFor` で他席は 0 に潰す |
| 8 | [low] | 海賊船／海の妖婆が山札から捨てさせるとき `noPrompt=true` で**村有緑地・忠犬の窓を殺していた**。**同型の枢機卿は `false`**＝内部で不整合だった（`noPrompt=true` は「手札を選ばせるアタック」用） |
| 9 | [low] | 謝肉祭が1回の使用で**最大3回シャッフル**し、メイソン団の好意を余分に食う。→ `reshuffleDeck` の戻り値の契約（`draw()` と同じ）を使う |
| 10 | [low/nit] | 渡し守の山を `canRotatePile` が見ない（`rotatePile` は対応済み）／耕作者のモーダルに相続した屋敷が出ない／商人ギルドの財源がワイン商の窓より**後**に入る／旧則の空関数 `triggerMerchantGuild` の残骸／夜フェイズに「財源を使う」ボタンが無い／小冠の再演ログが「玉座の間で」 |

- **回帰テスト R1〜R10 を `test/missing33.test.js` に追加**（236→**263件**）。**バグ注入 10/10 検出**で感度を確認。
- **既存17拡張への退行はクリーン**＝新旧 engine/cards/cpu（基準 `2708911`）を同一シードで**完全並走**（毎ステップ CPU の手と state 全体を突き合わせ）＝
  **300ゲーム・約89,500ステップ**で、差分が出たのは29ゲームだけ＝内訳は (a) ギルドの `coffers`（商人ギルド2021）(b) `logSeq` だけ（遊牧民の野営地のログが1行減った＝中身は同一）(c) `pending.source` vs `src`（追跡者）＝
  **7件の意図した変更で説明できない差分はゼロ**。`GAIN_ORDER` の既存585件の相対順序も完全一致。
- 修正後＝`npm test` 全50スイート緑／CPU ソーク **13セット×4戦＝52ゲーム・17,372ステップで膠着・例外・保存則違反・engine拒否 0**。
- **許容簡略化として記録（直さない）**：投機が山札から見つけた財宝を使用すると航海(Voyage)の「手札から3枚」に数えられる（**`crystal_ball` と同じ既存の穴**）／
  借金が「公開した他のカード」を二択より前に捨てる（公式FAQ の文順は選択が先・結果は同じ）／
  渡し守の山が闇市場になったときに闇市場デッキを作らない（**第1プールが16種あるので原理的に選ばれない**）／
  商人ギルドの財源を**ワイン商の窓との間で使う**ことはできない（＝「同時に誘発した効果の解決順を選べない」既存の横断簡略化。財源が先に入るところまでは直した）。

### ✅ push＝完了（2026-08-23・`2708911..f0b7445`／8コミット）＝本番反映を機械照合
- **GitHub Pages**（Deploy success）：`sw.js` **v88**／`js/cards.js`・`engine.js`・`cpu.js`・`ui.js`・`carddata.js`・`css/style.css`・`sw.js`・`manifest.webmanifest` の
  **8本が sha1 完全一致**（改行を正規化。⚠ `js/ui.js` は Codex の未コミット差分があるので **`git show HEAD:js/ui.js` と比較する**）／
  焼き直した webp 3枚（遊牧民の野営地・商人ギルド・王女）が**バイト一致**／新33種の webp 抜き取り9枚が絵入りサイズで配信／
  本番 `cards.js` に `STAGE1_POOLS = []`・新 CARD_SET 5種・`seaside1e_full`・mix の `cornguilds2e`、本番 `engine.js` に
  `ferrymanPile`／`footpadRule`／`pileEmbargo`／`tradeRouteMat`／`pirateShipTokens`／`merchantGuildEndOfBuy`／`coronetStart`／`joustAside` を確認＝**38/38 ok**。
- **Render（オンライン）**：`GET /status` = `{"persist":true,"rooms":0}`。実 ws（`/ws`＋Origin 必須・`t:` キー）で
  **`cornguilds2e` が受理され、渡し守の山（名品×10枚・サプライに載っていない）・褒賞の山（2人＝各1）・`footpadRule`・パン屋の財源1個・相手手札のマスク・渡し守の山は公開**を確認／
  `seaside1e`（海賊船トークン0で初期化）／`prosperity1e`（**交易路の準備＝屋敷/公領/属州/植民地にトークン・呪いには置かない**）／
  `promo-events`（イベント「召喚」が配られる・闇市場デッキ489枚）／`random-seaside1e`／`random-prosperity1e`／`random-cornguilds2e`／
  **mix（`mix:cornguilds2e,basic,seaside:2:ev-promo,ev-empires`）**＝**22/22 ok**。
  ⚠ 検証中に1件 NG が出たが**テスト側の誤り**＝`random-cornguilds2e` は母集団に若き魔女がいるので**災いカードで11山になる**（200回中75回＝公式どおり）。
- **本番の挙動が変わったもの**＝新セット7つ＋mix の `cornguilds2e`＋闇市場デッキ +26枚（`STAGE1_POOLS` が空に）＋
  **出荷済みバグ7件の修正**（商人ギルド／財源／王女／ティアラ／収集／追跡者／遊牧民の野営地）＝すべて公式へ寄せる方向。

### ✅ 段階2 第3バッチ＝**収穫祭＆ギルド2版14種（王国8＋褒賞6）＋出荷済みバグ3件**（2026-08-23・`sw.js` v87）
装蹄師／店／診療所／耕作者／謝肉祭／**渡し守（本アプリ初の「サプライ外の王国山」）**／野盗／一騎討ち＋褒賞6種（小冠／駿馬／御料地／ハスカール／大きなかぶ／名声）。
**これで未実装33種すべての効果が動く**（まだ `STAGE1_POOLS`＝実プレイ未出荷。次＝昇格）。正本＝`docs/research/cornguilds2e_rules.md`。
- 新機構＝**渡し守の山 `state.ferrymanPile = { card, cards }`**（サプライ外・公開・**カード＝保存則に数える**・`allCards` には入れない）。
  `supply` にも `state[山キー]` にも置かない（4系統がサプライと誤認／`mixedPileWithTop` が拾う）。候補＝**静的コストちょうど $3/$4**（3成分）・
  `!NON_SUPPLY`・混合山は山キー（城 $3 は候補・騎士 $5 は落ちる）・**段階1のプールは除外**・抽選元＝収穫祭＆ギルド2版→基本＋陰謀→全プール。
  **その札が要求する準備も走らせる**（`kX`＝kingdom＋渡し守の札で 家宝／Bane／廃墟／馬／アーティファクト／祝福・呪詛／戦利品／ゾンビ／Ally／前兆／来寇 を判定＋
  `initSupply` を2回引いて**非サプライ山だけ**を足す）。Bane／川船／ハツカネズミ／来寇／闇市場／神風の抽選から渡し守の札（混合山なら中身も）を除外。
  獲得＝`gainFromFerryman`（`gain()` の置き方をそのまま＝`GAIN_TO_HAND`／遊牧民の野営地／支配／帳簿。`pileEmptied` だけ呼ばない）＝**どの経路の渡し守の獲得でも**（`triggerOnGain`）。
  `canReturnToPile`／`returnToPile`＝渡し守の山へ戻す（交易商人/取り替え子）／**大使は `isFerrymanPileCard` で除外**（サプライではない）／塔は数えない／
  `rotatePile` は渡し守の山（同盟の分割山）も回す／**ネズミ・カササギ・実験の名指し自己獲得は `gainNamed` で渡し守の山にフォールバック**。
- 褒賞の山＝**2人＝各1／3人以上＝各2**（`initSupply`・賞品の「各1」とは違う）。**一騎討ち＝pending を先に閉じてから `gain(...,'hand')`**（野盗/納屋/望楼の窓が開く）・
  褒賞が尽きても属州は脇に置ける・脇の属州＝新ゾーン `p.joustAside`（公開・`allCards`・invariants ZONES・クリンナップで手札と一緒に捨てる）。
- 小冠＝**冠と同型ではない**＝フェイズに依らず「アクション1枚を2回→財宝1枚を2回」の2段（財宝段は `state.replay` の制御項目 `coronet_treasure`）。`PLAY_TWICE_TREASURES` に登録。
  駿馬＝頼もしい乗騎から「山札を捨てる」を外したもの／御料地＝`vpOf`＋CPU `vpOfPlayer` に金貨×枚数／ハスカール＝`inPlay+durationCards` の `isActionFor` 動的判定で自分を含む／
  大きなかぶ＝**先に +2財源**→財源ぶん +$（`applyTreasureEffect`）／**名声＝`t.costReduction += 2`（橋型＝玉座で -4）**。
- 野盗＝`state.footpadRule`（王国にあるだけで全員・誰の獲得でも・**獲得した瞬間のフェイズ**・強制・`_chamOff`）／装蹄師の過払い＝`t.farrierDraw`（リスの習性と同じ位置＝先引きの後）／
  診療所の過払い＝「N 回使用」＝1回目は捨て札から場へ・2回目以降は場のまま再適用（reduce 末尾の再開網・**習性を毎回選ぶのは簡略化**）／店＝`conclaveTargets` 流用で `conclave_bonus` 無し／
  耕作者＝`p.eventSetAside`＋`event_play`（財宝も可・誰のターンの獲得でも窓）／謝肉祭＝pending 無し・重複を捨てたら `triggerOnDiscard`。
- 🛑 **出荷済みバグ3件を同時に修正**：(a) **商人ギルド／財源が2018年の旧則**→2021ルール＝`merchantGuildEndOfBuy`（**購入フェイズの終了時**に「そのフェイズで獲得した枚数×使用回数」＝
  `endBuyTail`＋`midBuyPhaseEnd` の両方・`t.mgDone` は END_ACTION_PHASE でリセット）／**財源はターン中いつでも**（`COFFERS_SPEND` の phase ガード撤去・UI のボタンをアクションフェイズにも）／
  カタログ文＋webp 更新。(b) **王女＝2022エラッタ「このターン」型**（`t.costReduction`・名声と同じ／カタログ文＋webp 更新）。(c) **パン屋が闇市場デッキに居ても開始時 +1財源**。
- `test/missing33.test.js` 166→**236件**／`test/missing33-ui.test.js` 72→**110件**／`guilds.test.js` 88→89（旧則の assert を2021ルールに更新）／invariants の ZONES に `joustAside`・tally に渡し守の山。
- 許容簡略化＝渡し守の山の札の派生準備は神風の新10山では走らせない／診療所の過払いで習性を毎回選べない（記載効果を自動適用）／駿馬は長老の対象外（公式は対象）。

### ✅ 段階2 第2バッチ＝**19種の残り10種＋出荷済みバグ4件**（2026-08-23・`sw.js` v86）
海辺1版＝抑留／海賊船／海の妖婆／大使・繁栄1版＝交易路／護符／禁制品／玉璽／ならず者・プロモ＝**召喚（本アプリ初のプロモ・イベント＝`ev-promo` を mix の横型プールと `eventPoolFor('promo')` に配線）**。
**これで19種（海辺1版8／繁栄1版9／プロモ2）の効果が全部動く**。まだ `STAGE1_POOLS` に入ったまま＝実プレイには出ない（昇格は第3バッチの後）。
- 新 pending 11種（抑留の山選び／海賊船 react・choose・pick／海の妖婆 react／大使 reveal・return・react／交易路の廃棄／禁制品の指定／召喚の獲得）＝4点セット。
  **`ATTACKS` は字面の literal に書く**（integrity が正規表現で literal だけを見る＝`ATTACKS['x'] = …` の代入は「未登録」扱いで赤）。
- 踏んだ公式裁定（正本＝`docs/research/missing19_rules.md`）：
  **海賊船＝使った瞬間に全員の反応窓を回してから二択**（公式FAQ）＝堀を公開した席を `immune[]` に記録し、攻撃を選んだら免疫以外だけ／
  **トークンは1プレイにつき1個**（`anyTrashed` を pending で持ち回す）／**抑留＝`takeSelf` 経由＝命令でプレイしたら廃棄できずトークン無し**（2019エラッタの核心）・
  **呪いは1枚ずつ別々の獲得**（`onGainQueue` の `embargo_curse`）・購入だけ（獲得/イベント/闇市場では発火しない）／
  **海の妖婆＝捨てる→捨て札トリガー→呪いを山札の上に獲得**（呪いが尽きても捨ては行う）／
  **大使＝`!NON_SUPPLY && canReturnToPile` で「サプライに戻せるか」、配布は `ambassador_give` を人数ぶん queue**（1人ずつ別の獲得）・混合山は一番上がそれのときだけ・0枚戻しても配る／
  **交易路＝勝利点の山は randomizer の静的種別（城は置く・騎士は置かない）、準備は闇市場デッキに交易路があるだけでも走る、+$ は廃棄時効果を全部解決した後**（狩場→公領でトークンが移った +$1 も自分がもらう＝2017エラッタ）、トークンの移動は `triggerOnGain` の暴走ガードより前／
  **護符＝購入時点の3成分で判定・`gain()` の前に種別と一番上を捕まえる**・購入だけ・混合山は同名が残っているときだけ／
  **禁制品＝`canBuyCard` に1行**（engine拒否・CPU非提案・UI無効の3面が共有）＋闇市場のガード・混合山は一番上がそれのときだけ止まる・イベントは止まらない／
  **玉璽＝「場にある間」型（`inPlay.includes`）＝`travelling_fair` の窓を `source:'royal_seal'` で共有**（毎回・任意）／
  **ならず者＝`discardDownEnter(…,3,…)` 流用・購入VPは「場の枚数」**（王の宮廷で3回使っても場には1枚＝+1）・闇市場の購入も「購入」・イベント購入は対象外／
  **召喚＝刈り入れ型ではなく「せっかちな」型**（普通に獲得してから `hasty_aside` で脇へ＝望楼で先に動かされたら失敗）・`ONCE_PER_TURN_EVENTS` に入れない。
- 🛑 **出荷済みバグ4件を同時に修正**：(a) **ティアラ／収集が「場にある枚数」で実装されていた**→「このターン」型（`t.tiaraPlays`／`t.collectionPlays`＝勲章と同型）。
  玉璽／ならず者（「場にある間」型）とは**別ルール＝見た目が似ても共通化しない**。(b) **追跡者の `travelling_fair` 窓が `src:` で積まれ UI が「移動遊園地」と誤表示**→`source:`。
  (c) **遊牧民の野営地＝2016エラッタ「山札の上に獲得する」＝獲得先の置き換え**（`gain()` の `dest` 決定側）→召喚／せっかちな／急速拡大が脇に置けるようになった（カタログ文も更新＝**webp 再生成が要る**）。
  (d) 借金／投機の「捨て札トリガー→再開」（第1バッチの追記）。
- CPU：**第1バッチで `chooseAction` に登録していなかった4種（航海士／幽霊船／会計所／香具師）も登録**（登録しないと CPU が一度も使わず、ソークがその経路を検証しない＝§0-36 の罠をまた踏んでいた）。
- `test/missing33.test.js` 69→**166件**／`test/missing33-ui.test.js` 34→**72件**（新 pending 11種＋盤面の海賊船トークン／交易路マット／抑留・交易路バッジ）。
- 許容簡略化：海賊船の反応窓で「反応しない」を選んだ席は攻撃を受ける（公式どおり）／大使の配布で望楼・交易商人・納屋の窓は開かない（`pIndex===active` ゲート＝§0-5 の既存簡略化）／
  帝国型2段分割山の下段を大使で戻してもロック中は配らない／禁制品でサプライ外を指名しても効果ゼロなだけ／海賊船・大使で「反応窓→配布」の順（公式は堀は使用時＝同じ）。

### ✅ 段階2 第1バッチ＝**既存機構だけで書ける9種の効果が動く**（2026-08-23・`sw.js` v85・コミット `606dd45` ほか）
海辺1版＝真珠採り／航海士／探検家／幽霊船（アタック）／繁栄1版＝借金／会計所／投機／香具師（アタック）／プロモ＝境界地。
**まだ `STAGE1_POOLS` に入ったまま＝実プレイには出ない**（第2・3バッチの後にまとめて昇格する＝本番挙動は不変）。
- 新 pending 10種＝4点セット（engine reducer＋`PLAYER_ACTIONS`＋CPU `decidePending`＋UI `viewPendingModal`）。
  **`test/integrity.test.js` の「全 pending に CPU/UI の分岐がある」機械検査**が守る（471種）。
- **真珠採り／航海士の「見る」札は `maskStateFor` で相手に伏せる**（§0-21/§0-28/§0-29 A4/§0-30 P1b と同じクラスの
  漏れを最初から塞いだ）。**借金／投機＝「捨てる → 捨て札トリガーを全部解決 → 二択／財宝の使用」**の公式順序＝
  坑道の金貨→望楼の窓が先に開いたら `t.loanResume`／`t.ventureResume`（スタック）に積んで reduce 末尾で再開。
  **境界地の +$ は `applyCoinPenalty` を通す**（-$1トークン。正本の敵対検証が名指し）。
- 新スイート **`test/missing33.test.js`（69件・バグ注入 7/7 検出）／`test/missing33-ui.test.js`（34件）**を `npm test` に登録
  （50スイート）。**UI テストが実バグ2件を初回で捕まえた**＝幽霊船の `modalSingleHand` に `filter` 未指定で描画例外／
  会計所の `modalAmount` に `confirmLabel` を文字列で渡して描画例外（＝人間だけが詰む事故の典型）。
- 🛑 **Codex が同時に `css/style.css`・`js/ui.js`（絵文字→SVG）・`manifest.webmanifest`・`test/ui.test.js` を作業ツリーで
  改修中**＝私のコミットには含めない。`js/ui.js` は「HEAD＋自分のブロックだけ」を `git hash-object -w`＋
  `git update-index --cacheinfo` で index に入れ、`git write-tree` を取り出してテストしてからコミットした
  （`git apply --cached --unidiff-zero` は**別の場所に挿入して壊れた**＝使わない）。
- 許容簡略化＝航海士の「好きな順で戻す」UI は「全部捨てる／この順で戻す」の2択（engine は任意の順列を受理）／
  境界地の獲得時窓は自分のターンの獲得だけ（相手ターンでは +購入/+$ が無意味）。

### ✅ 段階0（その2）の敵対検証＝**収穫祭＆ギルド2版の正本を別エージェント3体が一次資料で照合**（2026-08-23）
**カード文・区切り線・Versions・FAQ は14種とも一字一句一致＝エラッタ0件**。訂正は実装の罠＝正本の末尾に3本の
レポートと「実装前に必ず読む要点」8項目として追記した（`docs/research/cornguilds2e_rules.md`）。**最重要の2件**：
1. 🛑 **商人ギルド(Merchant Guild)と財源(Coffers)が出荷済み `guilds` セットで旧則（2018）のまま＝実バグ**。
   公式（2021）＝「**購入フェイズ終了時**に、そのフェイズで**獲得した**カード1枚につき +1財源（使用回数ぶん累積・
   ヴィラで複数回・Ferryman の追加獲得も数える）」／「財源は**ターン中いつでも**使える」。本アプリは「購入するたび即
   +1財源・購入フェイズ中だけ使える・**購入後の同フェイズに使えてしまう**」。→ **第3バッチと同時に直す**
   （`endBuyTail` 系＋ヴィラ/騎兵隊の `midBuyPhaseEnd` の両方＝§0-32 の鉄則）。
2. **Courser は公式には長老(Elder)の対象**（wiki `Elder` の一覧に明記）＝登録しないなら「同盟9種のみ」の既存簡略化に載せる。
   **Princess の 2022 エラッタ（"this turn"）は未印刷・デジタル限定**＝Journey と同じ採否クラス（Renown は印刷済み）。
他＝渡し守の山からの獲得は `gain()` の置き方（`GAIN_TO_HAND`・帳簿）をそのまま使い `pileEmptied` だけ呼ばない／
**実験(Experiment)** も名指し自己獲得・自己返却の対象／塔(`isFromEmptySupplyPile`)は渡し守の山由来なら false／
**大使の「サプライに戻す」と「その山へ戻す」を述語で分ける**／一騎討ちの褒賞獲得は pending を先に閉じてから
`gain(...,'hand')`（`TOURNAMENT_PRIZE` のコピー禁止）・褒賞の選択 UI/CPU は明示列挙（`modalGainSupply`/`bestGain` は
`NON_SUPPLY` を弾く）／野盗の +1カード＝獲得した瞬間のフェイズ・誰の獲得でも・`_chamOff` 付き `draw()`。

### ✅ 段階1＝**未実装33種のカタログ＋絵＋webp が全部入った**（2026-08-23・`sw.js` v84）
**`DOM.CARDS` 585→618（縦型32）／`DOM.LANDSCAPES` 226→227（横型1＝召喚）＝合計 811→844枚。全部に絵入り。**
- 絵＝ユーザーが Codex 経由で ChatGPT に生成させた `asset/dominion_card_art_33.zip`（manifest.csv 付き）。
  manifest の name でアプリ側と突き合わせて **33/33 一致**→ `asset/art/<id>.png` に配置 → webp 生成
  （縦型 119〜184KB／横型 召喚 92KB＝全枚絵入りサイズ・モンタージュで全33枚の絵と名前を目視）。
- 日本語名＝**日本語wiki で裏取り済み**（Goons＝**ならず者**／Loan＝**借金**／Mountebank＝**香具師**／
  Marchland＝**境界地**＝2024年11月ホビージャパンのキャンペーンで配布済み）。
  ⚠ **収穫祭＆ギルド第2版だけ日本語版未発売**＝日本語wiki の仮訳を採用（装蹄師／店／診療所／耕作者／謝肉祭／
  渡し守／野盗／一騎討ち／駿馬／御料地／ハスカール／大きなかぶ／名声）。
  🛑 **Coronet は既存の賞品 `diadem`（宝冠）と日本語名が衝突する**ので「**小冠**」にした。
- 新プール＝`seaside1e`(8)／`prosperity1e`(9)／`cornguilds2e`(8)／`rewards`(6＝非サプライ)／`promo_new`(1)。
  **`DOM.STAGE1_POOLS` に5つとも入れた**（闇市場に出さない）。召喚は `DOM.EVENTS_PROMO` として定義だけ
  （`eventPoolFor` / `MIX_LANDSCAPE_POOLS` には**配線していない**＝段階2で）。
- 新種別 **`reward`（褒賞）**＝`carddata.js` の JP/EN・`ui.js` の TYPE_JP・`integrity.test.js` の JP/EN の4箇所。
  褒賞6種は engine `NON_SUPPLY`（`REWARDS`）と cpu `NON_SUPPLY_SET` の両方に登録（賞品と同型）。
- **封じ込めを機械検算**＝CARD_SETS 56種×60回の抽選で王国にも横型にも0枚／闇市場デッキ40局・18,520枚に0枚／
  mix-all のプールに未登録／`canBuyCard`・`BUY` が褒賞を賞品と同じく拒否／`emptyPileCount` に数えない。
- 資本主義の財宝集合 147→**156**（+9＝抑留/航海士/海賊船/交易路/香具師/ならず者/店/一騎討ち/駿馬＝
  「財宝でないアクション かつ +N コイン」と機械検算で一致）。
- `npm test` 全48スイート緑（整合性 5564→5850）／`verify:e2e` 9/9／カード一覧で33枚すべて実ロード（404ゼロ）。
⚠ **効果はまだ空（段階1）**＝次は段階2（engine/CPU/UI）。19種の正本＝`docs/research/missing19_rules.md`、
残り14種（収穫祭＆ギルド第2版）は **Ferryman の「サプライ外の山」** があるので段階0（研究）から。

### ✅ 段階0（その2）＝**収穫祭＆ギルド第2版14種の公式ルール正本を作成した**（`docs/research/cornguilds2e_rules.md`・90KB・2026-08-23）
収集3体（王国A＋総論／王国B／褒賞6種）＝**14/14 収録を機械検算**。一次資料＝英語wiki＋**RGG 公式第2版ルールブック PDF**。
✅ **敵対検証3体は同日中に実施済み**（上の「段階0（その2）の敵対検証」節＝正本の末尾に3レポートを追記）。
確定した要点（正本の冒頭「実装前に必読」15項目）：
- 🛑 **褒賞(Rewards) の山は「2人＝各1枚／3人以上＝各2枚（計12枚）」**＝賞品(Prizes) の「各1枚」と違う。
  段階1で書いた「各1枚」のコメントは誤り＝engine/cards のコメントを訂正した。
- 🛑 **Ferryman の山＝サプライ外の「山」＝専用フィールド `state.ferrymanPile = { card, cards }`**。
  `supply` にも `state[山キー]` にも置かない（4系統がサプライと誤認／`mixedPileWithTop` が拾う）。
  選び方＝`pickBane` 型の自動抽選・**ちょうど $3 か $4（3成分・静的コスト＝`costExact` は候補ゼロ）**・分割山は
  randomizer のコスト（城・卜占官・衝突・城砦・叙事詩・魔法使い・投石機・剣闘士・サウナが候補）・
  **その札が要求する準備も走らせる**（川船の7系統＋`initSupply` の系統）。獲得＝Ferryman の on-gain で
  どの経路でも。`pileEmptied` は呼ばない／交易商人で置換されたら山へ戻す。
- **過払い＝BUY／闇市場の2経路だけ。額＝軽減後コストを超えた残コイン。** Farrier の「ターン終了時 +N」は
  `t.squirrelDraw` の位置（先引きの後）。Infirmary の「N 回使用」は捨て札から `playCardNoAction`（継続と同型）・
  **習性を毎回選べる**。Farmhands は `p.eventSetAside`＋`event_play`。Shop は `conclaveTargets` 流用で
  `conclave_bonus` だけ積まない。Footpad の常設ルールはシャーマン／官僚制と同型。

### 🛑 段階0の副産物＝**過払い(Overpay) の出荷済み実バグを修正**（2026-08-23）
`maybeStartOverpay` が `state.pending` を**無条件に上書き**していたため、**望楼を手札に持って名品を買い
残コインがあると、`gain()` が立てた望楼の窓が過払いの窓に潰され、過払いを0で閉じても二度と開かない**
（残コイン0なら開く＝非対称。交易商人も同型。node で再現）。公式（2022エラッタ）＝
`"overpay" abilities happen when the card is gained, and **are timed with other such abilities**`。
→ 窓が既にあれば `onGainQueue` に `overpay_ask` を積み、消化側で `overpay` を開く。
ギルド 81→**88件**（バグ注入＝旧形に戻すと赤を確認）。**Farrier／Infirmary を足す前提条件**でもあった。

### ✅ 段階0＝**未実装19種の公式ルール正本を作成した**（`docs/research/missing19_rules.md`・308KB）
既存機構でほぼ書ける19種（海辺 第1版8／繁栄 第1版9／プロモ2）を多エージェント10体で研究
（5群を起草 → **各群を別エージェントが一次資料に当たり直して敵対検証**）。**19/19 収録を機械検算**。
🛑 **敵対検証が起草を覆した重大な訂正2件**（正本の冒頭「実装前に必読」に反映済み）：
1. **`canReturnToPile` / `availableInSupply` は「サプライ由来か」を表さない**＝
   **非サプライ山も `state.supply` に数値キーを持つ**（略奪品15／狂人10／傭兵10／賞品／馬）ので両方 true。
   そのまま大使(Ambassador)を書くと**賞品や略奪品をサプライに戻して全員に配れる＝枚数の不変条件が壊れる**。
   ⇒ **`!NON_SUPPLY.has(id)` の併用が必須**（真の前例＝§0-37 の `receiveTributeTargets`）。
2. **英語wiki の Versions 表は、印刷済みの行だけ Announced と Printed を `colspan="2"` で統合する**
   ＝そこを「未印刷」と読み違えると版の判断を丸ごと誤る。抑留の 2019-09「If you did」は未印刷だが
   **2020-10 に同じ条件付きで印刷済み**＝「未印刷エラッタを採るか」（§4 D1 の旅行の論点）は**存在しない**。
⚠ **残る14種（収穫祭＆ギルド 第2版）は Ferryman の「サプライ外の山」があるので別途研究する。**

### 未対応（この節では手を付けていない・次にやる候補）
- 🛑 **出荷済みの実バグ（正本の敵対検証で判明・第2／第3バッチで直す予定）**：
  (a) **商人ギルド／財源が2018年の旧則**（上の「段階0（その2）の敵対検証」1）。
  (b) **ティアラ(tiara)／収集(collection)が「場にある枚数」で実装されている**＝公式は「このターン」型（使用回数＝`t.insignia` と同型）。
  Royal Seal／Goons（「場にある間」型）を足すときに**共通化してはいけない**（見た目が似るが別ルール）。
  (c) **追跡者(tracker)の `travelling_fair` 窓が `src:` で積まれ UI が「移動遊園地」と誤表示**（他は `source:`）。
  (d) **遊牧民の野営地(nomad_camp)のカタログ文が2011年版のまま**（2016エラッタ＝`This is gained onto your deck`）＋
  実装が「獲得後に移動」なので **せっかちな／急速拡大（／召喚）が脇に置けない**＝`gain()` の `dest` 決定側で `'deck'` に置換する
  根治が要る（webp 再生成）。
  (e) **Baker は闇市場デッキに居ても開始時 +1財源**（2024RB 明記）＝`kingdom.includes('baker')` だけでは漏れる。
  (f) **`revealFromDeck`／`farming_village`／`fortune_teller` が「2度目のシャッフル禁止」（メイソン団）を守っていない**（mix-all 限定）。
- ✅ **【2026-08-24・§0-41 で完済】日本語名の誤り**＝**「14件」ではなく実際は50件**だった（騎士2件と
  基本/陰謀/海辺/錬金術/繁栄/収穫祭/プロモ を監査していなかった）。全844枚を日本語wiki と行完全一致で
  突き合わせて修正済み＝**同名カードの重複4組もすべて解消**（役人／従者／使者／投資）。詳細＝§0-41。
- ✅ **【2026-08-24・据え置きと決定】`harem`（ハーレム）は改名しない**＝`Farm` は Dominion Online と
  ドイツの別アート版だけで、**印刷版の陰謀はまだハーレム**（Donald X. 逐語＝`someday it may replace Harem in Intrigue`）。
- ✅ **【2026-08-24・完済】`hoard` の二重登録を解消**＝`POOLS.promo` から外した（繁栄のカード）。母集団 13→12。
- ✅ **【2026-08-24・完済】`alchemy` の CARD_SET 表示名**＝「錬金術セット（第二版）」→**「錬金術セット」**（錬金術に第2版は無い）。
- ✅ **【2026-08-24・完済】要塞(Stronghold) の種別の並び順**＝`['action','duration','victory','fort']` に直した
  （表示が「アクション・持続・勝利点・城砦」＝公式順になった）。**webp を焼き直し済み**。

---

## 0-39. **旭日＝王国25種の敵対レビュー（2体）＝確定10件を全修正**（2026-08-22・未push・`sw.js` v82）

§0-38 で予言とイベントを洗ったので、残る**王国カード25種**を13種／12種に分けて敵対レビューさせた。
**確定10件（[medium]4／[low]6）＝全件を自分で node 再現してから修正**。
**うち1件は `random-promo` で今日到達する出荷済みバグ**。

### [medium] 4件
1. **公家(Aristocrat) が場に0枚のとき +3購入を出していた**（公式FAQ＝**0枚・9枚・10枚は何も起きない**）。
   素の `n % 4` だと **0枚が「4枚と同じ」に落ちる**。命令（船長／大君主／はみだし者／王子）は
   カードを場に出さないので**必ず0枚**＝**`random-promo` の闇市場で到達する**
   （`STAGE1_POOLS = []` なので旭日カードが闇市場デッキの母集団に入る。実測で 97/97 の局に `aristocrat` が入り、
   うち 28/97 は王国に船長か王子が居た）。**CPU は闇市場で旭日を買わない＝人間だけが通る道**。
2. **交替(Change) が廃棄カードのコストを獲得後に測り直していない／差の絶対値でもない**。
   公式逐語＝`You re-check the costs of **the trashed card and the gained card** right before calculating
   how much D to take.`（行人→属州で**負債0**）／`If the gained card **now costs less**, **you still take D
   for the difference**.`（漁師→銀貨で**負債2**）。実測＝行人 2／漁師 1／デストリエ 2（公式は 0／2／3）。
   🛑 **正本が「廃棄した時点で差を確定する素朴実装は必ず壊れる」と名指しで事前警告していた箇所**。
3. **札差(Rice Broker) が廃棄時効果を全部解決する前にドローしていた**。日本語wiki 逐語＝
   `+5ドロー(=③の処理)を行った後に「手札に青空市場があるのでリアクションする」という動きはできない。`
   ＝`onTrashQueue` の消化より先に引くと**リシャッフルの母集団そのものが変わる**（実測で最終手札が 0枚 vs 2枚）。
   → **`t.riceBrokerResume` ＋ reduce 末尾の再開網**（正本が指定した形）。
4. **狐(Kitsune) の選択肢が記載順に解決されず、獲得時対話を握りつぶしていた**。公式FAQ＝`do them in the
   **order listed**`。旧実装は「呪いだけ最後にまとめて開く」形で、`kitsuneEnterVictim` が `state.pending` を
   無条件に代入するため**銀貨の獲得が開いた望楼の窓が消えた**。→ `kitsuneStep` で1つずつ解決し、
   pending が立ったら `t.kitsuneRest` に積んで中断する（長老の `t.elderRest` と同じ形）。

### [low] 6件
- **浪人(Ronin)** が `draw(7 - hand.length)` の一括ドローで、**-1カードトークンだと6枚で止まっていた**
  （公式＝`draw cards **one at a time**`／日本語wiki＝「実質的に影響を受けない」）。
  ⚠ 1枚ずつにしただけだと今度は**0枚**になる（トークンが食った回を「引けなかった」と誤判定）＝
  **終端は「山札と捨て札が両方空」で判定する**（正本 ⚠2 の指定どおり）。
- **浪人 × カメレオンの習性**＝`Draw until you have 7 cards in hand` が **+$7 に変換されていた**。
  公式＝`only **+Cards** is affected by Way of the Chameleon`。→ `state._chamOff` で `draw()` の変換フックを外す。
- **川船(Riverboat) の「このゲームで使わないカード」判定が2重に不完全**＝
  (a) **混合山の中身は `kingdom` に載らない**（載るのは山キー）ので**使用中の騎士9種・小さい城**を脇札にしていた
  （騎士と同居で **66%**）／(b) **来寇(Approaching Army) の11山目と衝突**していた（**出荷セットで 2.5%**）。
  公式が禁じた `The chosen card **does not have a pile**` を破る＝「無料の11枚目」が毎ターン使える状態。
- **支配(Possession) 中、オンラインで被支配者の山札の影札が見えない**＝engine は `PLAY_ACTION` を受理するのに
  UI の影の群が出ず**人間だけが詰む**。→ `revealHand` と同じ条件で**影札の位置だけ**晒す。
- **田舎の村の「捨てない」が0枚選択では押せなかった**（`allowZero = false`。同じ旭日の川の社は true＝拡張内で不整合）。
- **狐のリアクション窓は「呪い」を選んだときだけ開く**＝**許容簡略化として維持**（下記）。

### 検証
- **npm test 全48スイート緑**＝`risingsun-r4` 209→**230件**／`risingsun-ui` 96→**98件**。
- **バグ注入10種で 10/10 検出**（公家の 0/9/10 ガード／交替の測り直し／札差の再開網／狐の記載順／
  浪人の1枚ずつ／カメレオンの `_chamOff`／川船の混合山ガード／来寇の除外／支配中の影札看破／
  田舎の村の `allowZero`）。⚠ **`allowZero` は engine テストからは見えない**ので UI テスト側に
  「任意の窓は1枚も選ばない状態で押せるボタンがある」の恒久検査を新設した。
- **新旧 engine の完全並走 310ゲーム・62,634ステップ・差分0**（旭日を含まない出荷31セット）。
- **CPUソーク 52ゲーム・問題0**（予言15種×2〜3人＋今回触った6枚を強制混成×2〜3人＋mix-all 10）。
  膠着・例外・保存則違反・CPU null・engine が状態不変で拒否した手 すべて0。
- `verify:e2e` 9/9／`verify:visual` はみ出し0。

### 既知の許容簡略化（意図的・§0-39）
- **狐(Kitsune) のリアクション窓は「呪い」を選んだときだけ開く**。公式は日本語wiki 逐語＝
  「他プレイヤーが【アタック誘発リアクション】でリアクションするのは、狐使用者が**使用する効果を選択する前**である」
  ＝**選択の前に全員へ窓を開く**。⚠ **失われるのはパトロンの +1財源だけではない**
  （`hasReaction` は**馬商人／番犬／隊商の護衛／物乞い**も含む＝番犬の +2〜4カード・物乞いの銀貨2枚・
  馬商人の脇置き＋次ターン +1カードが丸ごと不発）。忠実化するなら `attackWindowEnter` ＋ `immune[]` 方式。

### 注意（次セッションが知らないと事故る・§0-39 固有）
- **「+N カード」という文字列を持たないドロー**（浪人の `Draw until you have 7 cards in hand`）は
  **カメレオンの習性で変換してはいけない**＝`state._chamOff` で `draw()` のフックを外す。
- **「N枚になるまで引く」は1枚ずつ引き、終端は「山札と捨て札が両方空」で判定する**
  （「引けた枚数が0なら止める」だと -1カードトークンが食った回で誤って止まる）。
  ⚠ **物見やぐら・呪われた村の同型ループは同条件で0枚になる既存クラスの穴**（旭日が持ち込んだ退行ではない）。
- **「このゲームで使わないカードを脇に置く」準備（川船／ハツカネズミ）は混合山の中身を `kingdom` から
  逆引きして弾く**（`kingdom` には山キーしか載らない）。**11山目を後から足す準備（来寇）とも衝突させない**。
- **選択肢を順に解決する効果は、pending が立ったら残りを `t.*Rest` に積んで中断する**
  （その場で全部回すと、途中の獲得/廃棄が開いた窓を後続が上書きして消す）。

---

## 0-38. **旭日＝予言15種の敵対レビュー（2体）＝確定11件を全修正 ＋ イベント10種を自前検算**（2026-08-22・未push・`sw.js` v82）

§0-37 で作り切った旭日を、**予言15種の忠実性だけを見る敵対レビュー2体（8種／7種に分割）**で洗い直した。
**確定11件（[high]1／[medium]5／[low]5）＝全件を自分で node 再現してから修正**。
並行して**イベント10種を正本の逐語と1件ずつ突き合わせ**（自前検算＝35＋α件）、うち**官僚制で実バグを1件発見**した。

### 🛑 [high] **山に戻せないカードを場から抜いてゲームから消していた（保存則違反）＝3箇所**
`canReturnToPile` を確認せずに `removeOne` → `returnToPile` していたため、**戻す山が無いとカードが1枚消滅する**
（オンラインは state をそのまま配信・永続化するので**部屋のカード総数が恒久的に狂う**）。
| 場所 | 到達 |
|---|---|
| **濡女(Snake Witch)** | 神風で王国10山が撤去された後／闇市場で買った濡女。しかも**戻せていないのに呪いを配っていた**（engine のコメント自身が「戻せたときだけ」と書いていた） |
| **馬の習性(Way of the Horse)** | **出荷済みの穴**（闇市場×習性＝神風なしでも踏む） |
| **チョウの習性(Way of the Butterfly)** | 同型。**レビューは「ガード済み」と誤認したが、自分で `returnToPile` の全呼び出しを洗って発見した** |
⚠ **窓を開く側も `canReturnToPile` に統一した**（チョウは `supply` を直読みしていた＝戦利品・混合山の扱いが違う）。

### [medium] 4件
1. **神風の「新しい10山の準備」が4系統まるごと漏れていた**。公式逐語＝`Deal out 10 new Kingdom cards.
   **Do any Setup for them that they require**`。`applyDivineWind` は `initSupply` しか走らせておらず、
   **`createInitialState` が `initSupply` の外でやっている**廃墟／馬／災いカード(Bane)／川船の脇札 が作られない。
   実測400回中 **廃墟96・馬191・災い34・川船28**＝「配られるのに機能が丸ごと死ぬ」。
   **川船は旭日単独で68%**（旭日プールは25種しかないので新10山に入りやすい）＝mix-all を待たずに踏む。
2. **進歩(Progress) が stop-moving を無視して手札から奪い返していた**。`zones` に `pp.hand` を足していたため、
   公式が名指しした2例が**両方逆**（そりで手札に移した札を奪う／**ヴィラが手札から消えて使えなくなる**）。
   → **獲得先(`q.dest`)だけを探す**＝「ゴーストタウン＝進歩が勝つ／ヴィラ＝進歩が負ける」がちょうど一致する。
3. **豊作／狼狽が「財宝の使用」を取りこぼしていた**＝`playTreasureCard` にしかフックが無く、
   **`playCardNoAction` を通る財宝**（薬草集め／急使／宝珠／呪符の巻物／博打／苦労／進軍／突貫／侵略…）で
   予言が静かに空振り。さらに**アクションフェイズの Action-Treasure**（冠／資本主義下のアクション）にも効かない。
   → **共通ヘルパ `noteTreasurePlayedForProphecy` に集約**し、`playTreasureCard`／`playCardNoAction`／
   `PLAY_ACTION` の3経路から呼ぶ。
4. **悟り(Enlightenment) が Action-Treasure に効かなかった**。`!DOM.isType(card,'action')` でゲートしていたが、
   公式逐語は `Enlightenment **even applies to Treasure cards that are already Actions**` ＝**逆**。

### [low] 5件
- **豊作が発動前に使った名前を記録せず、遡って恩恵を出していた**（純・旭日で到達＝継続→前兆）。
  **記録は `prophecyActive` の外で毎ターン行い、恩恵だけをゲートする**。
- **偉大な指導者が相続の屋敷で +1アクションを出さない**（積む側は `inheritedEstate` を見ているのに消化側が静的判定）。
- **成長が資本主義で財宝になったアクションで誘発しない**（`isTypeSupply` → **`isTreasureFor`**）。
- **悟り × カメレオンの習性**＝カメレオンだけは「指示に従わせる」習性なので**悟りが勝つ**（公式）。
  一律に習性を先に返していたため**アクション権を1消費して何も起きない**（丸損）状態だった。
- **病／神器が「開始時効果の途中で予言が発動したターン」に働かない**。
  `resolveDurationStartEffects` はキューを**組み立てる瞬間**に `prophecyActive` を見るので、その後に
  川船／遅延／王子が脇の前兆を使って発動しても当ターンぶんが積まれない。
  → **`popStartQueue` がキューを畳む直前に1回だけ再チェック**（`t.rsStartQueued` で二重に積まない）。
  ⚠ **安全網の条件も `.length` だけでは空配列を素通りする**ので `|| !rsStartQueued` を足した。

### 🛑 自前検算で見つけた実バグ＝**官僚制(Bureaucracy) が負債コスト・ポーション費用のカードを取りこぼす**
「コスト0でないカードを獲得したとき銅貨1枚」の判定を**コイン成分だけ**で見ていたため、
**大名($0+負債6)／絵師($0+負債8)／山の社($0+負債5)／大君主／技術者／ブドウ園($0+P)／変成** で銅貨が付かなかった。
正本の逐語＝`コイン以外のコスト表記がある時点で0コストのカードではない`。→ **3成分の判定**に修正。

### イベント10種の自前検算＝**実装はすべて正しかった**（35＋α件が一致）＋正本が名指しした回帰を固定
| イベント | 固定した罠 |
|---|---|
| **蓄積** | `inPlay` + **`durationCards`** の実物だけを見る（公式FAQ が持続を名指し）。**永続持続（雇人）が居座ると以後ほぼ永久に空振り**／悟り発動中は場の財宝もアクション |
| **苦行** | 上限は `min(残コイン, 手札枚数)`・**0 が合法**・ちょうど N 枚・廃棄は1枚ずつ（城塞が手札に戻る） |
| **信用** | 🛑 **夜行の除外リストを書かない**（人狼＝アクション+夜行は取れるのが公式）／負債は**獲得後**のコスト（デストリエ 6→5）／**支配中はどのプレイヤーも負債を負わない** |
| **海上交易** | 🛑 廃棄の上限は「**最初に数えた場のアクション枚数**」＝**実際に引けた枚数ではない**。**-1カードトークンを持たせた回帰**は正本が名指ししていたのに1件も無かった |
| **金継ぎ** | `gainedGoldThisGame` は `triggerOnGain` に置けば `gain` と `gainFromOutside` の両方で立つ（墓暴きで実測）／**青空市場の反応で金貨を得てから判定**（廃棄の後） |
| **川の社** | 🛑 **`t.bpGained` を掴んではいけない**（`END_ACTION_PHASE` で 0 に戻るので**継続で購入フェイズが2回**になると窓が開く）＝`t.buyPhaseGained` が正しい |

### 検証
- **npm test 全48スイート緑（exit 0）**＝`test/risingsun-r4.test.js` 96→**183件**。
- **バグ注入で感度を実証**＝敵対レビュー分 **12/12 検出**／イベント分 **7/8 検出**
  （8件目＝海上交易の上限を `min(acts, hand.length)` に丸める注入は**公式と観測上同値**なので素通り＝正しい）／
  川の社 **2/2**／官僚制 **1/1**。
- **新旧 engine の完全並走**（毎ステップ CPU の手と state 全体を突き合わせ）＝旭日を含まない出荷**31セット×
  5シード×2〜3人＝310ゲーム・62,634ステップ・差分0**。唯一の差は `turn.startQueue` の空表現（`[]`⇔`null`）＝同値。
- **CPUソーク 58ゲーム・問題0**（予言15種×2〜3人＋神風×mix-all 12＋既存8セット×2）。
  膠着・例外・保存則違反・CPU null・**engine が状態不変で拒否した手** すべて0。
- `verify:e2e` 9/9／`verify:visual` はみ出し0。`sw.js` v81→**v82**。

### 注意（次セッションが知らないと事故る・§0-38 固有）
- 🛑 **「山に戻す」効果を書くときは必ず `canReturnToPile` を先に確認してから場から抜く**。
  確認せずに `removeOne` → `returnToPile` すると**カードがゲームから消滅する**（闇市場で買った札・神風で
  撤去された山の札）。**窓を開く側も同じ述語を見る**こと（`supply` の直読みは戦利品・混合山でずれる）。
- 🛑 **神風に新しい派生セットアップを足すときは `applyDivineWind` の ⑤ ブロックにも足す**。
  `initSupply` の中にあるもの（ポーション・賞品・略奪品・精霊・祝福/呪詛）は自動だが、
  **`createInitialState` が `initSupply` の外でやっているもの**（廃墟・馬・災いカード・川船の脇札）は手動。
- **「財宝を使用した」ときのフックは `noteTreasurePlayedForProphecy` の1箇所に書く**
  （`playTreasureCard` だけに書くと `playCardNoAction` 経由が全部落ちる）。
- **`t.bpGained` は `END_ACTION_PHASE` で 0 に戻る**＝「このターン購入フェイズに獲得したか」は
  **`t.buyPhaseGained`**（ターン中一度も落ちない）を使う。継続／ヴィラ／騎兵隊で購入フェイズが2回になると割れる。
- **進歩(Progress) のような「獲得したカードを動かす」効果は獲得先(`dest`)だけを探す**＝
  探す場所を増やすと stop-moving の線引き（獲得**先**が特殊 vs 獲得**後**に移動）がそのまま壊れる。

---

## 0-37. **旭日（Rising Sun）＝段階2 R4〜R7 完了＝17拡張目が実プレイ可能になった**（2026-08-21・未push・`sw.js` v81）

**旭日50種すべての効果が動き、`CARD_SETS` に昇格した**＝
`risingsun`（固定10種）／`risingsun-events`／`random-risingsun`／**mix-all も 16→17拡張**。
これで **17拡張＋プロモ＝811枚（縦型585＋横型226）が実プレイ可能**（**絵は旭日50枚だけ未回収**＝枠＋文字）。
正本＝`docs/research/risingsun_rules.md`（847KB）。

### バッチ構成（各バッチ末で `npm test` 全緑を確認してコミット）
| バッチ | 内容 |
|---|---|
| R4a | 予言9種（官僚制/成長/進歩/急速拡大/厳冬/偉大な指導者/来寇/豊作/好機到来/神器/病）＋**前兆2種**（狐・川の社＝R3 で漏れていた） |
| R4b | 狼狽(Panic)／盛大な取引(Flourishing Trade) |
| R4c | **神風(Divine Wind)**＝準備手順がゲーム中に再走する唯一の機構 |
| R4d | 悟り(Enlightenment) の基幹＝`isActionFor` と使用時の置換 |
| R4テスト | `test/risingsun-r4.test.js` 96件（**バグ注入6種で感度確認**）＋徴税の横取りバグ修正 |
| R6 | 残りの王国5種（大名/川船/絵師/米/侍） |
| R5 | イベント10種 |
| R7 | CARD_SET 昇格 |

### 予言15種で踏んだ公式裁定（次に触るとき忘れると壊す）
- 🛑 **予言の効果は必ず `prophecyActive(state, id)` で書く**（`hasProphecy` はゲーム開始直後から真＝準備処理専用）。
- 🛑 **§E の一般則＝予言はその瞬間に有効になる。ただし「起動した前兆自身が恩恵を受けるか」は誘発点で決まる**：
  **「後（After／たび）」型は受ける**（偉大な指導者・来寇）／**「先に（first）」型は受けない**（豊作）。
  ＝略奪の "next time" 型（その1枚自身は誘発しない）とは**正反対**なので `fireNextTime` を流用してはいけない。
- **偉大な指導者／来寇＝`notePlunderPlay` の隣に `t.prophecyPlayed` を無条件で積み、reduce 末尾で消化**する
  （🛑 `t.allyPlayed` に相乗りすると `state.ally` が3種のときしか積まれず**同盟が混ざらない対局では1度も発火しない**）。
  **有効判定は消化側で行う**。消化は**港の村の `hvPending` 精算より後**（公式＝来寇の +$1 は港の村の判定に影響しない）。
- **偉大な指導者の +1アクションは必ず `addActions` を通す**＝公式が名指しした唯一の例外＝**雪深い村**
  （1アクション権から雪深い村を使うと残り**4**）。
- **来寇の準備＝11山目（アタックの王国カードの山）を追加する。予言が発動しなくても起きる**
  （＝`hasProphecy` を使ってよい数少ない場所）。**コスト制限は無い**（ポーション/負債コストのアタックも可）／
  アタック判定は**randomizer（山キー）の静的 `types`**（`isTypeSupply` を使うと分割山で誤判定）。
- **神器＝ターン開始時＋最後の Sun を取り除いた瞬間の2つの発動点**（＝**同じターンに2枚あり得る**。
  1ターン1回のガードを入れてはいけない）。**コスト制限なし**＝同盟の `woodworkersCanGain` をそのまま使う。
  🛑 **発動フックで `state.pending` を直接立てない**＝`queueProphecy` に積む
  （前兆6種のうち4種が直後に自分の窓を開いて上書きする）。
- **病＝「ちょうど3枚」捨てる**（`discardDownEnter`＝N枚に**なるまで** を流用すると公式違反。正しい前例は `FORUM_DISCARD`）。
  呪いの山が空でも「呪いを獲得」を選べる（＝終端保証。「候補ゼロなら窓を開かない」定石をここに適用しない）。
- **好機到来＝クリンナップ開始時に手札を伏せて脇へ。🛑 ドロー5枚は普通に行う**
  （＝次の手札は**5枚＋脇の枚数**／相手のターン中も手札は空にならない＝リアクション機構は一切変更不要）。
  同じバケツの**友好的な／忍耐強い／川の社より後**に置く（前者2つは手札から取るので先だと死ぬ）。
- **狼狽＝2つの効果は完全に独立**（公式FAQ＝場に出した後に発動しても「山に戻す」だけは起きる）＝
  「使用時に旗を立てて片付けで見る」実装は公式違反。(a) `playTreasureCard` の**入口**（効果適用の外＝
  追いはぎで効果を消されても +2購入は出る）／(b) `discardFromPlayRouted`（場から捨てる唯一の入口）。
- **盛大な取引＝得点計算にも効く**＝`scoringCost` に足す（`cardCost` は最終ターンの橋/街道を拾うので使えない）。
  公式＝得点に効くコスト軽減は**安価な(Cheap) と 盛大な取引だけ**。
  「アクション権を購入権として使う」は**置換型**（`buysAvailable` / `spendBuy`＝新 action を作らない）。
- **悟り＝`isTreasureFor`（資本主義）の完全な鏡像 `isActionFor`**。**得点計算には効く**（資本主義とは真逆）。
  🛑 **「山の種別」は変わらない**（公式逐語＝`Enlightenment only affects cards and not piles.`）＝
  randomizer を見る側（植民／災いカード／山トークンの置き先／発明家の家族／汚された神殿／オベリスク）には使わない。
- **神風＝`supply[id] = 0` ではなく `delete`**（0 だと `emptyPileCount` が「空」と数え、10山撤去した瞬間に
  3山終了が成立して即死）。**保存則ハーネスに `state.kingdomEpoch` を新設**＝epoch が変わった瞬間だけ基準を取り直す。
  抽選元＝**今の王国カードが属する拡張プールの和集合**（作者発言からの推定＝公式の逐語は無い）。
  **予言は配り直さない**／**特性・オベリスク・災いカードは撤去山にも効き続ける**／**調査(Search)は誘発しない**。

### R6（王国5種）で踏んだところ
- **絵師＝場に「ちょうど1枚だけ」あるカードの種類数**（🛑 `new Set(場).size` とは別物＝銅貨3枚は「0」）。
  魔法のランプと共通の **`singlesInPlay`** に切り出した。**0枚ドローは正常系**。
- **米＝場の「異なる種別」数**。`typeCountFor` を **`typesFor(state,pi,id) → string[]`** に一般化して共用
  （**相続した屋敷の `command`/`victory` の取りこぼしも同時に修正**／悟りにも自動追随）。
  `playAllOrder` の順位＝**米 → ペンダント → 大金**（同順位にすると手札の並び順で前後が決まって取りこぼす）。
- **侍＝永続持続**（`permanentDurationCounts` に `p.samurais`）＋**アタックは使用時の1回だけ**
  （忍者と同じ `discardDownEnter(...,3,...)` を共有＝自前モーダルを作ると堀/盾の穴を再発させる）。
  【許容簡略化】公式は「場を離れたら止まる」だが、本アプリの永続持続は全部「止まらない」で統一されている。
- **大名＝旗艦とほぼ同型**（旗艦＝持続で「次回・後のターンでも」／大名＝**このターン中だけ**）。
  🛑 大名は `duration` 種別を持たないので、**持続の仕分けゲートを `(isType(c,'duration') || cnt[c] > 0)` に緩めないと
  linger が黙って捨てられる**。使い切れなかった予約は自分の片付けで捨てる。**累積する**（大名2枚＋名匠＝3回）。
- **川船＝準備で「使わない・持続でない・ちょうど$5のアクション」1枚を脇に置く**。
  🛑 候補述語に **`costExact` を使うと必ず候補ゼロ**（`gainableBase` が supply>0 を要求する＝§0-28 悪魔祓いの裏返し）
  ＝静的コストで判定する。🛑 `pickMouseCard` の分割山・城・騎士の除外を**コピーしてはいけない**
  （公式が名指しした例＝騒がしい村・$5の騎士9種・小さい城 が落ちる）。
  脇札は**カードとして数えない**（`allCards`・保存則の外＝`mouseCard` と同じ）／**公開**／
  **その札が要求する準備も走らせる**（馬・祝福・呪詛・アーティファクト・戦利品・Ally・予言の7系統に配線）。

### R5（イベント10種）で踏んだところ
- **蓄積／海上交易の「場のアクション」＝`inPlay` ＋ `durationCards`**（公式FAQ が前ターンの持続を名指し）。
  ⚠ **永続持続を1枚出すと蓄積は以後ほぼ永久に空振り／海上交易は逆に永久に強くなる**。
- **信用＝負債は `addDebt`**（`takeDebt` はコスト欄を読むので効果では黙って0になる）。
  🛑 **支配のターンではどのプレイヤーも負債を負わない**（§0-23 の「負債は支配者が負う」と逆＝公式逐語）。
- **洞察＝新ゾーン `p.foresightAside`（公開）**。手札へ戻すのは**先引きの後**（＝手札6枚）。
- **金継ぎ＝新フラグ `p.gainedGoldThisGame`**（`triggerOnGain` に1箇所＝`gain`/`gainFromOutside` の両方を拾う）。
  🛑 判定は**廃棄の on-trash を全部解決してから**（青空市場が金継ぎの廃棄で金貨を得たら獲得段が有効になる）。
- **稽古＝フェイズを触らない**（購入フェイズのまま＝冠が財宝モードにならない。**継続と真逆**なので共通化しない）。
  **群A**＝山札の影札も選べる＝`A_WINDOWS` 表に1行追加（18→19）。
- **賛辞＝コスト制限が一切無い**（負債/ポーション費用も取れて負債も負わない）＝
  `gainableBase` から**コスト比較だけを外した** `receiveTributeTargets` を新設。
- **参集＝$3→$4→$5 の固定順**（賛辞は `in any order`＝逆）。**コストは各段の直前に評価し直す**。
- **継続＝`ONCE_PER_TURN_EVENTS`**（無いと無限ループする＝作者が明言）。
  🛑 **獲得は購入フェイズ扱い／使用はアクションフェイズ扱い**＝フェイズを戻すのは
  `onGainQueue` の `continue_play`（獲得時対話が全部片付いた後・使用の前）。1行ずれると両方壊れる。

### この作業で見つけた実バグ
| # | 内容 | 到達 |
|---|---|---|
| ① | **徴税(Tax) が `hasEvent(state,'tax')` を見ていなかった**＝`state.pileDebt` は徴税を使わないゲームでも `{}` として常に存在するので、**山に負債を置く別の効果（厳冬）の負債を横取りし、厳冬がまた2個置く**＝無限に負債が湧く | 旭日×帝国（出荷済みの経路では発火しない） |
| ② | **`typeCountFor` が相続した屋敷に `'action'` しか足していなかった**（公式は アクション-勝利点-命令 の3種別）＝略奪の「置き去り」が1種別ぶん取りこぼしていた | mix-all |
| ③ | **CPU の `vpOfPlayer` が engine の `vpOf` と乖離**（悟りで engine を動的にした時点で発生）＝hard CPU の終局読みがズレる | 旭日 |
| ④ | **蓄積の CPU が engine と違う述語を見ていた**（engine は「$5以下**かつアクション**」／CPU は `bestGain`）＝ **engine拒否×CPU提案で本番 livelock**（強制購入ソークで20000step 未終局を実測） | `risingsun-events` |

### 検証
- **npm test 全48スイート緑（exit 0・整合性5561・不変条件12）**／
  `test/risingsun.test.js` 476→**498件**／**`test/risingsun-r4.test.js` 新設113件**（48スイート目）／
  `test/risingsun-ui.test.js` 39→**96件**（新 pending 18種の網羅検査＝
  **engine と CPU は受理するのに UI に導線が無く人間だけ詰む**という本プロジェクト最頻の事故を構造的に防ぐ）。
- **バグ注入16種でテストの感度を確認**＝R4 の6種（指導者を `hasProphecy` に／病を民兵型に／神風を `supply=0` に／
  狼狽の「山に戻す」を殺す／盛大な取引を得点から外す／好機到来のドローを止める）＋
  レビュー修正の10種（継続のボーナスを消す／使用の前へ戻す／海上交易と蓄積を静的判定へ戻す／
  稽古から相続を外す／神風の川船除外を殺す／参集の pending を先に閉じるのをやめる／
  稽古の treasure_replay を殺す／金貨フラグを殺す）＝**すべて赤になる**。
- **UI テストもバグ注入4種で感度を確認**（病・継続・川船のモーダルを消す／稽古から影札の pool を外す）。
- **イベント強制購入ソーク 40/40 完走**（10イベント × 2王国 × 2〜3人。
  ⚠ **CPU は旭日のイベントを3種しか買わない**ので通常のソークでは経路が通らない＝
  §0-29 A5 の「闇市場×同盟＝人間だけが通る道」と同じ構図）。
- **神風発動後のCPUソーク 36/36 完走**（2〜4人 × 派生セットアップ入り王国＝若き魔女／ネクロマンサー／
  ドルイド／暗躍者／国境警備隊／ページ／農民／騎士／馬上槍試合）。
- **出荷3セット CPUソーク 36/36** ／ **広域ソーク 40/40**（旭日×他拡張の mix 6種＋**17拡張全部乗せ mix-all**）。
- `verify:e2e` 9/9（webp 811/0）／`verify:visual` 320〜768px 全幅はみ出し0。

### 既知の許容簡略化（意図的）
- **悟りの「for all purposes」は主要サイトだけ**＝使用（`PLAY_ACTION`）・得点（ブドウ園/果樹園/凱旋門）・
  種別ボーナス（貢物/鉄工所/変成/生贄/馬丁/札差）・UI の4箇所。
  **命令4述語（王子/船長/大君主/はみだし者）には広げていない**（`playAsCommand` が `applyEffect` を直接呼ぶので
  財宝を選んでも何も起きない死に選択肢になるため）。他の約250サイトも静的判定のまま。
- **悟り経由で財宝を使っても商人の「最初の銀貨」とサウナの `t.saunaPlays` は誘発しない**
  （`playTreasureCard` の中にあるため。mix-all 限定）。
- **厳冬は非サプライ山（戦利品/褒賞/馬）に負債を置かない**（`state.loot` に supply キーが無いため。mix-all 限定）。
- **厳冬×徴税の解決順は選べない**（既存の横断簡略化）。
- **苦行で手札枚数を超えて払えるか＝裁定未確認**＝`min(残コイン, 手札枚数)` に丸めた（払い損を出させない安全側）。
- **狐は「呪いを配る」を選んだときだけリアクション窓を開く**（公式はアタックカードの**使用時点**で公開できる
  ＝日本語wiki 逐語「他プレイヤーが【アタック誘発リアクション】でリアクションするのは、狐使用者が
  **使用する効果を選択する前**である」）。
  ⚠ **【§0-39 で訂正】失われるのは「パトロンの +1財源」だけではない**＝`hasReaction` は
  **馬商人／番犬／隊商の護衛／物乞い**も含むので、**番犬の +2〜4カード・物乞いの銀貨2枚・馬商人の脇置き＋
  次ターン +1カード・隊商の護衛の +1カード が丸ごと不発**になる（初出時の記述は影響を過小評価していた）。
  忠実化するなら `attackWindowEnter` ＋ `immune[]` 方式。
- **神風の抽選元＝作者発言からの推定**（公式の逐語は無い）。
- **CPU は旭日のイベントを3種（参集／蓄積／信用）しか買わない**（残り7種は ritual/banquet/windfall/tax と同じ skip）。
- **CPU は旭日セットを MONEY として遊ぶ**（`evaluateKingdom`＝他の9拡張と同じ既定方針）。

### 多エージェント敵対レビュー（UI詰み／既存16拡張への退行／忠実性／CPU非ループ）＝**確定9件を全修正**
各 finding は担当エージェントが **node / jsdom で再現してから確定**（偽陽性は棄却）。
**[high] 1件**
1. **賛辞(Receive Tribute)に「やめる」ボタンが1つも出ない＝人間だけが3枚を強制獲得**。
   `modalGainSupply` の footer は `(skipOnEmpty && (!elig.length || alwaysSkip))` ＝
   **第7引数 `alwaysSkip: true` だけでは辞退ボタンは絶対に出ない**（第6引数に関数が要る）。
   公式は "gain **up to** 3"＝任意で、engine も CPU も `card: null` を受理できるのに人間だけが断れなかった
   （空山が2つある局面では自分の意思に反してゲームを終わらせられる）。**出荷セット `risingsun-events` で到達**。
   ⚠ **engine のコメント自身が「やめるボタン必須」と書いていたのに UI が満たしていなかった**。
**[medium] 3件**
2. **継続(Continue) の解決順が逆**＝`continueReturnToAction` が「フェイズ復帰」「購入フェイズ終了時の窓」
   「+1ア+1購入」を1関数にまとめ、**使用の前に**全部やっていた。公式は
   `Return to your Action phase and play it. **+1 Action and +1 Buy.**`。
   → `continueReturnPhase`（①戻る瞬間）と `continueBonus`（③使用の後）に分割し、`onGainQueue` を
   `continue_play`（①＋後ろへ積む）→ `continue_play2`（②使用→③ボーナス）の2段にした
   （**キューは FIFO なので①の窓が pending を立てても順序が保たれる**）。
3. **蓄積・海上交易が「場のアクション」を静的判定していた**＝悟り下では場の財宝もアクション（公式）。
   同じ engine の米・稽古は動的判定なのに**この2枚だけ静的**＝内部で不整合だった。
   実測＝海上交易が +3カードのはずが**0枚**（$4 が完全に死ぬ）／蓄積が逆に**獲得してはいけないのに獲得**。
4. **CPU の `chooseAction` に R4a/R6 で足した7種が未登録**（川船/川の社/狐/山の社/大名/絵師/侍）＝
   **CPU が手札に来ても一度も使わない＝CPUソークがその経路を1度も検証しない**（§0-36 が名指しした罠）。
**[low] 5件**
5. **稽古が相続(Inheritance)した屋敷を対象にできない**（同型の窓は全部 `inheritedEstate` を通している）。
6. **参集(Gather)が獲得時リアクション窓を3つ潰す**＝`finishGain` は pending を残したまま `gain()` を呼ぶので
   望楼などの `!state.pending` ゲートに引っかかる。正本が名指しした**植民(Populate)型**（pending を先に閉じる）へ。
7. **神風の一意カード除外がデッドコード**＝`pl.riverboatCard` を読んでいた（正しくは `state.riverboatCard`）。
   実測で神風400回中34回（8.5%）、川船の脇札と同じカードが新しい王国山として配られていた → 0/400 に。
8. **稽古×悟りで財宝を選ぶと2回目が空振り**＝素の replay は `applyEffect` を呼ぶので財宝では case が無い。
   冠(Crown)と同じ `treasure_replay` 振り分けに（実測 +$2→**+$4**）。
9. **金貨フラグが連鎖の暴走防止ガードの後にあった**＝7段以上ネストした獲得の金貨を取りこぼす（金継ぎが過小判定）。
**UI の表示・防御**＝川船の脇札を盤面に（公開・買う前に見えないと判断できない）／洞察・好機到来の脇札を盤面に／
盤面の「N点」バッジが `vpOf` に `state` を渡していない（悟り下で終局画面と食い違う）／
強制獲得5種＋金継ぎの廃棄に「閉じる」導線／川船モーダルの「「null」を使う」／金継ぎに金貨の獲得済み表示。

**レビューでクリーンだったもの**＝新旧 engine の完全並走（出荷53セット×複数シード＝**446ゲーム・91,425ステップ・差分0**
／私の担当では 208ゲーム・46,683ステップ・差分0。唯一の差は promo-pack の**闇市場デッキ +25枚**＝R7 の意図どおり）／
`cardCost`・`scoringCost`・`vpOf`・`permanentDurationCounts` の並走（**82万回の比較で差分0**）／
CPU非ループ（**約4,300ゲーム**・livelock 0・`decide` の null 0・例外0）／
UI の全 pending 描画（**2,403回・23種**で押せる選択肢ゼロ 0件）／マスク（秘密9ゾーンの漏れ0・公開14ゾーンの誤マスク0）／
旧スナップショット互換（新フィールド13個を削除しても予言15種・出荷17セットとも完走）。

### 【次にやること】
1. **絵（webp）50枚の回収**＝ユーザーが ChatGPT で生成する必要がある。
   **指示文5バッチは作成済み**（scratchpad の `risingsun_art_prompts.md`）。
   ⚠ **連番＝生成順を信用せず、全候補から全単射で判別する**（過去に取り違えが起きている）。
   ⚠ 紛らわしいペア＝**侍／忍者／大名／浪人**（全部「武士」系）・**山の社／川の社**・**茶屋／魚屋**。
2. **push はユーザー確認**（未pushが17コミット）。
3. その後＝**第17拡張 Arcana**（2026年予定）だが**カード名すら未公開なので着手不能**。

---

## 0-36. **旭日＝段階2 R3（王国カード20種の効果）＝完了**（2026-08-21・未push・`sw.js` v79）

**影5種＋前兆6種＋素直な王国9種＝20種が実際に動く**（残る5種＝大名/川船/絵師/米/侍 は R6）。
旭日は `CARD_SETS` 未参照のままなので**本番挙動は不変**。正本＝`docs/research/risingsun_rules.md` 第2〜5章。

### 新機構＝**`addDebt(state, pi, n)`＝カードの効果で負債を得る唯一の入口**
🛑 既存の **`takeDebt(state, pi, cardId)` は cardId から `C()[cardId].debt`（＝コスト欄）を読む**ので、
効果で得る負債（名匠+2／室+3／勅使+2／駕籠+1／金山+4／交替＝コスト差）に使うと**黙って0になる**。
支配(Possession)の振り分けは `debtHolder` に一元化＝**負債を負うのは支配者**（両方が同じルールで動く）。

### 実装した20種（罠は正本の逐語で確認済み）
| カード | 要点 |
|---|---|
| 魚屋/浪人/茶屋/勅使/駕籠/室/公家 | 素直系。**公家は「場に出している公家の枚数」**で分岐（プレイ回数ではない＝公式FAQ） |
| **小路** | +1カード+1アクション→手札1枚を捨てる。🛑 **山札の影札は捨てられない**（群B・公式が名指し） |
| **忍者** | +1カード＋民兵型アタック。**`down` は 3**（剣は4）＝既存 `discard_down` を流用（新 pending 不要） |
| **狸** | 改築と同型。🛑 **`costUpTo` の第4引数 `spec` を必ず渡す**（公式例＝絵師 $0+負債8 を廃棄して大名 $0+負債6 を取れる）。**獲得では負債を負わない** |
| **歌人** | 🛑 **`costUpTo` を使わない**（`gainableBase` 込みでサプライ外を弾く。見るのは山札の一番上）＝`costLE(costOf(...))`。$3より高ければ**山札の上に残す** |
| **田舎の村** | 🛑 **ちょうど2枚**捨てて+1カード＝`discardDownEnter`（N枚に**なるまで**）を流用しない。捨てる→捨て札トリガー→**その後に**引く |
| **山の社** | 廃棄は任意だが**判定は廃棄しなくても必ず走る**。手札0枚なら窓を開かない |
| **濡女** | 手札が全部異なれば公開して**山に戻す**（獲得でも廃棄でもない＝`returnToPile`）。**戻せたときだけ**呪いを配る（アタック） |
| **札差** | 🛑 「財宝**かつ**アクション」なら **+2 と +5 の両方**（排他の if/else で書くと壊れる＝公式FAQ 明記） |
| **交替** | 🛑 「より高い」は**コイン成分だけの厳密比較**（`costUnder`/`costUpTo` は3成分＝使えない）。負債の差は**獲得を解決した後**に測り直す／**獲得できなければ負債を取らない** |
| **金山** | 「してもよい」は**セット**に掛かる（金貨だけ取って負債を避けられない）。**金貨の山が空でも「やる」を選べる** |
新 pending 12種はすべて**4点セット**（engine reducer＋`PLAYER_ACTIONS`＋CPU `decidePending`＋UI `viewPendingModal`）。
CPU の `chooseAction` にも20種を登録＝**足さないとソークで影札が1枚も使われず、山札からの使用経路の検証が0回になる**。

### 🛑 R2 の積み残しを1件塞いだ＝**資本主義×魚屋が購入フェイズに山札から出せなかった**
公式 Other rules clarifications 逐語＝`If you have bought Capitalism, you can also play Fishmonger from your
deck whenever you could normally play Treasures from your hand.` なのに `PLAY_TREASURE` が手札限定で、
**UI 側は光る**＝**押しても何も起きない死にボタン**だった（実測で再現）。
→ **1枚タップ（`PLAY_TREASURE`）だけ**通す。**「財宝を全部出す」（`PLAY_ALL_TREASURES`）には入れない**
＝ボタン1つで山札の影札まで出す事故を避ける（§0-24 の `playAllResume` の轍）。

### 🛑 保存則 fuzz が**自分の入れた実バグ**を捕まえた
**`finishGain` は boolean を返す**（state ではない）のに `return finishGain(...)` と書いてしまい、
**`reduce` が `true`/`false` を返して state 全体が消えた**（名匠・狸の2箇所）＝**オンラインなら部屋が丸ごと壊れる**。
CPUソーク40試行では再現せず、**`invariants` と同じシードで回して初めて出た**ため、テストにパッチを当てて
「`reduce` の戻り値が壊れた瞬間」を捕捉して特定した。既存の呼び方（`if (!finishGain(...)) return state;`）へ修正。
⚠ **`finishGain` / `takePlayable` / `playPlayable` は boolean を返す**＝`return` してはいけない。

### 検証
- **npm test 全47スイート緑（exit 0）**＝`test/risingsun.test.js` 424→**476件**（R3 の裁定を1枚ずつ固定）。

### 【次にやること】R4＝予言15種（**神風は最後**）
`prophecyActive` で書く（`hasProphecy` は準備処理専用）／窓が要る予言は **`queueProphecy`**（pending 直代入は禁止）。
**神器(Kind Emperor)は「最後の Sun を取り除いた瞬間」に `state.prophecyOnBy` の席だけが獲得する**。
**来寇(Approaching Army)は準備で11山目を追加**（予言が発動しなくても起きる＝`hasProphecy` を使う数少ない場所）。
その後 R5 イベント10種（`practice`＝稽古は**新設の群A窓**＝`A_WINDOWS` 表に1行足す）→ R6 残り5種 → R7 昇格。

---

## 0-35. **旭日＝段階2 R2（影 Shadow の基盤）＝完了**（2026-08-21・未push・`sw.js` v79）

**影5種（魚屋/小路/忍者/浪人/狸）の「効果」はまだ空**（R3 で実装）＝**山札から使える機構**だけを作った。
旭日は `CARD_SETS` 未参照のままなので**本番挙動は不変**。正本＝`docs/research/risingsun_rules.md` 第1章 §2。

### 実装（できたこと）
- **シャッフルするとき「シャッフルした束の一番下」に置く**＝`reshuffleDeck` の**「運命の(Fated)」ブロックの直後**
  （公式が `They can also be mixed with any other cards you specifically put on the bottom, such as Fated cards
  from Plunder.` と両者の同居を名指ししている位置）。**非対話**＝底の中の相対順は自動（許容簡略化）。
  ⚠ **獲得したときは底に置かない**（シャッフルのときだけ）／**底に置かれた後も普通に動く**。
- **山札のどこにあっても手札と同じように使える**＝`PLAY_ACTION` が山札の影札を受理する。
  **アクション権を普通に消費する**（Donald X. 逐語＝`you have to be allowed to play an Action, it doesn't get
  around that`）＝アクション権0では使えない。**手札にある影札は手札を優先**して出す。
- **共通述語**＝`deckShadows` / `handPlayable`（手札＋山札の影札）/ `canPlayFromHandOrShadow` / `takePlayable`。
  ⚠ **`handPlayable` を使ってよいのは「最終的に場(inPlay)に出る」窓だけ**（正本 §2-9 の群A）。
  **「手札から捨てる／廃棄する／脇に置く／山に戻す」窓（群B）には絶対に使わない**
  （判定の目安＝そのカードが最後に `inPlay` に入るなら群A、`discard`/`trash`/`setAside`/`supply` なら群B）。
- **`maskStateFor`**＝**自分の山札は「どの位置にどの影札があるか」まで見える**（見えないと山札から使う操作ができない。
  へそくり Stash と同型＝裏面が違うカードは位置が公開情報）。**相手の山札は伏せたまま**＝公式が明文で許すのは
  「**自分の**山札の裏面を見てよい」だけなので**据え置き＝許容簡略化**（決定D3）。
- **影は「手札」ではない**＝手札の枚数に数えない（小路の捨て札・地下貯蔵庫・民兵・書庫・浪人の「7枚になるまで」）。
  **逆に「所有カードの総数」には数える**（庭園/品評会/絹の道/城/博物館）＝`allCards` が `p.deck` を含むので自動的に正しい。
- CPU＝`chooseAction` の `has` 述語1箇所に足すだけで全カードに効く／UI＝**「影（山札から使える・手札ではない）」の
  専用群**を手札の下に出す（**手札の群に混ぜない**＝手札扱いでないことが見た目で分かる）。

### 検証
- **npm test 全46スイート緑（exit 0）**＝`test/risingsun.test.js` 106→**134件**。
  ⚠ `reshuffleDeck` は**全拡張のシャッフルが通る唯一の入口**なので、既存45スイートに退行が無いことが重要。

### R2 の後半＝**「手札から使わせる」18窓 × 4面の横断改修＝完了**
正本 §2-9 の群A（玉座の間／王の宮廷／行進／一等航海士／王家のガレー船／専門家／長老／市場の町／ゴンドラ／
苦労／コンクラーベ／インプ／杖／冠(action)／首謀者／門下生／侵略／**鼓舞する**）。
⚠ **研究doc の群A表は「鼓舞する(inspiring_play)」が抜けていた**＝正本を機械的に潰すと取りこぼす（下調べで検出）。
- **共通化できた3つ**＝`conclaveTargets`（コンクラーベ＋インプ）／`inspiringTargets`（鼓舞する）は
  **述語1箇所を `handPlayable` に寄せると engine窓・受理・CPU・UI の4面が同時に揃う**。
- 🛑 **`allyHandHasAction` は群A（市場の町＝使用）と群B（木工ギルド＝廃棄）で共有されていた**＝
  そのまま影対応にすると**木工ギルドが山札の影札を廃棄できる**（公式違反）→ `allyPlayableHasAction` に分離。
- 🛑 **`playCardNoAction` に `p.deck` を素で渡すと `fromHand` が偽になり、航海(Voyage)の3枚制限・将軍(Warlord)・
  `t.handPlays` の計上が黙って全部スキップされる**（＝「玉座経由なら航海の制限を突破できる」抜け道）→
  第7引数 `asHand` を新設し、ラッパ **`playPlayable`** が必ず立てる＝`PLAY_ACTION` と対称にした。
- UI＝`modalSingleHand` に**任意の第8引数 `pool`**（渡さなければ従来どおり手札だけ）＝
  **群B 30箇所超は1ビットも変わらない**＝退行リスクが構造的にゼロ。`modalPlayCardEvent` は zone 差し替えのみ。

### 🛑 ガードテストが**自分の改修の穴を13窓ぶん検出した**（4面整合の機械検査を新設）
`test/risingsun.test.js` に **群A 18窓のデータ駆動表**を置き、各窓を「**手札にアクション0枚・山札に影札1枚**」で
駆動して ①窓が開く ②engine が state 不変で拒否しない ③影札が山札から出て実際に使われる ④CPU が影札を返し
その手を engine が受理する ⑤膠着しない ⑥**群Bには漏れない** ⑦UI にチップが出る を検査する。
**新しい窓を足したら表に1行足すだけで4面が自動検査される。**
初回実行で **21件が赤**＝私の改修は**受理側が6窓しか直っていなかった**（窓・CPU・UI だけ広げていた）。内訳：
- **[high] 市場の町＝本番 livelock**（engine が state 不変で拒否 → CPU が同じ手を返し続ける）。
  敵対レビューの CPUソークで **market_towns の48戦中4戦（8.3%）が膠着**として実再現された。
- **[high] コンクラーベ＝リソースが湧く**（影札を選ぶと**カードは使われないのに +1アクションだけ手に入る**）＝
  `conclave_bonus` の予約をプレイの**前**に積んでいたため。**成功したときだけ積む**形に直した
  （専門家／王家のガレー船と同じ形）。
- **[high] 12窓で「UI にチップが出るのに engine が使わない」**（4窓は死にボタン／8窓は**プレイが丸ごと消える**＝
  「冠を使ったのに何も起きずに窓が閉じる」）。
- **[medium] `playPlayable` の山札分岐が4窓でデッドコード**（`hand.indexOf` の旧ガードが手前に残っていた）／
  **ゴンドラの onGainQueue 消化側が手札限定**（山札に影札しか無いと窓が開かない）／**侵略が4面とも未対応**。
- 感度は**6種のバグ注入**（窓だけ／受理だけ／CPUだけ／UIだけ／群Bへの漏れ／`asHand` 無視）で全件赤を実証。

### 検証
- **npm test 全47スイート緑（exit 0）**＝`test/risingsun.test.js` 134→**424件**／`risingsun-ui.test.js` 39件。
  既存46スイートに退行なし（`reshuffleDeck` と18窓を触ったので重要）。

### 【次にやること】R3＝影5種と素直な王国カードの効果
魚屋($2)／小路($4)／忍者($4・アタック)／浪人($5)／狸($5) ＋ 前兆6種の「+1 Sun」の配線（`removeSun` を
**効果の一番最初**で呼ぶ）。⚠ **小路の「手札1枚を捨てる」・忍者の捨て札・浪人の「手札が7枚になるまで」は
影札を数えない**（手札ではない）。その後 R4 予言15種（**神風は最後**）→ R5 イベント10種 → R6 残りの王国 → R7 昇格。

---

## 0-34. **旭日＝段階2 R1（前兆/予言/Sunトークンの基盤）完了**（2026-08-21・未push・`sw.js` v79）

**予言15種・前兆6種の「効果」はまだ空**（R3/R4 で実装）＝**器と発動タイミングだけを作った**（略奪 P1a と同じ形）。
旭日は `CARD_SETS` 未参照のままなので**本番挙動は不変**。正本＝`docs/research/risingsun_rules.md` 第1章 §1。

### 実装（`state` に3＋1フィールド／すべて**非カード**＝保存則 tally・`allCards` に入れない）
| フィールド | 意味 |
|---|---|
| `state.prophecy` | そのゲームの予言id（**王国に前兆(Omen)が1枚でもあれば1枚だけ**配る＝前兆が何枚あっても1つ）。公開・対局中不変 |
| `state.sunTokens` | 予言の上に残っている Sun トークン数（**2人5／3人8／4人10／5人12／6人13**）。公開 |
| `state.prophecyOn` | 予言が**発動済み**か（**取り除き切るまでテキストは一切効かない**）。一度立ったら下ろさない |
| `state.prophecyOnBy` | **最後の Sun を取り除いた席**＝神器(Kind Emperor)が獲得する唯一の席（R4 で使う） |
- **`removeSun(state, pi)` が「+1 Sun」の唯一の入口**（前兆の効果の**一番最初**で呼ぶ）。
  **最後の1個を取り除いた瞬間**に `onProphecyActivated`（**汎用**の発動フック）を**同期的に**呼ぶ
  ＝**神器と神風は「その瞬間」に即時発火する**ので、「次のターン開始時にまとめて」に遅延させると公式違反。
  **全部取り除いた後の「+1 Sun」は何もしない**（空振り＝前兆の残りは普通に解決する）。
- 述語＝`hasOmen(kingdom, extraIds)` / `sunTokensFor(n)` / `hasProphecy` / `prophecyActive` / `queueProphecy`。
  ⚠ **`hasProphecy` は準備処理専用**（来寇の11山目）。**予言の効果は必ず `prophecyActive` で書く**
  （`hasProphecy` で書くとゲーム開始直後から予言が効く＝公式違反。隣の `hasAlly` と形がそっくりで誤用しやすい）。
- 盤面に予言ブロック（サムネ＋「☀残り N個」／発動後は「発動中（ゲーム終了まで有効）」）。**残数は公開情報**。

### 敵対レビュー（3観点＝忠実性／既存機構・オンライン互換／UI・テストの質）＝**確定14件を全対応**
**[high] 2件**
1. **新設テストの感度がほぼゼロだった**＝レビューが**バグ注入4種（`hasOmen` の縮退／フック呼び出しの削除／
   フックへ渡す席のずれ／`prophecyActive` が `prophecyOn` を見ない）を入れても 54/54 緑のまま**であることを実証。
   とくに **「予言は配られているが Sun が残っている状態での `prophecyActive`＝偽」を一度も検査していなかった**
   （これは R3/R4 の全予言のゲート）。`ok(typeof s.prophecy === 'string', '前兆が2枚でも予言は1つ')` のような
   **原理的に落ちないトートロジー**も混ざっていた。→ テストを **54→106件**に作り直し、
   **6種のバグ注入で 6/6 検出**を機械確認（`_r1_sens.tmp.js` で engine のコピーを改変して実測）。
2. **UI 差分（予言ブロック24行）にテストが1件も無かった**→ **`test/risingsun-ui.test.js` を新設（39件）**。
**[medium] 4件**
3. **発動フックの契約が未定義＝R4 で必ず踏む穴**。フックは前兆の効果の**一番最初**から呼ばれるので、
   ここで `state.pending` を直接立てると**前兆6種のうち4種（川の社／田舎の村／狐／山の社）が直後に自分の窓を
   開いて上書きする**＝神器の獲得が黙って消える（**レビューが狐で実測**＝pending が `kitsune_choose` になる）。
   → **`state.prophecyQueue` と reduce 末尾の再開網**を先に用意し、「pending 直代入は禁止」をコメントで固定
   （`rotatePile` を A2 で先に用意したのと同じ＝R4 まで積む側は無い）。
4. 「フックが**その場で**走る」「フックが受け取る席」を検査していない→ `prophecyOnBy` を記録して固定。
5. テストが **`reduce()` を一度も通していなかった**（実ゲームループ・終局が0件）→ CPUソークで終局まで回し、
   3フィールドが生き残ることを検査。
6. **旧スナップショット互換**（v78以前＝3フィールドを持たない state）の検査が無い→ 追加
   （サーバは state を無変換で復元する＝抜けると部屋が固まる事故クラス）。
**[low] 8件**（主なもの）
- `removeSun` が**席を検証していなかった**（不正な席でもトークンを消費して `state.players[pi]` を触る＝R4 で TypeError）。
- **`hasOmen` が `kingdom` しか見ていない**＝R6 で川船(Riverboat) を実装すると、**茶屋($5)／狐($5) が脇札に来た
  ゲームでサプライに前兆が無いのに「+1 Sun」が走る**（正本 §1-8 の訂正Iが名指しで警告）→ `extraIds` 引数を開けた。
- **client 資産を変えたのに `sw.js` が v78 のままだった**→ **v79**。
- 「前兆が無い王国では**予言のために乱数を消費しない**」の回帰テストが無い（消費すると既存の決定論シードの
  回帰テスト群と出荷 CARD_SET の抽選が全部ズレる）→ 追加。
- 封じ込め検査が**縦型の王国カードだけ**だった→ **横型（イベント10・予言15）と mix プールと闇市場デッキ**も検査。

### 検証
- **npm test 全46スイート緑（exit 0）**＝`test/risingsun.test.js` **106件**／`test/risingsun-ui.test.js` **39件**を新設。
  整合性5558・不変条件12。既存45スイートに退行なし。

### 【次にやること】R2＝影(Shadow)の基盤
`reshuffleDeck` の「運命の(Fated)」ブロックの直後に**シャッフルした束の一番下へ置く**処理を足す（同関数が
シャッフルの唯一の入口＝**対話不要**）／**山札のどこにあっても使える**経路（アクション権を消費する）／
`maskStateFor` の私的看破（**自分の山札の「どの位置にどの影札があるか」を自分に見せる**＝見えないと操作できない）／
玉座等の「手札から使う」窓に山札の影札も並べる。⚠ **影札は手札にあるわけではない**（小路の捨て札・
地下貯蔵庫・民兵・書庫の手札枚数に数えない／**逆に庭園・品評会など所有カードの総数には数える**）。

---

## 0-33. **旭日（Rising Sun）＝段階1（カタログ50種＋webp 50枚）完了**（2026-08-20・未push・`sw.js` v78→v79）

**`DOM.CARDS` 560→585／`DOM.LANDSCAPES` 201→226＝合計 761→811枚**。
**まだ CARD_SETS 未参照＝実プレイには出ない**（`DOM.STAGE1_POOLS = ['risingsun']` で闇市場からも塞いである）。
正本＝`docs/research/risingsun_rules.md`（段階0）＋`C:/tmp/risingsun_research/g0_jp_pairs.md`（EN/JP 対応表）。

### 規模＝50種（縦型25＝王国／横型25＝イベント10＋予言15）
- `DOM.POOLS.risingsun`（25）／`DOM.EVENTS_RISINGSUN`（10）／`DOM.PROPHECIES_RISINGSUN`（15）。
- **新種別 `omen`（前兆）6種**＝山の社/歌人/川の社/田舎の村/茶屋/**狐（アクション-アタック-前兆）**／
  **`shadow`（影）5種**＝魚屋/小路/**忍者（アクション-アタック-影）**/浪人/狸。
  表示ラベルは同盟以来の「types 配列の順に連ねる」汎用規則に乗せた（`js/carddata.js` の ALLIES_TYPE_JP/EN・
  `js/ui.js` の TYPE_JP・`test/integrity.test.js` の JP/EN の**計5箇所**）。
- **横型の新 kind `prophecy`（予言）**＝`tools/build-landscape.js` にスキン新設（**iris blue（菖蒲色）**・コスト欄なし）。
- 負債コストは `cost:0, debt:N`（山の社5／大名6／絵師8）＝帝国の技術者/大君主と同形。**rice は $7・samurai は $6**。

### カタログ生成＝多エージェント（6群を起草 → 各群を別エージェントが一次資料と逐語照合）＝12体
- **私（メイン）の記述ミスをエージェントが捕まえた**＝存在しない `kabuki` を `POOLS` に書いていた（正しくは
  **`tea_house`（茶屋）**）。**2つの検証エージェントが独立に、一次資料4つを示して指摘**した。
- 日本語文面＝**Dominion Online 訳（決定D1）**を既存カタログの言い回しへ正規化
  （「+N カードを引く」→「+N カード」／`--------` → `————`／負債アイコン → 「+N 負債」）。
- **狐(kitsune) の選択肢は「／」区切りの1行**＝「異なる2つを選ぶ」を持つ既存3枚（従者/執事/道化棒）が3枚ともこの形。
- **狐の選択肢の並びは英語原文の順**（+2 アクション → +2 コイン → 呪い → 銀貨）＝日本語wiki の訳文だけが入れ替わっており、
  英語wiki のカード文・Versions 表・RGG ルールブック PDF の**独立3ソース**が原文順で一致。

### 統合後の独立監査（4観点＝王国の逐語／横型の逐語／既存761枚との整合／表示）＝**確定13件を全対応**
**カード文・コスト・種別・区切り線は一次資料と完全一致**（取りこぼし・捏造・取り違え ゼロ）。以下は体裁と配線の指摘。
**[medium] 2件**
1. **縦型の webp が「絵の窓」を透明のまま出力していた**（`tools/build-cards.js` に else が無い）＝
   UI の `.card.has-art` のクリーム地が透けて**「絵が未回収」ではなく「画像が壊れている」**ように見える。
   横型（`build-landscape.js`）は暗い板を敷いており**縦横で食い違っていた**（旭日が持ち込んだ回帰ではないが、
   全761枚に絵がある現在は表に出ておらず、この段階1を出すと**旭日25枚だけが壊れて見える**）。
   → **横型と同型の else を縦型にも追加**（実ピクセルで `alpha=0` → `rgba(13,27,50,255)` を確認）。
2. **起草コメントが「段階1で `eventPoolFor` と `MIX_LANDSCAPE_POOLS` にも配線せよ」と誤指示**していた
   （2観点が独立に指摘）＝従うと **mix-all の横型抽選に「買っても何も起きない死に札イベント」が出る**。
   → コメントを「**段階1では配線しない・R7 で効果を実装してから**」に訂正。
**[low] 11件**（主なもの）
- **予言スキンの色が hex（濃い紫）/ally（濃い藍）に全パレット中で最も近かった**（ΔE76 で 14.1／14.8＝
  既存10色の最小ペア artifact–state 18.4 より悪い）→ **青寄りへ振り直し**（`[86,74,150]`→`[78,104,190]`）＝
  **prophecy の最小 ΔE 23.6（既存フロア 20.0 を上回る）**を機械確認。
- **選択肢を `・` で始める行が webp 上で「・・」と二重の中黒になる**（既存の transport／scrounge で出荷済み・
  旭日の sickness が3例目）→ **レンダラ2本（縦型/横型）で根治**（行頭が `・` なら字下げにする）。既存2枚も焼き直した。
- **交替(change) を3行に割ると「負債を得る」が無条件に見える**（公式は1段落・`You don't take any [D] if you don't
  gain a card`）→ 2行に統合。**公家(aristocrat) の行頭の全角空白**はレンダラの「折返し継続行」マーカーと同じ記号
  → 削除。解消済みの作業指示コメント（`kabuki`）も撤去。
- **`LS_KIND_LABEL` に恒久検査が無かった**（縦型の `TYPE_JP` にはある）→ `test/ui.test.js` に**全 kind 網羅検査**を新設。
  ※この差分で**既存の出荷済み表示バグも直っている**＝`ally`/`trait`/`boon`/`hex`/`state` の拡大表示が
  長らく「ランドマーク / Landmark」と誤表示されていた（`LS_KIND_LABEL` の登録漏れ）。

### 検証
- **npm test 全44スイート緑（exit 0・整合性 5307→5558・不変条件12）**／`verify:e2e` 9/9／
  `verify:visual` 320〜768px 全幅はみ出し0。
- **資本主義の財宝集合 140→147**（旭日で +$ を持つアクション7枚＝魚屋/公家/交替/狐/茶屋/侍/山の社）。
  番人側（金山/勅使/駕籠＝「+N 負債」であって +$ ではない／米＝元から財宝／大名・絵師・名匠・室）も追加した。
- **webp 811枚 欠落ゼロ**を機械確認。旭日50枚は枠＋文字（**絵は未回収**）。
- **段階1の封じ込めを実証**＝`STAGE1_POOLS` で闇市場から除外／**CARD_SETS 53種×各60回抽選で王国も横型も漏れ0**／
  `MIX_KINGDOM_POOLS`・`MIX_LANDSCAPE_POOLS` に未登録＝mix-all にも出ない。

### 【次にやること】
1. **絵（webp）50枚の回収**（ChatGPT で生成 → `asset/art/<id>.png` → 再生成）。
   ⚠ **影札は裏面が5種とも違う絵**＝裏面画像を作るかを決める（表だけでも段階2は進む）。
2. **段階2＝engine/CPU/UI**（正本の **R1〜R7**）＝R1 前兆/予言/Sunトークンの基盤 → R2 影の基盤 →
   R3 素直な王国 → R4 予言15種（**神風は最後**）→ R5 イベント10種 → R6 残りの王国 → R7 CARD_SET 昇格。
   ⚠ **R7 で `DOM.STAGE1_POOLS` から `'risingsun'` を外す**／`GAIN_ORDER` の25件を**実強度順の位置へ移す**／
   `eventPoolFor` と `MIX_LANDSCAPE_POOLS`（`ev-risingsun` / `proph-risingsun`）を**そこで初めて**配線する。

### 注意（次セッションが知らないと事故る・段階1固有）
- **`tools/build-cards.js` / `build-landscape.js` は行頭が `・` の行に中黒を付けない**（字下げにする）。
  カタログ側で選択肢を `・` で書く流儀はそのままでよい。
- **絵が無いカードの窓は暗い板を敷く**（縦横とも）。透明の穴にすると UI のクリーム地が透けて壊れて見える。
- **横型の新しい kind を足したら `js/ui.js` の `LS_KIND_LABEL` にも足す**（恒久検査が捕まえる）。
- **予言は「カード」ではない**＝`DOM.CARDS` に入れない／保存則 tally・`allCards`・庭園/品評会・
  「カード名を宣言」から除外／**横型の「合計2枚まで」にも数えない**（同盟の Ally と同型）。
- **負債コストのカードは UI 上「コスト0」と表示され、一覧の並び順でも最安として先頭に来る**
  （帝国の技術者/大君主からの既存挙動。旭日で3枚増える）＝**許容簡略化**。

---

## 0-32. **§0-31 の「残っているバグ候補」6件＝全件実バグ確定 → 全修正＋敵対レビュー確定5件も修正**（2026-08-17・未push・`sw.js` v77）

**旭日の段階1に入る前の宿題を完済**。候補6件を6エージェントが **node 再現＋公式裁定の裏取り**で検証＝
**全件 CONFIRMED（偽陽性0）**→ 全修正 → 回帰テスト **+82 assert**（担当6エージェント・**全件バグ注入で感度確認済み**）→
多エージェント敵対レビュー4観点＝**確定5件（うち [high] 1件）→ 全修正・回帰テスト +35 assert**。

### 修正した7件（⑦は [doc]）
| # | 内容 | 修正の要点 |
|---|---|---|
| ① [med] | **冠(crown)/首謀者(mastermind)/門下生(disciple)が `canPlayHandCard`（将軍ブロック＋航海の手札3枚制限）を通らず、`notePlayFromHand` も呼ばない**。公式FAQ＝将軍は玉座を名指しで「手札から使わせる」経路も止める／航海は「玉座＝本体+選んだ札の2枚」と数える | **engine（受理3＋窓を開く述語3）＋CPU 3＋UI 4 を同一コミット**（玉座 14184 と同型のゲート＋note。冠の treasure モードは playTreasureCard が note 済み＝ゲートのみ）。livelock 防止のため CPU/UI のフィルタも同じ述語 |
| ② [med] | **元手(capital)の「場から捨てるとき負債6」が支配(Possession)の振り分けを通らず被支配者に付く**（takeDebt/BUY_EVENT は振り分け済み＝負債サイト間で非対称。公式FAQ＝`You also get any D tokens that player would have gotten`） | `cleanupAndAdvance` の capital ブロックを takeDebt と同じ述語 `(possessedBy != null && pi === active) ? possessedBy : pi` に。**残コインの即時返済は本人ターンのみ**（被支配ターンのコインで支配者の負債は返さない） |
| ③ [low] | **`gainFromOutside` が `t.lastGainedAny` を更新しない**＝行人(wayfarer)のコストが廃棄置き場/闇市場からの獲得（待ち伏せ/墓暴き/シャーマン/リッチ/闇市場ほか全9呼び出し）で動かない | 冒頭に1行（`!== 'wayfarer'` ガード・支配分岐より前）。レビュー指摘で **gain()/gainLoot の支配分岐にも追加**（支配中の獲得も「このターンに獲得されたカード」＝経路の非対称を解消） |
| ④ [low] | **ヴィラ/騎兵隊のフェイズ復帰（購入→アクション）で「購入フェイズ終了時」効果（ワイン商の呼び出し窓／野外劇）が誘発しない**。公式＝Cavalry FAQ `it does make "end of Buy phase" abilities happen`（Villa の2021年の逆裁定は wiki が Deprecated 扱い） | on-gain が `onGainQueue` に **`buy_phase_end_mid`** を積み、消化時に **`midBuyPhaseEnd()`** がワイン商→野外劇を **mid:true 付きの既存 pending** で順に開く。**mid の解決は endBuyTail へ進まない**（ターンが途中で終わる）。探査(exploration)は対象外＝ヴィラ/騎兵隊の獲得自体で `bpGained>=1`。発進(launch)は従来どおり許容簡略化 |
| ⑤ [推定→実バグ] | **回避(avoid)が `reshuffleDeck` の state 省略呼び出し（84箇所中72箇所）で発動しない**＝書庫/六分儀などのシャッフルで空振り（**出荷済み `plunder-events` で到達**） | `reduce()` 入口で処理中 state をモジュール変数 **`_reduceState`** に保持し `reshuffleDeck` 冒頭でフォールバック（reduce は同期・再入なし＝安全。占星術師団/メイソン団のログも全経路で出るようになる）。書庫/六分儀のループに **noMoreShuffle ガード**（draw() と同じ masonsLeft 契約） |
| ⑥ [推定→実バグ] | **徴税(TAX_PILE)が空のサプライ山を拒否**。公式＝カード文 `Add [2D] to a Supply pile.` に非空条件なし・Empires ルールブックが神殿/ワイルドハント/農家の市場で「山が空でもトークンは置く」を明記・教師の山トークンの前例・旭日の厳冬の逐語も同方向 | 受理＝**supply キー不存在のみ拒否**（NON_SUPPLY 除外・pileKeyOf 正規化は維持）／窓ゲートを「キーがあれば開く」に緩和／UI は `modalGainSupply` に allowEmpty。**厳冬(Harsh Winter)実装時に TAX_PILE をコピーしても食い違わない** |
| ⑦ [doc] | cards.js の旅行(journey)コメントが直下の text と engine（2023エラッタ＝決定D1）と**正反対**（「3ターン連続の文言があったら誤り」と書いていた） | コメントをエラッタ側の英文＋D1 の注記に修正 |

### 敵対レビュー（4観点＝忠実性①②③／忠実性④⑤⑥／livelock・人間の詰み／退行・オンライン互換）＝**確定5件→全修正**
各 finding は担当エージェントが node / jsdom で再現してから確定（偽陽性0）。
1. **[high] 首謀者(mastermind_play)モーダルの辞退ボタンが死んでいた（既存バグ・出荷済み menagerie で到達）**＝
   `modalSingleHand` の skip 引数に `{label,on}` でなく **boolean `true`** を渡すと label/onclick が undefined の
   **死にボタン**になる。候補ゼロ（首謀者2枚→翌ターン2窓目で手札にアクション0＝正規経路で到達）だと
   **閉じる手段が無く人間が完全に詰む**。同型の skip=true が ヤギ飼い/聖域/ドブネズミの習性 にも（辞退不能＝強制化）。
   → **4箇所とも正しい辞退ボタンに修正**（engine は null 辞退を受理済み）。
   ⚠ **`modalSingleHand` の skip は `{label,on}` オブジェクト必須**（`true` を渡すと死にボタン）＝今後の鉄則。
2. **[low] 徴税×全サプライ山が空＝CPU livelock**（窓ゲート緩和と CPU 候補の非対称＝この diff 起因）→
   CPU に「非空ゼロなら在庫条件を外して選ぶ」フォールバック（engine受理⊇CPU提案の対称）。
3. **[low] `modalGainSupply` の allowEmpty が canBase の安全網を素通し**＝supply に数値キーを持つ非サプライ山
   （賞品/成長先/馬/戦利品等）が徴税モーダルに「押しても何も起きない」死にチップで並ぶ（この diff 起因）→
   engine に **`isNonSupplyPile`** を公開し allowEmpty 分岐でも弾く。
4. **[low] 回避は `reshuffleDeck` を通らないシャッフルでは依然発動しない**＝併合(ANNEX_KEEP)/宿屋(INN_GAIN)/飢饉は
   `shuffle(deck.concat(...))` 直呼び＝**許容簡略化として記録**（公式は宿屋のケースも発動。根治は「山札に混ぜる」
   共通ヘルパ化＝運命の/占星術師団も同罪の既存穴）。engine のコメントも事実に合わせて修正。
5. **[low] 支配中の lastGainedAny が経路で非対称**（gain()/gainLoot の支配分岐が未記録）→ 両分岐に1行（上表③に合流）。
- **退行レビューはクリーン**＝新旧 engine/cpu の**完全並走66ゲーム・13,375ステップ**（出荷固定25＋random4＋mix2＋
  plunder-events）＝差分は意図した修正のみ・吸収後は bit 一致。v76 形（mid 無し）pending の JSON round-trip 互換OK。
  `isNoConsentUndoableBuy` への影響なし。CPUソーク（villa/cavalry/wine_merchant/pageant/tax/avoid 王国 計96戦）＝膠着0。

### 検証
- **npm test 全44スイート緑（exit 0）**。回帰テストの増分（全件バグ注入で感度確認済み）＝
  同盟 492→**514**／帝国 280→**286**／移動動物園 253→**267**／冒険 59→**81**／略奪 508→**523**／
  イベント 153→**170**／移動動物園UI 64→**75**／夜想曲UI 87→**91**／帝国UI 82→**88**。
- `sw.js` v76→**v77**（js/engine.js・js/cpu.js・js/ui.js・js/cards.js を変更）。

### 注意（次セッションが知らないと事故る・§0-32 固有）
- **`modalSingleHand` の skip は `{label,on}` 必須**（boolean `true` は label 無し・押せない死にボタンになる）。
- **新しい「購入フェイズ終了時」効果を足したら `endBuyTail` 系と `midBuyPhaseEnd`（ヴィラ/騎兵隊のフェイズ復帰）の
  両方に足す**。mid 窓の解決 reducer は `pd.mid` を見て片付けへ進まないこと。
- **`reshuffleDeck` は state を渡さなくても `_reduceState` で救済される**が、`shuffle()` を直接呼ぶ
  「山札に混ぜる」経路（併合/宿屋/飢饉）は回避/運命の/占星術師団の対象外（許容簡略化）。
- **allowEmpty の獲得モーダルは `isNonSupplyPile` で非サプライを弾く**（canBase は在庫条件を含むので使えない）。

---

## 0-30. **新拡張＝略奪（Plunder）に着手**：段階0＝公式ルール研究 完了 ＋ `spoils` を公式訳「略奪品」へ改名（2026-08-15・未push・`sw.js` v70）

**§4 のユーザー決定C（収益化せず全拡張制覇）により、残る未着手は 略奪 → 旭日 の2拡張だけ**。その1本目に着手。
**`DOM.CARDS`/`LANDSCAPES` はまだ不変＝略奪は実プレイに何も出ない**（本番挙動が変わるのは下記の改名だけ）。

### 経緯＝**別セッションの研究成果を統合した**（同じWFを二重に回さなかった）
- この日、**別のセッション（`d64221b1…`）が略奪の段階0研究WFを走らせ、7群の調査文書を書き上げていた**
  （`C:/tmp/plunder_research/` の `kingdom1-3.md` / `loot.md` / `events.md` / `traits.md` / `mechanics.md` ＝**計27万字**）。
  KICKOFF は「中間成果がある」としか書いておらず、**セッション開始直後の `ls` では4ファイルしか見えなかった**が、
  作業中にファイルが増え続けたことで並行セッションの存在に気づいた（プロセス一覧とセッション transcript の
  更新時刻でも裏取り）。
- **85種すべてを網羅していることを機械検算**（王国40／Loot 15／イベント15／特性15）。7群のうち**6群は敵対検証済み**。
- → **ユーザー決定＝既存成果を使って統合**（16エージェントの再収集はしない）。
  未検証だった **`kingdom1.md`（$2〜$4 の13枚）だけ敵対検証を2体で追加**し、7群すべてが検証済みになった。
  **⚠ 次に同じことが起きたら、まず `C:/tmp/<拡張>_research/` を `ls` して既存成果の有無を確かめること。**

### 規模：**500枚／実装対象85種**（RGG ルールブック p.2 の逐語で確定）
`400 Normal Kingdom cards`（40種×10枚）＋`30 Loot cards`（15種×2枚）＋`40 Randomizer cards`
＋`15 Event cards`＋`15 Trait cards` ＝ 500。
→ 縦型（`DOM.CARDS`）＝王国40＋Loot 15＝**55**（505→**560**）／
横型（`DOM.LANDSCAPES`）＝イベント15＋特性15＝**30**（171→**201**）。**王国カードの種類数は全拡張中で最大**。
発売＝2022年12月19日。**印刷は初版のみ（第2刷は無い）＝2022年12月のテキストが現行**。
機能エラッタは **Journey 1枚だけ**（2023年9月・**未印刷**）＝要ユーザー確認。

### 正本＝`docs/research/plunder_rules.md`（543KB・7章）＝**実装前に必ず冒頭の「実装前に必読」20項目を読む**
新機構＝**Loot（伏せた非サプライの混合山）／特性(Trait)（サプライの山1つに付く横型）／
"next time"（次に〜したとき）型の持続**。とくに危ないのは：
1. **"next time" 型持続（ちょうど7枚＝檻/調査/秘境の社/豊穣/旗艦/上陸部隊/切り裂き魔）は相手のターンに条件が満たされ、
   「相手の片付け」で自分の場から捨てられる**。本アプリの `cleanupAndAdvance` は
   **`state.turn.active` の場しか片付けない**（コードで確認済み）＝**新しい経路が要る**。
   条件を一度も満たさなければ**ゲーム終了まで場に残る**（永久持続）。
   **その事象で場に出た1枚自身は誘発しない**／**誘発したら空振りでも消費される**
   （＝「候補ゼロなら窓を開かない」という本アプリの定石をここに適用してはいけない）。
2. **Loot の山＝非サプライ かつ 混合山 かつ 中身が完全に伏せられている**＝本アプリに前例が無い組み合わせ
   （既存の `MIXED_PILE_KEYS` は全部サプライ山／`NON_SUPPLY` は全部同名か個別山）。
   **廃墟と違い一番上も見えない**。`maskStateFor` と**サーバの「同意なしの1手もどす」**の両方に配線が要る。
   `test/invariants.test.js` の tally に `(s.loot||[]).forEach(add)` を足すこと。
3. **Search（調査）＝「サプライの山が空になった瞬間」という新しい誘発点**（`emptyPileCount` では表現できない）。
   **非サプライ山が空になっても誘発しない**／**無謀な(Reckless)が山へ戻すと山が復活して再誘発しうる**。
4. **特性(Trait)＝「カード」ではなく「山に由来するカードid の集合」に付く**（山が空でも効く／分割山は全種に効く）。
   **選出は準備手順の最後**（災いカードの山も候補）／**準備中には効かない**／**カード種別を増やさない**。
5. **無謀な(Reckless)＝`state.replay` に乗せると必ず壊れる**（カードではない／習性では2回にならないが山へは戻る／
   使用回数は1回／「このターン最初に使ったか」は2回とも1回目扱い／山へ戻す＝3山終了と Search の判定が巻き戻る）。
6. **旗艦(Flagship)＝命令(Command)**。除外すべき Command は**公式に8つ**で、**王笏(Scepter)** と
   **相続した屋敷(Inheritance)** が本アプリで漏れやすい（**相続の屋敷は動的＝静的 `types` だけ見ると漏れる**）。
   ＝**忠実性ではなく無限ループ防止の必須条件**。さらに**持続を再演すると旗艦自身も場に残る**
   ＝§0-25/§0-28 の「玉座×持続では玉座が残らない」という**既存の許容簡略化と正面衝突する**。
7. **シャーマン(Shaman)＝王国にあるだけで全員・毎ターン・強制**＝**ターン1から開く**必要がある
   （§0-29 A3 の [medium] 3 とまったく同じ穴）。
8. **運命の(Fated)／回避(Avoid)＝シャッフルの最中に対話が要る**（§0-22 の星図・§0-29 の占星術師団と同型）。
9. **王の隠し財産(King's Cache)＝財宝を3回使用**（既存機構は2回までしか無い）。
10. **密航者(Stowaway)は「next time」型と逆で、自分自身の獲得にも反応できる**／
   **手番プレイヤーの獲得時効果を全部処理した後**に他プレイヤーのリアクション窓を開く。

### 【ユーザー決定・2026-08-15】`spoils`（暗黒時代）を公式訳「略奪品」へ改名（`ea0c091`）
※ 一時この見出しに「ユーザー未承認」と書かれたが**事実誤認だった**（この日は**2つのセッションが並行して
動いており**、片方には他方のユーザーとのやり取りが見えなかったため）。**実際には選択肢を提示して
ユーザーが「略奪品へ改名」を選んでいる**。並行セッションの事故については下の「経緯」節を参照。
**Loot の公式和名が「戦利品」**（日本語wiki `戦利品`＝種別「財宝-戦利品」）と確定したため、
PROGRESS §0-3 が「**将来 Plunder/Loot を入れる時に再考**」と先送りしていた宿題を解消した。
＝**出荷済みの忠実性バグの修正でもある**（公式は一貫して Spoils＝略奪品）。
放置すると mix-all で暗黒時代と略奪が同居したとき**「戦利品置き場」が2つ出る**。
- `js/cards.js` の name と カード文（spoils / marauder / bandit_camp / pillage の「戦利品置き場」）／
  `js/engine.js` のログ文字列／`js/ui.js` の群見出し／CPU・テスト・`docs/adding-cards.md` を一括追随。
  **現時点で「戦利品」は全部 spoils を指していた**（Loot 未実装）ので一括置換で安全だった。
- **webp 4枚を再生成**（このPCのみ）＋ `sw.js` v69→**v70**。モンタージュで「略奪品／略奪品置き場」を目視確認。

### 一次資料の取り方が変わった＝**`tools/wikidirect.js`（英語wiki の直読み）を常設化**
別セッションが書いた Anubis（proof-of-work）ソルバを **`tools/wikidirect.js`** として整備した
（生HTMLの出力は `RAW_DIR` を指定したときだけ＝リポジトリを汚さない）。
- **`node tools/wikidirect.js <Page> [...]`＝1ページ数秒・常にライブの現行ページ**。
- **`tools/wikifetch.py`（Wayback 経由）は予備に格下げ**。3つの罠＝(a) 1ページ30〜60秒で 429/接続拒否が頻発
  （**この日、web.archive.org がこの環境から丸ごと落ちた時間帯があった**）／
  (b) **`snapshot=` の年は古さの証拠にならない**（最寄りキャプチャにリダイレクトされる。
  **逆に古いキャプチャを掴むと Journey の Errata 節がまだ無い**）／(c) 2025年12月以降のキャプチャは Anubis の画面。
- **日本語wiki 用に `tools/jpwiki.py` も常設化**（wikiwiki.jp は**429 を出すので逐次＋2秒以上あける**）。
- ⚠ **カードページは安定だが「拡張の総論ページ」は編集が入る**（`Plunder (expansion)` は 2024キャプチャに
  `If a Duration card leaves play somehow, …` の1行が無く、ライブ版にはある）。
- ⚠ **2025年9月ごろ、英語wikiで「脚注付きの裁定」がまとめて削除された形跡がある**（現場監督×増築・旗艦×はみだし者が同時に消失）。
  → **現行版だけを見て起草すると裁定を取りこぼす**。
- ⚠ **strip 済みテキストでは表の「列」と `<hr>`（区切り線）が消える**＝Print 列か Digital 列か、
  カード文に区切り線があるかは **`RAW_DIR` で生HTMLを見て**判定する。

### ✅ 版の選択・割れていた裁定＝**4件とも決着済み**（ユーザーから一任。詳細と根拠は §4「決定事項」）
1. **旅行(Journey)＝2023年9月「Extra turn errata」側を採用**。`ONCE_PER_TURN_EVENTS` に入れるのは **`launch` だけ**。
2. **日本語のカード文面＝Dominion Online 訳で85種を統一**（`maelstrom` だけ例外＝日本語wiki の記載が省略形のため）。
3. **港の村(Harbor Village)×習性(Way)由来の +$ ＝ボーナスを「得ない」**（mix-all 限定）。
4. **旗艦(Flagship)は持続を再演したら場に残す（例外を設けない）**＝同盟の専門家(Specialist)と同じ実装。

### 🛑 経緯＝**この日、2つのセッションが並行して同じリポジトリを触っていた**（再発防止の記録）
片方が段階0の統合と `spoils` 改名を、もう片方が同じ研究の統合と段階1のカタログを進め、
**同じ `PROGRESS.md` / `KICKOFF.md` に矛盾する内容をコミットし合った**。実害は2件：
- **一方が「`spoils` 改名はユーザー未承認」と誤って訂正した**（他方のユーザーとのやり取りが見えないため）。
- **`journey` の版が正反対に決定された**（2022印刷版 ⇔ 2023エラッタ）。
→ **ユーザーがセッションを1本化して解消**。**大きな調査・生成の前に成果物ディレクトリを `ls` すること**
（メモリ `check-existing-research-first`）。**セッション開始時に空でも、途中で別セッションが書き込むことがある。**

### 検証
- **npm test 全40スイート緑（exit 0・整合性4765・不変条件12）**＝改名で件数は1件も変わっていない
  （＝テストは表示名を検査していない＝本番挙動の変化はカード文・ログ・群見出しの表示だけ）。
- `verify:e2e` 9/9（**webp 505/0**）。

### ✅ push＝完了（2026-08-15・`ea0c091..9d13d32`／8コミット）＝本番反映を機械照合
- **GitHub Pages**（Deploy ワークフロー success）：`sw.js` **v72**／
  `js/cards.js`・`js/engine.js`・`js/cpu.js`・`js/ui.js`・`css/style.css` が**ローカルと sha1 完全一致**（改行を正規化）／
  webp 10枚を抜き取りで**バイト一致**（`journey` / `spoils` / `marauder` / `bandit_camp` / `cage` / `shaman` /
  `sword` / `reckless` / `kings_cache` / `amphora`）／本番 `cards.js` に `plunderexp`・
  `STAGE1_POOLS = ['plunderexp', 'loot']`・`kind: 'trait'`・`name: '略奪品'`・旅行のエラッタ文面を確認。
- **Render（オンライン）**：`GET /status` = `{"persist":true,"rooms":0}`。実 ws（`/ws`＋Origin 必須・`t:` キー）で
  `allies` 対戦を開始し、**王国10種が正常に立つ**（＝**85種を足した新しい `cards.js` をサーバが読んでも壊れていない**）
  ＋**略奪のカードが王国に出ていない**（`CARD_SETS` 未参照）ことを確認。
- **本番の挙動が変わったのは**：`spoils`→「略奪品」の表示（カード文・ログ・カード一覧の群見出し）／
  カード一覧に略奪85種が並ぶ（**実プレイには出ない**）。

### 【次にやること】段階1（カタログと画像）
1. **要ユーザー確認4件を先に決める**（とくに `journey` の版と日本語文面の版）。
2. `DOM.CARDS` +55／`DOM.LANDSCAPES` +30／孤立プール／`GAIN_ORDER`／カード一覧の群。
   ⚠ **`DOM.STAGE1_POOLS` に必ず入れる**（今は `[]`。入れないと闇市場に「買っても何も起きない死に札」が $0 で並ぶ）。
   ⚠ **横型の新 kind `trait` のスキンを `tools/build-landscape.js` に新設**（直近の前例＝同盟の `ally`。
   **特性はコスト欄が無い**）。**Loot 15種のコストは `$7*`（星付き＝非サプライ）**。
   ⚠ **英語 id `plunder` は帝国の分割山（鹵獲品）で使用済み**＝プール名・セットIDは別名にする（例 `plunderexp`）。
   ⚠ **カード文の区切り線の有無**は生HTMLで `<hr>` を確認してから焼く（wiki の表記が不統一）。
3. webp 85枚（枠＋文字）を生成 → 段階2（P1〜P7 のバッチ順は正本の §「段階2 の推奨バッチ順」）。

---

## 段階2 **P1a＝戦利品(Loot)の山の基盤＝完了・push済**（2026-08-15・`186e368..d5d8e92`・`sw.js` v73・本番反映を機械照合）

**略奪はまだ `CARD_SETS` 未参照＝実プレイには出ない**（本番挙動は不変）。正本＝`docs/research/plunder_rules.md`
第1章 §1／第5章 §17。

### なぜ新機構が要ったか＝**非サプライ かつ 混合山 かつ 中身が完全に秘密**は本アプリに前例が無い
- 既存の `MIXED_PILE_KEYS`（廃墟/騎士/城/同盟6）は**全部サプライ山**。
- 既存の `NON_SUPPLY`（賞品/略奪品/馬/精霊…）は**全部同名か個別山**で `supply` の数値キーを持つ。
- → **`state.loot`（実カードid配列30枚）をトップレベルに持ち `supply` には載せない**
  （載せると3山終了に数えられる）。**枚数は人数によらず常に30枚**（15種×2）。

### 実装
- **`DOM.LOOT_GIVERS`**（戦利品を配る12種＝王国6／イベント5／特性1）＝**これが1枚でもあるときだけ山を作る**。
  **イベント/特性も配る**ので `createInitialState` は kingdom だけでなく **events / traits も走査する**。
- **`gainLoot(state, pi, dest)`＝「戦利品1枚を獲得する」の唯一の入口**。山の一番上を取り、
  **`reveal()` で全員に公開してから**獲得（＝パトロンが正しく誘発する）。支配・負債・獲得トリガー・
  `gainedThisTurn`/`lastGainedAny` は既存の獲得と同じ。**山が空なら `null`**。
  ※`gainFromOutside` と違い **`_gainOutside` は立てない**＝戦利品には戻せる山があるので、
  交易商人で銀貨に置換されたら `returnToPile` が山の一番上へ**裏向きに**戻す（公式）。
- `NON_SUPPLY` に15種を登録＝**4系統（3山終了／購入／闇市場デッキ母集団／汎用獲得）から一括除外**。
  CPU の `NON_SUPPLY_SET` にも同じ集合を入れた（engine拒否とCPU非提案はセット）。
- **`maskStateFor` で山を全部 `'back'`**。日本語wiki 逐語「**一番上のカードのみが公開される廃墟などとは
  異なるので注意**」＝**一番上も見せない**。獲得した1枚は `reveals` 側で全員に見える。
- **サーバの `isNoConsentUndoableBuy` が `state.loot` も比較**する＝戦利品を獲得すると山の一番上が変わり
  獲得札が公開される＝**情報が増える**ので同意なしの巻き戻しは認めない（§0-14 の伏せ札の騎士と同型）。
- 盤面に「戦利品: 🎁 山 N枚（裏向き・中身は非公開）」（§0-28 の非サプライ5山と同じ理由＝残枚数が見えないと戦略が立たない）。
- **`test/invariants.test.js` の tally に `(s.loot||[]).forEach(add)`**＝**戦利品はカードなので保存則に数える**
  （祝福/呪詛は非カードなので数えない。**ここを取り違えると即赤**）。

### 検証
- **npm test 全42スイート緑（exit 0・整合性5303・不変条件12）**／新設 **`test/plunder.test.js` 29件**
  ＋**`test/plunder-ui.test.js` 9件**（42・43スイート目）。

### ✅ push＝完了（2026-08-15・`186e368..d5d8e92`）＝本番反映を機械照合
- **GitHub Pages**（Deploy success）：`sw.js` **v73**／`js/cards.js`・`js/engine.js`・`js/cpu.js`・`js/ui.js` が
  **ローカルと sha1 完全一致**（改行を正規化）／本番 `engine.js` に `gainLoot` / `LOOT_IDS` / `state.loot` /
  マスクの `s.loot = new Array(...).fill('back')`、本番 `cards.js` に `DOM.LOOT_GIVERS` を確認。
- **Render**：`GET /status` = `{"persist":true,"rooms":0}`。
- **本番の挙動は不変**＝**出荷 CARD_SET 49種×8回抽選で戦利品の山が1度もできない**ことを機械確認
  （`DOM.LOOT_GIVERS` はすべて略奪のカードで、略奪は `CARD_SETS` 未参照）。

### 【次にやること】P1b＝**戦利品15種の効果**
`gainLoot` はできたが**カードの効果はまだ空**（＝獲得しても +$ 以外は何も起きない）。
`coin:` はカタログに入っているので **11枚は +$3・尽きぬ杯は +$1 が既に出る**（財宝の共通処理）。
実装が要るのは下記。**新しい pending は4点セット必須**（engine reducer＋`PLAYER_ACTIONS`＋CPU＋UI）。

| 難度 | カード | 要点（正本＝第5章の該当節） |
|---|---|---|
| 易 | ダブロン金貨 | 獲得時に金貨（**強制**・山が空なら何もしない）。**`state.onGainQueue` に積む**（入れ子の獲得） |
| 易 | 船首像 | 持続＝次のターン開始時 +2カード |
| 易 | 賞品のヤギ | 手札1枚を廃棄してよい（**財宝の使用中に廃棄が起きる**＝`playAllResume` に載る） |
| 易 | ハンマー | **`costUpTo(state,id,4)` で$4以下を強制獲得**（素の `cardCost<=4` は禁止）。候補ゼロなら窓を開かない |
| 中 | 剣 | アタック＝**手札が4枚になるまで**捨てさせる（既存 `discard_down` を n=4 で流用） |
| 中 | 盾 | **堀と同じ免疫リアクション**（手札に残る）。`hasReaction` ＋ `SHIELD_REVEAL` ＋ `reactOptions` に1行。**1ターンに何度でも** |
| 中 | 六分儀 | 上5枚を見て任意枚数を捨て、残りを任意順で戻す（`look_arrange` 系。**見る＝マスク必須**） |
| 中 | パズルボックス | 脇に伏せて置き**次の手札を先引きした後**に手札へ。**持続ではない**。新ゾーン＝`allCards`/ZONES に登録 |
| 中 | 勲章 | このターン**獲得したカードすべて**を任意で山札の上へ＝`t.insignia` カウンタ＋**`onGainQueue` 側**に載せる |
| 中 | 杖 | **購入フェイズに**手札のアクション1枚を使用（`playCardNoAction`）。**杖自身は当ターンに捨てる**（玉座と逆） |
| 難 | 宝石 | 次のターン開始時に**山札の一番下へ**（強制・**捨て札置き場を経由しない**＝捨て札トリガーを1つも通さない） |
| 難 | アンフォラ | 「今」か「次のターン開始時」を**プレイのたびに独立に**選ぶ。**1回でも「次」なら持続**（回数で持つ） |
| 難 | 尽きぬ杯 | **永続持続**（`p.hirelings` 型）。ターン開始時に使用させると**その場で2回発動** |
| 難 | 宝珠 | 捨て札を全部見る（**選択の前に必ず**）→「捨て札からアクションか財宝を使用」or「+1購入+$3」。<br>**`ELDER_CHOICE_ORDER` に登録**（長老の追加選択。据え置くなら許容簡略化として明記） |
| 難 | 呪符の巻物 | **アクションでも財宝**＝プレイ経路が2つ・アクション権の消費が非対称。<br>廃棄→**これより安い**カードを獲得→アクション/財宝なら使用してよい。<br>**両辺とも `cardCost` を通す**（渡し船/発明家の家族で取れる集合が実際に変わる） |

---

## 段階2 **P1b＝戦利品(Loot) 15種の効果＝完了・push済**（2026-08-15・`393baba..4091cc9`・`sw.js` v74・本番反映を機械照合）

**これで P1（Loot の山＋15種）が完了**＝`gainLoot` で得た戦利品が全部ちゃんと働く。
**略奪はまだ `CARD_SETS` 未参照＝実プレイには出ない**。正本＝`docs/research/plunder_rules.md` 第5章。

### 実装（コイン成分は `coin:` で計上済み＝効果側には書かない）
| カード | 要点 |
|---|---|
| ダブロン金貨 | 獲得時に金貨を**強制**獲得（山が空なら何もしない）。戦利品は公開してから獲得するので**入れ子の獲得**になる |
| 船首像 | 持続＝次のターン開始時 +2カード |
| 賞品のヤギ | 任意の廃棄。**廃棄が別の窓を開く**ので reducer は廃棄の前に `pending=null` にする |
| ハンマー | **`costUpTo(state,id,4)`** で強制獲得。**候補ゼロなら窓を開かない** |
| 剣 | アタック＝**手札が4枚になるまで**（既存 `discardDownEnter(...,4,...)`） |
| 盾 | **堀と完全に同型の免疫**（公開しても手札に残る・1ターンに何度でも） |
| 六分儀 | 上5枚→任意枚数を捨て→**捨て札トリガーの後に**残りを戻す（公式＝後から戻す残りが上） |
| パズルボックス | 裏向きの脇ゾーン `p.puzzleBox`。**先引きの後**に手札へ。**持続ではない** |
| 勲章 | このターン獲得したカードを**毎回**山札の上へ（`t.insignia`・`travelling_fair` の窓を共有） |
| 杖 | **購入フェイズのまま**アクションを使わせる。**杖自身は当ターンに捨てる**（玉座の間と逆） |
| 宝石 | 次ターン開始時に**山札の一番下**へ。**捨て札置き場を経由しない**＝捨て札トリガーを1つも通さない |
| アンフォラ | 「今／次」を**プレイのたびに独立に**選ぶ。**「次」を選んだ回数ぶん** `armDuration`（旗1つにしない） |
| 尽きぬ杯 | **永続持続** `p.endlessChalices`（雇人と同型）＝毎ターン開始時に稼働数ぶん $1 +1購入 |
| 宝珠 | 捨て札を**先に全部見て**から二択。**宝珠自身は当ターンに捨てる**（使わせた持続だけ場に残る） |
| 呪符の巻物 | **アクションでも財宝**＝経路2つを `spellScrollEffect` に集約。**廃棄できたときだけ獲得**（命令経由は不発）。<br>「これより安い」は**両辺とも `cardCost`** を通す |

新 action 10種はすべて4点セット（engine reducer＋`PLAYER_ACTIONS`＋CPU `decidePending`＋UI `viewPendingModal`）。

### 【この作業で見つけた実バグ】盾のボタンが embedded 型アタックの3モーダルに無かった
`modalMilitia` / `modalDiscardDown` / `modalTorturer` は **`reactOptions` を通らず独自のボタン列を持っている**ため、
engine は `SHIELD_REVEAL` を受理し CPU も返すのに、**人間だけが盾を使えない**状態だった
（§0-29 A4 の [high]「追いはぎ/将軍のリアクション窓に UI が無い」と**まったく同じクラス**）。
**新設した UI テスト（全 pending にモーダルと押せる選択肢があるか）が自動で捕まえた。**

### 【横断リファクタ】CPU の堀チェック60箇所を `immuneReveal(p)` に集約
`p.hand.includes('moat')` を個別に書いていたので、**免疫札を1枚足すたびに60箇所を直す**構造だった。
`immuneReveal(p)` が `MOAT_REVEAL` / `SHIELD_REVEAL` を返す形にして、**新しい同型の札はこの1関数に1行**で済む。

### 検証
- **npm test 全42スイート緑（exit 0・整合性5303・不変条件12）**／
  `test/plunder.test.js` 29→**136件**／`test/plunder-ui.test.js` 9→**37件**。

### ✅ push＝完了（2026-08-15・`393baba..4091cc9`／5コミット）＝本番反映を機械照合
- **GitHub Pages**（Deploy success）：`sw.js` **v74**／`js/cards.js`・`js/engine.js`・`js/cpu.js`・`js/ui.js` が
  **ローカルと sha1 完全一致**／本番 `engine.js` に `gainLoot`・`spellScrollEffect`・`endlessChalices`・
  `SHIELD_REVEAL`／`SEXTANT_RESOLVE`／`AMPHORA_CHOOSE`／`ORB_RESOLVE`・`puzzleBox` を確認／
  **本番 `ui.js` の `SHIELD_REVEAL` が 4箇所**（`reactOptions` 1 ＋ embedded 型モーダル 3）＝盾の穴が塞がっている／
  **本番 `cpu.js` に `immuneReveal` があり、素の `hand.includes('moat')` は 0箇所**。
- **Render**：`GET /status` = `{"persist":true,"rooms":0}`。
- **本番の挙動は不変**（略奪は `CARD_SETS` 未参照）。ただし**盾の UI 修正と CPU の共通述語化は既存の堀にも効く経路**
  なので、堀の挙動が変わっていないことは全42スイートで担保している。

### 既知の許容簡略化（意図的）
- **宝珠(Orb)は choose-one だが `ELDER_CHOICE_ORDER` に登録しない**（長老の追加選択の対象にしない）＝
  §0-29 A4 の「長老で追加選択できるのは同盟の9種のみ」に合わせた。mix-all 限定の差。
- **尽きぬ杯を「ターン開始時に使用」させると公式は即座に2金2購入**（「現在」＋「各ターンの開始時」）だが、
  本エンジンでは開始時効果の解決後に稼働数が増えるので「現在」ぶんだけになる。
  ⚠ **そういう経路（略奪の準備 Prepare／王子／船長）を実装したら再確認すること。**
- **`p.puzzleBox` の脇札は「1枚ずつどのパズルボックス由来か」を追跡しない**（枚数だけ）。
  公式でも全部ターン終了時に手札へ戻るので差は無い。

### 【次にやること】→ **P2〜P7 まで完了**（下の §「段階2 P2〜P7」を見ること）。

---

## 段階2 **P2〜P7＝略奪(Plunder)の残り70種を実装し CARD_SET 昇格＝実プレイ化 完了**（2026-08-15・`sw.js` v75）

**これで略奪85種すべてが実際に遊べる**（王国40＋戦利品15＋イベント15＋特性15）。
`DOM.CARD_SETS` に **`plunder` / `plunder-events` / `plunder-traits` / `random-plunder`** を追加し、
**mix-all も 15→16拡張**（`plunderexp` ＋ 横型プール `ev-plunder` / `trait-plunder`）。
**これで実装済み＝16拡張＋プロモ＝761枚（縦型560＋横型201）が実プレイ可能**（絵は略奪85枚だけ未回収）。

### バッチ構成（各バッチ末で `npm test` 全緑を確認してコミット）
| バッチ | コミット | 内容 |
|---|---|---|
| P2 | `d10dcdf` | **"next time" 型持続の共通機構**＋7枚（檻/調査/秘境の社/豊穣/旗艦/上陸部隊/切り裂き魔） |
| P3 | `90b2ca7` | 王国カード19種（岩屋/宝飾卵/シャーマン/セイレーン/密航者/現場監督/キャビンボーイ/坩堝/財宝9種ほか） |
| P4 | `02c98b2` | **特性(Trait) の基盤と15種** |
| P5 | `3ea0821` | **イベント15種** |
| P6 | `de70c7f` | 残りの王国カード9種（財産目当て/地図作り/拡大/一等航海士/フリゲート船/鉱山道路/操舵手/トリックスター/王の隠し財産） |
| P7 | `05704b8` | **CARD_SET 昇格**（固定10種＋イベント/特性セット＋random＋mix-all） |

### P2＝"next time" 型持続の共通機構（設計の正本）
- **器**＝`p.delayedEffects` に `{card, type, nextTime:'<誘発点>'}` を積む（`armNextTime`）。
  **`resolveDurationStartEffects` はこの予約を消費しない**（＝条件を満たすまで何ターンでも持ち越す）。
  条件を一度も満たさなければ**ゲーム終了まで場に残る**（cnt が自然に保持する）。
- **誘発点は4種**＝`gain`（檻＝勝利点／秘境の社＝財宝／豊穣＝アクション／切り裂き魔＝**誰かの**$5以上の財宝）／
  `pile_empty`（調査）／`play_action`（旗艦）／`first_treasure`（上陸部隊）。
  `fireNextTime(state, trigger, ctx)` が**手番プレイヤーからターン順に**、**呼び出し時点の予約だけ**を発火する
  （＝その事象で場に出た1枚は誘発しない＝公式）。空振りでも消費される（候補ゼロで窓を開かない定石を適用しない）。
- **⚠ 調査(Search)の `pile_empty` は「サプライの山が空になった瞬間」＝新しいフック**。
  `gain` / `trashFromSupplyPile` / `exileFromSupply` / `exchangeCard` の**全減算サイト**に配線した
  （`emptyPileCount` は「今いくつ空か」なので使えない）。**非サプライ山（戦利品/馬/賞品）では誘発しない**。
- **⚠ 相手のターンに誘発した持続は「相手の片付け」で持ち主の場から捨てる**＝`t.nextTimeSweep` に
  **印を付けたその1枚だけ**を `cleanupAndAdvance` が捨てる。**全体掃除にしてはいけない**
  （隊商の護衛＝相手のターンにプレイして自分の次の片付けまで残す持続や、前哨地/航海の追加ターンを巻き込む）。
- **旗艦(Flagship)** ＝命令(Command)＝`notePlunderPlay` が `DOM.isType(card,'command')` を除外（無限ループ防止の必須条件）。
  はみだし者経由でプレイされた札の再演は **`playAsCommand` で「サプライに残したまま」**行う（§0-17 の [MED] 回避）。
  **持続を再演したら旗艦も場に残す**（決定D4）＝`flagship_linger` 予約が再演相手の予約が尽きるまで残る。
- **切り裂き魔** ＝予約を張るのは**アタックを全部解決した後**（`discardDownEnter` の `next:'cutthroat'`）＝
  公式「相手が坑道を捨てて金貨を獲得しても誘発しない」。戦利品($7財宝)の獲得が**さらに**誘発するが二重発火しない。
- **新ゾーン `p.cage`**（伏せ札）＝`allCards`／invariants の ZONES／`maskStateFor`（相手には枚数だけ）／盤面表示に配線。

### P3/P6＝王国カードで踏んだ公式裁定（次に触るとき忘れると壊す）
- **シャーマン**＝**王国にあるだけで全員・毎ターン・強制**（`It applies even if no-one ever gets a Shaman.`）＝
  `createInitialState` の末尾でも開く（**ターン1から**）。候補述語 `shamanTargets` は**非サプライ**（廃棄置き場）なので
  `costUpTo` を使ってはいけない（リッチ `lichTrashTargets` と同型）。
- **港の村**＝「次に使ったアクションが**その効果で** +$ を出したか」＝`applyEffect` をラップして**コイン差**で判定。
  選択待ちを挟む効果（執事の +$2）は `t.hvPending` で解決後に精算。**習性(Way)由来の +$ ではボーナスなし**（決定D3）。
- **現場監督**＝「これより後に」コスト**ちょうど$5**（3成分）を獲得したか＝`t.taskmasterWatch`。
  **繰り返しは「アクションの使用」ではない**（`noteAllyPlay`・`actionsPlayed`・山トークンを触らない）。
- **つるはし/坩堝**＝コスト参照は**廃棄の後**（漁師が捨て札ありで$5になる例）。
  **`PLAY_ALL_EXCLUDE` に入れた**（出すと手札1枚の廃棄が強制＝ボタン1つで事故る）＝**CPU も同じ集合を見る**
  （見ないと PLAY_ALL_TREASURES を返し続けて**本番 livelock**＝ソークで実際に踏んだ）。
- **銀山**＝「**自身より安い**」＝コスト減で弱くなる（動的な自身のコストで毎回引く）。**手札に直接獲得**。
- **乗組員/宝石**＝場から**山札の上／一番下**へ＝**捨て札置き場を経由しない**（捨て札トリガーを1つも通さない）。
- **埋められた財宝**＝獲得したとき**使用する**（強制・誰のターンでも）。先に動かされていたら失敗（stop-moving）。
- **一等航海士**＝同名を好きなだけ＝**何枚使うか宣言しない**（1枚解決するごとに再オファー）＋**入れ子**
  （一等航海士で一等航海士）＝`t.firstMateStack`。draw-to-6。
- **フリゲート船**＝相手の**アクション使用の解決後**に手札4枚まで捨てさせる linger アタック。
  免疫は**使用した瞬間**に確定／**誰にも影響しないなら当ターンの片付けで捨てる**（沼の妖婆と違い次ターンに何もしない）。
- **操舵手**＝**インスタンス単位の脇**（`p.quartermasters=[{id,cards}]`＝資料庫と同型）＝永続持続。
  獲得は**捨て札を経由しない**／脇のカードは所有カード（得点に数える）／場を離れたら機能停止。
- **鉱山道路**＝累積するが**同じ1枚を2回は使えない**／「使う」を選んだときだけ回数を消費。
- **トリックスター**＝クリンナップで場から捨てる財宝を脇へ→**先引きの後**に手札へ。
- **王の隠し財産**＝財宝を**3回**（`treasure_replay` を2つ積む）＝既存機構は2回までしか無かった。
- **財産目当て/地図作り**＝「**見る**」＝`maskStateFor` の私的看破リストに追加（§0-21/§0-28/§0-29 と4回目の同型）。

### P4＝特性(Trait) の基盤
- **`state.traits = { 特性id: 山キー }`**。判定は **`hasTrait`＝`pileKeyOf` 正規化**（分割山/混合山の中身にも効く・
  **山が空になっても効き続ける**・**カード種別を増やさない**）。**選出は準備手順の最後**（災いカードの山も候補・
  randomizer の種別で「アクションか財宝の王国の山」を判定・**同じ山に2枚は付けない**）。
- **安価な**＝`cardCost` に -$1（全員・常時・**得点計算も**・coin 成分だけ）。
- **無謀な**＝`state.replay` の**専用ラベル**（玉座ではない）＝**プレイは1回**（`actionsPlayed`/`noteAllyPlay`/
  山トークンを増やさない・共謀者に数えない）／**財宝も2回**／**習性・女魔術師・追いはぎで書き換えられたら2回にならない**
  （`applyEffect` を通らないので自動的にそうなる＝カメレオンの習性だけ例外的に2回＝公式と一致）／
  **場から捨てるとき山へ戻る**（`returnToPile`＝3山終了が巻き戻る）。
- **疲れ知らずの**＝場から捨てるとき脇へ→**先引きの後**に山札の上（cleanup ローカル保持＝新ゾーン不要）。
- **受け継がれた**＝開始デッキを差し替える（**獲得ではない**＝獲得トリガーを引かない／サプライが人数ぶん減る＝
  3山終了に影響／銅貨は山へ戻し屋敷・避難所・家宝は箱へ）。**どの開始カードを替えるかは自動（屋敷優先）＝許容簡略化**。
- **運命の**＝`reshuffleDeck` で**自動選択**（アクション/財宝は上・他は下）＝§0-22 の星図と同型の許容簡略化。
- **せっかちな/忍耐強い**＝遅延/刈り入れと同じ `p.eventSetAside`＋`event_play` に相乗り。
- **友好的な/忍耐強い**＝クリンナップ開始時窓（増築と同じ再入型）／**内気な**＝ターン開始時窓
  （**捨てる→捨て札トリガー→引く**の順＝坑道の金貨がリシャッフルに入る）。

### P5＝イベント15種
- **旅行(Journey)＝2023 Extra turn errata 版**（決定D1）＝`ONCE_PER_TURN_EVENTS` に**入れない**（2枚目以降は空振り）。
  `t.journeyKeep` が**場のカードだけ**片付けで捨てないようにする（**手札は普通に捨てる**）。
  **増築/策謀/カエルの習性/天幕/角笛/元手の負債は起きない**（＝「捨てるはずのカード」が無い＝公式）。
  追加ターンは島民/航海と同じ `chain<2` 判定＋タイブレーク外（共通機構に `journeyExtra` を配線）。
- **発進(Launch)** ＝`ONCE_PER_TURN_EVENTS`。ヴィラと同型（`treasuresLocked` 解除・闘技場の再武装）→**その後**に +1カード。
- **回避(Avoid)** ＝`reshuffleDeck` で自動選択（**累積**・次の1回で消費・**捨てる扱いではない**＝
  `triggerOnDiscard` を呼ばない・**2度目のシャッフルをしない**）。**襲撃(Foray)は逆に捨て札トリガーを誘発する**
  （同じ拡張の中に両方ある＝共通化してはいけない）。
- **突貫(Rush)は累積しない旗／鏡映(Mirror)は累積カウンタ**＝**別実装**（§0-29 A4 の散兵/追いはぎと同型の罠）。
- **準備(Prepare)** ＝新ゾーン `p.prepareAside`（公開）＋**1つの開始時効果**の専用再開網（強制・全部使うまで・
  間に他の開始時効果を挟まない）。**配達(Deliver)** ＝新ゾーン `p.deliverAside`（公開）＝獲得を脇へ→先引きの後に手札へ。
- **侵略(Invasion)** ＝①任意アタック（解決後に再開網で②〜④）→③**コスト上限なし**で山札の上→④戦利品を獲得して使用
  （望楼で動かされたら使用に失敗＝stop-moving）。
- **繁栄(Prosper)** ＝**カードの種別**で判定（`isTreasureFor`＝資本主義対応。植民の `populatePiles` とは逆）＝
  名前で重複判定・1枚ずつ解決・**やめられる**。
- **大渦巻(Maelstrom)** ＝**アタックではない**（堀・灯台・チャンピオンで防げない）＝`ATTACKS` に登録しない。
- **物色(Scrounge)** ＝廃棄置き場からの獲得＝`costUpTo` を使わない／**遂行できない選択肢も選べる**。

### P7＝CARD_SET 昇格
- **`DOM.KINGDOM_PLUNDER` 固定10種**（自作 showcase）＝宝飾卵/調査/シャーマン/現場監督/港の村/切り裂き魔/
  乗組員/巡礼者/一等航海士/戦利品の袋。**戦利品を配る4種**・**next-time 持続2種**・シャーマンの常設ルール・
  topdeck 持続・入れ子ループ・村枠 を網羅。コスト＝$2×3／$3×1／$4×1／$5×4／$6×1。
- **`plunder-traits` が新機構**＝`traitsForSet` / `traitPoolFor` を新設し **`landscapesForSet` が `traits` も返す**
  （mix の `kind:'trait'` 分岐も追加）→ `NEW_GAME` / `js/ui.js`（startLocal・restartLocal）/
  `server/gameServer.js`（startGame）に配線。**横型の合計2枚制限に数える**。
- **`DOM.STAGE1_POOLS = []`**＝闇市場デッキに略奪の**王国40種**が入る（**戦利品15種は `NON_SUPPLY`＝入らない**）。
- CPU の `GAIN_ORDER` は略奪40種を**実強度順の位置**へ移動（戦利品15種は非サプライのまま末尾）。

### 多エージェント敵対レビュー（4観点＝P5/P6 忠実性・CPU非ループ・UI詰み・P2〜P4 忠実性）＝**確定21件 → 全修正・回帰テスト付き**
各 finding は担当エージェントが **node / jsdom で再現してから確定**（偽陽性は棄却）。

**[high] 3件**
1. **一等航海士(First Mate)の入れ子が「1つの旗」で潰れ合う**＝`t.fhResume` を単一オブジェクトで持っていたので、
   一等航海士が使わせたアクションがさらに一等航海士だと**外側の残りカードが丸ごと消える**（手札が数枚死ぬ）。
   → **スタック（配列）にした**。回帰テストは**内側が pending を開く札（呪符の巻物）を使わせる形**でないと
   感度が出ない（最初に書いたテストはバグを戻しても緑のままだった＝**再現手順そのものを検証した**）。
2. **拡大(Enlarge)が手札0枚で窓を開いて閉じない**＝人間が詰み／CPU が livelock。→ 終端保証＋UI の辞退ボタン。
3. **繁栄(Prosper)の獲得モーダルに「やめる」が無い**＝候補ゼロで閉じられない。→ `alwaysSkip` を渡す。

**[medium] 7件**（無謀な×愚者の黄金/岐路の「1枚目」判定／セイレーンが勲章で逃げられない／上陸部隊の予約が
相手の片付けで消える／銀山が `costUnder` を通っていない／補給係の候補ゼロ／宝物庫係の `anyOk`／
一等航海士の `canPlay` ガード漏れ）。

**[low] 11件**（現場監督が「獲得後」のコストで判定していた／置き去りが資本主義・相続の動的種別を数えない／
隊商の護衛・番犬のリアクションで旗艦が誘発しない／内気な・岩屋のドローが捨て札トリガーより先に走る／
追いはぎの自己捨てが疲れ知らずを通らない／道具の獲得モーダルに閉じるボタンが無い ほか）。
- **最後の1件＝無謀なアタックのリアクション窓が2回開く**（公式＝`a single Shield reveal ... will block
  both attacks`）。→ `t.recklessBlock`（1回目に堀/盾を公開した席）＋`t.recklessIter` を新設し、
  **2回目は窓を開かずそのまま免疫にする**。`t.recklessIter` は**新しいカードの使用が始まったら落とす**
  （＝同じターンに同名の2枚目を使ったら改めて公開できる）。

### 既存16拡張への退行＝**クリーン**
新旧 engine を**同一シードで完全並走**（毎ステップ CPU の手と state 全体を突き合わせ）
＝**294ゲーム・75,519ステップ・差分0**。**v74 のオンライン永続化スナップショットを v75 の engine が
そのまま読める**ことの証明にもなっている。

### 検証
- **npm test 全44スイート緑（exit 0・整合性5307・不変条件12）**／
  `test/plunder.test.js` 136→**497件**（P2〜P7 の裁定＋敵対レビュー回帰21件＋各バッチのCPUソーク＋
  出荷4セット8戦＋mix混成3戦）／`test/plunder-ui.test.js` 37→**148件**。
- `verify:e2e` 9/9（webp 560/0）／`verify:visual` 320〜768px 全幅はみ出し0。
- **4点セットの機械検算**＝pending 54種すべてに engine reducer・`PLAYER_ACTIONS`・CPU `decidePending`・
  UI `viewPendingModal` が揃っている（欠けゼロ）。**pending 到達 54/54**（44 は通常ソークで自然到達、
  残り10は強制注入＝**人間だけが通る道**＝A5 の「闇市場×同盟」と同じ構図）。

### ✅ 絵（webp）85枚の回収＝完了
ユーザーが ChatGPT で85枚を生成（10バッチ）→ **10グループに1体ずつエージェントを立て、
85候補全体から全単射で判別**（どのバッチかは伝えない方式）＝取り違え0。
- **1バッチ（L2）が既知の失敗モード＝5枚を1枚のコラージュにしてしまい、さらに同じ主題（六分儀）を4枚描いた**
  → 判別エージェントと目視の両方で検出し、**「4種類とも描く／同じ主題を複数枚描かない」を明記して再生成**。
- webp が 47〜67KB（枠＋文字）→ **絵入り**に。Downloads は空にした。

### 【次にやること】→ **push 完了（2026-08-16・`27c3be3..57dd267`／11コミット）＝本番反映を機械照合**
- **GitHub Pages**（Deploy success）：`sw.js` **v75**／`js/cards.js`・`js/engine.js`・`js/cpu.js`・`js/ui.js`・
  `css/style.css` が**ローカルと sha1 完全一致**（改行を正規化）／webp 12枚を抜き取りで**バイト一致**
  （すべて 88〜200KB＝絵入り）／本番 `cards.js` に `id: 'plunder'` / `'plunder-events'` / `'plunder-traits'` /
  `'random-plunder'`・`STAGE1_POOLS = []`・`plunderexp: '略奪'`、本番 `engine.js` に `fireNextTime`・`armNextTime`・
  `hasTrait`・`recklessBlock`・`firstMateFinish`・`discardThenDraw`・`typeCountFor` を確認。
- **Render（オンライン）**：`GET /status` = `{"persist":true,"rooms":0}`。実 ws（`/ws`＋Origin 必須・`t:` キー）で
  **`setConfig kingdomSet=plunder-traits` が受理され**、対戦開始で**固定10種 10/10・特性2枚
  （無謀な＝巡礼者／豊かな＝一等航海士）・戦利品の山30枚がすべて `'back'`（マスクOK）**を確認。
- → 次は **旭日（Rising Sun）＝§0-31**。

---

## 0-31. **新拡張＝旭日（Rising Sun）＝段階0（公式ルール研究）完了**（2026-08-17・未push・`sw.js` v76）

**正本＝`docs/research/risingsun_rules.md`（847KB・9258行）が完成した**。
`DOM.CARDS`/`LANDSCAPES` はまだ不変＝**旭日は実プレイに何も出ない**（次は段階1＝カタログ）。
**この研究の副産物として、出荷済みの実バグを7件見つけて全部直した**（下の §「出荷済みバグ7件」）。

### ⚠ **拡張の日本語名は「旭日（きょくじつ）」**＝PROGRESS が長らく書いていた「日の出づる国」は**誤り**だった
英語wiki の `Rising Sun > Trivia > Official releases in other languages` が **`Japanese: 旭日 (pron. kyokujitsu)`**、
日本語wiki（wikiwiki.jp/dominiondeck）のページ名も **`旭日`**（拡張一覧でも 略奪 と Arcana の間に `旭日`）
＝**独立2ソースが一致**。この節で PROGRESS/CLAUDE の全21箇所を修正した。

### ⚠ **実は「最後の1拡張」ではない＝第17拡張 `Arcana` が存在する**（ただし現時点では実装不能）
英語wiki `Arcana`＝**500枚・王国37山（うち勝利点5）・Release 2026 (expected)**・
機構＝**Study／Cart／Project（プロジェクト再登場）**・「ポーションもポーション類似の機構も入らない」と Donald X. が明言。
**カード一覧・追加ルール・ギャラリーの各節がすべて空**＝**カード名すら未公開なので段階0すら着手できない**。
日本語wiki も `Arcana (仮置き)` の表記。→ **旭日を作り切った後、Arcana のデータが出たら着手する**。

### 規模＝**300枚／実装対象50種**（英語wiki の Info と RGG ルールブック 2024年版 p.2 の逐語で一致）
`250 Kingdom cards`（25種×10）＋`25 Randomizers`＋`10 Events`＋`15 Prophecies` ＝ 300。
→ 縦型（`DOM.CARDS`）＝王国**25**（560→**585**）／横型（`DOM.LANDSCAPES`）＝イベント10＋予言15＝**25**（201→**226**）。
＝**略奪85種より小さい**（合計 761→**811枚**）。発売＝**2024年8月10日**（第16拡張）。
トークン＝負債40・**日の出(Sun)13**。

### 機構＝**5つのうち3つは実装済み**＝新規は2つだけ
| 機構 | 状態 |
|---|---|
| **負債(Debt)** | ✅帝国で実装済（`p.debt`／`REPAY_DEBT`／`takeDebt`）。⚠ ただし**返済タイミングが違う可能性**＝ルールブック逐語は `at any point in their turn`／本アプリの `REPAY_DEBT` は **`t.phase !== 'buy'` で拒否**している。段階0で確定させる |
| **イベント(Event)** | ✅冒険/帝国で実装済（`BUY_EVENT`） |
| **持続(Duration)** | ✅実装済。ルールブックが「持続を追加で使わせたカード（玉座の間/王笏/首謀者/専門家/**旗艦**/**大名**）も場に残る」と明記＝**略奪の決定D4と一致**（例外を設けない判断が正しかった） |
| **前兆(Omen)／予言(Prophecy)／日の出(Sun)トークン** | 🆕**新規**。王国に前兆が1枚でもあれば**予言を1枚だけ**配る（前兆が何枚あっても1つ）。トークンは**2人5／3人8／4人10／5人12／6人13**。「+1 Sun」＝予言からトークンを1個取り除く。**最後の1個を取り除いた瞬間**に予言の効果が有効になり以後ずっと有効。全部取り除いた後の「+1 Sun」は**何もしない**。**「+1 Sun」は前兆カードの記載の一番最初に必ず来る** |
| **影(Shadow)** | 🆕**新規・5枚**（魚屋／小路／忍者／浪人／狸）。**裏面が違う**・**山札から使える**・**シャッフルするとき山札の一番下に置く**（獲得時は置かない）・**山札の裏面はいつでも自分で見てよい**・アクションを使えるときならいつでも山札から使える・玉座の間が「手札から使う」と言ったときも使える・**ただし手札にあるわけではない**（小路の捨て札には使えない）。**略奪の「運命の(Fated)」と一緒に一番下に置ける** |

### 実装の下調べ（コードを読んで確認済み）
- **影は `reshuffleDeck` の「運命の(Fated)」ブロックの直後に足せる**（`js/engine.js` の同関数が**唯一のシャッフル入口**で、
  `p.deck = p.deck.concat(shuffled)` ＝ `shuffled` の末尾＝山札の一番下）。**シャッフル中の対話は要らない**
  （公式が「一番下に置く」と決めているので自動＝星図/運命のと違い**許容簡略化ですらない**）。
- **大名(Daimyo)＝命令(Command)** かつ「このターン、次に命令でないアクションを使ったら再使用する」
  ＝**略奪の旗艦(Flagship)とほぼ同型**（旗艦は持続で「次回」、大名は「このターン中」）。`notePlunderPlay` を流用できる。
- **神風(Divine Wind)＝最後の Sun トークンを取り除いたとき、サプライの王国カードを全部取り除き、
  新しくランダムな王国10山を出す**＝**本アプリに前例が無い破壊的な操作**（3山終了・山トークン・特性・
  闇市場デッキ・CPU の王国評価キャッシュ が全部巻き込まれる）。**最大の難所**。
- **悟り(Enlightenment)＝財宝をアクションとしても扱う**＝`isTreasureFor`（資本主義）の**裏返し**が要る。

### 日本語名＝日本語wiki（`旭日`）から全50種を取得済み（`C:/tmp/risingsun_research/jp_expansion.txt`）
王国25＝魚屋/濡女/公家/名匠/室/川船/交替/小路/忍者/歌人/川の社/田舎の村/金山/勅使/駕籠/札差/浪人/狸/茶屋/狐/侍/米/山の社/大名/絵師。
イベント10＝蓄積/苦行/信用/洞察/金継ぎ/稽古/海上交易/賛辞/参集/継続。
予言15＝来寇/好機到来/官僚制/神風/悟り/盛大な取引/豊作/偉大な指導者/成長/厳冬/神器/狼狽/進歩/急速拡大/病。
訳語＝**Omen＝前兆／Prophecy＝予言／Shadow＝影／Sun トークン＝Sunトークン**。

#### ✅ 英↔日の対応は**全50種をカードページ自身から確定**（並び順の推測ではない・`g0_jp_pairs.md`）
日本語wiki の**個別カードページは「英語名・英語カード文」と「日本語名・日本語カード文」を同じ表に並べて載せている**
（`※日本語訳はDominion Onlineより`）ので、**1ページから対応と両言語のカード文が同時に取れる**。50/50 一致。
`Kind Emperor＝神器` も確認済み。**区切り線も `--------------------` として本文に出る**ので `<hr>` の位置も分かる。
- ⚠ **wikiwiki.jp は数ページ続けて叩くと 429 を返す**（50ページ中36ページが 429 になった）。
  → **`tools/jpwiki.py` に「6秒間隔＋429 なら 20/40/80/120 秒待って再試行」を入れた**（`JPWIKI_DELAY` で調整可）。
  **エージェントを並列で走らせるとこのサイトは必ず全滅する**＝日本語wiki だけは逐次で自分で取ること。

#### この過程で見つかった重要な事実
- **「旭日発表に伴う2024年版エラッタ」がある**＝**王笏(Scepter) のカード種別に [命令] が追加**された。
  → 本アプリは既に `scepter: types: ['treasure','command']` になっており**対応済み**（略奪 P2 で入れた）。
- **ホビージャパン版の印刷テキストが日本語wiki本文（Dominion Online 訳）と違うカードがある**
  （**川船／好機到来／米／絵師／進歩／盛大な取引** の6ページが言及）。各ページの「余談」節に差異が書いてある。
  ⚠ **`Biding Time`＝「好機到来」は日本語wiki 自身が「財産目当て並みの誤訳」と書いている**
  （原意は「好機を**待つ**」。DO の未発売時の訳は「待機」だった）。**印刷版の名前は「好機到来」**。
- **`Riverboat`（川船）の "play the set aside card, leaving it there" は はみだし者と同じ命令型の処理**
  （日本語wiki が明示）＝**本アプリの `playAsCommand` をそのまま使う**。

### 一次資料は全部手元にある
- `C:/tmp/risingsun_research/rulebook.pdf` / `rulebook.txt`（RGG 公式ルールブック **2024年版**・`pdftotext -layout` 済み 119KB）
- `C:/tmp/risingsun_research/_expansion.txt`（英語wiki の総論ページ）／`jp_expansion.txt`（日本語wiki）
- **`tools/wikidirect.js` に 301/302 のリダイレクト追従を追加した**（拡張の総論ページは正規URLへ 301 されるため、
  追わないと本文が1バイトも取れなかった）。スペースは `%20` ではなく `_` にする。

### 研究の進め方（3ワークフロー・**計40エージェント**）＝そのまま次の拡張でも使える型
| WF | 構成 | 成果 |
|---|---|---|
| 1 | **8群を分担収集 → 各群を別エージェントが一次資料で敵対検証**（16体・pipeline） | 確定訂正 **43件**（[high]2／[medium]12／[low]29）。**捏造ゼロ** |
| 2 | **訂正を反映した確定版8章 → 各章に完全性の批評**（16体・pipeline） | 批評の指摘 [must] **約40件** |
| 3 | **[must] を各章へ反映**（8体・parallel。**批評を鵜呑みにせず自分で grep／wiki で裏取り**させた） | 各章が「本アプリの実在する機構名で書かれた実装メモ」つきに |
- **50種すべてが本文にあることを機械検算**（50/50）。各カードに 英語カード文（逐語）／日本語カード文（DO訳）／
  区切り線の本数／版（エラッタ）／公式FAQ／**⚠ 実装で危ないところ** を載せた。
- **[must] を「反映せよ」だけでなく「疑わしければ自分で確かめて、間違っていたら理由つきで不採用にせよ」と
  指示したのが効いた**＝実際に「`allyScoreForCards` を `cardCost` に通せ」という誤った指摘が1件棄却された
  （公式は「橋のような軽減は得点計算に効かない／効くのは Cheap と Flourishing Trade だけ」）。

### 確定した中身（詳細は正本の「実装前に必読18項目」を読むこと）
- **300枚／実装対象50種**＝王国250（25種×10）＋ランダマイザー25＋イベント10＋予言15。**カードの機能エラッタは0件**。
- **機構5つのうち3つ（負債・イベント・持続）は実装済み**＝新規は **前兆/予言/Sunトークン** と **影(Shadow)** の2つだけ。
- **前兆6種**＝山の社(5D)/歌人($4)/川の社($4)/田舎の村($4)/茶屋($5)/**狐($5)＝アクション-アタック-前兆**。
- **影5種**＝魚屋($2)/小路($4)/**忍者($4)＝アクション-アタック-影**/浪人($5)/狸($5)。
  **裏面は5種とも別の絵**（wiki の `File:NinjaBack.jpg` 等を実DLして確認）＝**裏を見れば何の影札かまで分かる**。
- **影は `reshuffleDeck` の「運命の(Fated)」ブロックの直後に足すだけ**（同関数がシャッフルの唯一の入口・**対話不要**）。
- **大名(Daimyo・6D・アクション-命令)＝略奪の旗艦(Flagship)とほぼ同型**（旗艦は持続で「次回」、大名は「このターン中」）。
- **最大の難所＝神風(Divine Wind)**＝王国10山（＋11山目）を撤去して新しく10種を配り直す。
  撤去後の扱い（特性とオベリスクは効き続ける／調査(Search)は誘発しない／Ally は必要なら配る／予言は配り直さない 等）まで公式が細かく決めている。

### 出荷済みバグ7件＝**この研究の副産物として全部修正した**（回帰テスト＋バグ注入で感度確認つき）
| # | 拡張 | 内容 | 到達 |
|---|---|---|---|
| ① | 帝国 | **負債はターン中いつでも返済できる**（2024エラッタ・帝国に遡及）。`REPAY_DEBT` が購入フェイズ限定だった | 出荷済み |
| ② | 帝国 | **負債を負うのは「購入」だけ**。`takeDebt` を `gain()` から `BUY`/`BLACK_MARKET_BUY` へ移した | 木工ギルド×帝国 |
| ③ | 海辺 | **宝物庫は「購入フェイズに獲得した勝利点」だけを見る**（`t.victoryGainedInBuy` を新設）。カード文も現行へ＋webp再生成 | 出荷済み |
| ④ | 略奪 | **旗艦が永続持続を再演しても旗艦だけ捨てられる**（永続持続は予約を積まずカウンタで持つため） | `random-plunder` |
| ⑤ | 同盟 | **高原の羊飼いが特性「安価な」を見ていない**（`scoringCost()` を新設） | mix-all |
| ⑥ | 基本ほか | **民兵／軍団兵ほか／公共広場が `triggerOnDiscard` を呼んでいない**＝坑道で金貨が出ない | **`promo-pack` で今日到達** |
| ⑦ | ルネサンス | **増築が「ゲーム終了まで場に残る操舵手」を廃棄できる**（`permanentDurationCounts()` に一元化） | `random-plunder` |
- **①②は3つの検証群が独立に同じ指摘を出した**（`Debt` ページが旧ルールを
  `Prior official rules (amended by the release of the Rising Sun expansion)` として過去扱いにしている）。
- **旧い誤った挙動を固定していたテストを2件、公式へ直した**（御守り×市街の負債 16→8／木工ギルドで技術者を獲得しても負債 0）。
- ⑤の最初の回帰テストは**屋敷($2)のせいで区別できておらず、バグを戻しても緑のままだった**ので書き直した
  （＝**回帰テストは必ずバグ注入で感度を確かめる**）。
- ⑦は**同じ一覧が2箇所に二重に書かれていて片方だけ漏れていた**＝一元化したので、
  **新しい永続持続は `permanentDurationCounts` に足すだけでよくなった**（1行外すとテストが9件落ちる）。

### ツールの改善（次の拡張でも効く）
- **`tools/wikidirect.js` に 301/302 のリダイレクト追従を追加**（拡張の総論ページ `Rising Sun` は正規URLへ 301 され、
  追わないと本文が1バイトも取れなかった）。**スペースは `%20` ではなく `_`**。
- **`tools/jpwiki.py` に 6秒間隔＋429 のバックオフ再試行を追加**（**50ページ中36ページが 429 になった**）。
  ⚠ **日本語wiki だけは並列で叩くと必ず全滅する＝逐次で自分で取ること**（エージェントには触らせない）。
- **日本語wiki の個別カードページは英語原文と日本語訳を同じ表に並記している**＝
  1ページから「対応・両言語のカード文・区切り線」が同時に取れる（`C:/tmp/risingsun_research/g0_jp_pairs.md`）。

### 検証
- **npm test 全44スイート緑（exit 0・整合性5307・不変条件12）**／
  海辺 101→**106**／異郷 83→**86**／帝国 269→**280**／同盟 489→**492**／略奪 497→**508**。
- `verify:e2e` 9/9（webp 560/0）／`verify:visual` 320〜768px 全幅はみ出し0。`sw.js` v75→**v76**。

### 【次にやること】
1. **残りの出荷済みバグ候補6件**（下の §「残っているバグ候補」）を検証して潰す。
2. **段階1＝カタログ50種**（`DOM.CARDS` +25／`DOM.LANDSCAPES` +25）＋webp。
   ⚠ **`DOM.STAGE1_POOLS` に必ず入れる**（今は `[]`）。⚠ 横型の新 kind **`prophecy`** のスキンを
   `tools/build-landscape.js` に新設（**iris blue（菖蒲色）**・**コスト欄なし**）。
   ⚠ **新種別 `omen`（前兆）／`shadow`（影）** の表示ラベルを **`js/carddata.js` / `js/ui.js` の TYPE_JP /
   `test/integrity.test.js` の JP/EN マップ**（計4〜5箇所）に登録する（登録漏れは恒久検査が捕まえる）。
   ⚠ **影カードは裏面が違う**＝カード画像（webp）の扱いを決める。
3. 段階2＝engine/CPU/UI（正本の R1〜R7 の順）→ 敵対レビュー → 絵50枚 → push。

### 残っているバグ候補（**未検証・段階1の前に潰すこと**。正本の各章末「⚠ 出荷済みの実バグ候補」が出典）
| 深刻度 | 候補 | 到達 |
|---|---|---|
| [medium] | **冠(Crown)／首謀者(Mastermind)／門下生(Disciple) が `canPlayHandCard` を通らず `inPlay.push` している**＝将軍(Warlord)に止められず、航海(Voyage)の3枚制限にも数えない | **`random-allies`**（首謀者・将軍・航海はすべて同盟の非分割25種） |
| [medium] | **元手(Capital) の「場から捨てるとき負債6」が支配(Possession)の振り分けを通っていない**（`p.debt` に直接加算。他の負債付与4サイトは全部 `t.possessedBy` を見ている） | mix-all（錬金術+帝国）・**人間だけが通る道** |
| [low] | **`gainFromOutside` が `t.lastGainedAny` を更新していない**＝廃棄置き場/闇市場から獲得しても行人(Wayfarer)のコストが動かない（修正は1行） | mix-all |
| [low] | **ヴィラ／騎兵隊／発進でアクションフェイズへ戻るとき、購入フェイズ終了時の効果（ワイン商／野外劇／探査）が誘発しない**（公式は誘発する。`launch` だけ許容簡略化として明記済み） | mix-all |
| [推定] | **`reshuffleDeck` の「回避(Avoid)」が `state &&` ガードの中にあり、85呼び出しのうち state を渡すのは12箇所だけ**＝六分儀・書庫などでは回避が発動しない | **`plunder-events`** |
| [推定] | **`TAX_PILE` が空の山を拒否する**（公式裁定は取れず未確定。厳冬(Harsh Winter)をコピーすると食い違う） | 帝国 |
| [doc] | `js/cards.js` の旅行(Journey)のコメントが、すぐ下のカード文と engine（2023エラッタ）と**正反対**（実行時バグではないが、次の実装者がコメントに従うと表示と挙動が食い違う） | — |

### 注意（次セッションが知らないと事故る・旭日固有）
- **予言は「カード」ではない**（公式逐語＝`Prophecies are not considered "cards" at all`）＝
  `allCards` にも保存則 tally にも入れない。**横型の「合計2枚まで」にも数えない**（同盟の Ally と同じ扱い）。
  Sun トークンも非カード。
- **「+1 Sun」は前兆カードの記載の一番最初に必ず来る**＝**予言はそのカードの残りの効果より前に発動しうる**。
  **`Kind Emperor` と `Divine Wind` は「最後の Sun を取り除いた瞬間」に即時発火する**＝
  **予言の発動を「次のターン開始時にまとめて」に遅延させる実装は公式違反**。汎用の発動フックを作ること。
- **影札は「手札にあるわけではない」**＝小路の捨て札・地下貯蔵庫・民兵・書庫の手札枚数には数えない。
  **逆に庭園/品評会/絹の道など「所有カードの総数」には数える**（山札の底も所有カード）。
- **影札の使用はアクション権を消費する**（Donald X.＝`you have to be allowed to play an Action, it doesn't get around that`）。
- **`Continue`（イベント・8D）は購入フェイズ→アクションフェイズへ戻る＝ヴィラと同じクラス**
  （闘技場の再武装／宝箱の再発動／`gainWasBuyPhase` を必ず通す）。**`Continue` だけが `Once per turn:` 付き**。
- **`River Shrine` の「このターンの購入フェイズで1枚も獲得しなかったか」は既存の `t.buyPhaseGained` を使う**
  （新しい旗を作らない／隣の `t.bpGained` は `END_ACTION_PHASE` で 0 に戻るので掴むと公式違反）。
- **`Sickness` は「ちょうど3枚捨てる」**＝民兵型の `discardDownEnter`（N枚に**なるまで**捨てる）を流用してはいけない。
  正しい前例は `FORUM_DISCARD` の `Math.min(3, hand.length)`。
- **`Rapid Expansion` は略奪の特性「せっかちな(Hasty)」そのもの**＝既存機構（`p.eventSetAside`＋`event_play`）を流用する。
- **`triggerOnGain` の `_gainDepth > 6` と `_gainDepth === 1 && !state.pending` ゲート**が、
  `Growth` の連鎖や `Kind Emperor` の獲得に効く（＝獲得時対話が開かない）。許容簡略化として明記すること。

---

## 0-29. **新拡張＝同盟（Allies）に着手**：公式ルール研究＋段階1（カタログ72種）完了（2026-08-12・未push・`sw.js` v63）

**ユーザー決定＝次の拡張は同盟**（発売順どおり。残る未着手は 略奪／旭日）。
**現時点では CARD_SETS 未参照＝実プレイには出ない**（本番挙動は不変。闇市場にも出ないよう明示的に塞いだ＝下記）。

### 規模：**72種＝王国49（非分割25＋分割山6組×4種24）＋同盟(Ally)カード23**
公式の400枚（通常王国250＋分割山96＋ランダマイザー31＋Ally23）と一致。王国の「山」は31個（非分割25＋分割山6）。
縦型（`DOM.CARDS`）＝王国49＋**分割山のプレースホルダ6**＝**55エントリ**（450→**505**）／
横型（`DOM.LANDSCAPES`）＝Ally23（148→**171**）。**合計676枚**。

### 正本＝`docs/research/allies_rules.md`（55万字）＝**実装前に必ず冒頭の「実装前に必読」9項目を読む**
多エージェント研究22体（11群を分担収集 → **各群を別エージェントが一次資料で敵対検証**）。
夜想曲と違い **RGG の PDF は現行（2023年12月・第2刷）で罠は無い**。機能変更は **Island Folk と Voyage の
「3ターン連続不可」の2枚だけ**（＋Elder の言い回し）。日本語名は日本語wikiで**全72枚を機械照合（一致72/72）**。
- **⚠️ Wayback の2025年12月以降のキャプチャは Anubis の bot 検知画面が保存されている**（実際に読める最新は2025年1月ごろ）。
  `tools/wikifetch.py` が返す **snapshot の年を必ず見ること**（`2019id_` にフォールバックすると同盟より前のページを読む）。

### 新機構（段階2で実装するもの）
1. **好意(Favor)トークン**＝`p.favors`（財源/村人と**完全に別枠**・公開・**得点にならない**・**上限なし**）。
   与えるのは**連携(Liaison)9種だけ**＋開始時1個（**輸入者があるゲームは5個**）。
2. **同盟(Ally)カード＝横型23種から1枚だけ**。**王国に連携が1枚でもあれば**選ばれる（横型の合計2枚制限に数えない）。
   → **災いカード(Bane)と同型＝`createInitialState` で決める**のが素直（サーバ権威・再戦も自動で安全）。
3. **循環(Rotate)**＝分割山の**先頭からの連続同名ブロック**を末尾へ回す。
4. **分割山6組**＝**4種×4枚＝16枚**。既存の `DOM.SPLIT_PILES`（下段id→上段id の**2段専用**）では表現できない。
   → **混合山（`state.castles`/`knights`）と同型**にする（実カードid配列＋一番上だけ購入/獲得可）。

### 段階1＝完了
- `DOM.CARDS` +55／`DOM.LANDSCAPES` +23（カタログ生成WF 18体＝9群を起草し**別エージェントが逐語検証**）。
- 新種別7つ＝**連携(liaison)／町民(townsfolk)／卜占官(augur)／衝突(clash)／城砦(fort)／叙事詩(odyssey)／魔法使い(wizard)**。
  表示ラベルは **`types` 配列の順に連ねる汎用規則**にした（組み合わせが20通り以上あり手書き分岐では現実的でない）。
  **持続との複合が多い**（輸入者/契約書/駐屯地/航海/将軍/要塞/霊術師）ので `duration` 判定より**前**に置く。
- 孤立プール `allies`(31＝山の数) / `allies_split`(24＝分割山の中身) ＋ `DOM.ALLIES_SPLIT_PILES`（山→中身4種の並び順）
  ＋ `DOM.ALLIES_LIAISONS`(9) ＋ `DOM.ALLIES_ALLY`(23) ＋ GAIN_ORDER ＋ カード一覧の群3つ。
- **闇市場に漏らさない2重の措置**：(1)分割山の中身24種を `mixedContents` に入れた（**恒久**＝山の一番上でしか得られない）／
  (2)**`DOM.STAGE1_POOLS = ['allies']`** を新設し闇市場デッキから除外（**段階2でこの配列から外す**）。
  ＝これが無いと promo-pack/random-promo で「買っても何も起きない死に札」が $0 で並ぶ。
- **webp 78枚生成**（縦55＝`build-cards.js`／横23＝`build-landscape.js`。**絵は未回収＝47〜67KB の枠＋文字だけ**）。
  横型に **`ally` スキン（濃い藍・コスト円なし・ラベル「同盟 / Ally」）** を新設。
- 資本主義の財宝集合 114→**130**（同盟の +$ を持つアクション16枚）。**大工/改造は「コストが最大2コイン高い」＝
  コスト参照であって +$ ではない**ので番人として除外リストに追加した（強制退去と同じ罠）。

### 検証
- **npm test 全38スイート緑（exit 0・整合性 4184→4763・不変条件10）**／`verify:e2e` 9/9（**webp 505/0**）／
  `verify:visual` 320〜768px 全幅はみ出し0。**既存テストの件数は資本主義の1件以外すべて不変＝本番挙動は不変**。

### 【次にやること】段階2（この順）
1. ~~★最初に決める★ 占星術師団／メイソン団の実装方式~~
   → **【ユーザー決定・2026-08-12】(a) 常設方針＋自動選択を採用（＝許容簡略化を1つ増やす）**。
   両者は「**シャッフルのたびに好意を払って全カードを見て、払うたびに選び直せる**」対話を要求するが、
   `reshuffleDeck(p)` は**同期・非対話**（74箇所から呼ばれ、`draw()`→`applyEffect` の途中で起きる）。
   星図・へそくりが**まったく同じ理由**で自動選択になっている（§0-22）。
   **完全非同期化は現実的に不可能**＝`case 'smithy': draw(state,pi,3)` の途中で止めるには、
   ドローする全カードを前半/後半に分割する書き換えが要り、14拡張すべてに回帰リスクが及ぶ。
   - **実装＝本人がいつでも変えられる常設方針「1回のシャッフルに好意を何個まで使うか」**（`p.stashPlacement`／
     `STASH_SETTING` と同型の新 action）＋**どの札を選ぶかはエンジンが最善を自動選択**
     （占星術師団＝一番良い札を上へ／メイソン団＝一番悪い札を捨て札へ。`starChartPick` の評価関数を流用）。
   - **根拠＝シャッフル直後はほぼ必ず引くので「良い札を上に／悪い札を捨て札に」がほぼ常に最善**＝
     戦略的に意味がある選択（**何個使うか**）だけを本人に残せば実質的な忠実性の損失は小さい。
   - ※「密使・下役は**好意をくれる前に**シャッフルを起こしうる＝まだ持っていない好意はそのシャッフルには使えない」
     という**評価順序**は自動選択でも守ること（公式FAQ）。
2. ~~**A1＝好意トークンと Ally 選定の基盤**~~ ✅**完了**（`9be79e4`）＝`p.favors`／`p.favorShuffle`（常設方針の器）／
   `state.ally`（連携があれば23枚から1枚・開始時 好意1個・輸入者なら5個）／`alliesHasLiaison`（**分割山の中身まで走査**）。
   ※好意の**付与（連携カードの効果）とUI表示・使用窓はまだ無い**＝A3/A4 で入る。
   ### 【この作業で見つけたテストのバグ】不変条件「祝福12種が消えない」が `state.boonQueue` を数えていなかった
   祝福は複数を順に受けるので pending を直接立てず `boonQueue` に積むが、**再演（山砦/玉座の間）が絡むと
   pending=null のまま列に祝福が残って reduce が終わる**ことがあり、検査点で「消えた」と誤検知していた
   （実際は次の reduce で解決される＝queue 込みで数えると常に12種であることを機械確認）。
   同盟のカタログ追加で fuzz の乱数列が変わって初めて表面化した**既存の取りこぼし**。修正済み。
   ※呪詛側は `hexQueue` が被害者の席番号しか持たず呪詛idは `t.currentHex` にあるので穴は無い。
3. ~~**A2＝分割山6組**（混合山モデル＋循環）~~ ✅**完了**（下の「段階2 A2」節が詳細）。
3b. ~~**A2b＝「カードの種別」を山の一番上で判定する横断修正**~~ ✅**完了**（下の「段階2 A2b」節が詳細）。
4. ~~**A3＝Ally 23種**（`createInitialState` で1枚決定＋盤面表示＋好意の使用窓）~~ ✅**完了**（下の「段階2 A3」節が詳細）。
5. **A4＝王国カード49種**（素直な系→持続→アタック→複雑）。
6. **A5＝CARD_SET 昇格** → 多エージェント敵対レビュー → UIテスト新設・CPUソーク・`verify:e2e`/`verify:visual`。
7. **絵（webp）の回収 72枚**（分割山のプレースホルダ6枚は最安カードの絵を流用＝城 `castles` と同じやり方）
   → PROGRESS 更新 → **ユーザー確認の上で** push。

---

## 段階2 **A2＝分割山6組（混合山モデル）＋循環(Rotate)＝完了**（2026-08-12・未push・`sw.js` v64）

### 混合山リテラルを1箇所に集約（**これが今回いちばん効いた**）
engine 内に散らばっていた `['ruins','knights','castles']` のリテラル比較を **`MIXED_PILE_KEYS`（＝廃墟/騎士/城＋同盟の6山＝9山）**
1本に集約した。`gain` の `isMixed`／`trashFromSupplyPile`／`returnToPile`／`availableInSupply`／`exileFromSupply`／
`cardCost`／`populatePiles`／封鎖(`BLOCKADE_GAIN`)／追放候補(`exilableSupplyIds`)／`maskStateFor`／闇市場 が一斉に追従する。
- **`MIXED_PILE_CONTENTS`**（中身＝廃墟5＋騎士10＋城8＋同盟24）＝名指しで購入できない／闇市場デッキに入れない集合。
- **`HIDDEN_MIXED_PILE_KEYS = ['ruins','knights']`**＝**中身が秘密**の混合山（無作為に積むので順序が情報になる）。
  **城は昇順で決定的／同盟の分割山は公式に「いつでも全部見てよい」＝どちらも全公開**。`maskStateFor` と
  サーバの「同意なしの1手もどす」が同じ集合を見る。
- **`test/invariants.test.js` ほか3スイートの保存則 tally も `MIXED_PILE_KEYS` を見る**ようにした（新しい山を足しても二重計上しない）。

### 分割山6組＝`state.augurs` … `state.wizards`（実カードid配列16枚）＋`supply[山キー]`（残数・同期）
- **4種×4枚＝16枚を安い順**に積む（`The cards start the game in order by cost.`）。**人数によらず常に16枚**（城のような調整は無い）。
- **買える／獲得できる／サプライから廃棄できるのは一番上だけ**。`cardCost(state,'augurs')` は**今の一番上**（$3→$4→$5→$6 と動く）。
- **「山のコスト・種別」はプレースホルダ（randomizer＝最安カード）**が持つ＝**この2つを混同しない**。
  例＝叙事詩は遠い海岸（勝利点）を含むが「勝利点の山」ではない（発明家の家族が置ける）／城は randomizer が勝利点なので置けない。
- **山を名指しする効果はその山の4種すべてに効く**＝`pileKeyOf` に中身24種→山キーの写像を追加（徴税の負債・冒険の山トークン）。
- **3山終了は16枚全部が無くなって初めて1山**。`emptyPileCount` は supply の残数で自然に効く。
- 塔（帝国）＝中身も「16枚すべて空」で初めて空山由来／オベリスク＝**その山の4種すべて**を数える／植民＝同盟の6山は対象（城は勝利点なので対象外）。

### 循環(Rotate)＝`rotatePile(state, pileId)`
- 公式逐語＝**一番上のカードと、その直下に連続する同名のカードだけ**を山の一番下へ。**離れた同名は動かさない**
  （wiki の例＝生徒1枚→リッチ4枚→生徒2枚 なら動くのは先頭の生徒1枚だけ）。
- **常に任意／空の山・中身が1種類だけの山を回しても合法**（何も起きないだけ＝拒否も例外もしない）。
- **戦闘計画(Battle Plan)＝「任意のサプライ山」**＝`rotatableSupplyPiles`（非サプライは返さない／2段分割山は上段キーに正規化）。
  **帝国5組・サウナ/アヴァントの2段分割山も回せる**＝`state.splitRotated {上段id:true}` で上下の入れ替わりを表し、
  `splitLocked` がそれを見る（`state.splitRotated` が無い旧スナップショットでも壊れない）。
- **カードの効果側（薬草集め/古地図/天幕/触れ役/生徒/戦闘計画）は A4 で配線する**＝現時点でどの reducer からも呼ばれない。
  **循環の位置はカードごとに違う**（生徒だけ「循環 → その後に強制廃棄」で循環が先）。

### 多エージェント敵対レビュー（5観点）＝**確定13件（うち9件は同盟と無関係な既存バグ）→ 全修正・回帰テスト付き**
各 finding は担当エージェントが **node／実ws で再現してから確定**（偽陽性は棄却）。
**[high] 1件**
1. **相続(Inheritance)が混合山の山キーを脇に置けてカードが1枚湧く（保存則違反）**。プレースホルダは実在する1枚ではないので
   `supply` だけ減って実カード配列が減らない。**$4以下のアクション山が同盟の6山しか無い王国では CPU が20戦中19戦で踏んだ**。
   原因＝`id !== 'knights'` という**「3つ組ではなく1つだけ」のリテラル**が残っていた（同型が計4箇所）。

**[medium] 5件**
2. **闇市場デッキに混合山の「山キー」が入っていた（既存バグ）**＝`knights`/`castles` を $3/$5 で買えて、
   実在しないプレースホルダ札が捨て札に湧き**終局後のデッキ公開にも出ていた**。中身だけ塞いで山キーを塞いでいなかった。
   **A5 で `STAGE1_POOLS` から `allies` を外すと同盟の6山ぶん増える**ことも実測されたので今のうちに塞いだ。
3. **オンラインの「同意なしで1手もどす」で伏せ札の騎士の山の次の1枚を無料で覗けた（既存バグ）**＝
   買う→2枚目が配信される→承認なしで巻き戻す、が実 ws で再現。`HIDDEN_MIXED_PILE_KEYS` の比較を追加。
   **城・同盟の分割山は全公開なので比較しない**（入れると買うたび承認制になり UX が落ちるだけ）。
4. **汚された神殿の山上VPが孤児化していた（既存バグ）**＝VPは**山キー**に載るのに獲得した実カードidで引いていた。
   同盟の分割山だけでなく**帝国の2段分割山の下段（騒がしい村/石/大金…）でも永久に取れなかった**。`pileKeyOf` で正規化。
5. **命令（船長/大君主/はみだし者）の対象に同盟の6山が出ていた**＝選ぶとアクション権を消費して何も起きない死に選択肢。
6. **山トークン（冒険）が財宝を使ったときに乗っていなかった（既存バグ）**＝公式逐語
   `The token can be put on the Odyssey pile, and then Sunken Treasure will also make +$1 when played.`
   帝国の分割山（石／鹵獲品／大金＝いずれも財宝）にも波及する取りこぼし。`playTreasureCard` の先頭に配線した
   （炉で中断しても取りこぼさない位置・二重発火なし）。

**[low] 7件**
7. **取り替え子(Changeling) が分割山・混合山由来のカードを交換できなかった**（公式は「その山の一番上に戻す」）。
   `canReturnToPile` を新設して `returnToPile` に委譲＝騎士/城の中身も同時に直った。
8. **循環中の2段分割山に冒険の山トークンを置けなくなっていた**（**この作業で入れた退行**）＝
   `splitLocked` の意味を変えたことで `actionSupplyPiles` の `!splitLocked` が死にコードから live になり、
   循環中の山が候補から丸ごと消えた。**山の種別は randomizer 固定＝循環しても置けるのが公式**なので条件を外した。
9. **獲得モーダルと山タップの拡大シートがプレースホルダを描いていた**（盤面の `pileEl` だけ直っていた）＝
   「城 $3」と表示して実際には「王城 $10」を買う食い違い。**表示idだけ一番上の実カード**にし、
   **dispatch は山キーのまま**（騎士/城でも従来から同じ乖離があった）。
10. **[出荷済みの表示バグ] `js/ui.js` の種別ラベル表（TYPE_JP）に12種別が欠けていた**＝
   `typeLabel` が undefined を連結し「アクション・アタック・」のように末尾が欠けたラベルが出ていた
   （暗黒時代の略奪者／夜想曲の夜行・幸運・不運・家宝・精霊・ゾンビ／同盟の7種別）。
   **`js/carddata.js` とは別の表を ui.js が持っている**のが原因。全種別を足し、
   **`test/ui.test.js` に「全カード種別が TYPE_JP にあるか」の恒久検査**を追加した（今後の足し忘れを構造的に防ぐ）。
11. **3スイートの保存則 tally が3つ組リテラルのまま**＝同盟の山を置いた瞬間に二重計上で偽陽性の赤になる。
12. **`BLACK_MARKET_BUY` に山キーのガードが無かった**＝デッキ構築側を直しても、**v63 以前に始まって
   オンラインで永続化された対局を復元すると、既にデッキへ入っている**ので買えてしまう
   （§0-17 の `pending.self` と同型＝**旧スナップショットの互換は受理側で守る**）。受理側にも同じガードを入れた。
13. **山トークンが「アクション権を消費しない使用」で乗っていなかった**（博打／苦労／進軍／遅延・刈り入れ）＝
   財宝の経路だけ直すと**非対称**になる。`playCardNoAction` にも配線して公式（「その山のカードを使ったとき」）に揃えた。

**CPU 非ループ／UI 詰み観点は クリーン（finding 0）**＝敵対レビューの**ソーク1168戦（危険な同居王国・
全プール混成・同盟×イベント/ランドマーク/習性/プロジェクト・2〜4人×全難易度）で
完走1168・膠着0・例外0・「engine が状態不変で拒否した action」0**。
`splitLocked` の書き換えは**帝国5組＋サウナの108通りで旧実装とビット一致**（不一致0）を機械確認。

### 検証
- **npm test 全39スイート緑（exit 0・整合性4763・不変条件 10→12）**／新設 **`test/allies.test.js` 148件**
  （A1 の好意・Ally 選定＋A2 の分割山・循環・既存機構との配線・敵対レビュー回帰＋CPUソーク）。
  `test/server.test.js` 82→**86件**（伏せ札の山の購入は承認制へ落ちる回帰）／
  `test/ui.test.js` 119→**121件**（種別ラベルの網羅検査）／`test/empires-ui.test.js` 75→**82件**（混合山の表示）。
- `verify:e2e` 9/9（**webp 505/0**）／`verify:visual` 320〜768px 全幅はみ出し0。
- **同盟そのものは本番に出ない**（CARD_SETS 未参照）。ただし上記の**既存バグ9件は本番の挙動が変わる**
  （闇市場の山キー／オンラインUndoの覗き見／汚された神殿／山トークン×財宝・×アクション権を使わない使用／
   取り替え子／獲得モーダルと拡大シートの表示／種別ラベル）。**すべて公式へ寄せる方向**。

### 段階2 **A2b＝「カードの種別」を山の一番上で判定する横断修正＝完了**（敵対レビュー2体が独立に指摘）
同盟の分割山は**中身の種別が不均一**（叙事詩に財宝・城砦/衝突/叙事詩に勝利点）なので、
`cardCost` は一番上を見るのに `DOM.isType(山キー, ...)` は randomizer を見る、という**対の不整合**が破れていた。
- 実証されていた誤り＝**待ち伏せ**が沈没した宝物（財宝）を「アクションカード」として廃棄できる／
  **昇進・大学・車大工**が同じものを「アクションカード」として獲得できる／
  **塩まき**が要塞（勝利点）を「勝利点カード」として廃棄できない／
  **鉱山・収税吏・英雄**が沈没した宝物を「財宝」として獲得できない。
- **修正＝`isTypeSupply(state, id, ty)` を新設**（＝`mixedTopCard(state,id) || id` の種別）し、
  **「サプライから獲得/廃棄するカードの種別」を見る述語だけ**を engine 21箇所・CPU 17箇所・UI 10箇所で寄せた
  （**engine/CPU/UI を同一コミット**＝片側だけ直すと engine拒否×CPU提案で本番 livelock になる）。
  `isTreasureFor` も内部で一番上を解決するようにしたので、財宝を参照する述語は全部自動で正しくなる。
- **「山のコスト・種別」を参照する効果は randomizer のまま**（公式逐語 `go with what's on the Randomizer card.`）＝
  植民／若き魔女の災い／冒険の山トークンの置き先／発明家の家族／汚された神殿／オベリスク。
  **直しすぎていないこと**も回帰テストで固定した（一番上が財宝でも叙事詩は「アクションの山」のまま）。
- **敵対レビュー（A2b 専任1体）＝確定3件（うち [high] 2件）→ 全修正・回帰テスト付き**。
  **どれも「私の置換漏れ＝3面のうち1面だけ直っていない」**という、まさに一番危険な形だった：
  1. **[high] 従者(squire)**＝engine の**窓を開く述語**だけ寄せて**受理する reducer と UI フィルタ**が randomizer のまま。
     分割山が「サプライで唯一のアタック」だと CPU が同じ手を返し続けて **livelock**（ソーク72戦中3戦で自然発生）。
  2. **[high] 待ち伏せ(lurker)**＝**CPU だけ**静的判定のまま。CPU は「安いアクション山」から選ぶので
     randomizer が $2〜$3 の同盟の分割山が必ず先頭に来る＝一番上が財宝/勝利点だと **livelock**（36戦中1戦）。
  3. **[medium] 塩まき(salt_the_earth)**＝engine だけ寄せて CPU/UI が randomizer のまま＝
     **騎士の一番上がデイム・ジョセフィーヌ（勝利点）のとき、engine は受理するのに人間が選べない**
     ＝mix-all（暗黒時代＋帝国イベント）で**今日すでに到達可能**な忠実性の欠落。終端保証も追加した。
  ＋副産物＝**カード名を宣言するモーダル（建て直し/秘術師/医者/熟練工）が混合山のプレースホルダしか出していなかった**
  （山キーを宣言しても絶対に一致しない死に宣言／本物のデイム・ジョセフィーヌを名指しできない＝追求と同型の既存バグ）。
- 「直しすぎ（山参照まで一番上にする）」「`isTreasureFor` の既存14拡張への退行」は**レビューでクリーン**
  （新旧で判定が変わる id は混合山の山キーだけ＝手札/場/山札の判定は完全に不変であることを機械確認）。
- 検証＝**npm test 全39スイート緑**（`test/allies.test.js` 148→**169件**／`darkages-ui` 57→**61件**）＋
  **A2b ソーク計150戦**（叙事詩/城砦/衝突/魔法使い/町民/卜占官を0〜12枚掘った状態 ×
  待ち伏せ/従者/大学/行進/建て直し/車大工/値切り屋/石工/鉱山/収税吏/英雄/塩まき/昇進/海路/宴会/墓暴き/祭壇 ×
  騎士/城/2段分割山/闇市場、2〜4人×全難易度）
  ＝**完走150・膠着0・例外0・保存則違反0・engine拒否0**。
  `verify:e2e` 9/9（webp 505/0）／`verify:visual` はみ出し0。`sw.js` v64→**v65**。

### 既知の許容簡略化（意図的）
- **密輸人(Smugglers)・巡礼(Pilgrimage) は「分割山/混合山の中身」をコピー獲得できない**。
  これらは `costUpTo`/`gainableBase`（＝supply キー基準）で候補を作るため、`acolyte` が山の一番上にあっても候補に出ない。
  **騎士/城でも従来から同じ**（engine拒否とCPU非提案は一致しているので livelock にはならない＝忠実性のみの差）。
  根治するには「山の一番上の実カードidを名指しで獲得できる」経路を `gainableBase` に足す横断変更が要る（A5 で再検討）。

### 注意（A3/A4 に入る前に知っておくこと）
- **新しい混合山を足すときは `MIXED_PILE_KEYS` に足すだけでよい**（9系統が自動追従）。
  中身も買えなくするなら `MIXED_PILE_CONTENTS`、中身を伏せるなら `HIDDEN_MIXED_PILE_KEYS` にも足す。
- **「山キーはプレースホルダ＝実在する1枚のカードではない」**。プレイヤーのゾーン・廃棄置き場・闇市場デッキ・
  相続の脇・命令の対象 に入れてはいけない（入れると保存則が壊れるか、効果ゼロの死に選択肢になる）。
  **`id !== 'knights'` のような「1つだけのリテラル」を書かない**＝必ず `isMixedPileKey(id)` を使う。
- **山の上に何かを載せる効果（山上VP・負債・山トークン）の READ は必ず `pileKeyOf` を通す**。
  実カードidで引くと分割山で永久に孤児化する（今回の汚された神殿・§0-20 の徴税と同型）。
- **`rotatePile` はまだどの reducer からも呼ばれていない**（A4 でカードに配線する）。配線するときは
  **`PLAYER_ACTIONS` 登録＋CPU `decidePending`＋UI `viewPendingModal` の4点セット**を忘れないこと。
  循環は**任意**なので「回さない」ボタンが必ず要る。
- **サーバの `isNoConsentUndoableBuy` は「購入で動きうる非プレイヤー状態」を全部比較する方針**にしてある
  （伏せ札の山・`splitRotated`）。A4 で「購入時に山を循環させる」効果を足すなら、この比較で自動的に承認制へ落ちる。

---

## 段階2 **A3＝同盟(Ally)カード23種＝完了・push済**（2026-08-12・`5c377b0`・`sw.js` v66・本番反映を実機確認）

**Ally の23枚すべてが実装済み**（盤面表示・好意の使用窓・全効果）。**同盟はまだ CARD_SETS 未参照＝実プレイには出ない**。
新 action **12種**すべて4点セット（engine reducer＋`PLAYER_ACTIONS`＋CPU `decidePending`＋UI `viewPendingModal`）。

### 誘発窓は9系統。**すべて `t.startQueue` / `state.onGainQueue` に積む**（`state.pending` 直代入をしない）
| 窓 | Ally | 実装の入口 |
|---|---|---|
| 自分のターンの開始時 | 穴居民/工芸家ギルド/砂漠の案内人/森の居住者/すり師団/山の民 | `allyStartOfTurn`（`resolveDurationStartEffects` の末尾＋**`createInitialState` の末尾**） |
| 自分の購入フェイズの開始時 | 銀行家連盟(自動)/発明家の家族/市場の町/平和的教団/木工ギルド | `allyBuyPhaseStart`（`END_ACTION_PHASE` の末尾） |
| 連携を使用した後 | 魔女の輪/小売店主連盟(自動) | `noteAllyPlay` → `t.allyPlayed` → reduce 末尾の `drainAllyPlayed` |
| アクションを使用した後 | 写本士の仲間たち | 同上 |
| カードを獲得したとき | 建築家ギルド/遊牧民団/都市国家/罠師の小屋 | `triggerOnGain` の末尾 → `onGainQueue` |
| クリンナップの手札捨て | 沿岸の避難港 | `endBuyTailSchemeOrCleanup` の末尾（`t.coastalKeep`） |
| ターンの終了時 | 島民 | `cleanupAndAdvance`（**先引きの後**）→ `finishTurnAdvance` へ分割 |
| シャッフルするとき | 占星術師団/メイソン団 | `reshuffleDeck`（常設方針 `p.favorShuffle` ＋自動選択） |
| 得点計算時 | 高原の羊飼い | `allyScoreForCards`（`scoreGame` の `allyVp`／CPU の `winsIfEnds` も同じ関数） |

- **`state.pileFavor`**（発明家の家族が山に置く好意トークン）＝**非カード・公開**（`state.pileVP`/`pileDebt` と同型）。
  `cardCost` で **全員に・常時・累積**でコスト軽減（$0未満にしない）。山キーは `pileKeyOf` で正規化＝**分割山の中身も安くなる**。
  置ける山の判定は **randomizer（山）の種別**＝城(Castles)には置けないが**騎士・同盟の分割山には置ける**（公式FAQ逐語）。
- **島民**＝`t.chain`（同じ席の連続手番数）で「3ターン連続不可」を判定。追加ターンは**タイブレークに数えない**（`freeTurns`）。
  **`cleanupAndAdvance` を `finishTurnAdvance` に分割**して、先引きの**後**に窓を開く（次の手札を見てから決められる＝公式）。
- **占星術師団/メイソン団**＝§0-29 の決定どおり「1回のシャッフルに使う好意の上限だけ本人が決め、札はエンジンが自動選択」。
  `p.shuffleAlly`（どちらの Ally か）＋`p.favorShuffle`（**人間は既定0／CPU は既定1**）＋盤面のトグルボタン（`FAVOR_SHUFFLE_SETTING`）。
- **森の居住者は既存の汎用 `look_arrange` に委譲**（`source:'forest_dwellers'`）＝マスク（私的看破）も自動で効く。
- **すり師団は `discard_down`（アタック用）を流用していない**＝専用 pending `ally_gang`。
  流用すると CPU の「アタックを受けたら馬商人を脇に置く」分岐が誤爆して engine 拒否×CPU提案の livelock になる。

### 敵対レビュー（多エージェント6観点＝ルール忠実性A/B・CPU非ループ・UI詰み・保存則/マスク/永続化・既存退行）
各 finding を**別エージェントが node で再現してから確定**（偽陽性は棄却）。**確定7件＋自己発見1件 → 全修正・回帰テスト付き**。
- **CPU 非ループ＝クリーン**（Ally 23種 × 敵対王国19種 × 2〜4人 × 全難易度 × 好意0/1/3/5/20 ＝ **2,668戦**
  ＝完走2668・未終局0・CPUのnull 0・engine拒否0。窓20バリアントすべてに到達）。
- **既存14拡張への退行＝クリーン**（A3 前後の engine/cpu/ui を同一シードで**完全並走**し、CPU の手と state 全体を
  毎ステップ突き合わせ＝出荷セット144ゲーム／全49 CARD_SET 245ゲーム／`finishTurnAdvance` 分割の等価性1200試行／
  `reshuffleDeck` 4000試行／`cardCost` 5591回／jsdom で旧新 ui.js の innerHTML 完全一致132レンダー ＝**すべて差分0**）。

**[high] 1件（＝A3 以前からある UI の穴が A3 で通常プレイに出るようになった）**
1. **`UI.selection` がターンをまたいで持ち越され、人間が完全に詰む**。
   `viewPendingModal` の選択リセットは **pending のキー（type+stage）が変わったときだけ**走る。A3 は
   **毎ターン同じキーで開く窓を3つ**足した（沿岸の避難港＝毎片付け／平和的教団＝毎購入フェイズ／すり師団＝毎ターン開始）。
   CPU の手番中はモーダルが描かれず（`interactive=false`）キーが変わらないので、**前ターンの手札インデックス**が残る。
   手札の枚数が変わって範囲外になると、そのチップは描画されない＝外す手段が無く、送信すると `cards:[undefined]` で
   engine が状態不変で拒否し続ける（END_TURN も財宝も pending に阻まれる）＝**完全な詰み**。初心者モードOFFだと
   `.modal-scrim` が盤面も☰も覆うので脱出口ゼロ。→ **確定した時点で選択を捨てる**（`takeSelection`）方式に統一し、
   描画時にも範囲外を間引く（`pruneSelection`）。**地下貯蔵庫/民兵/拷問人/衛兵など A3 以前からの同型の穴も同時に塞がった**。

**[medium] 5件**
2. **「カードを使用した後」の Ally が、別のカードの効果で起きた1回目のプレイで誘発しない**（再演では誘発するのに非対称）。
   玉座の間×下役＝+$1（公式+$2）／王の宮廷×下役＝+$2（公式+$3）／大君主・技術革新＝0（公式+$1。**FAQ が Innovation を名指し**）／
   冠×道化棒＝1回（公式2回）。→ `noteAllyPlay` を **22箇所**（玉座/王の宮廷/行進/門下生/冠/首謀者の1回目・
   `playAsCommand`・技術革新・家臣・伝令官・ゴーレム・ピアッツァ・水晶玉・会計士・船乗り・狂戦士・狂信者の連鎖・
   サウナ連鎖・鷹匠/牧羊犬/村有緑地のリアクション・**財宝の2回目 `treasure_replay`**）に配線。
   ※`playAsCommand` は `commandId !== 'estate'` のときだけ note する（相続の屋敷は呼び出し元が既に note 済み＝二重に数えない）。
3. **ゲームの最初のターンだけ開始時 Ally の窓が開かない**（先手だけ すり師団を1ターン免れる非対称／初期好意1をターン1で使えない）。
   `allyStartOfTurn` の唯一の親 `resolveDurationStartEffects` は `cleanupAndAdvance` からしか呼ばれない＝先頭手番が素通りしていた
   （持続もプロジェクトもターン1には存在し得ないので今まで露見しなかった構造的な穴）。→ `createInitialState` の末尾で最小限に実行。
4. **砂漠の案内人が捨て札トリガーをドローの後に解決していた**＝坑道の金貨がリシャッフルに入らない／織工が完全に空振り／
   村有緑地・忠犬が「押しても何も起きない死に選択肢」になる（§0-28 の羊飼いで踏んだのと同型・**同じコミット内の穴居民は正しい順**）。
5. **メイソン団が1回のドロー指示の中で何度もシャッフルする**＝好意を払って捨て札に残した札をそのまま引き直し（＝払い損）、
   さらに常設方針の上限を超えて好意を多重消費する（上限1で2個、上限2で5個）。公式FAQ逐語
   `you only shuffle one time, even if Order of Masons put some cards back in your discard pile.`
   → **`reshuffleDeck` が「残したか」を返し、`draw()` と吟遊詩人のループが同じアクセスでは2度目のシャッフルをしない**。
   ※フラグの寿命は `draw()` 1回ぶん（望楼/熟練工のように1枚ずつアクセスするカードは次の呼び出しで改めてシャッフルする＝公式）。

**[low] 2件**
6. **星図(Star Chart) × 占星術師団**＝自動選択が星図の置いた札を選び直すので、星図が完全に無効化され好意が1個ドブに捨てられる
   （星図あり/なしで結果が完全一致することを200試行で実測）。公式は「星図で1枚を無料で置いた**うえに**追加の1枚を置ける」。
   → 星図のぶんを候補から除外する。
7. **森の居住者：解決中に山札の上へ置かれたカードが、戻した残りのカードより上に来る**（公式は逆）。
   ＝汎用 `look_arrange`（夜警／太陽の恵みと共有）の**A3 以前からの挙動**。忠実にするには「戻す残り」を pending に
   保持する再開網が要る割に影響が極小＝**許容簡略化として記録**（下記）。

**棄却（偽陽性・既知の割り切りと同種）**：罠師の小屋/都市国家がヴィラ等（獲得先が手札に変わる札）で発火しない
（`onGainQueue` の else-if 連鎖組と同じ既存簡略化）／メイソン団を「保存則違反」と呼ぶこと（カードの増減は一切ない）ほか計17件。

### 検証
- **npm test 全40スイート緑（exit 0・整合性4763・不変条件12）**／`test/allies.test.js` 169→**310件**／
  **`test/allies-ui.test.js` 新設99件**（40スイート目＝全 pending にモーダルと押せる選択肢がある・CPU が有効な action を返す・
  **選択を持ち越したまま同じ窓を2回描く**回帰。既存ハーネスは `showAs` が毎回リセットするので構造的に検出できなかった）。
- `verify:e2e` 9/9（webp 505/0）／`verify:visual` 320〜768px 全幅はみ出し0。`sw.js` v65→**v66**。

### ✅ push＝完了（2026-08-12・`a6589a3..5c377b0`／3コミット）＝本番反映を実機確認
- **GitHub Pages**（Deploy ワークフロー success）：`sw.js` **v66**／`js/engine.js` に `noteAllyPlay`・`allyStartOfTurn`・
  `pileFavor`／`js/ui.js` に `takeSelection`・`pruneSelection`・`.pile-favor`／`css/style.css` に `.pile-favor` を配信確認。
- **Render（オンライン）**：`GET /status` = `{"persist":true,"rooms":0}`（サーバ側のコード変更は不要＝
  `state.ally` は `createInitialState` が決めるので自動追従）。
- **本番の挙動が変わるのは UI の選択持ち越し修正だけ**（同盟は CARD_SETS 未参照＝実プレイに出ない）。
  地下貯蔵庫を1ターンに2回使うと、旧実装では2枚目の確定ボタンが「1枚 捨てる」と表示しながら実際は0枚しか
  捨てない（前回の選択インデックスが残る）ことを新旧比較で実測＝**既存カードの実バグも直っている**。

### 既知の許容簡略化（意図的・A3）
- **同時に誘発した効果の解決順を選べない**（既存の横断簡略化）＝小売店主連盟×御料車/山砦は公式なら順番を選んで +$2 に
  できるが本エンジンは +$1／銀行家連盟は「購入フェイズ開始時に好意を増やしてから数える」ができない。
- **ターン開始時効果の解決順を選べない**（先入れ順）＝すり師団だけは手札が少ないほど得なので **startQueue の先頭**に入れる。
- **`look_arrange`（森の居住者／夜警／太陽の恵み）で、解決中に山札の上へ置かれた札が戻した残りより上に来る**（公式は逆）。
- **発明家の家族は廃墟(ruins)の山を候補に出さない**（公式には置けるが廃墟は$0で購入もできず軽減が無意味＋
  `'ruins'` はカタログに無い山キーなので UI で描画できない）。
- **`noteAllyPlay` の配線は上記22箇所**（ネクロマンサー等の残りの経路は未配線）。

### 注意（次セッションが知らないと事故る・A3 固有）
- **新しく「カードを使用する」経路を足したら `noteAllyPlay(state, pi, card)` を呼ぶ**（＝魔女の輪／小売店主連盟／
  写本士の仲間たちが誘発する）。**再演（`runReplays`）だけ誘発して1回目が落ちる非対称**が今回のバグの正体だった。
- **選択系モーダルを新しく書くときは、確定時に必ず `takeSelection()` を通す**（`UI.selection` を持ち越さない）。
  `UI.selection` は**インデックス**なので、同じ pending キーで再度開くと前回のインデックスが残って人間が詰む。
- **`reshuffleDeck` は「メイソン団が捨て札に札を残したか」を返す**。新しく「山札が足りなければシャッフルする」ループを
  書くときは戻り値を見て **同じアクセスで2度目のシャッフルをしない**こと。
- **開始時 Ally の窓は `createInitialState` の末尾でも開く**（ターン1）。`allyStartOfTurn` に窓を足したら、
  ターン1で開いてよいかを確認すること（購入フェイズ開始時の窓はターン1の開始時には開かない＝正しい）。
- **`state.pileFavor` は非カード**＝保存則 tally・`allCards`・庭園/品評会/壁 に混ぜない。READ は必ず `pileKeyOf` を通す。
- **Ally が起こす攻撃（魔女の輪／すり師団）を `ATTACKS` に登録しない**（堀で防げてしまう）。
  A4 で連携カードを実装するときも、Ally 側の攻撃はリアクション窓を開かないこと。

---

## 段階2 **A4＝王国カード49種＝完了・push済**（2026-08-12・`c0bddab..8a042d7`／8コミット・`sw.js` v67・本番反映を実機確認）

**同盟の王国カード49種（非分割25＋分割山6組×4種24）をすべて実装した**。これで **同盟72種のうち Ally 23 と王国 49 の
両方が動く**＝残るは **A5＝CARD_SET 昇格**（`DOM.STAGE1_POOLS` から `'allies'` を外す）と**絵72枚の回収**だけ。
**まだ CARD_SETS 未参照＝実プレイには出ない**（本番挙動は下記の横断修正ぶんだけ変わる）。

### バッチ構成（各バッチ末で `npm test` 全40スイート緑＋CPUソーク緑を確認してコミット）
| バッチ | コミット | 内容 |
|---|---|---|
| 1 | `ba59ad4` | 素直な系17種＋**循環(Rotate)の共通窓**＋「場から捨てるとき山札の上へ」の共通窓 |
| 2 | `68768d7` | **連携(Liaison)9種**（＝好意を配る経路が揃った）＋循環の配線6枚＋ガレリア |
| 3 | `f325fe9` | 持続・再演系6種（王家のガレー船/霊術師/航海/専門家/長老/リッチ）＋改造 |
| 4 | `649cb88` | **アタック7枚**（蛮族/射手/将軍/追いはぎ/散兵/女魔導士/魔導士） |
| 5 | `a3c0ab3` | 残り5枚（歩哨/大工/急使/交換/侍祭） |

### 新機構（A4 で足したもの）
1. **循環(Rotate) の窓 `rotate_pile`**（4点セット）。**常に任意**。**回しても順序が変わらない山（空／1種類だけ）では
   窓を開かない**（結果が完全に同一＝観測不能なUX改善）＝`canRotatePile` が正本。
   **循環の位置はカードごとに違う**＝触れ役/薬草集め/古地図/天幕/戦闘計画は**最後**、
   **生徒だけ「循環 → その後に強制廃棄」で循環が先**（公式カード文の行順）。
   **戦闘計画だけが「任意のサプライ山」**（`rotatableSupplyPiles`＝騎士/廃墟/城/サウナも対象）。
2. **好意の付与 `gainFavors`**（連携カードが配る唯一の入口。Ally が居ないゲームでは配らない）。
3. **`t.handPlays` / `canPlayFromHand`**＝航海(Voyage)の追加ターンの「手札から3枚まで」。
   **財宝も数える**／玉座は「玉座そのもの＋選んだ手札のカード」の2枚で**再演は数えない**／
   手札以外からのプレイ（ゴーレム等）は数えない。**engine拒否・CPU非提案・UI無効化の3面**に通した。
4. **`p.skipTurns`**＝リッチの「1ターンスキップする」。判定は**ターンを取ろうとする直前**（航海/前哨地の
   「ターンとターンのあいだ」より**後**）。**飛ばしたターンも「取っていたのと同じ数え方」でタイブレークに数える**。
5. **長老(Elder)の「追加で異なるもの1つを選ぶ」**＝`t.elderBoost` ＋ `normalizeChoices` / `runChoiceOptions` /
   `applyChoiceOption` の汎用機構。解決は必ず**カード記載順**。片方が選択待ちを立てたら `t.elderRest` に積んで
   reduce 末尾の再開網が続ける。UI は `modalChoice` が `pd.elder` を見て1択/2択を切り替える。
6. **新ゾーン `p.contractSetAside`**（契約書／王家のガレー船の脇札＝**表向き＝公開**）。
   `allCards`・invariants の ZONES に登録。`maskStateFor` では伏せない。
7. **「このターン」型の設置**（ガレリア／ギルドマスター／散兵／道化棒の4つ目）＝`t.galleria` 等に**使用回数**を積む。
   `groundskeeper` の "while this is in play"（場の枚数）とは**別機構**＝場を離れても効き、累積する。
8. **`warlordBlocks` / `highwaymanBlocks`**＝将軍・追いはぎの「相手のターンをフックする」述語。
   将軍は engine/CPU/UI の3面共通。追いはぎは**累積しない**（`t.highwaymanDone` の per-turn フラグ）。

### 実装で踏んだ公式裁定（次に触るとき忘れると壊す）
- **蛮族**＝「$3以上か」は**コイン成分だけ**、「より安いか」は**3成分の厳密比較**という**非対称**。
  種別一致には**連携(Liaison)も分割山種別（町民/卜占官/衝突/城砦/叙事詩/魔法使い）も入る**（公式FAQ逐語）。
  1枚も廃棄できなければ**呪い**／候補が無ければ**何も獲得しない**（呪いには落ちない）。この2つを取り違えない。
- **追いはぎ**＝「何もしない」は**使用時の記載効果を丸ごとスキップ**（コインだけでなく／嫉妬・銅細工師の
  額の書き換えも上書きする）。カードは普通に場に出て「使用した」と数え、片付けで普通に捨てられる。
  次ターン開始時は**先に自分を場から捨ててから** +3カード（引いた3枚に自分が入り得る）。**捨てられなくても3枚引く**。
- **散兵**＝**使用した瞬間に必ずアタック窓を開く**（獲得時には堀を公開できない＝公式）。発動は**使用回数ぶん独立**。
  ⇔ **追いはぎは累積しない**。**この2枚を同じヘルパで書くと必ずどちらかが壊れる**。
- **射手**＝**被害者が隠す1枚を選び／使用者が捨てさせる1枚を選ぶ**の2段（pending の持ち主が跨ぐ）。
  **隠した1枚は pending に載せない**（使用者にも見えてはいけない）。手札4枚以下の相手は完全に無事。
- **歩哨**＝5枚を**deck から抜いて脇に持つ**（見ている間は山札のカードではない）＝ネズミ/狂信者を廃棄したら
  「その時の山札」から引く。**mix-all では必ず到達する**。
- **急使**＝「捨てる → **捨て札トリガーを全部解決** → その後で捨て札置き場を見る」の順。
- **薬草集め**の「山札を捨て札に置く」は**捨て札トリガーを誘発しない**（公式FAQ逐語）。一方 **古地図の「1枚捨てる」は
  誘発する**。同じ拡張の中に両方あるので共通化しすぎない。
- **交換**＝「山に戻す」は廃棄でも獲得でもない第3の移動（`returnToPile`／supply が増える＝3山終了に影響）。
  **戻した後**に候補を数える（分割山では戻した札が一番上に載る＝公式例）。獲得先は**手札**。
- **侍祭**＝2つの能力は独立。②の「卜占官1枚を獲得」は**コスト制限が無い**（自分を廃棄した後の山の一番上）。
- **沈没船の財宝／専門家のコピー獲得／侍祭の卜占官獲得**は**コスト制限が無い**＝`costUpTo` を掛けてはいけない。
- **リッチの on-trash** は ①これを廃棄置き場から**捨て札へ**（**獲得ではない**）→ ②廃棄置き場から**これより厳密に安い**
  カードを獲得（可能なら強制）。**獲得元がサプライではない**ので `costUpTo`/`costUnder` を使うと候補ゼロ→livelock
  （§0-28 の悪魔祓いと同型）＝`lichTrashTargets` が正本。
- **専門家は持続を2回使うと専門家自身も場に残る**／**長老は「1回だけ」なので場に残らない**（**扱いが逆**）。
- **要塞・駐屯地は条件つき持続**（選んだとき／トークンが乗ったときだけ予約を張る）。
  **駐屯地のトークンは好意ではない**（場のインスタンス単位のカウンタ。玉座で2回使うと1獲得につき2個・**除去は1回**
  ＝3回獲得で合計6個＝ +6カード。12ではない）。
- **首都**＝①2枚捨てて+2コイン（**2枚ちょうど捨てたときだけ**／手札1枚でも「捨てる」を選べる）→ ②2コイン払って+2カード。
  **①で得たコインを②に使える**ので順序を守る。
- **狩人**＝アクション→財宝→勝利点の**順で強制**（要塞が唯一のアクションなら勝利点枠は空になる）。
- **下役**＝ +1カード（＝シャッフルが起きうる）→ +1アクション → **+1好意** の順（公式が明記）。
  占星術師団/メイソン団があるゲームで「まだ持っていない好意はそのシャッフルには使えない」が守られる。

### 【royal_galley の版の決定】**現行の印刷版（脇に置く）を採用**
2026年5月に「脇に置かず場に残す」機能エラッタが *announce* されたが **未印刷**で公式FAQも未更新
（英語wiki も Unofficial FAQ 扱い）。本プロジェクトの「現行＝**印刷済み最新＋公式エラッタ**を採用」の方針では採らない。
※採る場合は脇置きを廃して `state.replay` に寄せるだけ（`p.contractSetAside` を使わなくなる）。

### 多エージェント敵対レビュー（5観点）＝**確定10件 → 全修正・回帰テスト付き**（`db37c64`）
観点＝非分割25種の忠実性／分割山24種＋循環／CPU非ループ／UIで人間が詰まないか／既存14拡張への退行。
各 finding は担当エージェントが **node / jsdom で再現してから確定**（偽陽性は棄却）。

**[high] 2件**
1. **追いはぎ／将軍のリアクション窓に UI が無く、人間が完全に詰む**。`lingerAttackEnter` は
   `state.pending.type` に**カード名を変数で入れる**ので、`type:'…'` のリテラル検索にヒットせず、
   UI 側の同型3兄弟（呪いの森／沼の妖婆／門番）への追加を**機械的な突き合わせがすり抜けた**。
   モーダルもスクリムも出ず、engine は `MOAT_REVEAL`/`LINGER_REACT` しか受理しないので脱出不能。
   **CPU は堀を返すのでソークでは絶対に露見しない**（人間だけが踏む）。
2. **粉屋・歩哨の「見た」札がオンラインで相手に丸見え**（`maskStateFor` の私的看破リスト漏れ）。
   §0-21 の偵察隊・§0-28 の夜警と**同じクラス**の再発。**「見る」効果を足したら必ずこのリストに足す**。

**[medium] 6件**
3. **改造を手札0枚で使うと「$1以下」のカードをタダで獲得できた**（番兵値 `coin:-1` に +2 していた）
   → `noTrash` 印を持たせ、獲得を選んでも何も起きないようにした（engine/CPU/UI の3面）。
4. **追いはぎがカード効果で使わせた財宝（急使／専門家／薬草集め）に効かず、権利も消費しなかった**
   → `playCardNoAction` の財宝分岐にも配線（公式FAQ＝「手札から購入フェイズに出した財宝」に限らない）。
5. **将軍が「手札から使わせる」他の経路（玉座の間／長老／専門家／王家のガレー船／市場の町）を素通り**
   → **`canPlayHandCard`（航海の3枚制限＋将軍）を新設**し、engine のゲート・CPU の候補・UI のフィルタの
   **3面**を同じ述語に寄せた。公式FAQが玉座の間を名指しで禁止している。
6. **航海の使い切れなかった予約が後の通常ターンに持ち越され、本来存在しない追加ターンが湧いた**
   → そのターンぶんの予約はその場で全部消費する（公式＝2枚目以降は「失敗」）。
7. **リッチが「同時に廃棄されたカード」を獲得できず、送った配列の順番で結果が変わった**
   → **`trashCardsTogether`**（全部を廃棄置き場に入れてから on-trash）を新設して歩哨に配線（公式例そのもの）。
8. **リッチのスキップが支配(Possession)の `possessedBy` を無関係な第三者へ引き継いだ**
   → 飛ばしたのが追加ターンなら種類を問わず通常手順へ戻す。
9. **【出荷済みの既存バグ】青空市場が城塞(fortress)の廃棄に反応しなかった**。城塞は途中で `return false`
   するので、`triggerOnTrash` の**末尾**にあった青空市場のリアクション窓に到達しない。
   `DOM.KINGDOM_DARKAGES` は**城塞と青空市場を両方含む固定セット**＝**今日の本番で踏める**。
   → 青空市場を関数の**先頭**へ移した（廃棄という事象自体は起きている＝公式の既知コンボ）。

**[low] 2件**
10. 追いはぎで無効化した財宝の「使った枚数」カウンタまで飛んでいた（愚者の黄金の2枚目が +$4 にならない／
    商人の「最初の銀貨」が永久に失われる）＝**止まるのはカード自身の記載効果だけ**（公式逐語）。
11. 長老のブーストがカードid単位でターン中ずっと有効だった（同名の2枚目・再演にも付いた）
    → `PLAY_ACTION` と `runReplays` で落として「長老が使わせたその1枚」だけに効かせる。
    ＋ 航海の3枚制限／将軍で手札が dim にならず「押しても何も起きない」ように見えた（`handCardPlayable`）／
    駐屯地のトークン数が盤面に出ていなかった（場のチップに ●N を表示）。

**[high] 追加1件＝上記5番の修正が入れた退行**（CPU非ループ担当が 2,492戦のソークで検出）
12. **将軍 × 玉座の間で engine拒否 × CPU提案 の無限ループ**（`clashes`＋`throne_room` の王国で **60戦中11戦が膠着**）。
    受理側（`THRONE_CHOOSE`）だけに `warlordBlocks` を足したため、**合法な対象がゼロでも窓が開き**、
    CPU は同じ手を返し続け、UI には辞退ボタンが無く人間も脱出できなかった。
    → **玉座の間／王の宮廷／行進を4点セットで揃えた**（窓の条件・受理・CPU候補・UIフィルタ＋辞退ボタン）。
    ＋ `playCardNoAction` の**戻り値を見ずに**脇置き/二択の予約を立てていた3箇所（王家のガレー船／専門家／長老）も
    「成功したときだけ」に直した（専門家×航海で `specialist_play` が無限に開く形が実際にあった）。
**教訓＝「engine の受理側だけを締める」のが本プロジェクトで最も再発する事故**（§0-23／§0-28 でも同型）。
**述語を1つ足したら、窓を開く条件・受理・CPU の候補・UI のフィルタの4面を必ず同時に直す。**

**既存14拡張への退行＝クリーン**（A4 前後の engine/cpu を同一シードで**完全並走**し、CPU の手と state 全体を
毎ステップ突き合わせ＝出荷 CARD_SET 35種＋mix-all 4種×2〜4人×4シード 156ゲーム／横断改修が触る機構を
強制した敵対王国17種×6シード 102ゲーム／CPU が踏まない5機構（支配/使節団/城塞/習性/苦労）を直接スクリプト
＝**合計263ゲーム・72,608ステップ・差分0**。ハーネスの感度も故意のバグ注入2種で証明済み）。

### 検証
- **npm test 全40スイート緑（exit 0・整合性4763・不変条件12）**／
  `test/allies.test.js` 310→**429件**（A4 の49種＋敵対レビュー回帰11件）／
  `test/allies-ui.test.js` 99→**231件**（A4 の全 pending にモーダルと押せる選択肢がある・
  CPU が null を返さない・選択の持ち越しで詰まない・長老つき2択）。
- CPUソーク 80戦（王国10種×2〜3人×4シード。**王国カードを各自2枚ずつ配って全経路を通す**）
  ＝完走80・膠着0・例外0・保存則違反0・engine拒否0。
  ＋レビュー担当の総当たりソーク **2,492戦**（固定王国20種×分割山の掘り段×2〜4人×20シード／Ally 23種／
  同盟プールからの無作為王国600／同盟×他13拡張の混成600）＝**12番の修正後は 2492/2492 完走・膠着0・engine拒否0**。
  **A4 の pending 52種すべてに到達**（Ally 23種もすべて出現）。
- `verify:e2e` 9/9（webp 505/0）／`verify:visual` 320〜768px 全幅はみ出し0。`sw.js` v66→**v67**。
- **同盟そのものはまだ本番に出ない**（CARD_SETS 未参照）。ただし上記 **9番は出荷済みの暗黒時代の挙動が変わる**
  （城塞を廃棄したときに青空市場が反応するようになる＝公式へ寄せる方向。
   `DOM.KINGDOM_DARKAGES` は城塞と青空市場を両方含むので**今日から効く**）。

### ✅ push＝完了（2026-08-12・`c0bddab..8a042d7`／8コミット）＝本番反映を実機確認
- **GitHub Pages**（Deploy ワークフロー success）：`sw.js` **v67**／
  `js/engine.js`・`js/cpu.js`・`js/ui.js`・`js/cards.js` が**ローカルと sha1 完全一致**（改行を正規化して比較）／
  本番ファイルに A4 のマーカー（`canPlayHandCard`・`trashCardsTogether`・`lichTrashTargets`・`warlordBlocks`・
  `ELDER_CHOICE_ORDER`・`skipTurns`・`voyageTurn`・`garrisonTokens`）を確認／
  `DOM.STAGE1_POOLS = ['allies']` のまま＝**同盟は実プレイにも闇市場にも出ない**。
- **Render（オンライン）**：`GET /status` = `{"persist":true,"rooms":0}`。実 ws（`/ws`＋Origin 必須・`t:` キー・
  部屋作成は `create`→`joined`、開始は `setCpu`/`setConfig`→`start`、盤面は `started`/`state`）で
  `darkages` 対戦を開始し、**配信 state の `turn` に A4 で足した8フィールドが 8/8 揃っている**ことを確認
  （`guildmasters` `galleria` `baubleTopdeck` `skirmishers` `garrisonTokens` `topdeckDone` `voyageTurn` `handPlays`）
  ＝**サーバが新しい engine で動いている決定的な証拠**。`state.ally` は null・好意 0/0（同盟は登場しない）。

### 既知の許容簡略化（意図的・A4）
- **長老で追加選択できるのは同盟の9種のみ**（町／蹄鉄工／触れ役／宿屋の主人／仲買人／要塞／堡塁／改造／専門家）。
  公式は陰謀の 玉座候補/執事/貴族/廷臣/待ち伏せ/手先、海辺の原住民の村、暗黒時代の 伯爵/墓暴き/従者/地下墓所、
  冒険の 魔除け/守銭奴、帝国の 野生の狩り、ルネサンスの 出納官、移動動物園の 解体、収穫祭の 追跡者、プロモの 総督
  も対象だが、**それぞれ reducer の形がバラバラで横断改修の回帰リスクが高い**ため見送った（mix-all でのみ到達）。
- **`t.elderBoost` はカードid で持つ**ので、同じターンに同名のカードを普通に使っても追加選択が付く
  （公式は「長老が使わせたその1枚」だけ）。インスタンス追跡を持たない本エンジンの制約。
- **航海の3枚制限は「手札から使用する」主要経路にだけ通してある**（`PLAY_ACTION`／`PLAY_NIGHT`／
  `PLAY_TREASURE`／`PLAY_ALL_TREASURES`／`playCardNoAction`＋玉座の1回目）。
  語り部のように独自にループする経路は数えるが止めない場合がある。
- **駐屯地が場を離れた（増築で廃棄された等）ときのトークン消失は「場に1枚も無ければ全部消える」で近似**
  （2枚あって片方だけ場を離れた場合は過大に数える）。公式は「場を離れたらそのカードのトークンは無い」。
- **「複数枚を同時に廃棄したら全部を廃棄置き場に入れてから on-trash を解決する」を使っているのは歩哨だけ**
  （`trashCardsTogether`）。礼拝堂／神殿／平和的教団など他の複数枚廃棄は従来どおり1枚ずつ解決する
  （＝リッチと同時に廃棄した札を、歩哨以外では獲得できないことがある）。既存の横断挙動をここだけ直した。
- **将軍はリアクション窓（黒猫／そり／牧羊犬／隊商の護衛）でのプレイを止めない**（公式は止める）。
  同盟単独では同居する札が無く mix-all 限定＝影響が極小のため見送った。

### 注意（次セッションが知らないと事故る・A4 固有）
- **`t.actions += n` / `t.coins += n` を直接書かない**（`addActions`/`addCoins`）は A4 でも同じ。
- **「選ぶ」カードを新しく足すときは `ELDER_CHOICE_ORDER` に**カード記載順で**登録し、
  reducer は `normalizeChoices` → `runChoiceOptions` を通す**（長老の追加選択が自動で効く）。
  選択肢の効果は `applyChoiceOption` に1箇所だけ書くこと（2箇所に書くと必ずズレる）。
- **`canPlayFromHand`（航海）と `warlordBlocks`（将軍）は engine拒否・CPU・UI の3面が同じ述語を見る**。
  片側だけ締めると**本番 livelock**（実際にソークで再現した）。
- **新しい「山から取る／山へ戻す」効果は `returnToPile` / `canReturnToPile` を通す**（混合山は一番上に載る）。
- **リッチのスキップは `finishTurnAdvance` の中で「次の席を決めた**後**」に消費する**。
  ここを「ターンとターンのあいだ」に移すと公式例（前哨地2枚＋リッチ）が再現できなくなる。
- **`lingerAttackEnter` を使う持続アタックを足したら、UI の `viewPendingModal` に必ず分岐を書く**。
  この関数は `state.pending.type` に**カード名を変数で入れる**ので、`type: '…'` のリテラル検索や
  機械的な突き合わせをすり抜ける（A4 の [high] はこれ）。`LINGER_REACT` の許可リストにも足すこと。
- **「見る（look at）」効果を足したら `maskStateFor` の私的看破リストに足す**（`pending.cards` を持つ型名を追加）。
  §0-21 偵察隊／§0-28 夜警／A4 粉屋・歩哨 と**3回続けて同じクラスの漏れ**を出している。

---

## 0-30b. **略奪（Plunder）＝段階1（カタログ85種＋webp）完了**（2026-08-15・`sw.js` v71・未push）

段階0（公式ルール研究＝`docs/research/plunder_rules.md`）の上に**カタログを載せた**。
**まだ CARD_SETS 未参照＝実プレイには出ない**（`DOM.STAGE1_POOLS = ['plunderexp', 'loot']` で闇市場からも塞いである）。

### 規模＝85種（縦型55＝王国40＋Loot 15／横型30＝イベント15＋特性15）
- `DOM.CARDS` 505→**560**／`DOM.LANDSCAPES` 171→**201**＝**合計761枚**。webp も761枚（**絵は未回収＝枠＋文字**）。
- **⚠ プール名は `plunderexp`**＝英語id `plunder` が帝国の分割山「鹵獲品」で使用済みのため。
  `DOM.POOLS.plunderexp`(40) ／ `DOM.POOLS.loot`(15＝非サプライ・抽選の母集団に入れない) ／
  `DOM.EVENTS_PLUNDER`(15) ／ `DOM.TRAITS_PLUNDER`(15)。
- **新種別 `loot`（戦利品）**＝表示ラベルは同盟と同じ「types 配列の順に連ねる」汎用規則に乗せた
  （`js/carddata.js` の ALLIES_TYPE_JP/EN・`js/ui.js` の TYPE_JP・`test/integrity.test.js` の JP/EN の**4箇所**）。
- **横型の新 kind `trait`（特性）**＝`tools/build-landscape.js` にスキンを新設（**深い臙脂・コスト欄なし**・
  ラベル「特性 / Trait」）。前例＝同盟の `ally`。
- **Loot 15種の種別は一様ではない**（研究の敵対検証で確定）＝`Treasure-Loot` 8／`Treasure-Duration-Loot` 4
  （アンフォラ/尽きぬ杯/船首像/宝石）／`Treasure-Reaction-Loot`（盾）／`Treasure-Attack-Loot`（剣）／
  **`Action-Treasure-Loot`（呪符の巻物）**。`Loot` は必ず種別の**最後**。

### カタログ生成＝多エージェント（6群を起草 → 各群を別エージェントが研究doc と逐語照合）
確定した訂正 **23件**。捏造・枚数過不足なし。id 衝突0（既存676枚と機械照合）。
- 区切り線 `
————
` の有無は**英語wiki の生HTML の `<hr>` を数えて**決めた（宝飾卵/ゴンドラ/地図作り/埋められた財宝ほか）。
- 「+1 カードを引く」→「+1 カード」等、**既存カタログの言い回しに正規化**した（DO訳そのままではない）。

### 検証
- **npm test 全40スイート緑（exit 0・整合性 4765→5303・不変条件12）**／`verify:e2e` 9/9（**webp 560/0**）。
- **資本主義の財宝集合 130→140**（略奪の +$ を持つアクション10枚。コスト参照の誤ヒットは無いことを1枚ずつ確認）。
- **テストを2件直した**（どちらも「次の拡張を段階1で足すと落ちる」書き方だった）：
  1. `test/allies.test.js` の「`STAGE1_POOLS` は空」→「**同盟のプールが入っていない**」に変更。
  2. `js/ui.js` の TYPE_JP に `loot` を追加（**A2 で入れた恒久検査が正しく捕まえた**）。

### 【次にやること】段階2（engine/CPU/UI）
研究doc の「段階1・2の計画」に従い **P1 Loot の山 → P2 "next time" 持続 → P3 素直な王国 → P4 特性 →
P5 イベント → P6 残りの王国 → P7 CARD_SET 昇格** の順。着手前に**冒頭「実装前に必読」20項目**を読むこと。

---

## 段階2 **A5＝CARD_SET 昇格＝同盟(Allies)の実プレイ化 完了・push済**（2026-08-13・`sw.js` v68・本番反映を実機確認）

**同盟が実プレイに出るようになった**＝`allies`（固定10種）／`random-allies`／**mix-all（14→15拡張）**。
これで**同盟72種（Ally 23＋王国49）が実際に遊べる**＝残るは **絵（webp）72枚の回収**だけ。

### 配線（`js/cards.js` の4点だけ。engine/cpu/ui は敵対レビューの修正ぶんのみ）
1. **`DOM.STAGE1_POOLS = ['allies']` → `[]`**＝**闇市場デッキに同盟の非分割25種が入る**（373→**398枚**）。
   分割山の中身24種と山キー6つは `MIXED_PILE_CONTENTS` / `MIXED_PILE_KEYS` が塞ぐ（**500王国を走査して漏れ0**）。
   これで段階1のプールは無くなった（＝新しい拡張を段階1で足したらまたここに入れる）。
2. **`DOM.KINGDOM_ALLIES` 固定10種**（自作 showcase。**公式の同盟専用10種は存在しない**）＝
   **道化棒／町民／商人の野営地／叙事詩／魔法使い／仲買人／宿屋の主人／町／蛮族／侯爵**。
   - **連携3系統**（道化棒＝財宝／仲買人＝廃棄して4択／**生徒＝分割山の中**）＝Ally と好意が必ず登場する。
   - **分割山3組**＝町民($2)・叙事詩($3)・魔法使い($3)。循環は 触れ役／古地図／生徒。
   - **航海(Voyage)の追加ターン × リッチ(Lich)の1ターンスキップが必ず同居する**（同盟で一番の地雷）。
   - **長老(Elder)の「追加でもう1つ選ぶ」対象**（町・宿屋の主人・仲買人）が揃っている。
   - アタック＝蛮族・魔導士／持続＝航海・霊術師。コスト分布＝$2×2／$3×3／$4×3／$5×1／$6×1。
3. `DOM.CARD_SETS` に `allies`（`kind:'standard'`＝UI の「拡張」タイル）と `random-allies`。
4. `DOM.MIX_KINGDOM_POOLS.allies = '同盟'`（mix＝**15拡張＋プロモ＝16プール**）。

### CPU（`js/cpu.js`）
- **`GAIN_ORDER` の同盟55件を実強度順の位置へ移した**（段階1で末尾に置いたままだった TODO を解消）。
  **分割山は「山キー」だけが実際に獲得される**（中身24種は山の一番上のときだけ）ので、山キーを先に置き
  中身24種はその後ろにまとめた。全505件・重複0・固定10種はすべて `estate` より前。
- **`evaluateKingdom` は allies を MONEY のまま**（＝異郷/暗黒時代/冒険/帝国/ルネサンス/移動動物園/夜想曲と
  同じ既定方針。§0 の「半端エンジンは無エンジンより弱い」）。実測でも王国カードの購入率・平均ターン数は
  夜想曲/移動動物園と同水準（12戦で購入16回・平均21.2ターン）。

### 多エージェント敵対レビュー（5観点）＝**確定10件 → 全修正・回帰テスト付き**
観点＝**闇市場経路（A5 で新規に開いた）／固定10種の相互作用／CPU非ループ／UIで人間が詰まないか／既存14拡張への退行**。
各 finding は担当エージェントが **node / jsdom / 実ブラウザで再現してから確定**（偽陽性は棄却）。

**[high] 1件（別々の2エージェントが独立に再現）**
1. **リッチ(Lich)の獲得窓が候補ゼロで開いて閉じない＝人間が完全に詰む／CPU が engine拒否を返し続けて livelock**。
   **1回の蛮族で2人以上が山札の上のリッチを廃棄すると窓が2つ積まれ**、廃棄置き場の「リッチより安いカード」を
   先の1人が取り切って枯れる。公式＝`Gaining a cheaper card is mandatory if possible.`＝**可能でなければ何もしない**。
   UI はチップもボタンも0でスクリムが盤面を覆う＝脱出口ゼロ。CPUソーク **276戦中8戦（約3%）で自然発生**（3〜4人戦）。
   ⚠ **蛮族と魔法使い(リッチ)は出荷する固定10種に同居する**＝**A5 で初めて到達可能になった**
   （A4 までは `STAGE1_POOLS` で塞がれていたので、同盟467件＋UI 261件のテストは緑のまま素通りしていた）。
   → **`onTrashQueue` を pending にする前に再検査**（候補ゼロなら捨てて次へ）＋**受理側 `LICH_GAIN` の終端保証**
   （旧スナップショット互換）＋**UI に「獲得できるカードがない（閉じる）」**（候補があるときは出さない＝強制のまま）。
   ＝§0-22 で明文化した「**回数制限つきの窓は解決時に必ず再検査する**」と同型。

**[medium] 2件（どちらも リッチ × 追加ターン）**
2. **航海2枚＋リッチで、2枚目の航海の追加ターンが消えていた**。公式（2023 Errata の Trivia 逐語）＝
   `If you play two Outposts and Lich on one turn, Lich can skip the first extra turn. **The second Outpost
   still happens, since you haven't taken 2 turns in a row yet.**`＝**飛ばしたターンは「行われていない」**。
   `p.voyageExtra` を「そのターンぶんを全部消費する旗1つ」で持っていたので公式例が再現できなかった
   （研究doc が**この実装方式を名指しで警告していた**）。→ **残り数で持ち、スキップされたら取り直す**。
   使い切れなかった残りは片付けの末尾で必ず捨てる（A4 の [medium] 6＝**持ち越し禁止**は維持）。
3. **相手のターンがリッチで飛ぶと連続手番カウンタ `chain` がリセットされ、航海が3ターン連続を許していた**。
   公式＝`Skipping a turn means ... nothing happens for that turn`／「直前のターンの持ち主」を見るカードは
   **実際にプレイされた最後のターン**を見る。→ スキップの結果また自分の番になったら `chain` を +1 する
   （**スキップした場合だけ**。支配(Possession)の追加ターンの数え方は変えていない）。

**[low] 7件**
4. **島民(Island Folk)×航海で「どちらの追加ターンを取るか」を選べず、必ず航海（手札から3枚まで）に固定**されていた。
   公式＝`you've hit the "but not a 3rd turn in a row" limit, so you will have to choose which extra turn to take`。
   島民のターンには3枚制限が無いので、好意5を払ってでも島民を選ぶのが得な局面がある＝選べないと明確に損。
   → 航海が立っていても島民の窓を開き、島民を選んだら航海の予約を捨てる。
5. **航海の追加ターンのログが移動動物園の「今を生きる」を名乗っていた**（航海も `seizeTurn` を立てるため）。
   → `voyageTurn` を**先に**分岐して「**航海：手札から3枚まで使えます**」と出す。
6. **【出荷済みの表示バグ】混合山から獲得したときのログが「獲得*後*の山の一番上」を名乗っていた（8箇所）**＝
   `gain()` が一番上を抜いた**後**に名前を引いていた。沈没船の財宝／大工（獲得・格上げ）／交換／蛮族／霊術師／改造／輸入者。
   **城(castles)は2人戦で各1枚＝帝国で毎回ズレる**（「粗末な城」を獲得して「崩れた城」と表示）。ログは `state.log`
   ＝オンラインでも全員に配信される。→ **`gain()` の前に実カードidを確定する**
   （`HILL_FORT_GAIN` だけが唯一正しく書けていたので同じ形に揃えた）。A2 の確定9番と同じクラスの再発。
7. **専門家の「同じカード1枚を獲得する」が、サプライに山が無い札でも押せた**（闇市場で買った札・非サプライ・空の山）＝
   押しても何も起きず「もう一度使う」を失う。公式は「遂行できない選択肢も選べる」（§0-21 の探索と同じ）ので
   **選択肢は消さず、ラベルに「（サプライに山が無いので獲得できません）」を添える**。
8. **発明家の家族のモーダルが分割山を山キー名で出す**（盤面は一番上の実カードを描くので食い違って見える）。
   → **山キーのままが正しい**（4種すべてが安くなる。一番上に描き替えると「1種だけ安くなる」と誤解させる）ので、
   説明文に「分割山に置くと4種すべてが安くなります」を追記して補う。
9. **リッチのスキップ予定が盤面に一切出ていなかった**（公開情報なのに、飛んでからログで気づく）→ バッジ「スキップ N」。
   **航海の「手札から3枚まで」の残りも出ていなかった**（engine が弾く理由が画面に無く「押しても何も起きない」）
   → バッジ「航海 N」（残り使用回数）。
10. **【出荷済みの CSS バグ】「ランダム」の抽選元セグ18個が 320px で幅22px・高さ113px に潰れて縦書きになっていた**
   （A5 起因ではない）。後ろにある `.field .seg-btn { flex: 1 1 0 }` が効いて **flex-basis が0になり `flex-wrap` が
   発動していなかった**。→ `.seg.seg-wrap .seg-btn { flex: 0 0 auto }`。実ブラウザで **18個が6行に折り返し・各38px**
   （ミックスの拡張チップ16個も5行）・横はみ出し0 を確認。

**CPU 非ループ＝クリーン（合計 2,316戦）**：`allies` 180／`random-allies` 360／`mix:allies,<他15拡張>` の1対1 300／
闇市場強制同居・分割山×汎用獲得・**Ally 23種の全強制** 603／非分割25種と**分割山の掘り段0〜3**を全部通す 618／mix-all 255
＝**膠着0・例外0・engine拒否0・CPU null 0・保存則違反0**。しかも**検出器の感度を故意のバグ注入4種で先に証明**してある。
**同盟の pending 71/71 すべて到達**（`ally_*` 21種・将軍・追いはぎ・散兵・射手・魔導士・女魔導士・蛮族・循環・長老・
リッチ・歩哨・交換・契約書）。

**既存14拡張への退行＝クリーン**：新旧 `cards.js` を別 sandbox に読んで**同一シードで完全並走**（毎ステップ CPU の手と
state 全体を突き合わせ）＝出荷固定29セット×12シード／ランダム17種×10シード／既存 mix 10種×8シード
＝**614ゲーム・186,643ステップ・差分0**。**闇市場を含む promo-pack / random-promo も、BM デッキを新旧で同一にすると
完全一致**（＝変わったのはデッキの中身だけ）。`DOM.CARDS`/`LANDSCAPES`/`POOLS`/`SPLIT_PILES`/既存 `KINGDOM_*` は
バイト単位で不変。整合性 4763→4765 の +2 も「`allies` の固定10種検査」「`random-allies` の母集団検査」の**ちょうど2件**と機械確認。

### 検証
- **npm test 全40スイート緑（exit 0・整合性4765・不変条件12）**／`test/allies.test.js` 429→**489件**
  （A5 の昇格検査＋敵対レビュー回帰＋出荷セット CPUソーク18戦＋mix 4戦）／
  `test/allies-ui.test.js` 231→**261件**（固定10種の盤面・分割山の一番上・A5 の UI 回帰）／
  `test/invariants.test.js` の出荷セット検証に `allies` / `random-allies` を追加。
- **`test/allies.test.js` の保存則 tally が `p.archives`（帝国・資料庫の脇置き）と支配の精算ゾーンを数えていなかった**
  （`invariants` は数えていた）。**A5 で同盟が mix-all に参加した今、mix 王国を足した瞬間に偽陽性の赤になる**状態
  だったので `invariants` と同じ集合に揃えた（CPUソーク担当が実際に3戦の偽陽性を踏んで発見）。
- `verify:e2e` 9/9（webp 505/0）／`verify:visual` 320〜768px 全幅はみ出し0／CPU序列 100/64/95 維持。
- **本番の挙動が変わるのは**：同盟が遊べるようになること／闇市場デッキ +25枚／**上記 [low] 6（混合山の獲得ログ）と
  [low] 10（CSS）は出荷済みの既存バグの修正**（城の混合山は帝国で毎回ズレていた）。

### ✅ push＝完了（2026-08-13・`2ef0f0d..9538fa2`／2コミット）＝本番反映を実機確認
- **GitHub Pages**（Deploy ワークフロー success）：`sw.js` **v68**／`js/cards.js`・`js/engine.js`・`js/cpu.js`・
  `js/ui.js`・`css/style.css` が**ローカルと sha1 完全一致**（改行を正規化して比較）／
  本番の `cards.js` に `DOM.STAGE1_POOLS = []`・`id: 'allies'`・`id: 'random-allies'`・`allies: '同盟'` を確認。
- **Render（オンライン）**：`GET /status` = `{"persist":true,"rooms":0}`。実 ws（`/ws`＋Origin 必須・`t:` キー）で
  **`setConfig kingdomSet=allies` が受理され**、対戦開始で
  **固定10種 10/10・Ally 1枚（cave_dwellers）・全員の好意1個・分割山3組が各16枚（一番上＝触れ役/古地図/生徒）**
  を確認。配信 state の `turn` に `voyageTurn`/`handPlays` があることも確認＝**サーバが新しい engine で動いている**。
  ※サーバは `DOM.CARD_SETS` から許可IDを導出するので**サーバ側のコード変更は不要**。
- ※この push の時点では絵が未回収だった。**絵78枚は 2026-08-13 に回収して `sw.js` v69 で焼き直した**（上記）。

### ✅ 絵（webp）72枚の回収＝完了（2026-08-13・`sw.js` v69）
ユーザーが ChatGPT で72枚を生成（8バッチ＝10×5＋9＋8＋8＋7）。Downloads には **80枚**落ちていた＝
**1バッチ（Ally 8枚）が二重ダウンロード**（Chrome の ` (1)` 付き。8組ともバイト一致）。
→ **8グループに1体ずつエージェントを立て、72候補全体から全単射で判別**（どのバッチかは伝えない方式）
＝**全72枚 high・取り違え0**。id の重複0・未割り当て0を機械検算してから回収した。
- ⚠ **今回も「連番＝生成順」ではなかった**：ファイル名が `(1) (10) (2) …` と文字列ソートされるため、
  同じ秒に落ちたバッチ（K1）と、秒はズレるが10枚目がある バッチ（K4）で並びが崩れた。
  **連番で機械割当していたら K1 は10枚中9枚が取り違え**になっていた。
- 縦型55＝`build-cards.js`／横型23＝`build-landscape.js`。webp が 47〜67KB → **81〜207KB（絵入り）**。
- **分割山のプレースホルダ6枚はその山の最安カードの絵を流用**（城 `castles` と同じやり方）＝
  卜占官←薬草集め／衝突←戦闘計画／城砦←天幕／叙事詩←古地図／町民←触れ役／魔法使い←生徒。
- モンタージュ4枚（縦2＋横2）で**78枚すべて絵と名前・枠スキンを目視確認**（Ally は濃い藍のスキン）。
- Downloads の原本80枚は `C:/tmp/dominion_art_originals/allies_2026-08-13/` へ移動（Downloads は空）。
**これで実装済みの全676枚（縦505＋横171）すべてに絵が入った＝枠＋文字だけの札はゼロ。**
**push＝完了（2026-08-13・`b847ca7`）＝本番反映を実機確認**：Pages の `sw.js` **v69**／
同盟の webp **78枚すべてが本番とローカルでバイト一致**（sha1・全枚 70KB 超＝絵入り）。

### 既知の許容簡略化（意図的・A5）
- **CPU は同盟セットを MONEY（純ビッグマネー）として遊ぶ**（`evaluateKingdom`）。他の8拡張と同じ既定方針。
- **CPU は闇市場で同盟のカードを買わない**（黒市の候補は `GAIN_ORDER` の先頭38枚＝銀貨より上だけ）。
  ＝**闇市場×同盟は「人間だけが通る道」**なので、CPUソークを何戦回してもこの経路の検証にはならない
  （A4 の [high]「追いはぎ/将軍のリアクション窓に UI が無い」と同じ構図）。レビューでは購入を強制注入して補った。
- **`random-allies` は約1.5%の確率で連携を1枚も含まない**＝その対局には Ally も好意も出ない（公式のランダマイザーと同じ）。

### 注意（次セッションが知らないと事故る・A5 固有）
- **`DOM.STAGE1_POOLS` は空になった**。段階1（カタログと画像だけ）の拡張を足したら**必ずここに入れる**
  （入れないと闇市場に「買っても何も起きない死に札」が $0 で並ぶ）。
- **混合山から獲得するログは `gain()` の前に `mixedTopCard` を評価する**。後だと次の札の名前になる（8箇所で踏んだ）。
- **`p.voyageExtra` は「残り予約数」**（旗ではない）。リッチのスキップで取り直すために残す設計なので、
  `= 0` で全部消す実装に戻すと公式例（航海2枚＋リッチ）が壊れる。**使い切れなかった残りは `finishTurnAdvance` の
  末尾で必ず捨てる**（後の通常ターンに持ち越すと本来存在しない追加ターンが湧く）。
- **`chain`（連続手番）は「実際にプレイされたターン」で数える**＝リッチで飛ばしたターンは連続を切らない。
  島民と航海の「3ターン連続にはできない」が両方これを見ている。
- **新しい「追加ターン」を足したら、島民の窓（`ally_island_folk`）で選べるようにするか決める**
  （公式は同時に成立したら**選ばせる**）。`state.turn.advanceCtx` に予約を積んで `finishTurnAdvance` へ渡す形。

---

### 注意（次セッションが知らないと事故る・同盟固有）
- **Ally が起こす攻撃は「アタックカードのプレイ」ではない＝堀で防げない**（魔女の輪・すり師団の公式FAQに明記）。
  **`ATTACKS` に登録してはいけない／`attackImmune` を通してはいけない**。素直に既存機構へ乗せると
  堀・灯台・チャンピオン・守護者で無効化されて**公式より弱くなる**。
- **循環(Rotate)は「先頭からの連続同名ブロック」を末尾へ**。離れた同名は動かさない（交換やウマの習性で乱れた後に効く）。
  **循環の位置はカードごとに違う**＝生徒(Student)だけ「循環 → その後に強制廃棄」で循環が先。
  **空の山・1種類だけの山を回しても合法**（何も起きないだけ＝選択肢ゼロの pending で詰ませない）。
  **戦闘計画(Battle Plan)だけが「任意のサプライ山」を回せる**（騎士/廃墟/城/サウナも対象）。他5枚は自分の山を名指し。
- **「山のコスト・種別」は randomizer 固定（＝最安カード）／「買うときのコスト」は今の一番上**。この2つを混同しない。
  実例＝発明家の家族は同盟の6分割山には好意を置けるが**城(Castles)には置けない**（randomizer が勝利点だから）。
- **生徒(Student)は魔法使い(Wizards)の分割山の中に居る連携**＝`kingdom` は山ID（`'wizards'`）を持つので、
  **山IDだけを見る判定では Ally も好意も出ないゲームになる**。`DOM.ALLIES_SPLIT_PILES` で中身4種を走査すること。
- **駐屯地(Garrison)のトークンは好意ではない**（カードの上に載る自前のカウンタ＝陰謀団と同型）。混同すると好意が湧く/消える。
- **獲得したばかりの好意はその場の窓で即使える**（ごますり・魔女の輪の公式FAQ）＝窓を開く前の値をスナップショットしない。
- **商人の野営地(Merchant Camp)は「場に出さずに使用」経路では山札の上に置けない**＝§0-17 の `takeSelf`/`playedByCommand`
  を通さないと**幻のカードが山札に増える＝保存則違反**。逆に**習性や女魔術師では置ける**（静的な捨て札トリガーなので
  一括で抑止すると公式違反）。位置は策謀と同じスロット＝**必ず先引きより前**。
- **歩哨(Sentinel)が見る5枚は deck から抜いて脇に持つ**（廃棄でドローするネズミ/狂信者と mix-all で必ず同居する）。
- **ごますり(Sycophant)の3枚は同時に捨てる**（1枚ずつにすると坑道→望楼の連鎖で不正が通る）。
  **on-trash はサプライからの廃棄でも発動**＝`opts.fromSupply` の抑止（青空市場）に混ぜない。
- **専門家(Specialist)で持続を2回使うと専門家自身も場に残る**（§0-25 の「玉座×持続」の既存簡略化と衝突する）。
  一方**長老(Elder)は「1回だけ」プレイなので場に残らない**＝同じ「アクションを使わせるカード」でも扱いが逆。
- **追加ターンの競合は一般則**（航海・島民は「3ターン連続不可」）＝§0-26 の 前哨地/使節団/今を生きる/艦隊/支配 の
  優先順位ロジックに組み込む。**航海の「手札から3枚まで」は Ally の能力も止める**（市場の町・都市国家）。
- **日本語名の完全一致が1件**＝移動動物園のイベント `alliance`（カード名＝**同盟**）と、本拡張の
  拡張名／`Ally` 種別／23枚の種別ラベルが同じ文字列。**id 衝突はゼロ**なので機能影響は無いが、
  カード一覧の全文検索・盤面の帯で意味が2つになる。→ 群見出しは「同盟（同盟拡張・横型…）」等で区別してある。
- **要塞(Stronghold)は4種別＝アクション・勝利点・持続・城砦**（分割山の中に「勝利点かつ持続」が入る）。
  **蛮族(Barbarian)の「種別を共有する」判定に連携も分割山種別も入る**＝種別を1つ落とすと静かに壊れる。

---

## 0-28. **夜想曲（Nocturne）＝段階2 完成（全77種が実プレイ可能）**（2026-08-12・未push・`sw.js` v61）

§0-27 の基盤（研究＋カタログ77種＋夜フェイズ）の上に **N0b〜N5 を全部実装**し、`nocturne` / `random-nocturne`
を CARD_SET に昇格した。**mix-all にも参加**（13→14拡張）。**これで実装済みは 14拡張＝縦型450＋横型148＝598枚**。

### N0b＝UI の夜フェイズ操作
- **[実バグ] 純粋な夜行カードは手札のどの群にも入らず1枚も描画されなかった**（`handGroups` が
  アクション/財宝/勝利点しか見ていない＝人間が操作不能）→ **「夜行」群を新設**。
- フェーズ表示「夜 フェーズ」／夜行カードのタップ→「使う（夜）」＝`PLAY_NIGHT`／
  夜フェイズでは購入UI（財宝を出す・財源・返済・購入）を出さず「ターンを終える」だけにする。
- 牧草地（家宝＝屋敷1枚につき1点）と生活苦(-2)/二重苦(-4)を **engine.vpOf と CPU.vpOfPlayer の両方**に入れた。

### N1＝祝福12・呪詛12・状態5 の共通機構
- **祝福＝`state.boonQueue` に積んで reduce 末尾の再開網が1件ずつ解決する**（ドルイド/愚者3枚/恵みの村/
  ピクシー2回＝**複数の祝福を順に受ける**ので `state.pending` 直代入は禁止）。山が空なら捨て札をシャッフルし、
  両方空なら「受けない」で終端。田畑/森/川の3種は受け手の前に置き、クリンナップで祝福の捨て札へ戻す。
  **川の恵みの +1カード は「次の手札を先引きした後」**（§0-25 のリスと同じ位置）。
- **呪詛＝リアクションを全員ぶん閉じてから1枚だけめくり**、免疫でない全員が同じ1枚に従う
  （被害者ごとにめくり直さない／**全員が免疫でも1枚めくって捨てる**）。適用は `t.hexQueue` の再開網。
- **状態＝錯乱/嫉妬は「持っている＝効いている」ではない**。購入フェイズ開始時に**返して**初めて発動する2階建て
  （`p.deluded`→`t.cantBuyActions` / `p.envious`→`t.enviousActive`）。`END_ACTION_PHASE` は1ターンに複数回走る
  （ヴィラ）ので毎回返す判定をし、立った旗は同ターン中に下ろさない。錯乱は engine(`canBuyCard`＋闇市場)・
  CPU・UI の3面で同時にアクション購入を止める。嫉妬は `treasureCoins` に入れた（冠/ティアラの2回目も $1）。

### N2＝夜行でない王国カード17種＋家宝7種／N3＝夜行カード＋非サプライのアクション／N4＝複雑系
- N2：恵みの村・墓地・コンクラーベ・呪われた村・ドルイド・忠犬・愚者・偶像・レプラコーン・ピクシー・
  プーカ・聖なる木立ち・秘密の洞窟・羊飼い・迫害者・追跡者・悲劇のヒーロー＋家宝7種。
- N3：カブラー・納骨堂・悪人のアジト・悪魔の工房・悪魔祓い・ゴーストタウン・守護者・修道院・夜警・夜襲・
  人狼＋ウィル・オ・ウィスプ・インプ・願い。
- N4：取り替え子（使用＋**$3以上の獲得すべてに交換の窓**）・ネクロマンサー＋ゾンビ3種・幽霊（2度使用）・
  吸血鬼↔コウモリ。**交換の共通入口 `exchangeCard`**（獲得でも廃棄でもない／`supply` は増減する）。

### 実装で踏んだ公式裁定（次に触るとき忘れると壊す）
- **コンクラーベ／インプの対象は「今その名前が場にあるか」だけ**（持続も選べる＝王子/船長/大君主の
  non-Duration 制限を流用しない）。**コンクラーベの +1アクションは使ったカードの解決が全部終わった後**
  （`state.replay` の `conclave_bonus`）＝雪深い村を使うと +1アクションは無視される。
- **守護者の免疫窓は灯台と違う**（"Until then"＝次の自分のターンの**開始時**まで。灯台は次のターン中も守る）。
  `p.guardianActive` を別に持ち、持続の解決で降ろす。**灯台の述語を流用すると1ターンぶん過剰に守る**。
- **レプラコーンの「場にちょうど7枚」は金貨の獲得（と獲得時リアクション）を解決した後に数える**
  （牧羊犬が7枚目になる公式裁定）＝`onGainQueue` が空になってから判定する再開網にした。
- **聖なる木立ちは +コイン を与えない祝福だけ共有**（田畑/森の恵みは共有しない）。共有は同じ1枚＝
  複数人の前に同じ祝福が置かれ得るので、片付けで祝福デッキに**二重に戻さない**。
- **ピクシーは廃棄しなければ祝福を受けない**（捨てられるだけ）。**愚者は既に森の迷子を持っていたら完全に空振り**。
- **偶像は「場にある偶像の枚数」で判定**（プレイ回数ではない）＝`applyTreasureEffect` に書く。
- **悪人のアジト/ゴーストタウン/守護者/夜警は「捨て札に獲得する場合だけ」手札に置き換わる**（`gain` の dest）。
- **忠犬は「ターンの終了時」＝このエンジンでは先引きの後**（角笛は逆に先引きの前）。相手のターンに捨てた
  忠犬も**そのターンの終了時**に戻る＝全プレイヤーぶん回収する。
- **納骨堂は「持続でない財宝」だけ**（2022エラッタ）。0枚なら持続にならない。**夜警は「見る」＝`reveal()` を通さない**。
- **修道院の上限は使用時点で固定**（廃棄の途中で獲得が起きても増えない）。**1枚ずつ**廃棄する。
- **ネクロマンサーは「先に裏返してから」使用**（2021エラッタ＝無限ループ防止）。裏向きフラグは廃棄置き場の
  **物理カード1枚ずつ**（インデックス列）でターン終了時に全解除。⚠ **Command 種別を持たない**ので
  廃棄置き場の大君主/はみだし者/船長/王子も使える（「命令は命令を使えない」ガードを適用しない）。
- **幽霊の2回使用は `state.replay`**（玉座の2回目と同型＝命令ではない／脇札は**公開情報**）。
- **取り替え子の交換窓は onGainQueue の先頭に積む**（後だとヴィラ/暗躍者/望楼が先に動いて lose-track で
  交換できなくなる＝公式「同時に起きる効果の順番は選べる」に合わせる）。

### 【fuzz が検出した実バグ】非サプライ札を `costUnder`/`costUpTo` で探すと候補ゼロになる
**悪魔祓いの精霊（ウィル・オ・ウィスプ/インプ/幽霊）は非サプライ**なので、`costUnder`（＝`gainableBase` を含む）
では候補が常にゼロ。CPU が `card:null` を返し続け engine が拒否して **本番 livelock**
（invariants の全プール混成 MIX5 が 20000step 未終局で捕捉）。UI も候補ゼロで人間が詰む状態だった。
→ engine に **`exorcistSpirits`** を新設し、**engine 拒否・CPU 候補・UI モーダルの3面が同じ述語を見る**ようにした。
**非サプライ札を候補にする効果を新しく書くときは `costUpTo`/`costUnder` を使ってはいけない**（§0-23 の裏返し）。

### N5＝CARD_SET 昇格
- `DOM.KINGDOM_NOCTURNE` 固定10種（自作 showcase）＝ドルイド／守護者／ピクシー／取り替え子／詩人／
  ネクロマンサー／墓地／暗躍者／納骨堂／吸血鬼。新機構6系統をひと通り味わえる。
- `nocturne`（kind:'standard'＝UI の「拡張」タイルに自動で出る）／`random-nocturne`／
  mix-all の `MIX_KINGDOM_POOLS.nocturne`。invariants の出荷セット検証にも追加。

### 敵対レビュー（多エージェント6観点＝祝福/呪詛/状態・王国カード・夜フェイズの横断影響・保存則/マスク・
### CPU非ループ・UIで人間が詰まないか。各 finding を独立エージェントが **node で再現してから確定**）＝**確定30件・棄却0**
**[high] 2件**
1. **錯乱中に CPU がアクションカードの購入を出し続けて本番 livelock**。`bestPotionBuy` が `chooseBuy` の
   最終ガード（`canBuyCard`）を **early return で飛び越えて**いた。ポーション費用の王国カードはブドウ園以外
   すべてアクションなので、錯乱を返したターンにポーションを1つでも持つと engine が必ず拒否 →状態不変 →
   同じ BUY を無限に返す（`mix:alchemy,nocturne` の fuzz で自然発生）。→ `canBuyCard`＋`splitBlocked` を通した。
2. **闇市場でも同じループ**（CPU が `cantBuyActions` を見ていない／UI は押しても何も起きないチップを出す）。

**[medium/low] 28件**（全部修正・回帰テスト付き）
- **玉座の間×迫害者（3人戦）で呪詛カードと被害者が消える**＝再演が祝福/呪詛の解決に割り込んでいた
  （`runReplays` に `nocturneQueueBusy` ガード）。呪詛は「カード」ではないので保存則 fuzz では捕まらない。
- **オンラインの情報漏洩**＝夜警／太陽の恵み／ゾンビの密偵の「**見る**」が `maskStateFor` のマスク一覧に無く、
  配信JSONを覗くと相手の山札が読めた（§0-21 の偵察隊と同型）。
- **オンラインの「同意なしで戻せる」証明が夜想曲の状態を素通し**＝呪われた村を買って生活苦(-2VP)や錯乱を
  受けても山札・手札が動かないので**無料でやり直せた**。→ 祝福/呪詛デッキ・状態・森の迷子も比較する。
- **ネクロマンサーの裏向きフラグが `state.trash` の「添字」**＝廃棄置き場からカードが抜けるとズレて
  **同じゾンビを同一ターンに2回使えた**。→ 「id→枚数」に変更。
- **悲劇のヒーロー**＝廃棄できなくても財宝は獲得する（ネクロマンサー/玉座/幽霊の2回目で取り逃していた）。
- **迫害者**＝「使用したその1枚以外」が場にあるかで判定（同名の別の迫害者も「他のカード」）。
- **羊飼い**＝捨て札トリガーを解決してから引く（坑道の金貨がリシャッフルに入らなかった）。
- **ウィル・オ・ウィスプ**＝コストは3成分（負債コストの札を「$2以下」として手札に入れていた）。
  ※ここで `costUpTo` を使ってはいけない（非サプライ除外を含むので願い/インプが手札に入らなくなる）。
- **秘密の洞窟**＝手札3枚未満でも捨てられる（全部捨てるがボーナスは無い）。
- **家宝に置き換えた銅貨をサプライの銅貨の山に戻す**（山が人数×家宝数だけ少なかった）。
- **戦争**＝ポーション費用/負債コストの札は「コスト$3/$4」ではない／**廃棄してから残りを捨てる**。
- **川の恵み**＝受けた回数で数える（ドルイドで脇から受けた／ピクシーで2回受けたぶんが働かなかった）。
- **祝福のターン資源**は受け手が手番プレイヤーのときだけ入る。
- **人狼は夜フェイズでも「アクションカードの使用」**＝習性(Way)／御料車・法貨の呼び出し窓／女魔術師の置換／
  浮浪児のトラップ が夜フェイズでも働く（純粋な夜行カードでは働かせない）。
- **人狼(+3カード側)・迫害者(インプ側)でもアタック誘発リアクションの窓を開く**＝新 pending `attack_window`。
- **片付け開始時の効果を購入フェイズ扱いにしない**＝`maybeEnterNight` が常に `phase='night'` にする
  （手札の夜行カードの有無で公会堂/列柱/徴税/行商人のコストが変わっていた）。
- **呪われた金貨を「財宝を全部出す」から除外**（出すと必ず呪いを獲得＝ボタン1つで事故）。engine だけ締めると
  CPU が無限ループするので engine/CPU/UI を同一コミットで直した。
- **山札から捨てたカードでも捨て札トリガーを誘発**（夜警/太陽の恵み/幽霊/飢饉/戦争。忠犬が脇に置けなかった）。
- **呪われた村の獲得時の呪詛は獲得処理中ならキューに積む**（同じ獲得で開くはずの窓を握りつぶしていた）。
- **[UI] 非サプライ5山（精霊3/願い/コウモリ）が盤面にまったく表示されていなかった**（馬も同様）。
- **[UI] 状態5種・前に置いた祝福・保持中の祝福・脇札2種・祝福/呪詛の山の残枚数**を盤面に出す。

### 検証
- **npm test 全38スイート緑（exit 0・整合性4184・不変条件10）**／CPU序列 100/64/95 維持。
  新設2スイート＝`test/nocturne.test.js` **371件**／`test/nocturne-ui.test.js` **87件**
  （全 pending に「押せる選択肢が1つ以上ある」ことを jsdom で検査＝人間が詰まないことの担保）。
- **祝福12種・呪詛12種が「消えない」ことを保存則 fuzz の恒久的な不変条件に追加**（非カードなので
  既存の tally では守られない。同型の再発を将来必ず捕まえる）。
- **CPUソーク105戦**（出荷2セット＋mix 6種＋**夜想曲33種を10枚ずつ全部カバーする王国33本** ×2〜4人×全難易度）
  ＝完走105・膠着0・例外0・保存則違反0・祝福/呪詛の消失0。
- **全48カードを1枚ずつ強制使用・獲得して CPU だけで終局まで進める総当たり検査 129件＝不具合0**。
- `verify:e2e` 9/9（webp 450/0）／`verify:visual` 320〜768px 全幅はみ出し0。

### 既知の許容簡略化（意図的・再議論しない）
- **幽霊が持続カードをプレイしても幽霊自身は場に残らない**（公式は残る）。片付けの持続仕分けは
  `p.delayedEffects` の残り枚数で数えるため、開始時に予約を消費する幽霊は残らない。
  **玉座の間×持続の既存挙動と同型**（§0-25 に既出）。保存則・非ループには影響しない。
- **ターン開始時の複数処理の解決順は選べない**（`startQueue` は先入れ順）＝納骨堂を複数持つ場合や
  恵みの村で複数の祝福を保持した場合の順序が固定（既存の横断簡略化と同じ）。
- **カメレオンの習性は祝福の +コイン/+カード も変換してしまう**（公式は祝福は変わらない）。mix-all 限定。

### ✅ 絵（webp）77枚の回収＝完了（2026-08-12・`sw.js` v62）
ユーザーが ChatGPT で77枚を生成（8バッチ＝10×7＋7・重複なし）→ **8体のエージェントが各グループの全枚を実見し、
77候補全体から id を確定**（「そのグループが何の分類か」は伝えず全単射で選ばせる）＝**全77枚 high・取り違え0**。
medium が1枚（嫉妬）出たので自分の目でも確認して確定（顔が緑がかった人物＝envious／骨ばった手の greed ではない）。
**今回は8バッチとも生成順どおりだった**（§0-26 のような不一致は無し。ただし判別は順序に依存しない方式でやった）。
- 縦型48＝`build-cards.js`／横型29＝`build-landscape.js`。webp が 29〜66KB → **59〜201KB（絵入り）**。
- 全77枚が旧サイズの1.4倍以上に増えたことを機械検算＋モンタージュ4枚で**絵と名前・枠スキンを全枚目視**
  （祝福＝黄金／呪詛＝紫／状態＝灰褐色）。
**これで実装済みの全598枚（縦450＋横148）すべてに絵が入った＝枠＋文字だけの札はゼロ。**

### ✅ push＝完了（2026-08-12・`b516a04..643cbd5`／24コミット）＝本番反映を実機確認
- **GitHub Pages**（Deploy ワークフロー success）：`sw.js` **v62**／`js/cards.js` に `nocturne`・`KINGDOM_NOCTURNE`／
  `js/engine.js` に `PLAY_NIGHT`・`exchangeCard`・`startHexAttack`・`exorcistSpirits`／
  **夜想曲の webp 77枚すべてが 200 かつ本番とローカルでバイト一致**（機械検算）。
- **Render（オンライン）**：`GET /status` = `{"persist":true,"rooms":0}`。実 ws（`/ws`＋Origin 必須）で
  `setConfig kingdomSet=nocturne` が**受理され**、対戦開始で
  **固定10種10/10・祝福デッキ9枚（ドルイドが3枚抜いた後）・呪詛12枚・廃棄置き場のゾンビ3枚・
  幽霊山6/コウモリ山10/ウィル山12・開始デッキの家宝2枚（ヤギ＋呪いの鏡）・銅貨の山50** をすべて確認。
  ※サーバは `DOM.CARD_SETS` から許可IDを導出するのでサーバ側のコード変更は不要。

### 【次にやること】
- **発売順の未着手拡張**（段階1すら未着手＝画像・カタログとも無し）：**同盟／略奪／旭日**。

---

## 0-27. **新拡張＝夜想曲（Nocturne）に着手**：研究＋段階1（カタログ77種）＋段階2-N0（夜フェイズ基盤）（2026-08-11・未push・`sw.js` v59）

**ユーザー決定＝次の拡張は夜想曲**（発売順どおり。残る未着手4つは全部重いので順番だけ決めた）。
**現時点では CARD_SETS 未参照＝実プレイには出ない**（本番挙動は不変）。段階2の残りは §「次にやること」参照。

### 規模：**77種＝縦型48（王国33＋家宝7＋非サプライ5＋ゾンビ3）＋横型29（祝福12＋呪詛12＋状態5）**
公式の500枚（王国332＋ランダマイザー33＋家宝42＋祝福12＋呪詛12＋ゾンビ3＋その他66）と一致。移動動物園（71枚）と同規模。

### 正本＝`docs/research/nocturne_rules.md`（49万字）＝**実装前に必ず冒頭の「実装前に必読」18項目を読む**
多エージェント研究14体（日本語名・エラッタ確定2体 → 6群を分担収集 → **各群を別エージェントが一次資料で敵対検証**）。
- **【最重要の発見】RGG 公式ルールブック PDF は「2017年 第1版」＝現行カードテキストではない**。
  一般ルールの逐語には使えるが、**カード文面の正本は英語wiki の `Versions` 表の「2021 printing」行**。
  敵対検証がこれを発見し、家宝7枚・沼の恵み・ネクロマンサー等の下書きを全部書き直した。
- **英語wiki（wiki.dominionstrategy.com）は Anubis の bot 検知で直接開けないが Wayback 経由で読める**
  ＝**`tools/wikifetch.py`**（常設）。コスト記号は `<img alt>` にあるので `[$4]` の形で本文に埋め戻す。
- **日本語名は日本語wiki（ホビージャパン印刷版）が正本**。**英語wiki の Japanese 行は17枚で実物と食い違う**。
  Bard＝**詩人** / Blessed Village＝**恵みの村** / Skulk＝**暗躍者** / Raider＝**夜襲** / Cobbler＝**カブラー** /
  Den of Sin＝**悪人のアジト** / Boon＝**祝福** / Hex＝**呪詛** / Night＝**夜行** / Fate＝**幸運** / Doom＝**不運**。
- **機能エラッタは3枚だけ**（納骨堂2022／ネクロマンサー2021／追跡者2022）。日本語版カードはこの3枚が旧テキストのまま
  ＝**本アプリは現行（エラッタ後）を採用**。他に2021年印刷の "from its pile" 削除（機能差ゼロ）。
- **取り替え子(Changeling) の日本語版カードは公式に誤訳**（「代わりに取り替え子を獲得してもよい」＝獲得の置換に読める）。
  正しくは「獲得したカードを取り替え子と**交換**してもよい」。印刷どおりに実装すると獲得時トリガーが全滅する
  ＝カタログのコメントに「差し戻し禁止」と明記した。

### 段階1＝完了（`13bf3fb`）
- `DOM.CARDS` +48／`DOM.LANDSCAPES` +29（カタログ生成WF 10体＝5群を起こして別エージェントが逐語検証）。
- 新種別 **night(夜行)/fate(幸運)/doom(不運)/heirloom(家宝)/spirit(精霊)/zombie** の表示ラベル（carddata・integrity の JP/EN マップ）
  ＋枠スキン（**夜行＝ほぼ黒**［持続より優先］／祝福＝黄金／呪詛＝紫／状態＝灰褐色）。
- 孤立プール `nocturne`(33) / `heirlooms`(7) / `nocturne_np`(5) / `zombies`(3) ＋ GAIN_ORDER ＋ カード一覧の群。
- **webp 77枚生成**（縦48＝`build-cards.js`／横29＝`build-landscape.js`。枠＋文字。**絵は未回収＝46〜66KB**）。
- 資本主義の財宝集合 108→**114**（夜想曲の +$ を持つアクション6枚＝詩人/コンクラーベ/聖なる木立ち/秘密の洞窟/迫害者/追跡者）。
  夜行カード（守護者 +$1・夜襲 +$3）は**アクションではない**ので対象外＝番人テストを追加。

### 段階2-N0＝夜フェイズと基盤（`576ff80`）＝**カードの効果はまだ空**
- **夜フェイズ**＝`turn.phase` に `'night'` を追加（アクション→購入→**夜**→片付け）。
  `endBuyTailExploration` の後に `maybeEnterNight` を挟み、**手札に夜行カードがあるときだけ止まる**。
  新 action **`PLAY_NIGHT`**（アクション権も購入権も消費しない・何枚でも使える）＋`PLAYER_ACTIONS` 登録。
  人狼のように **Action でもある**夜行カードを夜に使うと、共謀者の数え／チャンピオン／山トークン／炉 は普通に働く（公式）。
  CPU の `decide` に夜フェイズ分岐＋`chooseNight`。
- **家宝**＝`DOM.HEIRLOOM_OF`（王国カード→家宝の対応表）。**開始デッキの銅貨を家宝に置き換える**（避難所と同時成立）。
- **非サプライ山**＝ウィル・オ・ウィスプ12／インプ13／幽霊6／願い12／コウモリ10（枚数は人数によらない）。
  家宝7種・ゾンビ3種と併せて engine の `NON_SUPPLY` と CPU の `NON_SUPPLY_SET` に登録（4系統から一括除外）。
- **祝福/呪詛デッキ**＝`state.boons {deck,discard,druid}` / `state.hexes {deck,discard}`（**非カード**）。
  `maskStateFor`＝**山は完全に秘密／捨て札は一番上の1枚だけ公開**（順序が漏れると残りが全部読める）。
- **状態**＝`p.deluded`/`p.envious`/`p.misery`/`state.lostInTheWoods`（非カード）＋
  `t.cantBuyActions`/`t.enviousActive`（**購入フェイズ開始時に「返して」初めて発動する2階建て**）。
- **脇札2種**＝`p.ghostSetAside`（**公開**）/`p.cryptSetAside`（**所有者のみ**）＝**物理カード**＝
  `allCards`・invariants の `ZONES`・`maskStateFor` の3点に配線。
- ネクロマンサーの準備＝**ゾンビ3枚を最初から `state.trash` に置く**（「廃棄」ではない＝墓所/下水道/青空市場は誘発しない）。

### 検証
- **npm test 全36スイート緑（exit 0・整合性 3695→4182・不変条件10）**／`verify:e2e` 9/9（webp 450/0）。
- **invariants の全プール混成 fuzz が夜想曲カードを引いて実際に夜フェイズに入り**、保存則
  （家宝の開始デッキ置換／非サプライ5山／ゾンビ3枚／脇札2ゾーン）を満たして終局することを確認。
  invariants のフェイズ妥当性チェックに `'night'` を追加した。

### 【次にやること】段階2の残り（この順）
1. **N0b＝UI の夜フェイズ操作**（手札の夜行カードを光らせて `PLAY_NIGHT` を dispatch・フェイズ表示・
   夜フェイズでも「ターン終了」が押せること）。**これが無いと人間が夜行カードを使えない**。
2. **N1＝祝福12＋呪詛12＋状態5**。`receiveBoon` / `receiveHex` の共通機構（**複数枚を順に受けるのでキューに積む**）。
3. **N2＝素直な王国カード**（夜行でない系）／**N3＝夜行カード15種**／
   **N4＝複雑**（ネクロマンサー＝廃棄置き場からプレイ／取り替え子の交換／幽霊の2回使用／吸血鬼↔コウモリ）。
4. **N5＝CARD_SET 昇格**（固定10種を選定）→ 多エージェント敵対レビュー → UIテスト新設・CPUソーク・`verify:e2e`/`verify:visual`。
5. **絵（webp）の回収 77枚** → PROGRESS 更新 → **ユーザー確認の上で** push。

### 注意（次セッションが知らないと事故る・夜想曲固有）
- **`turn.phase === 'buy'` の誤爆が最大のリスク**。夜フェイズは購入フェイズではない。
  **`maybeEnterNight` は夜行カードの有無にかかわらず必ず `phase='night'` にする**（片付け開始時の効果を
  購入フェイズ扱いにしないため）＝ここを「夜行があるときだけ」に戻すと公会堂/列柱/徴税/行商人のコストが壊れる。
- **非サプライ札を候補にする効果に `costUpTo`/`costUnder` を使ってはいけない**（`gainableBase` が非サプライを弾くので
  候補が常にゼロ→CPU が null を返し続けて本番 livelock）。悪魔祓いの精霊は `exorcistSpirits` が正本。
- **祝福/呪詛の解決中は再演（玉座の2回目など）を割り込ませない**（`runReplays` の `nocturneQueueBusy` ガード）。
  外すと玉座×迫害者の3人戦で**呪詛カードと残りの被害者が消える**（呪詛は非カードなので保存則 tally では捕まらない）。
- **人狼は夜フェイズでも「アクションカードの使用」**＝習性(Way)・御料車/法貨の呼び出し窓・女魔術師の置換・
  浮浪児のトラップ が働く。純粋な夜行カードでは働かせない（`PLAY_NIGHT` の `nIsAction` 分岐）。
- **呪われた金貨は `PLAY_ALL_EXCLUDE`**（出すと必ず呪いを獲得する）。engine だけ締めると CPU が
  PLAY_ALL_TREASURES を返し続けて無限ループするので、engine・CPU・UI は必ず同一コミットで直す。
- **祝福12種・呪詛12種は非カード**だが「消えない／複製されない」が invariants の独立した不変条件になっている。
  祝福の置き場所を触るときはこのテストを必ず見ること。
- 旧・§0-27 由来の注意（下記）はすべて実装済み。以下は**実装後も守るべき不変条件**として残す。
**`docs/research/nocturne_rules.md` の冒頭「実装前に必読」18項目が正本**。特に：
- **`turn.phase === 'buy'` 判定の誤爆**が最大のリスク。夜フェイズは購入フェイズではない
  （冠のモード分岐／ヴィラ／公会堂／列柱／汚された神殿／徴税の `gainWasBuyPhase`／闘技場／浴場／
  `t.treasuresLocked`／ピアッツァ／使者／行商人のコスト／-$1トークン／Undo の同フェイズ判定 を全部見直す）。
- **祝福・呪詛・状態は「カード」ではない**（`allCards` にも保存則 tally にも入れない／庭園・品評会・壁にも数えない）。
  **家宝・ゾンビ・脇札2種は物理カード**（数える）。この線引きを取り違えると保存則テストが即赤になる。
- **錯乱/嫉妬は「持っている＝効いている」ではない**。購入フェイズ開始時に**返して**初めて発動する。
  `END_ACTION_PHASE` は1ターンに複数回走る（ヴィラ/騎兵）＝毎回返す判定をするが、立った旗は同ターン中に下ろさない。
- **呪詛はリアクションを全員ぶん閉じてから1枚だけめくる**（被害者ごとに引き直さない）。
- **「交換(exchange)」は獲得でも廃棄でもない**（吸血鬼↔コウモリ／取り替え子）＝`triggerOnGain`/`triggerOnTrash` を呼ばない。
  ただし `supply` が増減するので3山終了には影響する。
- **川の恵み・忠犬は「先引きの後」**（§0-25 のリス／§0-21 の保存と同じ位置。§0-22 の角笛は逆に前）。
- **ネクロマンサーは Command 型を持たない**＝「命令は命令をプレイできない」ガードを適用してはいけない
  （廃棄置き場の大君主/はみだし者/船長/王子を使えるのが公式）。**幽霊の2回使用は `state.replay`**（命令ではない）。
- **ゾンビの石工の「コスト+$1 以下」は3成分（コイン/ポーション/負債）版のヘルパが要る**（既存 `costUpTo` はコイン上限のみ）。

---

## 0-26. **移動動物園＝イベント20種を実装して完成**（M4 完了・全71枚が実プレイ可能）（2026-08-11・未push・`sw.js` v57）

§0-25 で残していた **イベント20種**を実装し、`menagerie-events` を CARD_SET に昇格。
**これで移動動物園（王国30＋馬＋習性20＋イベント20＝71枚）が全部 実プレイ可能**になった。
正本＝`docs/research/menagerie_rules.md` §4（イベント20種）＋§6（敵対検証で出た訂正）。

### 実装した20種（負債コストは無い＝すべてコインのみ。基盤は帝国/冒険の `BUY_EVENT` をそのまま流用）
同盟(alliance) / 放逐(banish) / 特価品(bargain) / 商売(commerce) / 遅延(delay) / 要求(demand) /
絶望(desperation) / 包領(enclave) / 増大(enhance) / 博打(gamble) / 投資(invest) / 進軍(march) /
植民(populate) / 追求(pursue) / 刈り入れ(reap) / 乗馬(ride) / 今を生きる(seize_the_day) /
暴走(stampede) / 苦労(toil) / 輸送(transport)

新 pending 16種はすべて**4点セット**（engine reducer＋`PLAYER_ACTIONS`＋CPU `decidePending`＋UI `viewPendingModal`）：
`bargain_gain` / `demand_gain` / `desperation` / `banish` / `invest` / `transport`(mode,exile,return) /
`toil` / `march` / `gamble` / `delay` / `event_play` / `enhance`(trash,gain) / `pursue` / `populate`

### 新しく足した機構（4つ）
1. **投資の +2カード 追跡＝`p.exileInvested`（id→枚数の非カードマップ・公開）**。
   公式は「投資したコピーはマットの下半分に差して区別せよ」＝**他の手段で追放した同名は +2カードを生まない**。
   `triggerInvest(state, actor, cardId)` を `triggerOnGain` と `INVEST_EXILE` の両方から呼ぶ
   （＝「他のプレイヤーが**獲得**または**投資**したとき」）。払い戻し/輸送で枚数が減ったら投資も減らす。
2. **遅延・刈り入れの脇置き＝`p.eventSetAside`（物理カード）**。`allCards`・invariants の `ZONES`・
   `gain()` の新 dest `'eventSetAside'`（刈り入れは2025エラッタで**捨て札置き場を経由しない**）。
   次の自分のターン開始時に `resolveDurationStartEffects` が `event_play` を `startQueue` に積む（強制・王子と同型）。
3. **今を生きる＝追加ターン**（`p.seizeExtra`／`t.seizeTurn`）。優先順位は **前哨地 > 使節団 > 今を生きる**で、
   負けた側の旗は**消さずに持ち越す**（公式：今を生きるは「直前が他プレイヤーのターンか」を見ない＝3連続もあり得る）。
   **この追加ターンは同点時のタイブレークに数えない**＝`p.freeTurns`／`scoreGame` の `tieTurns`。
   終了条件のチェックは追加ターンより前（`cleanupAndAdvance` が `isGameOver` を先に見る）＝終局なら追加ターンは無い。
4. **`playCardNoAction`＝「アクション権を消費しないカードの使用」の共通入口**（苦労/進軍/博打/脇置きの開始時使用）。
   **習性（Way）も選べる**（公式：カードを使用するときはいつでも選べる）／**炉(kiln)も通る**（＝カードの使用だから）。

### 実装で踏んだ要点
- **植民（Populate）は「山の種別」で判定する**（今の一番上のカードの種別ではない）＝`populatePiles`。
  分割山は**上段カード**で判定し、上段が尽きていれば下段（財宝の「石」でも）を獲得する。城は勝利点の山＝対象外。
  廃墟/騎士はアクションの山＝一番上の1枚。**獲得順はプレイヤーが選べる**ので `t.populateQueue` に残りを持ち、
  **reduce 末尾の再開網**が（獲得時対話を解決してから）次の選択待ちを開く（「おまかせで全部」ボタンもある）。
- **「1つの効果で複数枚を続けて獲得する」ときに pending を直接代入してはいけない**。要求(demand)で踏んだ＝
  馬の獲得が開いた望楼の窓を握りつぶしていた。`state.pending` があれば `onGainQueue` に積む。
- **絶望(desperation) は `ONCE_PER_TURN_EVENTS` に入れた**が、**一次資料は割れている**（研究doc §6 に相反する
  2つの訂正がある：①"Once per turn:" は同テンプレの施し/保存等と同じで購入自体が1回／②絶望の公式FAQには
  購入制限の記述が無く「買えるが2回目は空振り」が既定）。差は「購入権1つを無駄にできるか」だけ。
  engine のコメントに両論を明記した。反対解釈にするなら `ONCE_PER_TURN_EVENTS` から外す1行でよい。

### 【既存の忠実性バグ修正】追放マットの払い戻しは「全部か0枚か」
公式ルールブック逐語 "You cannot discard just one of them."＝**一部だけは捨てられない**。
`EXILE_DISCARD` は `n !== 0 && n !== have` を状態不変で拒否する。UI も枚数ステッパー→2択ボタンに変更。

### 【既存バグ修正3件】2025年2月エラッタの波及（§0-25 で未修正としていたぶん）
1. **[実挙動バグ] 戦車競走（帝国）が `p.deck.shift()` 直読みで `draw()` を通っていなかった**。現行は "+1 Card,
   revealing it." ＝通常のドローなので、**-1カードトークン（遺物/借入）を持っていると1枚も引けず、
   公開できないのでボーナスも得られない（トークンは消費する）**。mix-all で到達可能。回帰テスト付き。
2. **使節団を `ONCE_PER_TURN_EVENTS` から外した**（現行エラッタで前置句が消えた＝1ターンに複数回買えて
   2枚目以降は空振り＝追加ターンは1回だけ）。
3. **儀式のコスト参照を廃棄「後」に変更**（`it cost` → `it costs`）。

### 敵対レビュー（多エージェント5観点＝ルール忠実性/追放・投資の帳簿/CPU非ループ/UI詰み/既存拡張への退行。
各 finding は node で再現してから修正）＝**確定9件＋UI4件 → 全修正・回帰テスト付き**
- **[high] 門番(Gatekeeper)が実質完全に無効だった**：追放した「まさにその1枚」に対して払い戻しの窓が開き、
  その場で取り戻せていた（**CPU は30/30で取り戻した**＝CPU戦では門番の追放が一度も成立しない）。
  公式は "discard the **other** copies"。→ **獲得した瞬間の枚数 `exiledBefore`** で門番の発動条件と
  払い戻し窓の**両方**を判定する（両者は排他）。**門番のテストは1件も無かったので新設**。
- **[medium] 門番×そり**：そりを捨てても獲得札は追放されたままでそりが1枚無駄になっていた。公式は
  stop-moving＝先に動かせば門番は失敗する。→ 門番の追放を **`onGainQueue` の非対話項目**にして
  そり/アザラシの窓の後に実行する（キュー消化が pending にせずその場で適用する）。
- **[medium] CPU `winsIfEnds` が `turns` で同点決勝を見ていた**（scoreGame は `tieTurns`）＝今を生きるが絡むと
  「勝てると思って買って負ける」。A/B で修正前は属州を買って winners=[1]、修正後は買わない。
- **[medium] CPU の `allCards` が追放マット等4ゾーンを数えていなかった**（終局読みが engine の得点とずれる）。
  → **engine の `allCards` を `DOM.engine` に公開して CPU がそれを呼ぶ**（新ゾーンを足しても自動追従）。
- **[medium] 相手の「投資」が盤面に出ていなかった**（買ってから相手が2枚引いて初めて気づく）→ 全員ぶん表示。
- [low] 強制の追放 pending で CPU が `null` を返し得た（オンラインでは `reduce(state, null)` が TypeError →
  **部屋が固まる**）→ engine 側に終端保証（候補ゼロなら窓を閉じる）＋CPU も null を返さない。
- [low] 要求が馬の獲得で開いた望楼の窓を握りつぶしていた／苦労で「相続した屋敷」が空振りしていた／
  移動遊園地が `eventSetAside` を知らず刈り入れの金貨を山札の上に置けなかった／
  追求が混合山のプレースホルダ（城/騎士）を指名候補に出し本物を指名できなかった／
  放逐の1段目に確認が無く誤タップから戻れなかった／絶望の購入条件がデッドコードだった（ソーク0回→109回）。
- **棄却（偽陽性）**：`p.exileInvested` のズレ（追放マットから出る経路は2つだけで両方クランプ済み）／
  オンライン永続化の後方互換（新フィールドを全部削除した旧スナップショットでも完走）／
  `EXILE_DISCARD` 2択化による engine/CPU/UI の食い違い／`tieTurns` の後方互換と結果画面の並び順／
  追加ターンの優先順位／populate 再開網と他の再開網の順序／`menagerie-events`・`ev-menagerie` とサーバ検証／
  特価品/刈り入れ/イベント購入AI が CPU を弱くする（A/B で有意差なし〜プラス）。

### 既知の許容簡略化（意図的・再議論しない）
- **1回の獲得につき「else-if 連鎖組」の獲得時対話は1つだけ**（望楼/ティアラ/交易商人/宿屋/スーク/公爵夫人/
  国境の村）。**同盟(6枚)/商売/暴走(5枚)/植民 はこの既存簡略化が最も見えやすい経路**（帝国の意外な授かり物でも
  同じ）。`onGainQueue` 組（牧羊犬/そり/鷹匠/移動遊園地/追放の払い戻し/アザラシ）は毎回正しく開く。
  根治するなら else-if 連鎖組を `onGainQueue` に載せ替える横断リファクタが要る。
- **ターン開始時の複数処理の解決順は選べない**（`startQueue` は先入れ順）。遅延＋刈り入れを同じターンに買うと
  順序固定。engine 横断の既存簡略化（持続の開始時効果も同様）。
- **輸送で「投資したコピー／していないコピー」のどちらを戻すかは自動**（投資を残す方が常に得なので最善を選ぶ）。

### 検証
- **npm test 全36スイート緑（exit 0）**：`menagerie.test.js` 126→**253**／**`menagerie-ui.test.js` 新設64件**
  （36スイート目＝全 pending にモーダルと「押せる選択肢」があることを jsdom で検査）／
  `events` 149→**153**／`empires` 269→**272**／invariants 9→**10**（移動動物園イベント節を新設）／整合性 **3695**。
- **CPUソーク72戦**（`menagerie-events`／全20イベント同時／習性つき／門番×そり×投資の王国／mix-all）
  ＝完走72・膠着0・例外0・保存則違反0。**CPU序列 100/64/95 維持**。
- `verify:e2e` 9/9（**webp 402/0**）／`verify:visual` 320〜768px 全幅はみ出し0。

### ✅ push＝完了（2026-08-11・`c87b171..d717205`）＝本番反映を実機確認
Pages デプロイ success・本番 `sw.js` **v57**・`js/cards.js` に `menagerie-events`・イベントの webp 200／
Render は `GET /status` = `{"persist":true,"rooms":0}`（サーバは `DOM.CARD_SETS` から新セットを自動受理＝サーバ側変更なし）。

### ✅ 絵（webp）71枚の回収＝完了（2026-08-11・`018740b`・`sw.js` v58）
縦型31（王国30＋馬）＝`build-cards.js`／横型40（習性20＋イベント20）＝`build-landscape.js`。
webp のサイズが 36〜60KB（枠＋文字だけ）→ **73〜201KB（絵入り・平均127KB）** になった。
**これで実装済みの全521枚（縦402＋横119）に絵が入った＝枠＋文字だけの札はゼロ**。
- 回収手順：ChatGPT が生成した **81枚**（＝71枚＋イベント後半10枚の二重生成）を Downloads からタイムスタンプで
  10グループに分け、**8体のエージェントが全81枚を実見して id を確定**（重複10枚は後発を採用）。
- **【重要な学び】ChatGPT は指示した順番どおりに生成するとは限らない**。今回、王国③・イベント①・イベント② の
  3グループは**連番と id 順が一致していなかった**（連番で機械割当すると10枚中9枚が取り違えになる状態）。
  **必ず全枚を実見して判別すること**（過去セッションは「生成順どおり」で通っていたので油断しやすい）。
- 紛らわしい「習性の馬 / 非サプライの馬」は2枚を並べて目視確認（黒たてがみ・真横向き・夕景＝習性／
  栗毛のたてがみ・斜め前向き・朝の野花＝馬）。
- 本番反映を実機確認：Pages `sw.js` **v58**／`animal_fair` `horse` `way_of_the_owl` `seize_the_day` `populate` の
  webp が**本番とローカルでバイト一致**。

### 【次にやること】
- **発売順の未着手拡張**（夜想曲／同盟／略奪／旭日）＝段階1すら未着手（画像・カタログとも無し）。

### 注意（次セッションが知らないと事故る・イベント固有）
- **「1つの効果で複数枚を獲得する」効果の後に `state.pending` を直接代入しない**（獲得時リアクションの窓を
  握りつぶす）。`if (state.pending) onGainQueue.push(...) else state.pending = ...` にする。
- **`onGainQueue` には非対話項目（`gatekeeper_exile`）も入る**。キュー消化は**その場で適用して次へ進む**
  ループになっている＝新しい非対話項目を足すときはこの分岐に足す（pending にすると無意味な確認が出る）。
- **CPU の `decidePending` で `null` を返さない**（オンラインで `reduce(state, null)` が TypeError → 部屋が固まる）。
  候補ゼロでも `{type:'X', card:null}` を返し、engine 側に「候補ゼロなら窓を閉じる」終端保証を書く。
- **CPU が所有カードを数えるときは `DOM.engine.allCards` を呼ぶ**（CPU 側で列挙を手書きしない）。
- **同点決勝は `tieTurns`**（`turns - freeTurns`）。engine と CPU の両方が同じ算出を使うこと。
- **`p.eventSetAside` は物理カード**（`allCards`・invariants の `ZONES`）。`p.exileInvested` は非カード。
- **`playCardNoAction` は相続の屋敷も見る**（`DOM.isType(card,'action') || inheritedEstate(p, card)`）。

---

## 0-25. **新拡張＝移動動物園（Menagerie）**：王国30＋馬＋習性20 を実装（M1〜M3・M5 完了／**イベント20種は未実装**）（2026-08-11・未push・`sw.js` v57）

**発売順では夜想曲が次だが、ユーザー決定で移動動物園を先に実装**（新機構が全部「既存の同型」に落ちる＝夜フェイズ・分割山・負債が無い）。
**現時点で `menagerie` / `menagerie-ways` / `random-menagerie` が実プレイ可能**（＝CARD_SET 昇格済み）。イベント20種はカタログのみ（段階1）。

### 規模：**王国30（縦型）＋馬1（非サプライ30枚）＋イベント20（横型）＋習性20（横型）＝71枚**
- 縦型31・横型40の **webp 生成済み**（枠＋文字。**絵は未回収＝暗い板**）。横型に **`way` スキン（深い黄緑・コスト円なし）** を新設。
- 新セット3つ：**`menagerie`**（固定10種）／**`menagerie-ways`**（固定10＋習性2枚抽選）／`random-menagerie`。
  固定10種 `DOM.KINGDOM_MENAGERIE` ＝ sleigh / camel_train / scrap / sheepdog / cavalry / groom / village_green / barge / coven / livery。
- mix-all にも参加（`MIX_KINGDOM_POOLS.menagerie`／`MIX_LANDSCAPE_POOLS['way-menagerie']`）。

### 正本＝`docs/research/menagerie_rules.md`（13.7万字）
多エージェント研究（19体・王国/イベント/習性/機構を分担収集 → **各群を別エージェントが一次資料で敵対検証**）で
**確定38件の訂正**を得た。英語ウィキが bot 検知で開けなかったため、**RGG 公式ルールブックPDFを実DL＋pdftotext で逐語確認**（＋Wayback）。
- **「Way」の日本語公式訳は「習性」**（"ウェイ" ではない）。**日本語の「移動動物園」は収穫祭の `menagerie`（$3）のカード名でもある**＝id/表示で区別すること。
- **2025年2月エラッタ**：ハツカネズミの習性が **non-Duration**（持続は脇に置けない）／刈り入れ・博打も変更。
- [high] 牧羊犬のリアクションは「**あなたが**カードを獲得したとき」＝自分の獲得だけ（「いずれかのプレイヤー」は鷹匠のほう）。

### 新機構3つ
1. **追放（Exile）＝`p.exile`**：islandMat 型の**公開ゾーン**（allCards／invariants の ZONES／得点に数える）。
   **「サプライから追放」は獲得ではない**（on-gain は誘発しないが山は減る＝3山終了に影響）／追放マットから捨てるのは
   本物の「捨てる」（on-discard が誘発）／**一般ルール＝同名を獲得したとき追放マットの同名を「全部」捨て札に戻してよい**
   （`exile_discard`。**一部だけは不可**＝公式 "You cannot discard just one of them."。§0-26 で修正済み）。
2. **馬（Horse）＝非サプライ30枚**：賞品と同型（`NON_SUPPLY`）。**「馬を獲得する」効果でのみ得る**（購入も汎用獲得も不可）。
   **使ったら山に戻す**（獲得でも廃棄でもない）＝命令経由では場に無いので戻らない（lose track）。
   山を用意するのは `DOM.HORSE_GIVERS`（馬を配る王国8種＋イベント4種＋ハツカネズミの脇札）がある時だけ。
3. **習性（Way）＝横型の第4種別**（`kind:'way'`・買わない）：**アクションの記載効果の代わり**に使う。
   engine の入口は `PLAY_ACTION` に **`action.way` を添える**形（UI の dispatch 元も1箇所）。
   チャンピオン／山トークン／山砦などライン下の外部能力は**そのまま働く**（公式）。女魔術師の置換より習性が優先。
   **ハツカネズミの習性は §0-17 の命令(Command)機構を流用**（`state.mouseCard` を `playAsCommand`）。

### 【重要】既存エンジンへの横断リファクタ2件（`isTreasureFor` / `reveal()` と同じ質のもの）
1. **`addActions(t, n)` に一本化（140箇所を一括置換）**＝**雪深い村（このターン以降の +アクション をすべて無視）**。
   公式は「村人の使用・チャンピオン・山トークン由来も含めて全部無視」なので、**`t.actions += n` を直接書かない**こと。
2. **`addCoins(state, n)` に一本化（133箇所を一括置換）**＝**カメレオンの習性（+カード ↔ +コイン を入れ替える）**。
   `draw()` 冒頭にも対の変換フックを入れた。**`t.coins += n` を直接書かない**こと。
   ※どちらも**消費側**（`t.actions -= 1` / `t.coins -= cost`）は対象外。

### 実装した pending（すべて4点セット＝engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal）
`exile_discard` / `barge_choose` / `bounty_hunter_exile` / `camel_train_exile` / `black_cat` / `cardinal`(react,pick) /
`coven` / `displace_exile`,`displace_gain` / `falconer_gain`,`falconer_react` / `goatherd_trash` / `groom_gain` /
`hostelry_discard` / `hunting_lodge_choose` / `mastermind_play` / `sanctuary_exile` / `scrap_trash`,`scrap_choose` /
`sheepdog_react` / `sleigh_react` / `village_green_choose`,`village_green_react` / `wayfarer_gain` / `kiln_gain` /
`way_butterfly`,`way_butterfly_gain` / `way_goat_trash` / `way_rat_discard` / `way_seal_topdeck`

### 実装で踏んだ設計上の要点
- **獲得時の対話が複数同時に起きる**（追放の払い戻し／牧羊犬／そり／鷹匠／旅籠／アザラシ）＝ 帝国 E5 で作った
  **`state.onGainQueue`** に積む（「1獲得＝1対話」の else-if 連鎖には足さない）。
- **炉（kiln）＝「次に使うカードの解決前に同名を獲得」**＝ 浮浪児の `maybeUrchinTrap` と同型で
  **PLAY_ACTION／playTreasureCard を中断**し、`KILN_GAIN` の解決で `applyEffect`（習性なら `applyWay`）を呼ぶ。
- **リス＝ターンの終了時に +2カード**は「**次の手札を先引きした後**」（保存 save と同じ場所）。
  **カエル＝場から捨てるとき山札の上**は片付けの「捨てる直前」（城壁のある村/宝物庫と同じ場所）。
- **財宝カードの効果は `applyTreasureEffect` に書く**（備蓄品/配給品を applyEffect に書いて空振りした＝自己回帰）。
- **動的コスト3枚**（デストリエ/漁師/行人）は `cardCost` に集約。**行人は「そのターンに獲得された直前の他のカード」＝
  誰の獲得でも記録する `turn.lastGainedAny`**。

### 【既存バグ修正1件】新しい持続アタックは `LINGER_REACT` の許可リストにも足す
門番（gatekeeper）を足したら **CAP-MIX6 の fuzz が 20000 step 上限で未終局**になった。原因＝
`gatekeeper/react` の pending に対し **CPU に分岐が無く END_TURN を返し続け**、さらに `LINGER_REACT` reducer の
`pd.type` 許可リスト（`haunted_woods`/`swamp_hag` 固定）に gatekeeper が無く**受理しても閉じなかった**。
→ CPU分岐・UIモーダル・reducer の許可リストの3箇所を追加。**「相手のターンをフックする持続アタック」を足すときの必須3点**。

### 【発見した既存バグ3件】2025年2月エラッタの波及（Menagerie とは独立）→ **§0-26 で全部修正済み**
裁定ワークフロー（一次資料＋反証エージェント）で判明。**いずれも既存拡張の忠実性バグ**。
1. **[実挙動バグ] 戦車競走（chariot_race・帝国）が `p.deck.shift()` 直読みで `draw()` を通っていない**。
   2025エラッタで **"+1 Card, revealing it."** になったので、**-1カードトークン（遺物/借入）を持っていると
   1枚も引けず勝てない＋トークンを消費する**。mix-all（`mix:adventures,empires`）で到達可能。
2. **[忠実性] 使節団（mission）が `ONCE_PER_TURN_EVENTS` に残っている**。現行エラッタで前置句が消え、
   **複数回買えて2枚目以降が空振り**が正しい（engine 側に `missionExtra` の空振り処理は既にある）。
3. **[忠実性] 儀式（ritual）のコスト参照が廃棄「前」**。2025エラッタで `it cost` → **`it costs`**（廃棄後の現在コスト）。
   現行プールでは差分ゼロだが1行で直せる。
- また **[許容簡略化として記録]**：命令/王子が**サプライの玉座の間**をプレイして持続を2回使うと、2025の
  「場を離れた持続は以後働かない」に反して**次ターンも2倍のまま**（`random-promo` で到達可能）。反証エージェントが node で実証。

### 検証（現時点）
- **npm test 全35スイート緑（exit 0）**：`menagerie.test.js` **新設126件**（追放/馬/王国30種/習性20種/CPU終端6戦）。
  整合性 3691→**3694**（新セット3つ）／不変条件 9（出荷セット検証に menagerie 3種＋`runGame` に ways を追加）。
- 既存テストの期待値更新2件：資本主義の財宝集合 98→**108**（移動動物園の +$ を持つアクション10枚。
  強制退去は「コスト+2コイン以下」と書くと機械判定に誤ヒットするので**カード文を「〜より最大2コイン高い」に直した**＝番人テスト追加）／
  カード一覧の横型枚数（+40）。

### 【次にやること】→ **1〜4 は §0-26 で完了。残るのは 5（絵の回収）と 6（push）だけ**
1. ~~**M4＝イベント20種**~~ ✅完了（§0-26）（`applyEventEffect` に case を足す。BUY_EVENT 基盤は完成済み）。
   **絶望(desperation)＝`ONCE_PER_TURN_EVENTS` に足す**（"Once per turn:" は**購入自体が1ターン1回**＝一次資料で確定）／
   **好機(seize_the_day)＝1ゲーム1回**（`p.seizedTheDay`・相続と同型）。
2. 上の**既存バグ3件**を修正（戦車競走は回帰テスト必須）。
3. **多エージェント敵対レビュー**（追放/馬/習性/獲得時対話の競合/CPU非ループ/既存退行）→ 確定バグを全修正。
4. `menagerie-ui.test.js` 新設・CPUソーク・`verify:e2e`／`verify:visual`。
5. **絵（webp）の回収**（71枚。ChatGPT で生成 → `asset/art/<id>.png` → 再生成）。
6. PROGRESS 更新 → **ユーザー確認の上で** push（`sw.js` v57）。

### 注意（次セッションが知らないと事故る・移動動物園固有）
- **`t.actions += n` / `t.coins += n` を直接書かない**（`addActions` / `addCoins` を通す）。雪深い村とカメレオンが静かに壊れる。
- **財宝の効果は `applyTreasureEffect`**（applyEffect は財宝では呼ばれない）。
- **獲得時の対話は `state.onGainQueue` に積む**（Menagerie は1回の獲得で複数の窓が開くのが普通）。
- **「サプライから追放」の候補は `DOM.engine.exilableSupplyIds`／`availableInSupply` が正本**（非サプライ山・
  ロック中の分割山下段・混合山の2枚目以降を弾く）。engine拒否・CPU候補・UIフィルタが同じ関数を見ること。
- **習性を足すときは `applyWay` に書く**。`isUsableWay` が採用判定の正本（engine/CPU/UI が同じ述語）。
- **相手のターンに使えるリアクション（牧羊犬/そり/鷹匠/村有緑地）が場に出したカードは、その人の次の片付けで捨てる**
  ＝冒険の隊商の護衛と同じ許容簡略化。
- **カメレオンの習性は「そのプレイの解決中」だけ効く**（選択待ちを挟むと中断ぶんは変換されない＝許容簡略化）。
- **玉座の間などの再演では習性を選び直せない**（公式は選べる＝許容簡略化）。
- **捨て札トリガー（村有緑地/坑道）は移動動物園内の捨て札経路にしか配線していない**（`.discard.push(` は engine 全体で
  113箇所＝共通ヘルパ化は未実施）。異郷の坑道が持つ制約と同じで、mix-all では他拡張の捨て札で誘発しない。

---

## 0-24. **UX 6件＋実バグ2件（1手もどす／終局後デッキ公開／カード検索／植民地終了／ティアラの出す順）＋敵対レビュー確定10件**（2026-08-06・**push済 `a84f1fa` → 修正 push**・`sw.js` v54）

### push＝完了（2026-08-06・`5ee170a..a84f1fa`）＝本番反映を実機確認済み
- **GitHub Pages**：`sw.js` **v53** → レビュー修正で **v54**（2026-08-06 実機確認：`js/ui.js` の `_imeComposing`／
  `js/engine.js` の `playAllResume`・植民地判定／`js/cpu.js` の `emptyPileCount`／`css/style.css` の `.with-undo`／
  `deck-modal` すべて配信を確認）。※途中で Pages のデプロイが**ゾンビ化**して不通になった（下の注意点参照）／`js/store.js` に `canUndo`／`js/engine.js` に
  `playAllOrder`・`deckCards`・`supply.colony` 判定／`js/ui.js` に `card-search-input`・`deck-modal`・`undo-btn`。
- **Render（オンライン）**：実 ws で本番接続して確認＝**自分の操作の後に `canUndo:true` が届き、`undo` で
  購入フェイズ→アクションフェイズに巻き戻り、`undoDone` が返る**（CPU1人＝他に人間がいないので即実行の経路）。

ユーザー要望から6件。うち**2件は実バグ**（植民地の終了条件・ティアラが空振りする出し順）。**全34スイート緑（exit 0）**／
`verify:e2e` 9/9（webp 371/0）／`verify:visual` 全幅はみ出し0。

### 【実バグ①】植民地の山が尽きてもゲームが終わらなかった
- 公式（繁栄）＝**植民地を使うゲームは「植民地の山が空」でも終了**（属州・3山と並ぶ独立の終了条件）。
  `isGameOver` は `supply.province<=0 || emptyPileCount>=3` しか見ていなかった。
- 修正＝`js/engine.js` `isGameOver` に `state.supply.colony != null && <=0`（**キーが無い＝植民地を使わないゲームでは判定しない**）。
  `scoreGame` の `reason` に「植民地の山が尽きた」を追加（属州 → 植民地 → 3山 の順）。
  **`js/cpu.js` `buyEndsGame` にも同条件**（engine が終わるのに CPU が気づかない＝終局読みのズレを防ぐ）。
- 遊び方の文にも「植民地を使うゲームでは植民地が尽きたときも終了」を追記。回帰テスト＝`test/prosperity.test.js`。

### 【実バグ②】ティアラを「財宝を全部出す」の**最後**に出していて効果が空振りしていた
- ティアラ（繁栄$4・財宝）＝**+1購入／このターン獲得したカードを山札の上に置いてよい／手札の財宝1枚を2回使ってよい**。
  最後に出すと手札に財宝が残っておらず「2回使う」が毎回無駄になっていた。**同型の 冠(crown)／偽造通貨(counterfeit) も同じ**。
- 修正＝`js/engine.js` に **`playAllOrder`（並び順の正本）＋`PLAY_TWICE_TREASURES`**。
  **① ティアラ/冠/偽造通貨を最初 → ② 銀貨（商人の「最初の銀貨」）→ ③ その他 → ④ 大金(fortune) を最後**（コイン2倍を最大化）。
- あわせて **`turn.playAllResume`** を新設＝PLAY_ALL_TREASURES が選択待ちで中断したら、
  **解決後に reduce 末尾で自動的に残りを出し切る**（storytellerResume と同型の再入）。ボタンを押し直させない。
  購入すると `treasuresLocked` で自然に止まる／`freshTurn` で turn ごと消える。

### 【新機能①】1手もどす（Undo）＝**初心者モード限定**
- **ローカル**（`js/store.js`）：`LocalStore` に履歴を持たせた。**積むのは「人間が自分で行った操作の直前の状態」だけ**
  （CPUの手番・アクションフェイズの自動スキップは積まない）＝1回押すと**自分が最後にした操作の直前**まで戻り、
  **間に挟まったCPUの手番はまとめて巻き戻る**（＝「うっかりターン終了」も救える）。`canUndo(seat)`／`undo(seat)`／`clearUndo()`。
  - state は `reduce` が毎回 clone を返す純関数なので**過去の state の参照を持つだけ**でよい（コピー不要）。上限40手。
  - `owner` を持たせて**自分が打った手より前には戻せない**（1台で人間同士のとき、前の人の手番を覗けない）。
  - `NEW_GAME`（再戦）で履歴を捨てる。
- **UI**（`js/ui.js`）：ヘッダの「直近の行動」行に `↩ 1手もどす`。**選択モーダル中も押せるよう、`viewGameDispatch` が
  pending モーダルの `.modal` に同じボタンを後付けする**（150ある pending 分岐に手を入れない）。
  巻き戻し時は CPU/自動スキップのタイマーと開きっぱなしの選択・確認・拡大を全部たたむ。
  **`UI._noAutoSkipOnce`**＝戻した直後に「手札にアクションが無い」自動スキップで飛ばされないよう1回だけ抑止。
- **オンライン**（`server/gameServer.js`）：サーバ権威なので**他の接続中の人間 全員の承認制**。
  `room.history`（人間の action の直前だけ・上限40・**永続化しない**＝Upstash肥大を防ぐ）＋`room.undoReq`。
  `undo` 要求 → 他に人間がいなければ即実行／いれば `undoAsk` → `undoVote` 全員 ok で `applyUndo` → `undoDone`。
  拒否・タイムアウト(45秒)・当事者の切断・**承認待ち中に局面が動いた**ら `undoDenied`。
  `broadcastState` が**席ごとの `canUndo`** を同送（ボタン表示の正本はサーバ）。
  **`applyUndo` は巻き戻し後に「今の接続状況」を state に貼り直す**（切断/復帰/猶予切れCPU代行まで巻き戻さない）。
### 【追加・2026-08-07】オンラインの「買い物だけは相手の同意なしで戻せる」（`sw.js` v56）
ユーザー提案＝「戻す対象を買い物に限定すれば同意なしでいけるのでは？」。engine を確認して**その通りだった**ので実装。
- **購入が安全な理由**：① **乱数を消費しない**（獲得札は捨て札に置くだけ＝ドローもシャッフルも起きない。
  シャッフルは片付け＝END_TURN の中）／② **情報が増えない**（サプライは公開・自分のコインは自分の情報）／
  ③ **購入では終局しない**（`gameOver` は `cleanupAndAdvance` の中でしか立たない＝**最後の属州を買っても
  結果画面＝`result.deckCards` の全員デッキ公開は出ない**＝覗いて戻すことができない）。
- **ただし購入にも例外がある**：愚者の黄金（相手に選択を投げる）／幽霊城・不正利得（相手の札が動く）／
  宿屋（自分の捨て札をシャッフル＝乱数を消費）。→ **カード名を列挙すると新カードで漏れる**ので、
  **`isNoConsentUndoableBuy` が「その場で証明する」方式**にした＝履歴の一番上が自分の `BUY` で、
  選択待ちゼロ・onGain/onTrashキュー空・同じターン/フェイズ・**自分以外の全プレイヤーの状態が完全一致**・
  **自分の山札と手札も完全一致**、を全部満たすときだけ同意不要。証明できなければ**従来どおり承認制へ落ちる**（拒否ではない）。
- 相手が切断中でも同意不要側は通す（その相手の状態は1ビットも変わらないため）。`broadcastState` が
  席ごとに `undoFree` を同送し、UI のボタン文言を「↩ 1手もどす（相手に確認）」→「↩ 1手もどす」に切り替える。
- 回帰＝`test/server.test.js` **70→82**（購入は同意なしで戻る／`undoFree` の配信／**不正利得は承認制へ落ちる**／
  購入以外は従来どおり承認制）。
- **push＝完了（2026-08-07・`1b4168d`）＝本番反映を実機確認**：Pages `sw.js` **v56**／`js/ui.js` に `netUndoFree`／
  本番Render に人間2人で実接続し、**銀貨を購入 → `undoFree:true` が届く → `undo` で相手に `undoAsk` を送らずに巻き戻り、
  `undoDone.free:true` が返る**ことを確認（＝同意なしで通った）。

- **注意（シャッフルは引き直しになる）**：`shuffle` は `Math.random()`（state にシードを持たない）＝巻き戻して指し直すと
  以降のシャッフルは別結果になる。初心者モード限定＋オンラインは相手の承認制、という設計で許容している。

### 【新機能②】終局後に**全員のデッキ**を見られる
- 対戦中は相手の山札・捨て札が `maskStateFor` で伏せられ、クライアントから復元できない。→ **`scoreGame` が
  `result.scores[i].deckCards`（所有カード全部の枚数）を確定して載せる**＝マスク配信でも**終了後だけ**全員に見せられる。
- UI＝結果画面の各行に「🃏 デッキを見る（N枚）」→ `viewDeckModal`（**タブで全員を切り替え**・アクション→財宝→勝利点の順・
  コスト降順・タップで拡大）。`.deck-scrim` は **z-index 30**＝カード拡大シート(38)が前面に出るように意図的に低くしてある。

### 【新機能③】カード一覧に検索
- `searchNorm`（ひらがな→カタカナ／全角→半角／小文字化）＋**空白区切りAND**。検索対象＝**カード名・id（英語）・効果テキスト・
  種別ラベル・コスト・その群の見出し（＝拡張名）**＝「繁栄」「帝国」で群ごと出る。縦型（`DOM.CARDS`）と横型（`DOM.LANDSCAPES`）の両方。
- `render()` は毎回 DOM を作り直すので、**入力中（`UI._searchActive`）だけフォーカスとカーソル位置を戻す**
  （初回表示では戻さない＝スマホのキーボードが勝手に開かない）。種別チップ（アクション/財宝/…）も用意。

### 敵対レビュー（多エージェント6観点＝Undoローカル/Undoオンライン/財宝の出す順/植民地終了/デッキ公開/UI退行。各 finding を node・jsdom・実ws・実Chrome で再現）＝**確定10件→全修正**（`sw.js` v54）
- **[high] オンラインUndo の承認バイパス**：`otherConnectedHumans` が「接続中か」で承認者を決めていたため、
  **相手が「猶予内の切断」中（本番5分・スマホのバックグラウンド落ちで日常的に起きる）は承認ゼロで何手でも巻き戻せた**
  （レビューは7手を実証）。しかも相手は復帰しても痕跡ゼロ。→ 承認者を **`otherHumanSeats`（`!m.expired`＝席がまだ人間のもの）**
  に変更し、**その中に未接続の人がいる間は undo を受理しない**（「相手の再接続を待っています」）。猶予切れでCPUに
  引き継がれれば expired=true になり自動的に承認不要へ落ちる。あわせて **`applyUndo` が `state.log` に「〜は1手もどした。」を残す**
  （log は state に載る＝切断していた人も resume で必ず受け取れる）。
- **[high] カード検索が日本語入力(IME)で壊れる**：`oninput` → `render()` が `app.innerHTML=''` で変換中の `<input>` ごと
  作り直すため、「むら」と打つと「むむら村」になり0件（実Chrome の `Input.imeSetComposition` で再現）。
  → **変換中（`compositionstart`〜`compositionend`）は再描画しない**。確定時に描き直す。実Chrome で「むら」→「村」＝21枚ヒットを確認。
- **[med] engine が状態不変で拒否した操作でも巻き戻し地点が積まれた**（押しても何も戻らない undo が積み上がり、
  相手の承認を無駄に消費し履歴メモリも食う）→ **局面が実際に変わったときだけ積む**（reduce は毎回 clone を返すので
  参照比較は使えない＝JSON比較）。あわせて **`UNDO_MAX` 40→12**（1局面 10〜25KB × MAX_ROOMS=2000 の想定メモリを抑える）。
- **[med] PLAY_ALL_TREASURES の自動再開が「押した時点で手札に無かった財宝」まで出した**（中断中に獲得した財宝・
  資本主義で財宝になったアクションを勝手にプレイ＝**高級市場が買えなくなる／アタックが無断発動する**）
  → **`turn.playAllResume = { player, queue }`＝中断時の残りを固定**し、再開は手札の再スキャンをしない
  （手札から消えた札・財宝でなくなった札は飛ばす）。
- **[med] CPU `buyEndsGame` の3山カウントが engine と食い違う**（非サプライ＝賞品/戦利品/成長先・分割山下段まで
  「空」に数える → engine はまだ続くのに CPU だけ「買うと終わる」と誤判定し、**終盤ずっと買い控える**）
  → **`DOM.engine.emptyPileCount` に一本化**（＝植民地条件を足したこの関数のついでに根治）。CPU序列は 100/64/95 のまま。
- **[med] 検索中は render のたびに検索欄へフォーカスが戻り、カードを拡大した瞬間にスマホのキーボードが開き直す**
  → 拡大シート/横型拡大を開いている間はフォーカスを戻さない＋開くときに「入力中」を解除する。
- **[med] 「1手もどす」を入れたことで直近ログ帯の1行省略(…)が効かなくなり、文字がボタンに重なって描かれた**
  → flex 化で `.last-move` 自身の `overflow/text-overflow` が無効になっていたので内側の span に付け直す（重なり0pxを実測）。
- **[low] `UI._noAutoSkipOnce` が対局をまたいで残り、次の対局の1ターン目で自動スキップが働かない**
  → `startConfigured` / `clearGameTimers` で解除＋UI 状態に明示宣言。
- **棄却3件**（偽陽性）：検索バーの `sticky top:46px`（実測で見出し直下に正しく付いており、指摘どおり60pxにすると崩れる）／
  オンライン再戦でカード拡大シートが残る（**変更前から在った挙動**）／**巻き戻すとシャッフルが引き直しになる**
  （＝PROGRESS に明記済みの意図的な許容。初心者モード限定＋オンラインは相手の承認制で許容している）。

### 検証
- **npm test 全34スイート緑（exit 0）**：`prosperity` 69→**97**（植民地終了・deckCards・ティアラ/冠/偽造通貨の出し順＋
  自動再開のキュー固定＋CPUソーク6戦）／
  `ui` 82→**119**（検索6件・IME回帰4件・フォーカス回帰2件・終局デッキ確認6件・LocalStore履歴7件・盤面の1手もどす9件）／
  `server` 49→**70**（オンラインUndoの承認/拒否/非手番拒否/巻き戻し配信/履歴を永続化しない＋**承認バイパスの回帰**＝
  切断中は受理しない・猶予切れ後は承認不要・巻き戻しがログに残る・無効操作は地点を積まない）。
- `verify:e2e` 9/9（webp 371/0・例外0）／`verify:visual` 320〜768px 全幅はみ出し0／CPU序列 100/64/95 維持。
- CPUソーク 56戦（mix含む・完走56・保存則違反0・膠着0・`playAllResume` 297回・植民地終局20戦）。
- 実Chrome での目視／CDP検証：IME（「むら」→「村」）・直近ログ帯の省略記号と重なり0px・検索/デッキ確認/1手もどすの画面。

### ✅ 帝国イベント13種の絵（webp）回収＝完了（2026-08-07・`sw.js` v55）
ユーザーがChatGPTで13枚生成（10枚＋3枚の2バッチ）→ **独立2系統の多エージェント視覚判別**（各系統が13枚を実見して
主題→idの全単射を確定）＝**全13枚 high confidence で2系統一致・取り違え0・生成順どおり** → `asset/art/<id>.png` に回収
（sha1 で13/13 完全一致を確認）→ `CARDS_ONLY=<13ids> node tools/build-landscape.js` で webp 再生成
（39〜60KB の「枠＋文字だけ」→ **83〜118KB の絵入り**に）。徴税/凱旋（負債の六角コスト）/制圧（$14）を目視確認。
**これで全450枚（縦371＋横79）すべてに絵が入った＝枠＋文字だけの札はゼロ**（実測で確認）。Downloads の原本は回収後に削除済み。
**push＝完了（2026-08-07・`5596f82`）＝本番反映を実機確認**：Pages の `sw.js` **v55**／13枚の webp が
**本番とローカルでバイト数13/13一致**（tax 117934 / triumph 114532 など＝絵入りサイズ）。
※このときも GitHub 側が停滞しており、**push しても run が作られなかった**ので `gh workflow run` で手動投入した。

### 【次にやること】
- **発売順の未着手拡張**（夜想曲／移動動物園／同盟／略奪／旭日）＝段階1すら未着手。

### 注意（次セッションが知らないと事故る）
- **絵の有無は「webp のファイルサイズ」で見分けられる**：枠＋文字だけの札は 39〜60KB、絵入りは 83〜125KB。
  一括チェックは `DOM.CARDS` ＋ `DOM.LANDSCAPES` の全idについて `asset/art/<id>.png` の存在を見るのが確実
  （2026-08-07 時点で欠けゼロ＝全450枚に絵あり）。再生成＝縦型 `CARDS_ONLY=<ids> node tools/build-cards.js`／
  横型 `CARDS_ONLY=<ids> node tools/build-landscape.js`（**このPCのみ**＝入力の `images/`・`asset/art/` は gitignore）。
- **新しい「財宝を出す順」を足すときは `playAllOrder` を直す**（PLAY_ALL_TREASURES と `playAllResume` の両方が同じ関数を見る）。
- **PLAY_ALL_TREASURES が中断したら `turn.playAllResume` が立つ**。新しく「財宝を出す」経路や購入フェイズの出入りを作るときは、
  この旗が残って意図せず財宝を出し切らないか確認する（`treasuresLocked` と `freshTurn` で止まる設計）。
- **`state.result.scores[i].deckCards` は終局時にしか作られない**（＝対戦中は漏れない）。
  新しいゾーンを `allCards` に足したら deckCards にも自動で乗る。
- **Undo の履歴は「人間が自分で行った操作」だけを積む**。新しく「UI から engine に action を送る経路」を足すときは、
  ローカルは `dispatch()`（＝`{undoSeat}` を渡すラッパ）を通すこと。`UI.store.dispatch` を直接呼ぶと戻り地点にならない
  （CPU駆動・自動スキップは**意図的に**直接呼んでいる）。
- **オンラインの巻き戻し履歴は永続化しない**＝サーバ再起動→`restoreRoom` の直後は 1手指すまで Undo できない（意図どおり）。

---

## 0-23. **mix-all モード（拡張を自由に混ぜるランダム対戦）＝エンジン硬化 完了・push済**（2026-07-14・`189a1f4`・`sw.js` v52）

### push＝完了（2026-07-14・`32313c4..189a1f4`）＝本番反映を実機確認済み
- **GitHub Pages**（Deploy ワークフロー success）：`sw.js` **v52**／`js/engine.js` に `gainableBase`/`costUpTo`/
  `gainFromOutside`/`trashFromSupplyPile`（96箇所ヒット）／`js/cards.js` に `MIX_KINGDOM_POOLS`・`isMixSet`・
  `landscapesForSet`／`js/ui.js` に `.mix-chip`（ミックス分類）。
- **Render（オンライン）**：`GET /status` = `{"persist":true,"rooms":0}`。push で自動再デプロイ（サーバは
  `isValidKingdomSet` で mix セットIDを検証済み＝サーバ側コード変更は §0-23 の配線ぶんが反映）。

### 状態：配線＋硬化とも完了。**全38スイート緑（exit 0）**。
- **npm test 38スイート緑**（整合性3401・不変条件**9**［mix-all fuzz 追加］・**新設 `test/mixall.test.js` 102件**）／
  `verify:e2e` 9/9（webp 371/0）／**mix CPUソーク 318戦（180＋修正後138）＝完走318・膠着0・例外0・保存則違反0**。

### 硬化の中身（`docs/research/mixall_hardening.md` の23件を完遂）
1. **獲得コスト述語を engine に1本化**（`costOf` / `gainableBase` / `costUpTo` / `costUnder` / `costExact` / `sameCost` を
   新設し `DOM.engine` に公開）。公式のコスト比較は **coin / potion / debt の成分別**（`$4+P` は "up to $4" ではない／
   `$0+負債8` は "exactly $0" ではない）。「より安い」は **component-wise strictly less**。
   **engine reducer・`anyGainable` ゲート・CPU の候補選び・UI のモーダル filter の4面が同じ関数を見る**。
   engine 約60箇所／CPU 約25箇所／UI 約40箇所を置換。**「ちょうど/以下」の pending には pot/debt を焼き込む**
   （旧スナップショット互換＝欠落時は 0／`underRef` で復元）。
2. **gain()/trashCard を通らない経路を統一**：封鎖＝`gain(dest:'setAside')`（新 dest）／待ち伏せ・剣闘士・塩まき＝
   新ヘルパ **`trashFromSupplyPile`**（混合山は一番上の実カード・supply 同期・`trashCard(opts.fromSupply)` で支配の退避をしない）／
   待ち伏せ・墓暴き・盗賊・出納官・泥棒・闇市場＝新ヘルパ **`gainFromOutside`**（負債付与・**支配の振り分け**・獲得トリガーを
   gain と同じに揃える）／造幣所＝`gainableBase`／密輸人＝候補と受理の両方で `costUpTo(6)`／交易商人＝**`returnToPile`**＋
   「サプライ由来でない獲得では窓を開かない」（`state._gainOutside`）。**自己廃棄5枚**（投資/祝宴/宝の地図/鉱山の村/豊穣の角）を
   `trashCard` 経由に統一。
3. **支配（Possession）**：負債は**支配者**が負う（`takeDebt`・BUY_EVENT/徴税も同じ振り分け）／**支配中の獲得でも獲得トリガーを
   発火**させる。**獲得者は支配者**（公式："any cards they would gain, you gain instead"）＝「他の各プレイヤー」句が正しく
   被支配者を含み、VP/財源/村人/アーティファクトは支配者に入り、「自分の手番」条件の on-gain（ヴィラ/物見やぐら/交易商人/
   貨物船/技術革新…）は発火しない＝**獲得札を動かす on-gain が被支配者の同名コピーを動かす事故が起きない**。
   塩まきの属州は被支配者に湧かない。自己廃棄5枚は possessionTrash 経由で返却される。

### 敵対レビュー（多エージェント6観点＝述語忠実性/保存則/支配/CPU非ループ/UI詰み/既存退行。各 finding を node 再現）＝**確定17件→全修正**
- **[high] 複製（duplicate）の「$6以下」ゲートが素の cardCost のまま**＝ポーション費用（ファミリアー/**支配**）・負債コスト・
  **非サプライ（戦利品）**をコピーできた（mix で到達）→ ゲートと受理の両方を `costUpTo(...,6)` に。
- **[high] 馬上槍試合の賞品ゲートが NON_SUPPLY を全部「賞品」扱い**＝戦利品/狂人/傭兵/トラベラー成長先が同居すると、
  賞品も公領も尽きているのに「賞品を獲得」pending が開き**CPU が永久ループ／人間が詰む**→ `PRIZE_SET`（5種）で判定＋終端保証。
- **[high] UI：獲得モーダルにポーションの山が出ない**（`SUPPLY_ORDER` は基本＋王国10種しか返さない）＝「ちょうど$4を獲得」等の
  **強制獲得で選択肢0の閉じられないモーダル**→ `modalGainSupply` が supply の残りキーも走査する。
- **[med] 交易商人が「廃棄置き場からの獲得」に反応して札をサプライの山へ戻す**（廃棄が巻き戻り・**空の山が復活して3山終了が
  巻き戻る**。カード総数は保存されるので保存則 fuzz では捕まらない）→ `_gainOutside` で窓を開かない。
- **[med] 封鎖の `dest:'setAside'` を on-gain の移動系が知らず、捨て札の同名コピーを動かす**（ヴィラ/遊牧民の野営地/物見やぐら/
  ティアラ/貨物船/技術革新/交易商人）→ **`zoneOf(p, dest)` に一本化**（'setAside' を含む）。
- **[med] 青空市場がサプライからの廃棄（待ち伏せ/剣闘士/塩まき）に反応して金貨**（公式＝「**あなたの**カードが廃棄されたとき」）
  → `triggerOnTrash` に `opts.fromSupply` を伝えて抑止。
- **[med] 支配中の獲得トリガーの獲得者**（不正利得の呪いを支配者が食う／VPが被支配者に入る／交易商人で二重取り）→ 上記3で根治。
- **[med] UI：王子のフィルタが負債コストを除外していない**（技術者が光るが engine が拒否＝死に選択肢）→ 除外。
- **[low] 値切り屋が gain() の**後**にコストを測る**＝混合山（城/騎士）で基準が1段ずれる → 購入時点の3成分を渡す。
- **[low] 待ち伏せのゲート（LURKER_CHOOSE）が gainableBase を見ない**＝候補ありと判定→受理が拒否で **CPU 無限ループ**→ 同述語に。
- **[low] CPU の「財宝を獲得」候補が静的 `isType('treasure')`**＝資本主義（`isTreasureFor`）と食い違い収税吏で livelock → 統一。
- **[low] UI：教師のトークンを空のアクション山に置けない**（公式は置ける）→ `modalGainSupply(..., allowEmpty)`。

### 【次にやること】次の拡張（未着手＝夜想曲／移動動物園／同盟／略奪／旭日）
- mix-all は**本番稼働中**。§6 の「ポーション/負債は同居しないので未修正」「闇市場に段階1プールが漏れる」注記は**解消済み**。
- 次に新カードを足すときは **必ず engine の述語（`gainableBase`/`costUpTo`/`costUnder`/`costExact`/`sameCost`）を使う**
  ＝素の `cardCost(state,id) <= N` を書くと mix で本番 livelock になる（今回のレビューで3枚が実際にそうなっていた）。

---

## （旧）0-23. mix-all の配線（硬化前のメモ）

ユーザー要望＝「ランダム対戦でどのシリーズを混ぜるか選びたい」（今は1拡張＋3つのプリセットしか無い）。
**ユーザー決定＝実プレイ可能な全13拡張を自由に組み合わせ／横型（イベント・ランドマーク・プロジェクト）も選べる／
全部混ぜて10種抽選（公式どおり）**。

### ✅ 配線は完了（`9101857`・全37スイート緑・既存挙動は不変）
セットIDを **1本の文字列** にエンコードして protocol（サーバ/永続化/再戦）を変えずに実現する：
```
mix:<王国プール,カンマ区切り>[:<横型の枚数0-2>[:<横型プール,カンマ区切り>]]
例) mix:seaside,empires,renaissance:2:ev-empires,pj-renaissance
```
- `js/cards.js`：`DOM.MIX_KINGDOM_POOLS`（13拡張）／`MIX_LANDSCAPE_POOLS`（lm-empires / ev-empires / ev-adventures /
  pj-renaissance）／`isMixSet`・`parseMixSet`・`makeMixSet`・`setDisplayName`。`kingdomForSet` が mix を解釈。
  **`DOM.landscapesForSet(setId)` を新設＝横型3種を一度に決める唯一の入口**（`landmarksForSet`/`eventsForSet`/
  `projectsForSet` を別々に呼ぶと mix で「合計最大2枚」を超えて3枚以上出る）。ui/server の呼び出しをこれに差し替え済み。
- `js/ui.js`：セット選択に**「ミックス」分類**（拡張の複数選択チップ＋横型の枚数/抽選元）。ロビー表示も mix 対応。
- `server/gameServer.js`：`isValidKingdomSet`（正規表現＋プール名の実在検証。不正文字列で意図しない王国になるのを防ぐ）。
- `css/style.css`：`.mix-chip`（折り返し・320px幅でも全部押せる）。

### ✅ **エンジン硬化は完了（上の §0-23 が最新）**。以下は硬化前に書いた計画メモ（履歴として保持）。
mix を解禁すると、PROGRESS §6 / §0-10 に**「どの出荷 CARD_SET でも同居しないから未修正」**と明記して先送りしてきた
簡略化の前提が崩れ、**全部が到達可能になる**。多エージェント監査＋敵対検証（各 finding を node で再現）＝**確定23件**。
**正本＝`docs/research/mixall_hardening.md`（作業指示書。上から順に潰せば終わる形）**。要点：
1. **汎用「$N以下 / ちょうど$N を獲得」が 非サプライ（賞品/戦利品/トラベラー成長先）・分割山下段・ポーション費用・
   負債コスト を除外していない**（例：基本＋収穫祭 で**工房が賞品を獲得**／基本＋錬金術 で**工房がブドウ園をタダ獲得**）。
   engine 約60箇所／CPU 約10箇所／UI 約40箇所。
2. **gain()/trashCard を通らない経路**：封鎖（supply直挿し）・待ち伏せ（混合山のサプライ廃棄）・造幣所・密輸人・
   交易商人（supplyにキーが生える）・自己廃棄5枚（投資/祝宴/宝の地図/鉱山の村/豊穣の角）。
3. **支配（Possession）× 他拡張**：負債の受取人が逆・サプライ外獲得（廃棄置き場/闇市場）が振り分けを素通り・
   塩まきの属州が被支配者に湧く・支配中の獲得で triggerOnGain が発火しない。
- **方針＝述語を engine に1本化**（`costUpTo` / `costUnder` / `costExact` / `gainableBase` / `sameCost` を新設して公開し、
  **engine reducer・anyGainable ゲート・CPU の候補選び・UI のモーダルフィルタの4面が同じ関数を見る**）。
  個別に `if` を足して回る修正は禁止（今回の穴は全部「同じ条件を2箇所に手書きして片方で落とした」もの）。
- **engine を締める修正と CPU を締める修正は必ず同一コミット**（engine だけ締めると CPU が拒否される札を提案し続けて
  本番 livelock ＝監査で実測済み）。

### （この節の「次にやること」は §0-23 冒頭に統合。硬化・敵対レビュー・ソーク・sw.js v52 まで完了済み）

---

## 0-22. 新拡張＝**ルネサンス（Renaissance）全50枚＋絵**（R1〜R6 全完了・**push済 `0bdd83e`**・2026-07-13）

### push＝完了（2026-07-13・`45a90f9..0bdd83e`／8コミット）
- **絵（webp）も回収済み**＝ユーザーがChatGPTで50枚生成 → 多エージェント視覚判別（各バッチ別エージェントが全10枚を実見し
  10ファイル↔10id の全単射を確定＝**全50枚 high confidence・生成順どおり・取り違え0**）→ `asset/art/<id>.png` → webp 再生成。
  縦型25＝`build-cards.js`／横型25＝`build-landscape.js`（プロジェクト＝赤茶／アーティファクト＝灰青）。`sw.js` v49→**v50**。
- `verify:e2e` 9/9（**webp 371枚すべて200**）・`npm test` 全37スイート緑。


**発売順では夜想曲が次だが、技術的相性でルネサンスを先に実装**（ユーザー決定）。夜フェイズ・負債・分割山・混合山・
非サプライ山が**一つも無い**＝新機構は3つだけ（村人／アーティファクト／プロジェクト）で、財源（ギルド）と
横型ランドスケープ基盤（帝国/冒険）をそのまま流用できる。`sw.js` v48→**v49**。

### 規模：**王国25枚（縦型）＋プロジェクト20種（横型）＋アーティファクト5種（横型・非カード）＝50枚**
- 新セット3つ：**`renaissance`**（固定10種）／**`renaissance-projects`**（固定10＋プロジェクト2枚抽選）／`random-renaissance`。
- 固定10種 `DOM.KINGDOM_RENAISSANCE` ＝ border_guard / ducat / lackeys / experiment / improve / patron / research /
  old_witch / swashbuckler / treasurer（村人・財源・アーティファクト4種・持続・クリンナップ格上げ・公開リアクション・アタックを網羅）。
- **webp 50枚生成済み**（枠＋文字。**絵は未回収＝暗い板**）。横型は project＝赤茶（コスト円あり）／artifact＝灰青（コスト無し）の新スキン。

### 正本＝`docs/research/renaissance_rules.md`（多エージェント研究＋1枚ずつ敵対検証）
**RGG公式ルールブックPDFを実DL＋pdftotextで逐語確認**し、**2019/2022/2024エラッタ**まで反映。日本語版カード（2020年2月）は
パトロン/技術革新/探査/山砦/王笏が**旧テキストで印刷**されているので、本アプリの表示は**現行（エラッタ後）**を採用。
カタログ投入用 JSON＝`docs/research/renaissance_catalog.json`。

### 新機構（3つ）
1. **村人（Villagers）＝`p.villagers`**：財源の完全同型だが、消費は**アクションフェイズ**で `t.actions += n`（新 action `SPEND_VILLAGER`）。
   公開・VP無関係・ターン跨ぎで持ち越す。財源とは**別枠**（混ぜて使えない）。
2. **アーティファクト＝`state.artifacts = {flag|horn|key|lantern|treasure_chest: 席番号|null}`**（トップレベルの公開スカラー
   マップ＝`state.pileVP` と同型＝**保存則 tally 対象外**・maskは素通し）。**同時に持てるのは1人**＝「取る」と相手から奪う。
   **「取る」は「獲得」ではない**＝獲得/廃棄トリガーは一切発火しない。付与カード（旗手/国境警備隊/剣客/出納官）が
   王国にあるときだけ `createInitialState` がキーを作る（`DOM.artifactsForKingdom`）。
3. **プロジェクト（Projects）＝横型の第3種別**：`state.projects`（採用2枚まで）＋`p.projects`（買ったもの）。新 action
   **`BUY_PROJECT`**（`BUY_EVENT` を雛形）＝購入権1消費・コイン払い・**カードは獲得しない**・`treasuresLocked`＋`buysMade++`。
   **キューブは各自2個＝1人2つまで／同じものは1回だけ／複数人が同じものを買える／コスト軽減を受けない／負債>0では買えない**。
   **`DOM.engine.canBuyProject` が正本**（engine拒否・CPU非提案・UIボタン無効の3面が同じ述語を見る）。

### 【重要】既存エンジンへの2つの横断リファクタ（trashCard 統一と同じ質のもの）
1. **`reveal()` に「公開」の共通フックを集約**（パトロン＝アクションフェイズ中に公開されたら +1財源）。engine 内の
   **約72の公開サイトが一度に対応**。「公開ではない表示」（家臣＝捨てる／闇市場デッキ／廃棄置き場からの獲得）は
   `opts.notReveal` で除外。**新しく「公開する」カードを足すときは `reveal()` を通せば自動でパトロンが効く**。
2. **財宝判定を `isTreasureFor(state, id)` に集約**（資本主義＝自分のターン中「+$」を含むアクションは財宝でもある）。
   **engine 内の `DOM.isType(x,'treasure')` 69箇所すべてを置換**。資本主義が無ければ `DOM.isType` と同値＝既存挙動は不変。
   **得点計算（砦 keep）だけは静的判定に戻す**（「ターン中」ではないため）。判定表＝日本語テキストの `+N コイン` 正規表現
   ＋除外リスト（**銅細工師**＝英語原文に「+$」記号が無い）。**新しい財宝参照を書くときは必ず `isTreasureFor` を使う**。

### 【本エンジン固有の罠】クリンナップの「先引き」順序（§0-22 の最重要）
このエンジンは**自分の手番終了時に次の手札を先引きする**。したがって：
- **角笛（Horn）＝場の国境警備隊を山札の上に置く処理は、必ず先引きより前**（後にすると1ターン遅れてほぼ無効化される）。
- **旗（Flag）＝「手札を引くとき +1カード」はその先引きに乗せる**（前哨地の3枚→4枚にも効く。学者/寄付には効かない）。
- **増築（Improve）＝クリンナップ**開始時**の窓**（`endBuyTailSchemeOrCleanup` の先頭に pending を挟む）。
- **ピアッツァ＝ターン開始時は `turn.phase === 'action'`**（ドミニオンに「開始フェイズ」は無い。ここを 'buy'/'none' に
  すると**帝国の冠（フェイズでモードを決める）が壊れる**）。

### 実装バッチ（各末で `node test/invariants.test.js` 緑＋`npm test` 全緑＋コミット）
- **R1（`7507647`）共通基盤**：カタログ50件＋村人＋プロジェクト＋アーティファクトの器（効果ゼロ＝既存挙動不変）。
- **R2（`4712098`）素直な王国15枚**：追従者/劇団/実験/根城/発明家/山村/司祭/絹商人/学者/徴募官/彫刻家/先見者/香辛料/悪党/老魔女。
- **R3（`3d0c7b7`）アーティファクト5種＋パトロン**：国境警備隊（角笛/ランタン）/旗手（旗）/剣客（宝箱）/出納官（鍵）/ドゥカート/パトロン。
- **R4（`68e7e60`）持続・クリンナップ・再演**：貨物船（新ゾーン `p.cargo`＝表向き）/研究（裏向き脇置き＝マスク必須）/増築/王笏。
- **R5＋R5b＋R6（`d00d0d3`）プロジェクト20種＋CARD_SET昇格**：資本主義を含む全20種／`bestProjectBuy`／sw.js v49。

### 主な公式裁定（実装で踏んだもの）
- **発明家**＝「獲得 → **その後**コスト減」（自分の獲得には効かない・累積する）。
- **司祭**＝`t.priestCount`。**自身が指示した廃棄には +2コインが乗らない**（予約の設置がその廃棄の後）。アタックの廃棄は
  owner=被害者≠手番＝乗らない（公式）。礼拝堂で4枚廃棄＝+$8。
- **徴募官**＝廃棄したカードの**現在**コイン費用ぶん村人（**行商人はアクションフェイズでは$8＝+8村人**）。
- **老魔女**＝**免疫者は呪いを獲得もせず、呪いを廃棄することもできない**（魔女と違う）。呪い山が空でも「手札の呪いを廃棄」は行える。
- **剣客**＝3枚引く**途中でシャッフルして捨て札が空になったら +1財源も宝箱も得られない**。財源4個以上の判定は +1財源の**後**。
- **宝箱**＝「購入フェイズの開始時」は1ターンに複数回起こり得る（ヴィラで戻れば再発動＝2022エラッタ）。
- **出納官**＝3択。**遂行できない選択肢も選べる**（engine は拒否しない＝人間が詰まない/CPUが無限ループしない）。
  廃棄置き場からの獲得は通常の「獲得」＝獲得時能力が誘発する（香辛料の+2財源など）。
- **貨物船**＝**脇に置いたときだけ持続になる**（1枚も置かなければクリンナップで捨て札）。獲得は最初の1枚でなくてよい。
- **研究**＝銅貨($0)を廃棄すると脇置き0枚＝持続にならない（最も踏みやすい分岐）。
- **技術革新**＝2022エラッタで「最初に獲得したアクション」→「そのターン獲得したアクションのうち任意の1枚（ターン1回）」。
  **使わなければ権利は消費しない**。／**探査**＝2022エラッタで「購入していない」→「**その購入フェイズ**に1枚も獲得していない」。
- **艦隊**＝ゲーム終了後、艦隊持ちが**通常の手番順で1周だけ**追加ターン（**終わらせた人の次から**＝本人が持っていれば最後）。
  その後は前哨地/使節団の追加ターンは一切起きない。
- **山砦**＝2022エラッタで「もう一度 play」→「**再演（replay）**」＝玉座の2回目と同じ扱い（自己移動は stop-moving で失敗）。
- **彫刻家**＝手札に獲得。**遊牧民の野営地も手札に入る**（獲得置換の競合＝獲得者が選ぶ／RGG明記）→ `triggerOnGain` の
  nomad_camp 句に `dest !== 'hand'` を追加した（既存挙動の小変更）。

### 検証
- `test/renaissance.test.js` **新設290件**（36スイート目）／`test/renaissance-ui.test.js` **新設62件**（37スイート目）。
- `invariants` に**ルネサンス節**（全20プロジェクトのペア＋全プール混成 fuzz＋**資本主義×全プール混成**）＝**不変条件 7→8件**。
  出荷セット検証に `renaissance` / `renaissance-projects` / `random-renaissance` を追加。ZONES に `cargo` を追加。
- **npm test 全37スイート緑（exit 0・整合性 3149→3364）**／`verify:e2e` 9/9（**webp 371枚すべて200**）。
- **CPUソーク 108戦（出荷3セット×2〜4人×全難易度）＝完走108・膠着0・例外0・保存則違反0**（プロジェクト購入201回・資本主義4戦）。

### 敵対レビュー（多エージェント5観点＝ルール忠実性/プロジェクト/保存則・マスク/CPU非ループ/資本主義リファクタの退行）＝**確定9件→全修正・回帰テスト付き**（`dcc5d2c`）
各 finding は独立エージェントが **node 実行で再現確定**（偽陽性は棄却）。
1. **[high] 増築が誘発した対話が「片付けの後」に開き、相手の手番に食い込む**：場を捨て・**次の手札を先引きし・手番が移った後**に
   技術革新／下水道／貨物船／ドゥカートの窓が開いていた（技術革新が**相手の手番中に**アクションを使う／下水道が**先引きした次の手札**を
   廃棄する）。→ `endBuyTailSchemeOrCleanup` は onGain/onTrashQueue が残っていれば **`t.cleanupWaiting` を立てて片付けを保留**し、
   reduce 末尾のキュー消化後に再入する（storytellerResume と同型）。**出荷セットで自然発生（CPUソークで検出）**。
2. **[high] 出納官 × 資本主義で人間が詰む**：UI が静的 `DOM.isType(id,'treasure')` で候補を絞っていたため、資本主義で財宝になった
   アクション（増築等）しか無いと**選択肢0・ボタン0の閉じられないモーダル**になった。→ **UI の財宝フィルタを engine と同じ
   `isTreasureFor` に一括統一**（viewPendingModal 全体）＋ engine 側にも終端保証。
3. **[med] 「1回だけ」の窓が2回発動**（技術革新／貨物船）：実験の on-gain が同一 reduce 内で2枚目を獲得するため窓が2件積まれた。
   → **解決時に残回数を再検査**（「回数制限つき onGainQueue 窓は解決時に必ず再検査」を方針として明文化）。
4. **[med] 増築を玉座/山砦/王笏で2回使っても格上げが1回だけ**：場の物理枚数で数えていた → `t.improvePlays`（策謀 `t.schemes` と同型）。
5. **[med] CPU 無限ループ：増築でポーション費用の札を廃棄すると獲得候補がずれる**（`bestGainExact` が potion を見ない）→ engine と
   同じ述語（コイン/ポーション/負債の全成分一致）に。
6. **[low] 冠/ティアラ/偽造通貨で「資本主義の財宝（アクション）」を2回使うと2回目が完全に不発** → `treasure_replay` でアクションなら
   `applyEffect`。`crownOpenPending` の候補も動的述語に。
7. **[low] 資本主義の財宝を出すと -$1トークンが食い込まない** → `applyCoinPenalty` を通す。
8. **[low] 資本主義の判定表が冒険の「+$N」表記を取りこぼす**（施し/使者/巨人/沼の妖婆/隊商の護衛/守銭奴/魔除け/ワイン商/工匠/引揚水夫）
   → 明示リスト `CAPITALISM_EXTRA` で補正し、**整合性テストで集合（98枚）を固定**（カタログ文を1文字触ると静かに変わるため）。
9. **[low] ピアッツァでターン開始時に出納官→鍵を取っても +$1 が入らない**（公式の明示例外）→ `t.inStartPhase`。
   **[low] CPU が村人を一度も使わなかった** → アクション権0＋手札にアクション＋村人あり で `SPEND_VILLAGER`（ソークで使用81回に）。

### 検証（修正後）
- **npm test 全37スイート緑（exit 0・整合性3401・不変条件8）**／`test/renaissance.test.js` **320件**／UI **62件**。
- **再ソーク 133戦**（出荷3セット×2〜4人×全難易度＋全プール混成×重点プロジェクト［資本主義/山砦/技術革新/ピアッツァ/艦隊/星図］）
  ＝**完走133・膠着0・例外0・保存則違反0・村人使用81回**。`verify:e2e` 9/9（**webp 371枚すべて200**）。

### 【次にやること】push（ユーザー確認）
- **push すれば本番 Pages/Render にルネサンスが出る**（`sw.js` v49）。サーバは `DOM.CARD_SETS`／`projectsForSet` から
  新セットを自動受理する（サーバ側コード変更は §R6 の startGame 配線ぶんが再デプロイで反映）。**ユーザー確認の上で** `git push`。
- その後の候補：**ルネサンスの絵（webp）回収**（王国25＋プロジェクト20＋アーティファクト5＝50枚。現在は枠＋文字）／
  冒険イベント20種の絵／**発売順の未着手拡張**（夜想曲/移動動物園/同盟/略奪/旭日）。

### 注意（次セッションが知らないと事故る・ルネサンス固有）
- **新しく「公開する」効果を書くときは `reveal(state, seat, cards, note)` を通す**（パトロンが自動で効く）。
  「公開ではない表示」（捨てる/獲得の可視化）は `{ notReveal: true }` を付ける。
- **新しく「財宝か」を見るときは `isTreasureFor(state, id)` を使う**（`DOM.isType(id,'treasure')` を直接書かない）。
  例外＝**得点計算**（ターン中ではないので静的判定）。
- **新しいプロジェクトの効果は `hasMyProject(state, pi, id)` で判定する**（`state.projects` に入っているだけでは効かない＝
  そのプレイヤーがキューブを置いたかどうか）。
- **`p.cargo`（貨物船の脇置き）は物理カード**＝`allCards` と invariants の ZONES に入れる。`p.villagers` / `p.sinisterPlot` /
  `state.artifacts` / `p.projects` は**非カード**＝tally に混ぜない。
- **ターン開始時の対話は `t.startQueue` に push する**（`state.pending` を直接立てない）。ピアッツァだけは「カードをプレイ
  する」ので pending を直接立て得る＝`resolveDurationStartEffects` 末尾で `if (!state.pending) popStartQueue(state)` にしてある。
- **山砦（citadel）は PLAY_ACTION／ピアッツァ／技術革新 の3経路のみ**で発火（家臣/伝令官/ゴーレム/命令 経由では発火しない
  ＝チャンピオン/教師トークンと同型の**許容簡略化**。出荷セット＝ルネサンス単独では到達しない）。
- **星図（star_chart）はシャッフル中に対話を挟めない**ので**最良の札を自動で選ぶ**（へそくり `stashPlacement` と同型の許容簡略化）。
- **角笛（horn）の「山札の上に置く」も自動**（城壁のある村/宝物庫の自動返却と同じ扱い＝許容簡略化）。
- **【敵対レビューで確定した方針】回数制限つきの on-gain 窓（技術革新／貨物船）は、解決時に必ず残回数を再検査する**
  （実験の on-gain のように「1回の獲得で窓が2件積まれる」経路があるため。窓を積む時点のチェックだけでは足りない）。
- **【敵対レビューで確定した方針】クリンナップ開始時の効果（増築）が対話を誘発したら、片付け（手札を捨てる・次の手札を
  先引きする・手番を渡す）を `t.cleanupWaiting` で保留し、対話を全部解決してから片付ける**。これを怠ると「相手の手番中に
  技術革新でアクションを使う」「先引きした次の手札を下水道で廃棄する」が起きる。
- **資本主義で財宝になるアクションの集合は `test/integrity.test.js` が98枚で固定している**。カタログ文（`+N コイン` / `+$N`）を
  触るとこの集合が静かに変わるので、テストが赤くなったら `CAPITALISM_EXTRA` / `CAPITALISM_EXCLUDE` と期待枚数を見直すこと。

---

## 0-21. 横型ランドスケープ 第3弾＝**冒険イベント20種**（AE0〜AE4 全完了・**push済**・2026-07-12／**絵も回収済み 2026-07-14**）

### 絵（webp）回収＝完了（2026-07-14・`sw.js` v50→v51）
ユーザーがChatGPTで20枚生成（10枚×2バッチ）→ 多エージェント視覚判別（各バッチ別エージェントが全10枚を実見し
10ファイル↔10id の全単射を確定＝**全20枚 high confidence・生成順どおり・取り違え0**）→ `asset/art/<id>.png` →
`CARDS_ONLY=<ids> node tools/build-landscape.js` で webp 再生成（event スキン＝茶褐色）。**これで横型イベントも枠＋文字→絵入りになった**。
`verify:e2e` 9/9（webp 371/0）・`npm test` 全37スイート緑。


### push＝完了（2026-07-12・`eacda63..aa0c185`）＝本番反映を実機確認済み
- **GitHub Pages**（Deploy ワークフロー success）：`sw.js` **v48**／`js/cards.js` に `adventures-events`・`EVENTS_ADVENTURES`／`js/engine.js` に `treasuresLocked`・`inheritedEstate`・`EVENT_TOKEN_PILE`／`js/cpu.js` に `noBuyCards`・`bestEventBuy`／イベント webp（`inheritance`/`ferry` 等）すべて 200。
- **Render（オンライン）**：`GET /status` = `{"persist":true,...}`。push で自動再デプロイ（サーバは `DOM.CARD_SETS`/`eventsForSet` から `adventures-events` を自動受理＝サーバ側コード変更なし）。


帝国イベント13種（§0-20・push済）に続き、**冒険（Adventures）イベント20種**を実装＝**冒険拡張が「縦型38枚＋横型20枚」で完全に本番実プレイ可能**になる。新セット **`adventures-events`**（冒険固定10王国＋イベント2枚抽選）。`sw.js` v47→**v48**。**イベント webp 20種 生成済み**（枠＋文字・絵は未回収＝暗い板）。

### 実装した20種（負債は無し＝コインのみ。トークン中心）
- **軽量**：alms(施し・$0)／borrow(借入・$0)／quest(探索・$0)／trade(交易・$5)／bonfire(焚火・$3)／raid(奇襲・$5)／ball(舞踏会・$5)／scouting_party(偵察隊・$2)／pilgrimage(巡礼・$4)／expedition(探検・$3)／travelling_fair(移動遊園地・$2)
- **山トークン6種**：lost_arts(+1アクション)／training(+$1)／pathfinding(+1カード)／seaway(+1購入・$4以下アクション獲得つき)／**ferry(-$2コストトークン＝新種別)**／**plan(廃棄トークン＝新種別)**
- **重量3種**：save(保存＝先引きの「後」に手札へ戻す)／mission(使節団＝追加ターン・3連続不可・カード購入不可)／**inheritance(相続＝屋敷が命令アクションになる)**

### 【重要】公式ルールの裏取りで判明した「既存エンジンの穴」＝全体ルールを2つ修正
1. **「一度でも購入したら、そのターンはもう財宝を出せない」（基本ルール）を実装していなかった** → `t.treasuresLocked`（`BUY`／`BUY_EVENT` で立てる）。これが無いと **施し（場に財宝が無ければ$4以下を獲得）を先に買ってから財宝を出す**抜け道ができる。**ロックは「購入フェイズ単位」**＝`END_ACTION_PHASE` で解除する（ヴィラで購入フェイズに入り直したら財宝を出し直せる＝公式。敵対レビューで踏んだ）。**闇市場はアクションフェイズなので立てない**。
2. **使者（messenger）の「そのターン最初の購入」にイベント購入が数えられていなかった** → `BUY_EVENT` でも `t.buysMade++`。

### 公式裁定（研究5体＋矛盾裁定1体で確定。`docs/research/landscape_cards.md` の記述より**新しい**）
- **焚火＝場の「銅貨」限定2枚まで**（2022エラッタ。旧＝場の任意カード＝無限コンボで廃止）。**立案＝「獲得したとき」**（2022エラッタ。購入以外の獲得でも／相手のターンの獲得でも発火）。※RGG がホストする Adventures ルールブックPDF（2021年版）は**2022エラッタ前**＝そのまま読むと必ず間違える。
- **施し/借入/保存/巡礼/使節団は1ターンに1回しか買えない**（"You can only buy this once per turn."＝**2回目の購入自体を拒否**＝購入権を無駄にしない）。**相続は1ゲーム1回**。
- **奇襲はアタックではない**（堀で防げない）。-1カード/-$1/旅トークンは各1個・**非スタック**。**-$1は「次にコインを得るとき$1少ない」**（手持ちを即減らさない＝`applyCoinPenalty` を購入フェイズでも変換）。
- **相続の脇置きカードは「獲得」ではないが、得点計算では自分のデッキに数える**（公式）＝`p.inherited`（新ゾーン・`allCards` と保存則 tally に入る・**サプライから1枚抜く**＝3山終了に影響）。

### 敵対レビュー（多エージェント4観点＝ルール忠実性/保存則・状態整合/CPU非ループ/既存機能への退行。各finding を node 再現）＝**確定9件→全修正・回帰テスト付き（F1〜F9）**
1. **[high] ヴィラ×財宝ロック（出荷済みセットの退行）**：`empires`/`empires-events` で **ヴィラを買った瞬間そのターン財宝を1枚も出せなくなる**（購入フェイズに入り直しても解除されない）→ `END_ACTION_PHASE` で解除。
2. **[high] 使節団の追加ターン×闇市場＝CPU無限ループ**：engine は `BLACK_MARKET_BUY` を拒否するのに CPU が買い続ける → CPU/UI に `noBuyCards` ガード。
3. **[med] 偵察隊の「見た山札の上5枚」がオンラインで相手に丸見え**（私的看破なのに `maskStateFor` の対象外）→ マスク対象に追加。
4. **[med] 保存の脇置き札の正体が `turn.savedCard` で相手に漏れる**（`setAside` は伏せているのに）→ 相手席には `'back'`。
5. **[med] 山トークンが分割山の下段に置かれると孤児化して永久に発火しない**（帝国の徴税で踏んだのと同型の再発）→ READ/WRITE 両方に `pileKeyOf`＋候補から下段を除外。
6. **[low] 移動遊園地×ヴィラ**（獲得先が捨て札→手札に変わる札）で山札上置きが黙って不発 → 実在ゾーンを探して移す。
7. **[low] 使者×イベント購入**（上記の全体ルール修正2）。
8. **[low] 門下生が相続の屋敷を対象にできない**（`adventures-events` の固定王国＝peasant があるので実プレイで踏む）→ `disciple` の対象述語に `inheritedEstate` を追加。
9. **[low] CPU の巡礼購入条件が反転**（買うと必ず旅トークンが裏返って効果ゼロ）→ 反転を修正。
- **副産物＝既存バグ1件**：CPU `case 'university'` に `NON_SUPPLY`/`splitBlocked` の除外が無く、ロック中の分割山下段（アヴァント等）を提案し続けて**無限ループ**（全プール混成 fuzz で到達・HEAD でも再現）→ 修正。

### 検証
- `test/events.test.js` 69→**149件**（冒険20種の裁定・1ターン1回/1ゲーム1回・財宝ロック・敵対レビュー回帰F1〜F9・CPU終端保証20種・CPUソーク12戦[全完走・イベント購入あり]）。`test/adventures-ui.test.js` 40→**67件**（全15 pending のモーダル・イベント帯・使節団の購入禁止・財宝ロック・相続の屋敷が光る・セット選択）。`test/empires-ui.test.js` は一覧の横型枚数を 34→54 に更新。
- `invariants` に **冒険イベント節（全20種ペア＋全部乗せ＋全プール混成fuzz＋帝国イベントとの混成）** を追加＝**不変条件 6→7件**（相続の脇置きはカード＝tally に数える／山トークン・旅/-1カード/-$1 は非カード）。出荷セット検証に `adventures-events` を追加。
- **npm test 全35スイート緑（exit 0・整合性3149・不変条件7・イベント149・冒険UI67）**／`verify:e2e` 9/9（webp 346/0・例外なし）。

### 【次にやること】push（ユーザー確認）→ その後の拡張
- **push**：`adventures-events` が本番 Pages/Render に出る（`sw.js` v48）。**ユーザー確認の上で** `git push`。
- その後の候補：**発売順の未着手拡張**（夜想曲/ルネサンス/移動動物園/同盟/略奪/旭日＝段階1すら未着手）／冒険イベントの**絵（webp）回収**（現在は枠＋文字のみ）。

### 注意（次セッションが知らないと事故る・冒険イベント固有）
- **財宝ロックは「購入フェイズ単位」**（`t.treasuresLocked` は `END_ACTION_PHASE` で解除）。新しく「購入」を作るときは `treasuresLocked` を立てる／新しく購入フェイズへ入る経路を作るときは解除すること。**闇市場（アクションフェイズ）では立てない**。
- **1ターン1回/1ゲーム1回のイベントは `DOM.engine.canBuyEvent` が正本**（engine拒否・CPU非提案・UIボタン無効の3面が同じ述語を見る）。
- **山トークンの山キーは `pileKeyOf` で正規化**（分割山は上段キー）。**READ（`applyPileTokens`／`cardCost` の渡し船）/WRITE（`EVENT_TOKEN_PILE`／`SEAWAY_GAIN`／`TEACHER_PILE`）の両方**に通し、候補（`actionSupplyPiles`）から下段を除くこと。
- **相続（inheritance）の許容簡略化（意図的）**：(1)**持続カードは対象外**（船長/大君主/王子と同じ＝屋敷が持続として場に残る追跡が要るため。公式は対象にできる＝`adventures-events` では amulet/caravan_guard が候補から落ちる）。(2)屋敷が「アクション」として見えるのは **PLAY_ACTION・門下生・CPU・UI の4経路のみ**（玉座の間/家臣/王子/ゴーレム/伝令官 等の `DOM.isType(card,'action')` ゲートは屋敷を弾く）。いずれも保存則OK・非ループ。
- **`p.inherited` はサプライから抜いた物理カード**＝保存則 tally に数える（`invariants` の ZONES に追加済み）。`allCards` にも入る（庭園/品評会/博物館等に数える＝公式）が **VPは屋敷のまま**（脇のアクションは0VP）。
- **探索（quest）は条件を満たさない選択肢も選べる**（公式：呪い1枚しかなくても「呪い2枚」を選べて、その1枚を捨てて金貨は得られない）＝engine は忠実に受理する。UIは「（金貨は得られません）」と明示して事故を防ぐ（選択肢は消さない）。


最終更新: 2026-08-31 / branch `main`（最新は `git log` で確認）。

**【現在地】§0-43／§0-44＝古い拡張の「公式FAQ・裁定」レベルの敵対レビューを 9拡張281枚に実施し、確定100件を全対応した**
（`sw.js` **v96**）。**Arcana 以外の公式全カード（縦型617＋横型227＝844枚）が実プレイ可能。**
- **§0-43**（繁栄・異郷・暗黒時代 125枚）＝確定46・43件修正。[high]＝**農地の無限VP連鎖**／**望楼が相手ターンに反応できず
  ジャンク配りへの防御が完全に死んでいた**。横断＝**アタックのリアクション窓を手札枚数で事前に絞っていた（10箇所）**。
- **§0-44**（基本・陰謀・海辺・錬金術・収穫祭・ギルド 156枚）＝確定54を全対応。
  塊＝**捨て札トリガーの呼び忘れ 約25経路**（共通ヘルパ `discardFromHand` 自体が呼んでいなかった）／
  **アタック免疫の席にリアクション窓が開かない 65箇所**（物乞い・馬商人・番犬・隊商の護衛・外交官が一度も使えなかった）／
  **コルセアが `ATTACKS` 未登録で堀でも灯台でも防げなかった** [high]。
  個別＝支配の2連続追加ターン／**追加ターンをタイブレークに数えていた**（前哨地・使節団・支配・艦隊）／廷臣の種別数／
  サルの枚数／鉱山の村の「これ」／船乗り／策士の強制／身代わりのアタック窓／番犬のボタン／灯台／封鎖／海賊／外交官／
  風車／薬草商／錬金術師／名馬×長老／石工のポーション過払い／地下貯蔵庫のカード文＋カメレオン。
  設計判断＝**闇市場デッキに「準備つきカード」を入れない**（`BM_SETUP_EXCLUDE` 74枚・恒久検査つき。
  公式が明示的に許す構成の自由＋Dominion Online と同じ方針で、渡し守・野盗・山師・魔女娘の4件を到達不能にした）。
- 検証＝`npm test` **全50スイート緑**（整合性5870・不変条件12）／**バグ注入 27/27 検出**／
  **全63出荷セット×2〜4人 CPUソーク＝189ゲーム・48,947ステップで問題0**／`verify:e2e` 9/9。
- **【次にやること】残り6拡張の同じレビュー**＝**冒険・帝国・ルネサンス・移動動物園・夜想曲・同盟（約400枚）**は
  この軸のレビューを一度も受けていない。281枚で100件出た密度なので相応に埋まっている見込み。
  その先＝**第18拡張 Arcana**（500枚・王国37山・機構＝Study/Cart/Project）。⚠ カード名が1つも公開されていないので着手不能。

**【前回の現在地】§0-41＝日本語カード名の全数監査で50件を公式へ統一し、§0-40 の宿題を完済**（`sw.js` v90・push 済み）。
同名カードの重複4組（役人／従者／使者／投資）が全解消。詳細は §0-41。

**【前回の現在地】§0-40＝「世の中に出ている全カード」との差分を監査＝残りは 33種 ＋ Arcana。旭日の絵50枚も回収済み**
（`sw.js` **v83**・**push 済み（2026-08-22・46コミット・`892af1d..b747e80`）＝本番反映を機械照合済み**）。
✅ **push 後の本番照合**＝Pages の Deploy success／`sw.js` **v83**／`js/cards.js`・`engine.js`・`cpu.js`・`ui.js`・
`carddata.js`・`css/style.css`・`sw.js` の **7本が sha1 完全一致**／旭日 webp 5枚＋仮面舞踏会が**バイト一致**／
Render `GET /status`＝`{"persist":true,"rooms":0}`／**実 ws で `setConfig kingdomSet=risingsun` が受理され、
固定10種 10/10・予言(progress)・Sun 5・川船の脇札(small_castle)・相手手札マスク が全部正常**
＝**本番で17拡張＋プロモ＝811枚が絵入りで遊べる状態になった**。
**2026-08-22＝旭日50枚の絵を回収して webp を焼き直した**（Codex 経由の zip＋manifest.csv・50/50 一致・
縦型110〜198KB／横型67〜119KB＝全枚絵入り・モンタージュで全50枚目視）＝**実装済み811枚すべてに絵が入った**。
push 前の検証＝`npm test` 全48スイート緑（整合性5564）／`verify:e2e` 既定9/9・旭日11/11／`verify:visual` はみ出し0／
**本番 v77 のスナップショット50件を v83 の engine で復元・完走（問題0）**＝オンラインの永続化部屋を壊さない。
多エージェント43体で公式（英語wiki）と1枚ずつ突き合わせ、**確定33件・偽陽性0**：
**海辺 第1版 8／繁栄 第1版 9／収穫祭＆ギルド 第2版 14（王国8＋褒賞6）／プロモ 2（Marchland・Summon）**。
**他の13拡張と横型226種・基本カード10種は欠落ゼロ**。
**第18拡張 Arcana はカード名が1つも公開されていないので着手不能**。
副産物として **仮面舞踏会の出荷済み実バグ**（2人戦で相手の手札が0枚だと1枚タダで献上する）を修正した。
残る指摘＝**日本語名の誤り14件**（`mandarin`「役人」が基本の `bureaucrat` と衝突）・`harem`→`Farm` 改名・
`hoard` の二重登録。**どれも webp 再生成が要るので旭日の絵入れとまとめるのが効率的**。
**2026-08-23＝未実装33種の段階1（カタログ＋絵＋webp）完了＝844枚すべて絵入り（`sw.js` v84）。
**14種の段階0も完了（正本 `cornguilds2e_rules.md`・⚠敵対検証は未実施）。過払いの出荷済みバグも修正。**
次＝① 14種の正本の敵対検証（13:30 のセッション上限明け）／② 33種の段階2（engine/CPU/UI）／③ push（ユーザー確認）**。

**【前回の現在地】§0-39＝旭日の王国25種も敵対レビュー2体で洗い直し、確定10件を全修正**
（`sw.js` v82）。**これで旭日50種すべて（王国25／予言15／イベント10）を正本の逐語と1枚ずつ突き合わせた。**
[medium] は **公家が場に0枚で +3購入**（**`random-promo` の闇市場で今日到達**）／**交替が廃棄カードの
コストを測り直さない**（正本が「必ず壊れる」と事前警告）／**札差が廃棄時効果より先にドローする**
（リシャッフルの母集団が変わる）／**狐が記載順に解決せず獲得時対話を握りつぶす**。
[low] は 浪人×-1カードトークン／浪人×カメレオン／**川船が使用中の騎士・来寇の11山目を脇札にする**／
支配中に影札が見えない／田舎の村の辞退ボタン。
`risingsun-r4` 230件・`risingsun-ui` 98件（**バグ注入 10/10 検出**）。
**新旧 engine の完全並走 310ゲーム・62,634ステップ・差分0**／**CPUソーク 52ゲーム・問題0**。
**次＝① 絵50枚の回収（ユーザーが ChatGPT で生成）／② push（ユーザー確認）**。

**【前回の現在地】§0-38＝旭日の予言15種を敵対レビュー2体で洗い直し、確定11件を全修正**（`sw.js` v82）。
[high] は **「山に戻せないカードを場から抜いてゲームから消す」保存則違反が3箇所**
（濡女／**馬の習性＝出荷済み**／チョウの習性）。ほかに**神風の新10山で派生セットアップが4系統漏れ**
（川船は旭日単独で68%）、**進歩が stop-moving を無視してヴィラを手札から奪う**、
**豊作／狼狽が `playCardNoAction` 経由の財宝で空振り**、**悟りが Action-Treasure に効かない** など。
並行して**イベント10種を正本の逐語で自前検算**し、**官僚制が負債コスト・ポーション費用を取りこぼす**実バグを1件発見。
`test/risingsun-r4.test.js` 96→**183件**（バグ注入 12/12・7/8・2/2・1/1 で感度を実証）。
**新旧 engine の完全並走 310ゲーム・62,634ステップで差分0**＝既存16拡張への退行なし。

**【前回の現在地】§0-37＝旭日（Rising Sun）＝段階2 R4〜R7 完了＝17拡張目が実プレイ可能になった**
（`sw.js` v81）。
**旭日50種すべての効果が動き `CARD_SETS` に昇格した**＝`risingsun`（固定10種）／`risingsun-events`／
`random-risingsun`／**mix-all も 16→17拡張**。これで **17拡張＋プロモ＝811枚が実プレイ可能**
（**絵は旭日50枚だけ未回収**＝枠＋文字。指示文5バッチは作成済み）。
新機構＝**前兆/予言/Sunトークン**（`prophecyActive` で書く・発動フックは `queueProphecy`）と
**影(Shadow)**（山札から使える裏面違い）。最大の難所だった**神風(Divine Wind)**（王国10山を丸ごと入れ替え）も実装済み
＝保存則ハーネスに `state.kingdomEpoch` を新設して基準を取り直す形にした。
**この作業で出荷済み・既存拡張の実バグを4件見つけて直した**（徴税の横取り／相続した屋敷の種別取りこぼし／
CPU の得点読みの乖離／蓄積の CPU livelock）。
**多エージェント敵対レビュー（4観点）で確定9件も全修正**（[high]＝**賛辞の「やめる」ボタンが1つも出ず
人間だけが3枚を強制獲得させられる**＝出荷セットで到達）。既存16拡張への退行は
**新旧 engine の完全並走 446ゲーム・91,425ステップで差分0**＝クリーン。
`npm test` 全48スイート緑（整合性5561・旭日498＋R4 113＋旭日UI 96）／`verify:e2e` 9/9／`verify:visual` はみ出し0。
**次＝① 絵50枚の回収（ユーザーが ChatGPT で生成）／② push（ユーザー確認）**。
その先の**第17拡張 Arcana はカード名すら未公開なので着手不能**。

**【前回の現在地】§0-36＝旭日の段階2 R3（王国カード20種の効果）＝完了**（`sw.js` v79）。
影5種＋前兆6種＋素直な9種が実際に動く（残り5種＝大名/川船/絵師/米/侍 は R6）。新機構＝**`addDebt`**
（効果で負債を得る。`takeDebt` はコスト欄用なので効果に使うと黙って0）。**保存則 fuzz が自分の実バグを捕捉**
（`finishGain` は boolean なのに `return` して state が消えた＝オンラインなら部屋が壊れる）。

**【前回の現在地】§0-35＝旭日の段階2 R2（影 Shadow の基盤）＝完了**（`sw.js` v79）。
シャッフルで**束の一番下へ**／**山札のどこからでも使える**（アクション権は消費）／`maskStateFor` で自分の山札の
影札位置が見える／**手札には数えないが所有カードには数える**／**「手札から使わせる」18窓 × 4面**の横断改修。
**新設した4面整合ガードが自分の改修の穴を13窓ぶん検出**（[high] 市場の町の本番 livelock を CPUソークで実再現／
コンクラーベでリソースが湧く）＝全部修正済み。`npm test` 全47スイート緑（旭日424＋UI39）。**次＝R3（影5種の効果）**。

**【前回の現在地】§0-34＝旭日の段階2 R1（前兆/予言/Sunトークンの基盤）まで完了**（`sw.js` v79）。
`removeSun` が「+1 Sun」の唯一の入口で、**最後の1個を取り除いた瞬間に汎用の発動フックを同期的に呼ぶ**
（神器・神風が即時発火する公式仕様）。**予言15種・前兆6種の効果はまだ空**＝R3/R4 で実装。
敵対レビュー3観点で**確定14件を全対応**（[high]＝**私のテストがバグ注入4種を素通りしていた**／
**UI 差分にテストが無かった**）。`npm test` 全46スイート緑。**次＝R2（影 Shadow の基盤）**。

**【前回の現在地】§0-33＝旭日の段階1（カタログ50種＋webp 50枚）が完了**（`sw.js` v78）。
`DOM.CARDS` 585／`DOM.LANDSCAPES` 226＝**合計811枚**（旭日は `CARD_SETS` 未参照＝**実プレイには出ない**）。
多エージェント12体で起草→逐語照合し、統合後に独立監査4観点で**確定13件を全対応**（[medium] 2件＝
**縦型の webp が絵の窓を透明のまま出していた**／**段階1で横型を配線せよという誤指示コメント**）。
`npm test` 全44スイート緑（整合性5558）／`verify:e2e` 9/9／`verify:visual` はみ出し0。
**次＝絵50枚の回収 → 段階2（R1〜R7）**。

**【前回の現在地】§0-32＝§0-31 の「残っているバグ候補」6件を全部検証・修正した**（`sw.js` v77・**push済**＝
2026-08-20 に本番反映を機械照合済み）。
6件とも node 再現で実バグ確定（冠/首謀者/門下生×canPlayHandCard／元手×支配／行人×gainFromOutside／
ヴィラ/騎兵隊×購入フェイズ終了時／回避×シャッフル経路／徴税×空山）＋敵対レビュー確定5件
（**[high] 首謀者モーダルの辞退ボタンが死んでいて候補ゼロで人間が詰む**＝既存バグ・出荷済み menagerie で到達）も修正。
回帰テスト +117 assert（全件バグ注入で感度確認）。**次＝旭日の段階1（カタログ50種＋webp）＝§5 の 2.**。

**【前回の現在地】§0-31＝新拡張「旭日（Rising Sun）」＝段階0（公式ルール研究）完了**（`sw.js` v76・**未pushが10コミット**）。
正本＝**`docs/research/risingsun_rules.md`（847KB・9258行）**＝多エージェント3ワークフロー（計40体）で
**収集 → 敵対検証（確定訂正43件）→ 確定版8章 → 完全性の批評（[must] 約40件）→ 全件反映**まで回した。
**捏造・枚数過不足・日本語欠落はゼロ**（50種すべてが本文にあることを機械検算）。
**着手前に冒頭の「実装前に必読18項目」と「決定事項 D1〜D5」を必ず読むこと。**
- **300枚／実装対象50種**＝王国25＋イベント10＋予言15（略奪85種より小さい）。**カードの機能エラッタは0件**。
- **機構5つのうち3つ（負債・イベント・持続）は実装済み**＝新規は **前兆(Omen)/予言(Prophecy)/Sunトークン** と
  **影(Shadow)** の2つだけ。最大の難所は **神風(Divine Wind)＝王国10山を丸ごと入れ替える**。
- ⚠ **拡張の日本語名は「旭日（きょくじつ）」**（PROGRESS が長らく書いていた「日の出づる国」は誤りだった）。
- ⚠ **実は最後の1拡張ではない**＝第17拡張 **Arcana**（2026年予定・500枚・王国37山・Study/Cart/Project）があるが、
  **カード名すら未公開なので現時点では着手不能**。

**この研究の副産物として、出荷済みの実バグを7件見つけて全部直した**（§0-31 の表）＝
負債の2024エラッタ2件（帝国）／宝物庫の条件（海辺）／旗艦×永続持続（略奪）／高原の羊飼い×安価な（同盟）／
**民兵ほかで坑道が金貨を出さない（`promo-pack` で今日到達）**／増築×操舵手（ルネサンス）。
**旧い誤った挙動を固定していたテスト2件も公式へ直した。**

**`DOM.CARDS`/`LANDSCAPES` はまだ不変＝旭日は実プレイに何も出ない**。
`npm test` **全44スイート緑**（exit 0・整合性**5307**・不変条件12）／`verify:e2e` 9/9（webp 560/0）／
`verify:visual` はみ出し0。
**【次にやること】① 残っているバグ候補6件（§0-31 の表）を潰す → ② 段階1＝カタログ50種＋webp → ③ 段階2（R1〜R7）。**
⚠ **push はユーザー確認**（未pushが10コミット＝上の7件の修正が本番にまだ出ていない）。

**【前回の現在地】§0-30＝「略奪（Plunder）」＝完成・push済**（2026-08-16・`sw.js` v75・Pages/Render とも機械照合済み）
＝**実装済み16拡張＋プロモ＝761枚（縦型560＋横型201）が実プレイ可能で、全部に絵が入っている**。
正本＝`docs/research/plunder_rules.md`。新機構＝戦利品(Loot)／"next time" 型持続／特性(Trait)。
既存16拡張への退行はクリーン（新旧 engine を同一シードで完全並走＝294ゲーム・75,519ステップ・差分0）。
⚠ **2026-08-15 は2つのセッションが並行して同じリポジトリを触り、矛盾するコミットが出た**（§0-30 の「経緯」節）。

**【前回までの現在地】§0-29＝新拡張「同盟（Allies）」＝**A5（CARD_SET 昇格）まで完了＝実プレイ可能になった**（`sw.js` v68・**push済＝本番反映を実機確認**）。
①公式ルール研究（多エージェント22体）②段階1（カタログ72種＋webp78枚）③**A1**（好意トークンと Ally 選定）
④**A2**（分割山6組＝混合山モデル＋循環 Rotate）⑤**A2b**（「カードの種別」を山の一番上で判定する横断修正）
⑥**A3＝Ally 23種**（v66 push済）⑦**A4＝王国カード49種**（v67 push済）
⑧**A5＝CARD_SET 昇格＝`allies`（固定10種）／`random-allies`／mix-all（14→15拡張）**（この節が詳細）。
＝**同盟72種（Ally 23＋王国49）が実際に遊べる**。⑨**絵（webp）72枚の回収も完了**（`sw.js` v69）。
`npm test` **全40スイート緑**（exit 0・整合性**4765**・不変条件12・**同盟489件**＋**同盟UI 261件**）／
`verify:e2e` 9/9（webp 505/0）／`verify:visual` はみ出し0／CPU序列 100/64/95 維持。
**A5 の多エージェント敵対レビュー（5観点）で確定10件を全修正**（[high] 1件＝**蛮族×リッチで獲得窓が候補ゼロで
開いて人間が詰む／CPU が livelock**＝出荷する固定10種に両者が同居する＝A5 で初めて到達可能になった）。
**うち2件は出荷済みの既存バグ**＝混合山から獲得したログが「獲得後の一番上」を名乗る（**城は帝国で毎回ズレる**）／
320px で「ランダム」の抽選元セグ18個が幅22pxに潰れて縦書きになる。**どちらも公式・意図へ寄せる方向**。
CPU非ループは**クリーン**（2,316戦・同盟の pending 71/71 到達）。既存14拡張への退行も**クリーン**
（新旧 cards.js を同一シードで完全並走＝614ゲーム・186,643ステップ・差分0）。
**【次にやること】略奪（Plunder）＝段階0（公式ルール研究）から**。§4 の**ユーザー決定C＝収益化はせず
このまま全拡張を作り切る**により、残るは **略奪 → 旭日** の2拡張だけ。
略奪＝**500枚／実装対象85種**（王国40・Loot 15・イベント15・特性(Trait)15）、新機構＝**Loot／特性／"Next time"持続**。
手順と規模の根拠は **§5 の 1.** に書いた。着手前に `docs/adding-cards.md` を読むこと。
落とし穴は **§0-29 の A5／A4 節の注意**と **`docs/research/allies_rules.md` 冒頭の「実装前に必読」9項目**。**

**【前回までの現在地】§0-28＝新拡張「夜想曲（Nocturne）」が完成し、絵まで入れて push 済（`sw.js` v62・本番反映を実機確認）。
全77種（王国33＋家宝7＋非サプライ5＋ゾンビ3＋祝福12＋呪詛12＋状態5）が実プレイ可能で、
`nocturne` / `random-nocturne` が CARD_SET にあり mix-all も 13→14拡張になった。
`npm test` **全38スイート緑**（exit 0・整合性4184・不変条件10・nocturne 371件＋nocturne-ui 87件）。
**多エージェント敵対レビュー（6観点・31エージェント）で確定30件・棄却0 を全部修正**（[high] 2件＝CPU の本番 livelock）＋
自己監査で実バグ4件を追加修正。CPUソーク105戦＝完走105・膠着0・例外0・保存則違反0。
`verify:e2e` 9/9（webp 450/0）・`verify:visual` はみ出し0・CPU序列 100/64/95 維持。
**これで実装済みの全598枚（縦450＋横148）に絵が入っている＝枠＋文字だけの札はゼロ。作業ツリーはクリーン。**
【次にやること】**発売順の未着手拡張＝同盟／略奪／旭日**（段階1すら未着手＝画像・カタログとも無し）。
夜想曲を触るときの落とし穴は **§0-28**／**§0-27**／**`docs/research/nocturne_rules.md` 冒頭の「実装前に必読」18項目**。**

**【前回までの現在地】§0-26＝新拡張「移動動物園（Menagerie）」が完成し、絵まで入れて push 済（`sw.js` v58・本番反映確認済）。
全71枚＝王国30＋馬＋習性20＋イベント20。`menagerie` / `menagerie-ways` / `menagerie-events` / `random-menagerie` が実プレイ可能。
`npm test` **全36スイート緑**（exit 0／整合性3695・不変条件10・menagerie 253・menagerie-ui 64）。
CPU序列 100/64/95。`verify:e2e` 9/9（webp 402/0）・`verify:visual` はみ出し0。
この作業で **2025年2月エラッタの波及3件**（戦車競走／使節団／儀式）と、**敵対レビュー確定13件**（うち
[high] 門番が実質完全に無効だった／[medium] CPU の終局読みが engine の得点とずれていた 等）も修正した。
【次にやること】**発売順の未着手拡張（夜想曲／同盟／略奪／旭日）**＝段階1すら未着手。
詳細と落とし穴は **§0-26 と §0-25** に全部書いてある。**

**【前回までの現在地】§0-24＝UX6件＋実バグ2件（1手もどす／終局後デッキ公開／カード検索／植民地の終了条件／
ティアラの出す順）＋敵対レビュー確定10件を修正して push 済**（`sw.js` v54）。CPU序列 100/64/95。

**【2026-08-07】帝国イベント13種の絵も回収し、`sw.js` v55 で焼き直した＝これで本当に全450枚（縦371＋横79）に絵が入った**
（枠＋文字だけの札はゼロ。実測確認済み）。

**§0-23＝mix-all モード（拡張を自由に混ぜるランダム対戦）は配線＋エンジン硬化（23件）＋敵対レビュー（17件）まで
完了し push 済（`189a1f4`・`sw.js` v52・本番反映を実機確認）＝実プレイ可能な13拡張を自由に混ぜて遊べる**
（横型＝イベント/ランドマーク/プロジェクトも合算2枚まで選べる）。

**【mix-all 以後の必読】獲得の可否・コスト比較は必ず `DOM.engine` の述語を使う**
（`gainableBase` / `costUpTo` / `costUnder` / `costExact` / `sameCost`）。素の `cardCost(state,id) <= N` を書くと、
非サプライ（賞品/戦利品/成長先）・ロック中の分割山下段・ポーション費用・負債コストを取りこぼし、
**engine が拒否×CPU が提案し続けて本番 livelock**になる（今回のレビューで複製/馬上槍試合/待ち伏せが実際にそうなっていた）。
**サプライ外からの獲得は `gainFromOutside`／サプライの山からの廃棄は `trashFromSupplyPile`／獲得先ゾーンは `zoneOf`**。

**§0-22＝新拡張ルネサンス（Renaissance）全50枚（王国25＋プロジェクト20＋アーティファクト5）＝実装・敵対レビュー修正・
絵まで完了し push 済（`sw.js` v50）。§0-21＝冒険イベント20種の絵も回収して push 済（v51）。
＝現在、実装済みの全カードに絵が入っている（枠＋文字だけの札はゼロ）。**
既存エンジンに横断リファクタ2件（`reveal()` に公開フック集約＝パトロン／財宝判定を `isTreasureFor` に集約69箇所＝資本主義）。

（以下は §0-21 までの旧サマリ）
**帝国（Empires）は 縦型36枚＋E8命令＋ランドマーク21＋イベント13 まで push済（§0-16〜§0-20・`sw.js` v47・本番反映確認済）。**§0-21＝横型ランドスケープ第3弾＝**冒険イベント20種は AE0〜AE4 全完了・push済（`aa0c185`・`sw.js` v48・本番反映確認済）**（新セット `adventures-events`＝冒険固定10＋イベント2抽選）＝これで**冒険拡張が「縦型38＋横型20」で完全に実プレイ可能**。この作業で**既存エンジンの全体ルールの穴を2つ修正**（①購入したらそのターンは財宝を出せない＝`t.treasuresLocked`／②使者の「最初の購入」にイベント購入を数える）。敵対レビュー4観点で**確定9件＋既存バグ1件を修正**（ヴィラ×財宝ロックの退行[high]・使節団×闇市場のCPU無限ループ[high]・偵察隊/保存のオンライン情報漏洩 等）。**次にやること＝push（ユーザー確認）**。以後の拡張も 完成→CARD_SET昇格→全テスト緑→**都度ユーザー確認の上で** push（勝手に push しない）。
公開: GitHub Pages https://ankake-web.github.io/dominion/ （クライアント）＋ Render（オンライン対戦サーバ）。
**新セッションは まず `npm test` を実行し 40スイート・オールグリーン（exit 0・整合性4765件・不変条件12・
同盟489件＋UI261件・夜想曲371件＋UI87件・移動動物園253件＋UI64件・ルネサンス320件＋UI62件・帝国272件＋UI75件・
ランドマーク80件・横型イベント153件・冒険59件＋UI67件・暗黒時代87件＋UI61件・新プロモ165件＋UI22件・繁栄97件・
異郷83件＋UI44件・収穫祭107件＋UI21件・ギルド81件＋UI25件・mix-all 102件・UI121件・
CPU序列 強vs弱100/強vs普通64/普通vs弱95）を確認**してから着手すること。
実ブラウザ検証（puppeteer・手動）: `npm run verify:e2e`（通しプレイスモーク＝9/9・webp346/0）／`npm run verify:visual`（320〜768pxはみ出し検査）。

---

## 0-20. 横型ランドスケープ 第2弾＝**帝国イベント13種**（EV0〜EV3 全完了・**push済**・2026-07-11）

ランドマーク（§0-19・push済）に続き、**帝国イベント13種**（ユーザー決定＝負債/VPトークン既実装で相性最高・帝国拡張の仕上げ）を **EV0〜EV3 まで全実装完了＝push済（`d1a7b34`・本番 v47 反映確認済）**。正本＝`docs/research/landscape_cards.md` §2/§3/§4＋`landscape_gaps.md`。ランドマークと違い**イベントは「買う」**＝新機構 `BUY_EVENT`（購入ディスパッチャ）＋CPUのイベント購入評価 `bestEventBuy`＋UIの購入ボタン。**これで帝国拡張（縦型36＋E8＋ランドマーク21＋イベント13）は完全に本番実プレイ可能**。

### 確定した方針（研究＋PROGRESS §0-18 で裁定済み）
- **13種**＝advance/annex/banquet/conquest/delve/dominate/donate/ritual/salt_the_earth/tax/triumph/wedding＋**windfall**（研究データに欠落＝$5・山札と捨て札が両方空なら金貨3枚／手札・場は判定外）。
- **`BUY_EVENT`**：購入フェイズで発火・**購入権を1消費・イベント自体は獲得しない・同じイベントを1ターンに複数回買える**（Delve等）・負債コストは `p.debt += debt`。**負債>0 の間はカードもイベントも購入不可**（`BUY` と同じ拒否を入れる）。**返済は購入権を消費しない**（既存 `REPAY_DEBT`）。`PLAYER_ACTIONS` に登録必須。
- **横型は `DOM.LANDSCAPES` が正本**（`DOM.CARDS` に入れない＝整合性テストに混ざらない）。イベントは `kind:'event'`・cost/debt あり。カタログ文は**現行エラッタ**（tax/basilica等の「購入時→購入フェイズの獲得時」）。
- **進め方＝帝国方式**（効果をバッチ実装→**最後に CARD_SET `empires-events` 昇格**）。実装中は `state.events=[id]` を直接セットしてテスト。新pendingは4点セット＋終端保証。**「≤$N/相手に獲得」系は engine述語とCPU候補の両側に `!NON_SUPPLY`・`!splitLocked`・`costIsPlainCoin`**。
- **カウンタ新設**（`freshTurn`）：Conquest＝今ターン獲得した銀貨数／Triumph＝今ターン獲得したカード数（`gain`/`triggerOnGain` でインクリメント・`actionsGainedThisTurn` と同型）。
- **重量順**：T3/T4＝delve/wedding/dominate/windfall/salt_the_earth/banquet/conquest/triumph/advance/ritual（先）→ T1＝tax(`state.pileDebt`)/donate(次ターン全掃討)/annex(捨て札選択＋reshuffle)（後）。

### 進捗
- **✅ EV0＝共通基盤（完了・未push）**：13イベントを `DOM.LANDSCAPES`（`kind:'event'`）＋`DOM.EVENTS_EMPIRES`＋`DOM.eventsForSet`。engine に `state.events` スロット（landmarks 同型・clone で mask 保持）＋`hasEvent`＋**`BUY_EVENT` reducer**（購入権1消費・イベント自体は獲得しない・複数回可・負債>0拒否・コスト軽減を受けない・負債コストは `p.debt+=`）＋`applyEventEffect` ディスパッチャ。`PLAYER_ACTIONS` に登録。
- **✅ EV1＝簡単イベント10種（完了・未push）**：delve/wedding/dominate/windfall/conquest/triumph（pending無し・カウンタは既存 `t.gainedThisTurn` を流用＝新カウンタ不要）＋salt_the_earth/banquet/advance/ritual（新pending4種＝4点セット完備：engine reducer[SALT_TRASH/BANQUET_GAIN/ADVANCE_TRASH/ADVANCE_GAIN/RITUAL_TRASH]＋PLAYER_ACTIONS＋CPU decidePending[終端保証]＋UI viewPendingModal）。CPU用 `firstGainable`/`plainCoin` を新設（engine の canGain と食い違わない獲得候補選び）。UI＝盤面イベント帯（買う横型・購入ボタン）＋拡大オーバーレイの種別/コスト表示。
- **検証**：`test/events.test.js` 新設**34件**（BUY_EVENT基盤・各イベント裁定・新pending・CPU終端）を package.json 登録（**35スイート目**）。`test/empires-ui.test.js` 60→**68件**（イベント帯＋pendingモーダル）。**全35スイート緑（exit 0）**。
- **注意**：イベントは「カード」でないのでコスト軽減（橋/街道）を受けず、購入時トリガー（商人ギルド/値切り屋/過払い）も発動しない。salt はサプライから直接廃棄（Tomb 発火のため `trashCard(state,pi,card)` を通す・保存則OK）。conquest/triumph の「今ターン獲得数」は `t.gainedThisTurn`（手番プレイヤーの獲得id列）で足りる。

### ✅ EV2＝重量イベント3種（完了・`52f7e7f`）
- **tax**：新 `state.pileDebt`（山上の負債トークン・公開・非カード＝保存則対象外・`maskStateFor` で残る）。準備で各サプライ山に負債1（**分割山は1山＝上段キーにのみ1**・混合山 castles/knights は numeric キーに1・非サプライ除外）。**自分の購入フェイズにサプライから獲得したとき、その山の負債を全部受け取る**（`triggerOnGain` で `gainWasBuyPhase`＆手番プレイヤー・**購入フェイズの非購入獲得＝delve/conquest の銀貨等でも取る**・アクションフェイズ/他人ターンは取らない）。Tax購入で山1つに+2（`tax_pile` pending）。UI に山負債バッジ `.pile-debt`（🟠）。**山キーの正規化 `pileKeyOf`**（分割山下段→上段・混合山→numericキー）を READ(triggerOnGain)/WRITE(TAX_PILE) の両方で通す。
- **donate**（負債8）：次の自分のターン開始時に「**最初に**」（持続効果より前）＝`resolveDurationStartEffects` 冒頭で `p.donateNext` を見て デッキ＋捨て札を全部手札に集約→`donate_trash` pending（任意枚数廃棄）→残りをシャッフルして5枚引く→**その後 `resolveDurationStartEffects` を再入**して通常の開始時効果を続行。
- **annex**（負債8）：`annex_keep` pending＝捨て札から最大5枚を残し、残りを山札に混ぜてシャッフル→公領獲得（捨て札/公領が空でも実行）。
- 新pending3種は4点セット完備。現行エラッタを研究エージェントで裏取り。`test/events.test.js` 34→69件。

### ✅ EV3＝CARD_SET昇格（完了・`39d3129`・`sw.js` v47）
- **CARD_SET `empires-events`**（帝国固定10＋`eventsFrom:'empires'` で2抽選・`kind:'standard'`）。ui(startLocal/restartLocal)・server(startGame)・NEW_GAME で events を landmarks と同型にサーバ権威で1度だけ確定・全席共有・再戦引き継ぎ。UI picker の「拡張」タイルに出る。
- **CPU `bestEventBuy`**：カード買いと比較してイベントを買う。**返すのは affordable かつ 負債0 のみ**＝BUY_EVENT が拒否しない＝無限ループ防止。負債イベントは購入後 debt>0 の返済分岐で1ターン1回に有界。dominate/conquest/wedding/annex/triumph/salt/donate/delve を評価（ritual/banquet/windfall/tax は買わない）。
- **敵対レビュー（多エージェント4観点＝ルール忠実性/保存則/CPU非ループ/UI・オンライン。各finding を node 再現）＝確定バグ3件→全修正・回帰テスト付き**：
  1. **[med] salt_the_earth × 城の混合山**＝プレースホルダ 'castles' を trash に積み `state.castles` を減らさず保存則違反（invariants soak が捕捉）→ 一番上の実カードを廃棄（`state.castles.shift()`＋`supply.castles` 同期）。
  2. **[low-fidelity] 分割山 × tax**＝上下段の両 supply キーに負債1ずつ乗る（公式は1山=1個）→ seeding で下段（`SPLIT_TOP[id]`）をスキップ＋`pileKeyOf` で下段→上段写像。
  3. **[low] TAX_PILE reducer が下段選択を正規化せず負債が孤児化**（2観点が独立検出）→ `pileKeyOf` で正規化＋UI/CPU 候補から分割山下段を除外。
- **CPUソーク**＝empires-events 40戦全完走・108回イベント購入（別レビューでは462戦 livelock 0・全8種発火）。invariants に empires-events soak（全13種ペア＋全部乗せ＋混成fuzz）で不変条件 5→6件。
- **イベント webp 13種を生成**（枠＋文字・絵は未回収＝暗い板／`build-landscape.js` の event スキン）。カード一覧に「イベント（帝国・横型）」群を追加。`test/empires-ui.test.js` 68→75件。**全35スイート緑（exit 0・整合性3148・不変条件6・events 69・帝国UI 75）**・`verify:e2e` 9/9（webp 346/0）。

### push＝完了（2026-07-11・`3cbe91b..d1a7b34`）＝本番反映を実機確認済み
- **GitHub Pages**：`sw.js` v47／`js/cards.js` に `empires-events`／`js/engine.js` に `BUY_EVENT`・`pileDebt`／イベント webp（`tax`/`donate`/`annex`/`delve`/`conquest` … すべて 200）を実機確認。Deploy ワークフロー success。
- **Render（オンライン）**：push で自動再デプロイ（サーバは `DOM.CARD_SETS`/`eventsForSet` から `empires-events` を自動受理＝サーバ側コード変更は §0-20 の startGame 配線ぶんが再デプロイで反映）。

### 【次にやること】次の拡張候補（着手前に `docs/adding-cards.md` 必読）
- **冒険のイベント20種**（横型枠は §0-18 対応済・カタログ研究は `docs/research/landscape_cards.md`・トークン中心/負債なし＝相性は別）。BUY_EVENT 基盤は帝国イベントで完成済みなので流用できる。
- **発売順の未着手拡張**（段階1すら未着手＝画像・カタログとも無し）：夜想曲/ルネサンス/移動動物園/同盟/略奪/旭日。

### 注意（次セッションが知らないと事故る・イベント固有）
- **イベントは「カード」でない**＝コスト軽減（橋/街道）を受けず、購入時トリガー（商人ギルド/値切り屋/過払い）も発動しない。**負債>0 の間はカードもイベントも購入不可**。返済は購入権を消費しない。横型は `DOM.LANDSCAPES` が正本（`DOM.CARDS` に無い＝整合性テストに混ざらない）。
- **徴税の山キーは `pileKeyOf` で正規化**（分割山下段→上段・混合山→numericキー）。**READ(triggerOnGain の tax ブロック)/WRITE(TAX_PILE reducer) の両方で通すこと**（片方だけだと負債が孤児化＝敵対レビューで踏んだ）。準備 seeding は分割山下段をスキップ（`!SPLIT_TOP[id]`）。
- **`state.pileDebt`／`p.donateNext` は非カード＝保存則 tally に混ぜない**（`state.pileVP` と同型）。`triggerOnGain` の tax は `if (state.pileDebt && …)`、donate は `if (p.donateNext)`、`pileDebtBadge` は `(state.pileDebt && …)` で旧スナップショット（pre-EV2）でも壊れないようガード済み。
- **donate は「次ターン開始時に最初に」**＝`resolveDurationStartEffects` 冒頭で処理し、`DONATE_TRASH` 末尾で同関数を再入して残りの開始時効果を続行する（持続ドローより donate が先＝公式）。
- **salt はサプライから直接廃棄**するが `trashCard(state,pi,card)` を通す（Tomb 発火・保存則OK）。**城の混合山を salt する場合は一番上の実カードを廃棄**（プレースホルダ 'castles' を積まない）。

---

## 0-19. 横型ランドスケープ 第1弾＝帝国ランドマーク21種の **engine 実装 完了**（2026-07-11・WIP・未push・`sw.js` v45）

**§0-18 の土台（横型枠パイプライン・研究・カタログ21枚）の上に engine を全実装**。新セット `empires-landmarks`（帝国固定10王国＋ランドマーク2枚抽選）を足し、**ランドマークが実プレイに影響する**（得点・獲得/廃棄トリガーが変わる）。**カタログ/webp変更なし**（`DOM.LANDSCAPES` は §0-18 で追加済み）。**残タスクは webp 回収のみ**（engine は完成・絵が無くても盤面は名前＋説明文で成立）。

### 実装（段0〜5）
- **段0＝共通基盤**：`state.landmarks=[id]`（対局中不変・公開・maskで残る）／`state.landmarkVP={id:個数}`（ランドマーク上の**有限リザーブ**6×人数・非カード＝保存則tally対象外）／`state.landmarkStash`（水道橋/汚された神殿の一時VP）／`state.obeliskPile`。`createInitialState` で準備（6×人数のリザーブ・水道橋=銀貨/金貨山に`pileVP`8ずつ・汚された神殿=集合以外の素のアクション山に2ずつ・オベリスク=素のアクション山を無作為選択）。ヘルパ `hasLandmark`／`takeLandmarkVP(state,pi,id,per)`。`maskStateFor` は clone でこれらを公開のまま残す（変更不要）。
- **段1＝得点計算専用11種**：`landmarkScoreForCards(state, cards, seat)` を新設し `scoreGame` から呼ぶ（**vpOf は state を持たないので scoreGame 側に加点**）。bandit_fort/fountain/keep/museum/orchard/palace/wall/wolf_den/tower/triumphal_arch/obelisk。**得点は負になり得る＝下限クランプ禁止**。keep=全員のデッキを同時比較（同数は各自+5）。tower=`isFromEmptySupplyPile`（分割山は上下とも空・混合山=廃墟/騎士/城は集約キー・非サプライ除外・勝利点除外）。obelisk=**分割山を選んだら両半分を数える**（敵対レビュー修正）。
- **段2＝簡単トリガー8種**：tomb（`trashCard` の push 直後・廃棄した本人・支配退避では非発火・城塞が戻っても発火）／battlefield・labyrinth・basilica・colonnade・aqueduct・defiled_shrine（`triggerOnGain` 末尾のランドマークブロック）／baths（`endBuyTail` 冒頭・1ターン1回）。**購入フェイズ判定は「獲得時点」のフェイズ（`gainWasBuyPhase`）**＝ヴィラの phase 変更に負けない（敵対レビュー修正）。
- **段3＝闘技場 arena（新pending・4点セット）**：`maybeArena`（END_ACTION_PHASE＝購入フェイズ開始・`t.arenaFired` で1ターン1回・ただし**ヴィラで購入→アクションに戻ると再武装**＝敵対レビュー修正）＋`ARENA_RESOLVE`（手札アクション1枚を**捨て札へ**[廃棄でない]→+2VP・任意）。CPU=購入フェイズの手札アクション（財宝兼を避け）を捨てる。UI=modalSingleHand。
- **段4＝峠 mountain_pass（新pending・逐次入札）**：最初の属州獲得で `state.mountainPassArmed`→endBuyTail で `startMountainPassBid`（獲得者の左隣から始め獲得者が最後・最大40負債）。`MOUNTAIN_PASS_BID` が逐次に入札を集約し、最高額（同額は先着）が **+8VP＋同額の負債**。完了後 `endBuyTailTravellers` へ合流（baths/hermit の二重発火なし）。1ゲーム1回（`mountainPassDone`）。CPU=手番数で価値を見て正直入札（超えないなら0）。UI=modalAmount(0..40)。
- **段5＝セット選択**：`DOM.landmarksForSet(setId)`／`DOM.pickLandmarks(n,pool)` を cards.js に新設。CARD_SET に **`empires-landmarks`**（`kind:'standard'`・`landmarksFrom:'empires'`）。NEW_GAME→`opts.landmarks`→`createInitialState`。ui.js の startLocal/restartLocal と server の startGame で landmarks をサーバ権威/ローカルで1度だけ確定して共有。**盤面にランドマーク帯**（名前・残VP・溜VP・オベリスク対象。`viewBoard` の `landscapeBlock`＝既存の supply-section/mats クラス再利用）。

### 敵対レビュー（多エージェント6次元→各finding node再現で確定）＝**確定6件（偽陽性0）→全修正・回帰テスト付き**
- **[medium] オベリスク×分割山の得点漏れ**（2次元で二重報告）：obeliskPile が settlers/catapult（分割山上段）のとき、同一山の相方（bustling_village/rocks）由来カードを数え落とし最終スコアが過小＝**勝者が変わり得る**。→ `landmarkScoreForCards` の obelisk 句で `SPLIT_BOTTOM[op]||SPLIT_TOP[op]` の相方も数える。
- **[medium] ヴィラ獲得のフェイズ変更が公会堂/列柱を握りつぶす**：購入フェイズにヴィラを獲得すると villa の on-gain が phase を 'buy'→'action' に変え、その後のランドマークブロックの `inBuy` 判定が偽になり **basilica/colonnade/汚された神殿(呪い) が発火しない**（過小得点）。→ triggerOnGain 冒頭で `gainWasBuyPhase` を捕まえ、それを使う。
- **[low] 闘技場がヴィラ再入場で再発火しない**：公式は購入フェイズ開始のたび。→ villa の action 復帰で `arenaFired=false` に再武装。
- **[low] CPU の tower 近似が分割山/混合山で engine 実得点と乖離**／**[low] CPU が keep を完全省略**：→ CPU `winsIfEnds` を近似 `landmarkVpApprox` から **engine 公開の `landmarkScoreForCards` 呼び出し**に置換（オベリスク分割山・塔の空山写像・砦の全員比較を engine と完全一致で見積る）。`DOM.engine.landmarkScoreForCards` を公開。

### 検証
- `test/landmarks.test.js` **新設80件**（共通基盤・得点11種[負得点含む]・トリガー8種・闘技場/峠の全裁定・セット選択・後方互換[landmarksフィールド無しの旧スナップショット]・敵対レビュー回帰6件・CPUソーク[膠着0/例外0・峠pending到達・闘技場の強制終端]）を package.json 登録（**34スイート目**）。`test/empires-ui.test.js` を **45→54件**（ランドマーク盤面帯・闘技場/峠モーダル・オベリスク表示・empires-landmarks picker）。
- `invariants` の出荷セット検証に **empires-landmarks（landmarks付き）** を追加＋新節「ランドマーク（全21種ペア＋全プール混成にランドマーク付与）」＝**landmarkVP/pileVP/landmarkStash は非カード＝保存則の tally に混ざらない**ことと、arena/mountain_pass の新pendingが CPU で終端することを確認（不変条件 4→**5件**）。
- **npm test 全34スイート緑（exit 0・整合性3147・CPU序列 100/64/95 維持）**／`verify:e2e` 9/9（webp 346/0・例外なし）。

### ✅ 絵(webp)回収＋絵表示 完了（2026-07-11・追記）→ 次は push（ユーザー確認）
- **絵の回収**：ユーザーが `images/` に21枚投入 → 3グループ×7をタイムスタンプで分割し、多エージェント視覚判別（各グループ別エージェント・全21候補から bijection）で**全21種を1対1確定（全 high confidence・`DOM.LANDMARKS_EMPIRES` と完全一致）** → `asset/art/<id>.png` 回収 → `node tools/build-landscape.js`（`CARDS_ONLY` で21種）で webp 生成。枠＋絵＋テキスト正常（峠/水道橋/狼の巣を目視確認）。
- **絵の表示**：`js/ui.js` に横型専用の `landmarkMini`／拡大 `viewLandmarkZoom`／`openLandmarkZoom` を新設（横型は `DOM.CARDS` に無く `cardEl`/`viewSheet` が使えないので別経路）。**盤面ランドマーク帯にアートのサムネ＋タップ拡大オーバーレイ**（`.landmark-thumb`）、**カード一覧に「ランドマーク（帝国・横型）」群**（全21枚）。render にオーバーレイ hook＋scroll-lock。`sw.js` v45→**v46**。
- **検証**：`test/empires-ui.test.js` を54→**60件**（サムネ／拡大／一覧の3経路）。**全34スイート緑（exit 0）**・`verify:e2e` 9/9（webp 346/0）・`verify:visual` 全幅はみ出し0・実ブラウザで拡大の `aqueduct.webp` が naturalWidth=1152 でロード確認・webp404ゼロ。一覧サムネは `loading=lazy`（遅延読込＝正常）。
- **push＝完了（2026-07-11・`13a4401`）**：`cb89e2a..13a4401` を push（枠パイプライン＝`c10cf47`／カタログ＝`4ef4ca8`／engine＝`c9f4863`／webp＋絵表示＝`13a4401` を一括）。**GitHub Pages デプロイ success・本番 `sw.js` v46・`aqueduct/wolf_den/mountain_pass/obelisk` の webp すべて 200** を実機確認。Render は push で自動再デプロイ（サーバは `DOM.CARD_SETS`/`landmarksForSet` から empires-landmarks を自動受理＝サーバ側変更は §0-19 の startGame 配線ぶんが再デプロイで反映）。

### （旧）【次にやること】絵(webp)の回収 → push（ユーザー確認）
1. **絵**：ユーザーがチャッピーでランドマーク21枚生成中（前セッションで3バッチの指示文を出した）。`C:\Users\b1242\Downloads` に来たら判別して `asset/art/<id>.png` に回収 → **`node tools/build-landscape.js`（縦型 build-cards.js とは別スクリプト）** で webp 生成 → 一覧/盤面の画像表示は今は名前＋説明文なので webp が無くても動くが、あれば `carddata`/`cards.html`/`ui.js` の別経路（`DOM.LANDSCAPES` 参照）で出す。`sw.js` は既に v45。※現時点で Downloads にあるのは 7/5 の旧・帝国縦カードバッチのみ＝ランドマークの絵は未着。
2. **push**：webp 回収後（or engine だけでも）**ユーザー確認の上で** `git push`。empires-landmarks が本番 Pages/Render に出る（サーバは `DOM.CARD_SETS`/`landmarksForSet` から自動で受理）。

### 注意（次セッションが知らないと事故る）
- **ランドマークのVPは3系統**：`state.landmarkVP`（ランドマーク上の有限リザーブ6×人数＝闘技場/公会堂/浴場/戦場/列柱/迷宮）／`state.pileVP`（山の上＝集合と共用・水道橋は銀貨/金貨山、汚された神殿は各アクション山）／`state.landmarkStash`（山→ランドマークへ移した一時VP＝水道橋/汚された神殿）。最終得点になるのは `p.vpTokens` に移った分だけ。
- **得点は負になり得る**（山賊の砦/壁/狼の巣）＝`landmarkScoreForCards` で下限クランプ禁止。**新しいランドマーク得点を足すなら `landmarkScoreForCards` に書けば CPU の終局読みも自動で一致**（CPU は `DOM.engine.landmarkScoreForCards` を呼ぶ）。
- **購入フェイズ依存のトリガー（basilica/colonnade/汚された神殿の呪い）は `gainWasBuyPhase`（獲得時点のフェイズ）を使う**＝ヴィラ等が途中で phase を変えても正しい。新しい「購入フェイズ限定」トリガーを足すときも同様に。
- **新pendingは4点セット必須**（engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal）＋終端保証。arena/mountain_pass が該当済み。
- **横型カードは `DOM.CARDS` に無い**ので整合性テスト（GAIN_ORDER網羅/POOL所属/3山終了/闇市場）に一切混ざらない。表示は `DOM.LANDSCAPES` を見る別経路。
- **イベント13種は未実装**（`BUY_EVENT`＋CPU購入AI＋負債経済連動が要る＝別の大仕事）。ランドマークだけ先行。

---

## 0-18. 横型ランドスケープ（イベント／ランドマーク）**着手：横型枠パイプライン＋研究＋カタログ21枚**（2026-07-10・進行中）

**第1弾＝帝国ランドマーク21種**（ユーザー決定）。ランドマークは「買わない」ので `BUY_EVENT` もCPUの購入判断も要らず、得点ルールを変えるだけ＝出荷単位として最もリスクが低い。

### 完了（`c10cf47` ＋本コミット）
- **横型枠パイプライン `tools/build-landscape.js`（新規）**。横型 master は存在しなかったが、**縦型 master が「丸角矩形＋一定断面の金ベベル」だけで出来ている**ことを利用し、**金レールの断面（`x=41..63, y=700` の23px 1次元プロファイル）を採取して任意サイズの丸角矩形／円に同じレールを描く**方式にした＝**枠のAI画像を新規生成せずに縦カードと同じ質感が出る**。
  - 出力 1152×768。イベント＝茶褐色＋コインメダル／ランドマーク＝深い青緑（コイン欄なし）。負債の六角トークン・2桁コスト・禁則処理（句読点を行頭に置かない）に対応。絵の窓は 620×543（縦横比1.14＝縦カードの窓と同じ＝既存の4:3素材の切れ方が揃う）。絵が無ければ暗い地の板を敷く。
  - `LANDSCAPE_PREVIEW=1` でサンプル3枚だけ描画（枠の目視確認）。`CARDS_ONLY` / `CARDS_OUT` は縦型と同じ。
- **研究（多エージェント・1枚ずつ敵対検証）** → `docs/research/landscape_cards.md`（54枚の公式データ＋機構分類＋重さティア）／`docs/research/landscape_gaps.md`（未確定事項の回答集）。**ランドマークは20ではなく21種**（依頼側の誤り）。和名2件訂正＝**basilica＝公会堂**（「バシリカ」は読み仮名）／**keep＝砦**（「天守」は誤り。`bandit_fort`＝山賊の砦 と別カード）。
- **`js/cards.js` に `DOM.LANDSCAPES`（帝国ランドマーク21）＋`DOM.LANDMARKS_EMPIRES`＋`DOM.GATHERING_CARDS`＋`DOM.isLandscape`**。**`DOM.CARDS` には入れない**＝GAIN_ORDER網羅・POOL所属・3山終了・闇市場デッキ に一切混ざらない（整合性3146・invariants ともに不変を確認）。

### 確定した公式ルール（`docs/research/landscape_gaps.md` が正本）
- **1対局に使う横型は 0〜2枚**（Events と Landmarks の**合算**で最大2）。**王国山は常に10のまま**＝`emptyPileCount`／3山終了に影響しない。若き魔女のBaneにはできない。
- **負債トークンがある間は カードもイベントも購入できない**（RGG 帝国ルールブック逐語）。イベント購入は**購入権を1消費**、同じイベントを1ターンに複数回買える、**返済は購入権を消費しない**。
- **Windfall（意外な授かり物・帝国イベント第13）＝$5「山札と捨て札が**両方**空なら金貨3枚を獲得」**（手札・場は判定に含めない）。研究データに欠落していた1枚。
- **Tomb の +1VP は「廃棄した本人」**に入る（相手ターンの詐欺師でも・塩まきのサプライ廃棄でも・複数枚は1枚ごと）。→ `trashCard`(engine.js) 本体の `state.trash.push` 直後にフック（城塞が手札に戻る場合も廃棄自体は起きているので発火）。
- **6VP型ランドマーク6種**（闘技場/公会堂/浴場/戦場/列柱/迷宮）＝準備で **6×人数**。**水道橋**＝銀貨に8・金貨に8（人数非依存）。**汚された神殿**＝**集合を持たない**各アクション山に2ずつ。ランドマーク上のVPが尽きたら以後そのランドマークでは得点できない。
- **峠**＝誰かが最初に属州を獲得した**そのターンの後**、各プレイヤーが1回ずつ最大40負債で入札（獲得者が最後）。最高額が +8VP＋その額の負債。

### 【次にやること】engine 実装（この順）
1. **共通基盤**：`state.landmarks = [id...]`（公開・maskで残す）／新スカラー `state.landmarkVP = {id:個数}`（ランドマーク上の有限リザーブ・**非カード＝保存則tally対象外**）／`state.landmarkStash = {aqueduct:n, defiled_shrine:n}`（山から移したVPの一時置き）／`state.obeliskPile`。`createInitialState` で準備処理（`pickBane` と同型）。**山に置くVPは既存 `state.pileVP` を再利用**（集合機構と同じマップ）。
2. **得点専用11種**（`museum`/`fountain`/`palace`/`bandit_fort`/`wall`/`wolf_den`/`orchard`/`triumphal_arch`/`keep`/`tower`/`obelisk`）＝`scoreGame(state)` に**横断採点ブロック**を追加（`vpOf(p)` は `state` を持たないので署名を変えず scoreGame 側に足す）。**得点が負になり得る**ので下限クランプ禁止。`keep` は全員のデッキを同時に見る。`tower`/`obelisk` は「カード→由来山」の写像が要る（分割山は両名・混合山は length 判定）。CPU `vpOfPlayer` にも同じ加点。
3. **トリガー型10種**：`tomb`(trashCard)／`battlefield`(triggerOnGain・勝利点)／`labyrinth`(そのターン2枚目の獲得)／`baths`(END_TURN・獲得0)／`basilica`(購入フェイズの獲得＋残コイン≥2)／`colonnade`(購入フェイズのアクション獲得＋場に同名)／`aqueduct`・`defiled_shrine`(pileVP→landmarkStash→vpTokens)／`arena`(購入フェイズ開始の任意捨て＝**新pending＝4点セット必須**)／`mountain_pass`(**新pending＝逐次入札**・CPU入札評価・UI入札モーダル)。
4. **セット選択**：`empires` の固定10王国に**ランドマーク2枚を付ける新セット**か、セット選択UIの**トグル**か（設計判断・未決）。**新しい CARD_SET を足したら UI の picker にも出す**（`test/ui.test.js` が守る）。
5. **テスト**：`test/landmarks.test.js` 新設＋`invariants` に「ランドマークVPは非カード」「得点が負になり得る」検証＋CPUソーク。
6. **絵**：ユーザーがチャッピーで21枚生成中（3バッチ）。`Downloads` から判別して `asset/art/<id>.png` に回収 → `node tools/build-landscape.js` で webp 生成 → `sw.js` VERSION を上げる。

### 注意（次セッションが知らないと事故る）
- **2022エラッタで `basilica`/`colonnade`/`defiled_shrine`/`tax` は「購入したとき」→「購入フェイズ中に獲得したとき」に変わっている**。カタログ文は現行を採用済み。研究の2文書で一部この点の記述が食い違うので、**実装時に `docs/research/landscape_cards.md` §6-4 の現行文を正とする**。
- **ランドマークのVPトークンは供給から取る有限リザーブ**＝`state.pileVP`（集合＝山の上・実質無制限）とは**別物**。水道橋/汚された神殿だけが両方を使う（山＝pileVP、ランドマーク上＝landmarkStash）。
- 横型カードは `DOM.CARDS` に無いので、`carddata.js` の `buildDisplay`／`cards.html` の一覧／`ui.js` の盤面表示は**別経路**が要る（`DOM.LANDSCAPES` を見る）。`verify:e2e` の webp404 検査は「ページが要求した webp」しか見ないので、一覧に出すまでは影響しない。

---

## 0-17. E8＝命令（Command）の忠実化＝**「命令がプレイした札は動かない」**（2026-07-10・**push済（`52bba46`）**）

**⚠️ 前セッションの E8 計画は誤りだった（廃止済みの2016年ルールを実装しようとしていた）。実装前に多エージェント研究WFで一次資料を取り直し、方針を反転させた。**

### 何が誤りだったか
- §0-16・旧KICKOFF は「**公式2022では命令カードが身代わりに動く**（大君主×陣地＝大君主が脇→大君主の山へ／大君主×農家の市場＝大君主自身を廃棄／船長×鉱山の村＝船長を廃棄して+$2／王子×島＝王子が島マットへ）」と書いていた。
- これは **RGG が今もホストしている 2016年版 Empires ルールブックPDF の記述**（"Overlord also gets the chosen card's cost, name, and types, until it leaves play. If you play Overlord as a card that moves itself somewhere ... Overlord will do that"）。**2019年エラッタで廃止済み**。
- **現行（2019エラッタ・2021以降の刷）＝逆**。Donald X. Vaccarino の公式エラッタ：shapeshifter 4枚（band_of_misfits / overlord / inheritance / prince）は「**play a card instead of becoming the card**」に変更。カード文も `"... leaving it there."` になった。
- **RGG Dark Ages(2022) ルールブックPDF の逐語**（検証エージェントが pdftotext で単語一致を確認）：
  > "Command is a type that appears on cards like this; it has no meaning beyond stopping these cards from playing each other. ... **The played Action card stays in the Supply; if an effect tries to move it, such as Death Cart trying to trash itself, it will fail to move it.** If the card checks to see if it was trashed, like Death Cart does, that part will fail, but if it does not, like Acting Troupe, **the rest of the effect will still happen**. Since the played card is not in play, "while this is in play" abilities (such as Highway's) will not do anything."
- **決定打**：我々のカタログ文は既に現行エラッタ側だった（大君主「そのカードはサプライに残す」／船長・王子「動かさずに使用する」）。旧ルールを実装すると自分のカード画像と矛盾する。→ ユーザー確認の上で **現行エラッタ準拠** に決定。

### 実装（＝現行ルール。旧 no-op はほぼ正しかったが3種類の取りこぼしがあった）
- **`state._cmd = { player, id, as }`** を、命令カードが代理で `applyEffect(card)` を呼ぶ間だけ立てる（新 `playAsCommand`。`finally` で削除＝state に残らない）。
  - **`as` で「今まさに命令が代理でプレイしているカード」を識別する**のが要。`herald`（山札上の本物のアクションを同期プレイ）/`vassal`/`crystal_ball`/`berserker`(on-gain) のように **applyEffect の内側で別の本物のカードをプレイする経路**があり、そちらの「これ(this)」は本物を指すため。
- **`playedByCommand(state,pi,cardId)`** ／ **`takeSelf(state,pi,cardId)`**（命令経由なら必ず `null`＝lose track。そうでなければ `removeOne(p.inPlay, cardId)`）。自己移動サイトを全部これに置換＝
  `feast` / `island` / `treasure_map` / `pillage` / `farmers_market` / `madman` / `encampmentSetAside` / `putOnTavern`（Reserve 全部）。
- **後から解決する選択待ちは pending に `self`（＝「これ」を廃棄できるか）を載せる**：`death_cart` / `raze`。`mining_village` と `encampment` は命令経由だと**選択自体が無意味なので pending を立てない**（死に選択肢を出さない）。旧 `pd.fromCommand` は廃止（大君主/はみだし者にしか付いておらず船長/王子で漏れていた）。
- **OVERLORD_PLAY / BAND_OF_MISFITS_PLAY / CAPTAIN_PLAY / PRINCE_PLAY / replayCommandAs** をすべて `playAsCommand` 経由に統一。
- **`DOM.engine.pendingSelf(state, pd, cardId)` を新設・公開**＝engine拒否・CPU非提案・UI表示が**同じ述語**を見る。後方互換（下記レビュー確定バグ）も内包。

### これで直った実バグ（現行ルール基準・すべて回帰テスト付き）
1. **【MED・出荷到達】「これ」が場の *同名の本物のコピー* を巻き込む**：船長/王子で `mining_village` を使うと、**場に出してある本物の鉱山の村が廃棄され +$2 が出た**（`random-promo` は captain と mining_village が同居）。同型で `raze`（場の本物の倒壊を廃棄）・`death_cart`（fromCommand が船長/王子に無い）・`encampment` も。
2. **【MED】倒壊の死に選択肢**：命令経由の `raze` で「これを廃棄」を選ぶと engine が state 不変で拒否＝pending が閉じない。UI にボタンが出ており、CPU も条件次第で提案し得た。
3. **【LOW・忠実性】取りこぼし**：`farmers_market` の「山上VPを取る」は廃棄に条件づかない（取れる）。`feast` の獲得も条件づかない（獲得できる）。`island` の「手札から1枚を島マットへ」は場所が明示されているので起きる。`death_cart` の「手札のアクションを廃棄して+$5」も起きる。← 現実装はいずれも正しく動くようになった。
- **`empires` 固定セットの「大君主と自己移動札を同居させない」制約の根拠（永久VPループ）は旧ルール前提の誤診だった**。現行ルールでは大君主×農家の市場は「山上VPを取るが何も廃棄しない」＝強コンボだが合法・非ループ。固定セットは今回いじっていない（変更する必要は無い）。

### 敵対レビュー（多エージェント6次元→各findingを node 再現で確定）＝**確定2件（偽陽性0・5次元クリーン）→修正済み**
- **【MED】`pending.self` を新設したことによる後方互換バグ（オンライン永続化）**：`server/gameServer.js` は `state.pending` を含む全 state をそのまま Upstash に保存し、再デプロイ後 `restoreRoom` が**無変換で復元**する。v43 で作られた pending には `self` が無く、v44 が読むと `undefined`（falsy）＝「これを廃棄できない」に化ける。
  - `raze`：**手札0枚の廃棄 pending が永久に閉じない**（CPU が無効 action を再送し続ける全体ソフトロック／人間は提出手段ゼロ）。
  - `death_cart`：「自身を廃棄して+$5」が黙って消える。
  - → **`pendingSelf` が旧来の意味へフォールバック**（`self` 欠落なら「場に本体があれば廃棄可」＋v43 の `fromCommand` も尊重）。engine/CPU/UI が同じ述語を見るので3面同時に直る。回帰テストを `adventures.test.js` に追加（JSON round-trip で v43 の pending 形状を再現）。
- 他5次元（`_cmd` のスコープ・自己移動サイト・CPU ソーク・ルール忠実性・拡張間相互作用）は**クリーン**。

### 意図的な据え置き（現行ルールとの差。E9 候補）
- **大君主/はみだし者は持続カードもプレイできる**（現行カード文に "non-Duration" が無い）。実装は除外したまま。公式は「プレイした札が場を離れたであろうターンの片付けまで命令カードを場に残す」＝`armDuration(state,pi,命令id,{type:対象id})` で表現できる見込み。
- **命令は混合山（騎士/城）の一番上もプレイできる**が除外したまま。
- **チャンピオンの+1アクション／教師の山トークン／浮浪児のトラップ**は命令がプレイしたアクションで発火しない（`PLAY_ACTION` のみ＝既存の簡略化）。
- **宝の地図**を命令でプレイした場合、手札のコピーも廃棄しない（公式裁定が取れず、廃棄しない側に倒した）。

### 検証
- `test/empires.test.js` **269件**（E8＝大君主×農家の市場/陣地/祝宴/略奪/倒壊/ワイン商/島/宝の地図・玉座×大君主×祝宴・大君主×伝令官の識別）／`test/promo2.test.js` **165件**（船長×鉱山の村の誤爆回帰・王子×島/鼠取り/鉱山の村/狂人）／`test/darkages.test.js` **87件**（はみだし者×死の荷車の self=false・手札ルート・CPU終端・×隠遁者）／`test/adventures.test.js` **59件**（`pendingSelf` の後方互換3件）。
- `invariants` の敵対王国に **+2種**（命令4枚×自己移動札×Reserve×玉座/王の宮廷）。**npm test 全33スイート緑（exit 0・整合性3146不変・CPU序列 100/64/95 維持）**／`verify:e2e` 9/9（webp 346枚）／`verify:visual` 全幅はみ出し0。
- カタログ：大君主の表示文を現行文言（「サプライにあるコスト5以下の、命令ではないアクションカード1枚を、サプライに残したまま使用する。」）に修正し **`overlord.webp` を再生成**（このPCのみ）。`sw.js` v43→**v44**。

### push＝完了（2026-07-10・`52bba46`）＝本番反映を確認済み
- **GitHub Pages**：`sw.js` v44 を配信／`js/engine.js` に `pendingSelf`・`playAsCommand`／`js/cpu.js`・`js/ui.js` が `pendingSelf` を参照／`js/cards.js` の大君主が現行文言／`asset/cards/overlord.webp` 200・ローカルとバイト一致（148292）。
- **Render（オンライン）**：WS `wss://dominion-server-1hc9.onrender.com/ws`（`/ws` パス＋Origin 必須・メッセージは `t:` キー）で `setConfig kingdomSet=darkages` を受理し、対戦開始で 王国にはみだし者・廃墟の山10枚・避難所ON・相手手札マスク すべて正常。`GET /status` ＝ `{"persist":true,...}`＝**永続化は本当に有効**（＝敵対レビューが見つけた「復元スナップショットの `pd.self` 欠落」は実在の到達経路だった）。
  ※サーバは `js/engine.js` を require するので、Render の再デプロイで E8 がオンラインにも効く（サーバ側コードの変更は無し）。

### 【次にやること】横型ランドスケープ or E9
- 候補：**横型ランドスケープ**（帝国イベント13＋ランドマーク20／冒険イベント20＝縦枠パイプライン未対応で段階1すら未着手）／**E9＝命令が持続カードをプレイできるようにする**（大君主/はみだし者。公式カード文に "non-Duration" が無い。`armDuration(state,pi,命令id,{type:対象id})` で「対象が場を離れたであろう手番の片付けまで命令カードを場に残す」を表現できる見込み）／CPU購入AIの拡張別チューニング。

---

## 0-16. 段階2＝帝国（Empires）E7＝Phase E＝CARD_SET昇格 **完了・push済（`15b605e`）**（2026-07-10）

**帝国が実プレイ可能に**（`empires` 固定10種＋`random-empires`）。`sw.js` v42→**v43**。**カタログ変更なし・webp は `castles.webp` 1枚を新規生成**（E5で作り忘れ＝下記⑤）。これで **縦型カードの実プレイ化は帝国まで完了**（残りは横型ランドスケープのみ＝§5）。

### 昇格の配線
- **`DOM.KINGDOM_EMPIRES` 固定10種（自作 showcase・公式の帝国専用10種は無い）**＝`engineer, overlord, settlers, catapult, castles, temple, villa, forum, wild_hunt, crown`。新機構6系統を全部味わえる＝負債(engineer/overlord)・集合＝山上VP(temple/wild_hunt)・分割山2組(settlers/騒がしい村・catapult/石)・城の混合山(castles)・命令(overlord)＋冠・villa。アタック＝投石機。on-gain＝公共広場(+1購入)/神殿(山上VP強奪)/石(銀貨)/城。
- **【重要】固定セットは 大君主 と「自己移動する札」（農家の市場＝自己廃棄・陣地＝自己脇置き）を意図的に同居させない**。命令の「自身が動く」clause 未実装（下記E8）のため、同居すると *大君主→農家の市場で山上4VPを回収しても何も失わない* 永久VPループになる。`random-empires` では同居し得る（保存則OK・非ループ・許容簡略化）。
- `DOM.CARD_SETS` に `empires`（kind:standard）と `random-empires` を追加。`initSupply`/`createInitialState` は既存の分割山補完・城の人数別セットアップで自動対応。サーバは `DOM.CARD_SETS` から許可IDを導出するのでオンラインも自動対応。

### 【実バグ修正①】大君主は負債コストのカードをプレイできない（公式）＝E7で初めて同居
- 研究WFで RGG 公式 Empires ルールブック(2022) 本文を確認：**コストは coin/負債/ポーション を成分ごとに比較**し、`$0+負債4` は "up to $5" では**ない**（`Examples: [$0+4D] is not "up to [$5]."`）。Workshop で技術者を獲得できないのと同じ理屈。
- engine に **`costIsPlainCoin(id)`（負債もポーション費用も持たない）** を新設し、**`overlordTargets` / `captainTargets` / `bandOfMisfitsTargets` / `princeEligible`** の4述語に適用（従来は `!potion` だけ）。CPU `bestPrinceTarget` も同条件に。
- 王子×技術者は**闇市場経由で到達可能**（random-promo）だったので、これも同時に塞がった。

### 【実バグ修正②③】UI：出荷済みの固定セットが画面から選べなかった（本番の実害）
- **② `3e0794f`（6/14「セット選択を4分類に整理」）以降、`kind:'standard'` の拡張セット8つ（海辺/錬金術/繁栄/収穫祭/ギルド/異郷/暗黒時代/冒険）が picker のどの分類にも出ておらず、ローカル/オンラインとも選択不能だった**（ランダム系でしか遊べない状態）。→ **「拡張」分類を新設**（`kind:'standard'` の basic/intrigue 以外をタイル表示・各セットに一行 `desc` を追加）。帝国セットもここに出る。
- **③「ランダム」分類の抽選元セグメントが 13→14個で `overflow:hidden` に切られ、320px 幅では「暗黒時代から」以降（冒険/帝国/初版など）が画面外で押せなかった**（実ブラウザで scrollWidth 405 vs clientWidth 248 を計測）。→ `.seg.seg-wrap`（折り返すチップ）を新設。`.seg` が後方定義なので**特異度を上げないと効かない**点に注意。
- 恒久回帰：`test/ui.test.js` に **「全 CARD_SETS がセット選択画面から選べる」**（各 id を描画して選択済み表示が出るか）を追加。

### 【実バグ修正④】CPU：混合山のコストを静的値で見ていた／終局判定の得点漏れ
- `kingdomAffordable`（弱CPUの購入候補）と `chooseBuyStrong` の「勝って終われる購入」ループが **`C()[id].cost` の静的コスト**で判定していた。城の山 `castles` はプレースホルダが$3（＝最安）なので、一番上が$10の王城でも「$3で買える」と誤認→engine が拒否→買いを空振り（最終ガードのおかげで無限ループにはならないが弱くなる）。→ **engine の実コスト `cardCost`＋`splitBlocked`＋`canBuyCard`** で判定し、混合山は新ヘルパ **`mixedTop`（一番上の実カード）**で得点も評価。
- CPU `allCards` に **`princes`/`tavern`/`archives`** を追加（engine.allCards と同じゾーン）＋`vpOfPlayer` に **城（粗末な城=城の枚数×1／王城=×2）・封土・遠隔地** を追加＝hard CPU の終局読みが正しくなる。

### 【実バグ修正⑤】`asset/cards/castles.webp` が存在しなかった（E5の作り忘れ）
- 城の混合山プレースホルダ `DOM.CARDS.castles` を E5 で足したのに webp を生成していなかった＝**カード一覧と（山が空のときの）盤面で 404**。`npm run verify:e2e` が検出（`ok=345 bad=1`）。→ `asset/art/castles.png`（王城の絵を流用）＋`CARDS_ONLY=castles node tools/build-cards.js` で生成。**この webp 再生成はこのPCでしかできない**（入力は gitignore）。

### 敵対レビュー（多エージェント6次元→node再現検証）＝**確定1件（偽陽性0）→修正**
- **[LOW] `winsIfEnds` の hypo デッキに酒場マットが無く、自分の遠隔地(4点)だけ消える**（相手は実オブジェクトで4点計上＝非対称に自己過小評価→勝てる終局購入を見送る）。今回 `vpOfPlayer` に遠隔地句を足したことで生じた新規バグ。→ **hypo に tavern を入れ直すと `allCards` で二重計上になる**（庭園/品評会/絹の道/城が狂う）ので、`winsIfEnds` で `+4×マット上の遠隔地枚数` を明示加算。回帰テストを `adventures.test.js` に追加（fix を外すと落ちることを確認済み）。
- 他5次元（配線／コスト述語の回帰／CPU／UI／固定セットの相互作用／オンライン・マスク）は**クリーン**。

### 検証
- `test/empires.test.js` **226件**（E7の配線・大君主×負債・出荷セットCPUソーク36戦）／`test/empires-ui.test.js` **45件 新設**（全pendingモーダル＋負債バッジ＋山上VP＋城の混合山＋分割山＋「拡張」タイル）。`invariants` の出荷セット検証に `empires`/`random-empires` 追加。
- 追加ソーク（empires / random-empires・2〜4人・全難易度 60戦）＝完走60・膠着0・例外0・保存則0（平均総手番60.3＝負債で膠着しない）。
- **npm test 全33スイート緑（exit 0・整合性3146・CPU序列 100/64/95 維持）**／`verify:visual` 全幅はみ出し0／`verify:e2e` 9/9（webp 346枚すべて読める）／実ブラウザで「押せないボタン」0。

### push＝完了（2026-07-10・`15b605e`）＝本番反映を実機確認済み
- GitHub Pages：`sw.js` v43／`js/cards.js` に `KINGDOM_EMPIRES`・`random-empires`／`js/ui.js` に「拡張」分類／`asset/cards/castles.webp` が 200。
- Render（オンライン）：WS `/ws`（Origin 必須）で `setConfig kingdomSet=empires` を受理し、対戦開始で 城8枚（先頭=粗末な城）・分割山下段（騒がしい村/石 各5）・`p.debt`・`state.pileVP`・相手手札マスク がすべて正常。
  ※ **サーバは `DOM.CARD_SETS` から許可IDを導出**するので、新セットを足せば自動で受理される（サーバ側の変更不要）。Render の反映は push から数分かかる。

### 【次にやること】E8 → 完了（§0-17）
- ~~E8＝命令(Command)の「自身が動く」clause~~ → **この節に書いていた「公式2022では命令カードが身代わりに動く」は 2016年初版ルールで、2019エラッタで廃止済みだった**。正しくは「命令がプレイした札は動かない／命令カード自身も動かない」。§0-17 で現行ルールに合わせて実装済み。
- 公式では **大君主/はみだし者は持続カードもプレイできる**（命令カードが場に残る）＝現実装は除外（船長/王子は "non-Duration" が card text にあるので正しく除外）。E9 候補。

---

## 0-15. 段階2＝帝国（Empires）Batch E6＝命令（overlord/crown）**完了**（2026-07-10・WIP・未push）

**Batch E6 完了＝帝国の新機構6系統すべて実装済み**。設計正本＝`docs/research/empires_rules.md` §1-5。`sw.js` v41→**v42**。**カタログ/webp変更なし**。**帝国はまだ CARD_SET 未昇格＝本番挙動は不変**。次は **E7＝Phase E＝CARD_SET昇格**。

### 実装（4点セット＝engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal）
- **大君主（overlord・$0+負債8・命令）**：`overlordTargets(state)`（captainTargets 同型・上限コスト5固定・非命令/非持続/非ポーション/非NON_SUPPLY/非splitLocked/非knights）＋`OVERLORD_PLAY`。サプライに残したまま使用（場に出ない・獲得しない）。対象があるうちは使用必須。DOM.engine に `overlordTargets` を公開（engine/CPU/UI で同じ候補を参照）。
- **冠（crown・$5・アクション＋財宝）**：新ヘルパ `crownOpenPending(state,pi)` が **その時点の `turn.phase`** を見てモードを決める（action＝手札のアクション1枚を2回／buy＝手札の財宝1枚を2回・どちらも任意）。`CROWN_CHOOSE` は玉座と同型で `state.replay` に2回目を積む。

### 【重要】財宝の「2回使う」を根治（冠＝新規／ティアラ・偽造通貨＝既存バグも同時修正）
- 旧実装は2回目を **`treasureReplayCoins`（コイン再計算＋3件の特例だけ）** で済ませており、**pending を立てる財宝・+購入/+VP を持つ財宝の2回目を丸ごと落としていた**。
- **`playTreasureCard` を「移動」と「効果」に分離**し、新 **`applyTreasureEffect(state,pi,card)`**（＝カードを動かさず効果だけ適用）を新設。2回目は **`state.replay` の新ラベル `'treasure_replay'`** に積み、**1回目が立てた選択待ちが解決してから** `runReplays` が適用する（行進の procession2/procession_finish と同じ形）。偽造通貨の廃棄は新ラベル `'counterfeit_trash'` で2回のプレイ後に走る。`treasureReplayCoins` は削除。
- 自己移動する財宝（**投資**＝`removeOne` ガードを追加／戦利品／法貨／私掠船の廃棄）は 2回目に removeOne が失敗して**自然に不発（lose track）**。
- これで **冠×御守り（二択が2回出る）／元手・大金の+1購入×2／鹵獲品の+1VP×2／愚者の黄金の動的$4／掘出物の獲得×2／水晶玉・金床・不正利得・豊穣の角の2回目の選択** が正しく出る。
- **副産物＝繁栄/暗黒時代の実バグ修正**：旧 `TIARA_PLAY`/`COUNTERFEIT_PLAY` は「1回目が反応待ち（相手の堀）なら2回目のアタックを丸ごと飛ばす」実装で、**3人戦で堀を持たない相手が銅貨を1枚しか受けなかった**（回帰テスト追加）。2回目のコイン計上タイミングも「1回目の完全解決後」に変わった（公式挙動・取りこぼしは無し）。

### 【重要】命令（Command）の「再演では選び直さない」ルール（公式）
- 公式：大君主/はみだし者を玉座の間等で複数回使っても、**何として使うかを選ぶのは1回目だけ**で以降は同じカード。
- `state.turn.commandAs[命令id]` に1回目の選択を記憶（`rememberCommandAs`）。`runReplays` が `state._replaying` を立て、`applyEffect` の `case 'overlord'` / `case 'band_of_misfits'` が `replayCommandAs` で記憶を再利用して**選択待ちを開かない**。
- **`_replaying` はゴーレムの2枚目では立てない**（＝「別カードの新しいプレイ」なので選び直せる）。
- **副産物＝暗黒時代の実バグ修正**：出荷 `darkages` 固定セットは **band_of_misfits と procession が同居**＝行進/玉座の2回目で別カードを選べてしまっていた（回帰テスト追加）。

### 敵対レビュー（多エージェント5次元→node再現検証）＝**確定9件（偽陽性0）→全件修正**
- 冠の2回目が 御守り/元手/大金/鹵獲品/愚者の黄金 の効果を落とす（high×1・medium×3・low×1）→ 上記 `applyTreasureEffect` で根治。
- 大君主が再演のたびに対象を選び直せる（medium）→ `commandAs` で根治（band_of_misfits も同時修正）。
- 語り部（アクションフェイズに財宝を出す）経由の冠が財宝モードになる（low）→ `crownOpenPending` が `turn.phase` を見るよう統一。
- UI：大君主モーダルの説明文が「持続除外」を書いていない（low）→ 船長モーダルと同文に修正。
- **既存バグも1件発見・修正（CPU）**：`pickSwindlerGift`（詐欺師）に `!splitBlocked` が無く、**ロック中の分割山下段（鹵獲品等）を贈与候補に出し続けて engine 拒否と噛み合いCPU無限ループ**（全プール混成fuzzで到達）。兄弟の `bestGain`/`bestGainExact` は持っていた書き漏れ。

### 検証
- `test/empires.test.js` を **全197件**に拡張（E6の裁定＋敵対レビュー回帰3群＋CPUソーク24戦）。`test/darkages.test.js` 79件（命令再演×行進/玉座＋偽造通貨の回帰）／`test/prosperity.test.js` 69件（ティアラ×ペテン師の3人回帰）／`test/promo2.test.js` 142件。
- **invariants の敵対王国に4つ追加**（冠×pending財宝×負債×分割山×城／命令の再演×玉座・王の宮廷／ティアラ×水晶玉・金床・投資・ペテン師／偽造通貨×戦利品×はみだし者×行進）。
- 追加ソーク（財宝再演×命令再演の敵対王国10種×12シード＝**120戦**）＝完走120・膠着0・例外0・保存則0・負リソース0。
- **npm test 全32スイート緑（exit 0・整合性3144不変・CPU序列 100/64/95 維持）**。

### 【次にやること】E7＝Phase E＝CARD_SET昇格（**ここで初めて本番に帝国が出る**）
- `DOM.KINGDOM_EMPIRES` 固定10種を選定＋`DOM.CARD_SETS` に `empires` / `random-empires` の2行。`POOLS.empires` から `castles`（混合山プレースホルダ）と分割山下段の扱いに注意（`randomKingdom` は下段→上段に正規化済み）。
- `test/empires-ui.test.js`（UIスモーク）新設＋`invariants` の出荷セット検証に `empires`/`random-empires` 追加＋CPUソーク（2〜4人・全難易度）。横型ランドスケープ（イベント/ランドマーク）は縦枠パイプライン未対応で対象外。

---

## 0-14. 段階2＝帝国（Empires）Batch E5＝城8（混合山・knights流用）**完了**（2026-07-08・WIP・未push）

**Batch E5 完了**＝城8種を1つの混合山（`state.castles`）として実装。設計正本＝`docs/research/empires_rules.md` §1-4/§3。`sw.js` v40→**v41**。**カタログ/webp変更なし**（研究WFで全8枚のカタログ＝公式一致・humble/plunder/rocksに続き humble_castle に coin:1 のみ追加＝表示文不変）。**帝国はまだ CARD_SET 未昇格＝本番挙動は不変**。次は E6（命令＝overlord/crown）＝帝国の最後の新機構。

### 城の混合山（`state.castles`＝knights 混合山流用）
- **pile-id `castles`**（knights 同型のプレースホルダ card を cards.js に追加＝`DOM.CARDS.castles`＋`POOLS.empires` に 'castles'／8種は `POOLS.castles`＝混合山の中身[非選択・昇順]）。`state.castles`＝実カードid配列を**コスト昇順**に積む（index0＝最安＝一番上）。`supply.castles`＝残数（同期）。**一番上だけ購入/獲得**（gain の `isMixed` に castles 追加＝先頭を shift・cardCost('castles')＝先頭の実コスト）。
- **人数別セットアップ**：2人＝各1（8枚）／3-4人＝**Humble/Small/Opulent/Kings を各2**（計12・昇順・重複隣接）＝createInitialState で構築。
- **公開情報**（昇順で決定的）＝maskStateFor で伏せない。**invariants/empires tally は state.castles を数え supply.castles はスキップ**（二重計上防止）。**混合山の中身（騎士/廃墟/城）を闇市場デッキから除外**（単体流出防止＝§6の既知リークも同時解消）。
- **可変VP（vpOf）**：粗末な城(humble)＝所有する城1枚につき1点／王城(kings)＝所有する城1枚につき2点（**自身を含む全ての「城」種別カードを数える**）。他6種は固定vp（crumbling1/small2/haunted2/opulent3/sprawling4/grand5）。

### 各カード（研究WFで公式裁定を裏取り・全カタログ一致）
- **粗末な城(humble・$3・財宝)**：+$1（coin:1）＋可変VP。**崩れた城(crumbling・$4)**：固定1VP＋**獲得または廃棄したとき +1勝利点トークン＋銀貨1枚**（triggerOnGain＋triggerOnTrash）。
- **小さい城(small・$5・アクション)**：固定2VP。プレイ＝これ（場）か手札の城1枚を廃棄→廃棄したら城1枚（一番上）を獲得（手札の城枝で城なし＝空振り可）。**華やかな城(opulent・$7・アクション)**：固定3VP。プレイ＝手札の勝利点カードを任意枚数 公開して捨て、1枚につき+$2（捨てるだけ＝VP保持・0枚可）。
- **幽霊城(haunted・$6)**：固定2VP。**自分のターンに獲得したとき 金貨1枚（自動）＋各相手（手札5枚以上）が手札2枚を山札の上へ**（非アタック＝堀不可）。**広大な城(sprawling・$8)**：固定4VP。獲得時 公領1枚か屋敷3枚を獲得（選択）。**壮大な城(grand・$9)**：固定5VP。獲得時 手札公開＋手札および場の勝利点1枚につき+1VP（自身は捨て札で数えない）。**王城(kings・$10)**：可変VP。

### 敵対レビュー（多エージェント6次元→node再現検証）＝確定バグ1件[MED]→修正
- **[修正] gainer（remodel/工房/拡張等の *_GAIN）経由で sprawling_castle/haunted_castle を獲得すると、獲得時効果が発火しない**：`finishGain` が gain() を呼ぶ時点で gainer 自身の pending が残っており、sprawling/haunted の on-gain が `!state.pending` ゲートで抑止される（BUY 経由は pending が null なので正常。crumbling/grand は自動効果でゲートなし＝無影響）。→ **新 `state.onGainQueue`（onTrashQueue 同型）を導入**＝sprawling/haunted の対話をキューに積み、reduce 末尾で選択待ちが空いたら発火（**border_village 等の意図的簡略化＝§6 の !pending on-gain対話ゲートは温存**）。他5次元クリーン（偽陽性0）。

### 検証
- 狙い撃ちテスト28/28（混合山の人数別/購入順/可変VP・全8枚・非アタックの堀不可・空振り・マスク）＋修正検証7/7。`test/empires.test.js` を **全140件**に拡張（E5裁定＋レビュー回帰＋CPUソークを42戦[E5城王国含む・全ゲーム完走で可変VP/混合山枯渇も検証]に拡大＝膠着0/例外0/保存則0）。
- **integrity 3134→3144**（'castles' プレースホルダ card 追加ぶん）。invariants は state.castles を数え supply.castles をスキップ（混合山の保存則）。**npm test 全32スイート緑（exit 0・knights 回帰なし）**。

### 【次にやること】Batch E6（命令＝overlord/crown）＝帝国 最後の新機構
- `docs/research/empires_rules.md` §1-5：**overlord**（負債d8・band_of_misfits/captain 流用＝サプライのコスト5以下・非命令アクションを、サプライに残したまま使う）／**crown**（$5・action+treasure・玉座同型だが現在フェイズで対象種別が変わる＝アクションフェイズ→手札のアクション1枚を2回／購入フェイズ→手札の財宝1枚を2回）。その後 **E7＝Phase E＝CARD_SET昇格**（`DOM.KINGDOM_EMPIRES` 固定10種＋`empires`/`random-empires`）＝**ここで初めて本番に帝国が出る**。横型ランドスケープ（イベント/ランドマーク）は縦枠パイプライン未対応で対象外。

---

## 0-13. 段階2＝帝国（Empires）Batch E4＝分割山5組（10枚）＋分割山機構の一般化 **完了**（2026-07-08・WIP・未push）

**Batch E4 完了**＝分割山5組の10枚を4点セットで実装＋**分割山機構を sauna/avanto 専用から一般化**。設計正本＝`docs/research/empires_rules.md` §1-3/§3。`sw.js` v39→**v40**。**カタログ変更なし・webp再生成なし**（研究WFで全10枚のカタログ文＝公式一致を確認。plunder/rocks に `coin:` フィールドのみ追加＝表示文不変）。**帝国はまだ CARD_SET 未昇格＝本番挙動は不変**。次は E5（城8・knights混合山流用）。

### 分割山機構を一般化（`DOM.SPLIT_PILES`）
- **`DOM.SPLIT_PILES`（cards.js・下段id→上段id の唯一の正本）**＝`{avanto:sauna（プロモ・両$4）, plunder:encampment, emporium:patrician, bustling_village:settlers, rocks:catapult, fortune:gladiator}`（安い方が上）。engine.js に `SPLIT_TOP`（下→上）/`SPLIT_BOTTOM`（上→下）/`splitLocked(state,id)`（下段が上段残存中で獲得不可か）を定義。
- **従来の sauna/avanto 専用ハードコードを全て一般化**：initSupply（各5+5）／createInitialState の相互補完（上下どちらかが王国にあれば両方置く）／gain() ガード／canBuyCard／validTeacherPiles／captainTargets／bandOfMisfitsTargets／emptyPileCount（分割山＝1山・上下とも0で空）／cpu.js `splitBlocked`／cards.js `randomKingdom`（下段→上段に正規化）。**sauna/avanto（promo2-pack）は回帰なし**（新プロモテスト緑）。

### 各カード（研究WFで公式裁定を裏取り・全カタログ一致）
- **陣地(encampment・$2上)**：+2カ+2ア。手札から金貨か鹵獲品を公開してよい→公開しないと脇へ→**片付け開始時に自分の分割山へ戻す**（`supply.encampment++`・捨て/廃棄ではない・玉座2回目は場に無く不発＝lose track・黒市経由で山が無ければ脇に残り所有カードとして数える）。**鹵獲品(plunder・$5下・財宝)**：+$2（coin:2）+1勝利点トークン/プレイ。
- **パトリキ(patrician・$2上)**：+1カ+1ア（+1カードが先に山札上を引く）→**新しい山札上を公開しコスト5以上なら手札へ**（未満は残す）。**エンポリウム(emporium・$5下)**：+1カ+1ア+$1。**獲得時（任意経路）場のアクション5枚以上なら+2VP**（inPlay+durationCards）。
- **開拓者(settlers・$2上)**：+1カ+1ア・捨て札から銅貨1枚を手札へ（任意）。**騒がしい村(bustling_village・$5下)**：+1カ+3ア・捨て札から開拓者1枚を手札へ（任意）。
- **投石機(catapult・$3上・アタック)**：+$1・手札1枚廃棄（強制）→**コスト3以上なら他全Pが呪い／財宝なら他全Pが手札3枚まで捨て（両方満たせば両方）**・堀/灯台で全防御・空手札なら副効果なし。**石(rocks・$4下・財宝)**：+$1（coin:1）・**獲得または廃棄したとき銀貨1枚**（購入フェイズ中なら山札上・そうでなければ手札＝triggerOnGain＋triggerOnTrash）。
- **剣闘士(gladiator・$3上・非アタック)**：+$2・手札1枚公開→左隣が同名を公開してよい→**公開されなければ+$1＋サプライから剣闘士1枚を廃棄**（分割山の上段が減る→尽きたら大金が見える）。**大金(fortune・$8+負債8・下・財宝)**：+1購入・**このターン初回の大金ならコイン2倍**（`t.fortunePlayed`・PLAY_ALL_TREASURESは大金を最後に出す）・**獲得時 場の剣闘士1枚につき金貨1枚**（負債は通常機構）。

### 敵対レビュー（多エージェント6次元→node再現検証）＝確定バグ1件[MED]→修正
- **[修正] exact-cost強制獲得が「ロック中の分割山下段を唯一の候補」として掴んで無限ループ/人間詰み**：gain()/canBuyCard は splitLocked を見るが、**「ちょうど$N獲得が必須か」を判定するゲート述語が splitLocked を除外していなかった**。**rocks は$4の分割山下段だが基本サプライに$4札が無い**ため、upgrade/remake（$3廃棄→ちょうど$4）や procession（$4アクション廃棄→ちょうど$5アクション＝emporium/bustling_village）で下段がロック中だと唯一候補になり、engineが拒否×ゲートは必須→pending不閉→CPU無限ループ/人間詰み。全プールfuzz（intrigue/dark ages×帝国）で到達可。→ **全ての exact-cost 強制獲得述語（upgrade/remake/procession/forge/farmland/governor/stonemason/swindler/develop/artificer/charm のゲート＋手動獲得）に `!splitLocked(state, id)` を追加**＋finishGain の辞退経路も splitLocked 除外（≤N/＜N は copper 等の基本札があり詰まないため対象外）。他5次元クリーン（偽陽性0）。

### 検証
- 狙い撃ちテスト41/41（分割山の初期化/購入ガード/sauna回帰・全10枚・アタック/堀・獲得時/廃棄時）＋修正検証7/7。`test/empires.test.js` を **全122件**に拡張（E4裁定＋レビュー回帰4件＋CPUソークを36戦[E4王国含む・2〜3人]に拡大＝膠着0/例外0/保存則0）。
- **invariants**：分割山カードは通常カード（保存則はsupply/trash経由）＝tally変更不要。全プール混成fuzz緑（exit0・exact-cost×ロック下段のデッドロックも解消）。**npm test 全32スイート緑（exit 0・整合性3134不変・新プロモのsauna/avanto回帰OK）**。

### 【次にやること】Batch E5（城8＝混合山）
- `docs/research/empires_rules.md` §1-4 の城8＝**knights 混合山流用**（`state.castles` = top-level id配列・コスト昇順に積む・一番上だけ購入/獲得・`isMixed` 分岐に castles 追加・invariants tally に forEach・maskで先頭のみ・emptyPileCount）。**2人＝各1枚計8／3人以上＝計12（Humble/Small/Opulent/Kings 各2）**。可変VP（humble=城数×1／kings=城数×2）＋各on-gain（small=trash→城獲得・crumbling=獲得/廃棄で+1VP&銀貨・haunted=自手番獲得で金貨&他P手札上げ・sprawling=公領1or屋敷3・grand=手札公開VP・opulent=勝利点捨てて+2コイン/枚）。以降 E6命令(overlord/crown)→E7=CARD_SET昇格。

---

## 0-12. 段階2＝帝国（Empires）Batch E3＝集合（Gathering・サプライ山上VPトークン）3枚 **完了**（2026-07-08・WIP・未push）

**Batch E3 完了**＝集合機構 `state.pileVP` ＋ temple/farmers_market/wild_hunt を4点セットで実装。設計正本＝`docs/research/empires_rules.md` §1-2/§3。`sw.js` v38→**v39**（js/css変更）。**カタログ/webp変更なし**（研究WFで全3枚のカタログ文＝公式一致を確認）。**帝国はまだ CARD_SET 未昇格＝本番挙動は不変**。次は E4（分割山5組・sauna/avanto流用）。

### 新機構＝集合（Gathering）＝`state.pileVP`
- **`state.pileVP = {[pileId]:個数}`**（トップレベル state・公開・**非カード＝保存則tallyに数えない**・createInitialState で `{}` 初期化・maskStateFor は clone でそのまま残る＝全員に見える）。サプライ山の上に置かれた勝利点トークン数＝**全プレイヤーで共有・累積**。UI＝山の右上に金色バッジ `⭐N`（`pileVpBadge`＋`.pile-vp` CSS）。プレイヤーVPトークン `p.vpTokens`（繁栄で既存・vpOfに加算済）へ移した時だけ得点になる。

### 各カード（研究WFで公式裁定を裏取り・全カタログ一致）
- **temple（神殿・$4）**：プレイ＝+1勝利点（本人）→**手札から名前の異なる1〜3枚を廃棄（強制・手札があれば最低1枚・同名重複不可）**→神殿の山にVP+1（`TEMPLE_TRASH`）。空手札なら廃棄0でも+1VP・山にVP。**獲得時（triggerOnGain・誰の獲得でも/購入含む/非購入獲得も）＝神殿の山上VPを全部自分の vpTokens へ**（山→0）。
- **farmers_market（農家の市場・$3）**：+1購入。**山のVPが4個以上なら全部得てこれを廃棄**（コインなし・場から trash へ）。そうでなければ山にVP+1、その後**山のVP1個につき+1コイン（置いた後に数える）**＝空山から +$1/+$2/+$3/+$4、5回目（山=4）で4VP取得＋廃棄。**4以上判定は置く前**。
- **wild_hunt（ワイルドハント・$5）**：二択（強制・`WILD_HUNT_RESOLVE`）。(a)+3カード＆山にVP+1。(b)屋敷1枚を獲得し、**獲得したら**山上VPを全部得る（屋敷山が空なら選べるが獲得もVPも無し＝合法だが無意味）。

### 敵対レビュー（多エージェント5次元→node再現検証）＝確定バグ1件[MED]→修正
- **[修正] temple_trash のUIソフトロック**：同一ターンに temple_trash が2回開く（玉座/王の宮廷/行進の再演・村＋神殿2枚）と、モーダルのリセットキー `pd.type+(pd.stage||'')` が `temple_trash` で不変のため `UI.selection` が持ち越される。1回目で廃棄して手札から消えた名前（幽霊選択）はチップが描画されず外す導線が無く、確定しても engine が `removeOne` 失敗で no-op 拒否→**人間が詰む**（engine は正しく拒否＝保存則OK・UI専用バグ）。**出荷帝国セット未昇格のため本番未到達**（Medium）。→ **temple_trash モーダル先頭で `UI.selection` を「現在の手札にある名前」だけに間引く**（幽霊選択を自己修復）。他4次元（rulings/保存則/CPU非ループ/cross-card＝玉座・闇市場・相手の神殿獲得）はクリーン（偽陽性0）。

### 検証
- 狙い撃ちテスト22/22（temple の同名拒否/0枚拒否・farmers_market の累積と4以上廃棄・wild_hunt の二択と屋敷空・獲得時全取得・マスク）。`test/empires.test.js` を **全95件**に拡張（E3裁定＋CPUソークを30戦[E3王国2種含む・2〜3人]に拡大＝膠着0/例外0/保存則0）。
- **invariants**：pileVP は非カードなので tally 変更不要（全プール混成fuzzで temple/farmers_market/wild_hunt を引いても保存則OK）。**npm test 全32スイート緑（exit 0・整合性3134不変）**。

### 【次にやること】Batch E4（分割山5組）
- `docs/research/empires_rules.md` §1-3/§2 の分割山＝**sauna/avanto と同型**（`js/engine.js` の分割山ガード4系統：gain冒頭/canBuyCard/emptyPileCount ペアで1山/CPU splitBlocked）。5組＝encampment/plunder・patrician/emporium・settlers/bustling_village・catapult/rocks・gladiator/fortune。**上下でコストが違う**点だけ sauna/avanto（両$4）と異なる。fortune は負債(E1)＋剣闘士on-gain金貨、emporium は on-gain VP(場アクション5枚以上)、rocks は on-gain/trash銀貨、catapult はアタック。以降 E5城8→E6命令→E7=CARD_SET昇格。

---

## 0-11. 段階2＝帝国（Empires）Batch E2＝既存VPトークン＆単独カード9枚 **完了**（2026-07-08・WIP・未push）

**Batch E2 完了**＝forum/sacrifice/groundskeeper/chariot_race/villa/charm/legionary/enchantress/archive の9枚を4点セット（engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal）＋終端保証で実装。設計正本＝`docs/research/empires_rules.md` §3。`sw.js` v37→**v38**。**帝国はまだ CARD_SET 未昇格＝本番挙動は不変**。次は E3（集合＝山上VPトークン）。

### 研究WF（9エージェント・web裏取り）でカタログ誤り2件を検出→修正（重要）
- **villa（ヴィラ）**：カタログの**プレイ時**効果に「これを手札に加える」が誤って入っていた（公式は**獲得時**のみ。プレイ時に手札へ戻すと無限コンボ）。→ カタログ修正（プレイ＝+2アクション+1購入+1コインのみ）＋**webp再生成**。実装は on-gain（triggerOnGain）で「手札に加え+1アクション・購入フェイズ中ならアクションフェイズに戻る」。
- **forum（公共広場）**：2022エラッタで「購入時+1購入」→**「獲得時+1購入」**に変更。→ カタログ「獲得時」に修正＋**webp再生成**、実装も on-gain（triggerOnGain）で+1購入。
- **chariot_race（戦車競走）**：**左隣が山札0枚のときは勝ちにならない**（`empires_rules.md` §3の「左隣0枚＝こちらの勝ち」は誤り。BGGスレ等4ソースで確認）。→ 実装は「**両者が公開でき、かつ自分のコストが厳密に高い**」時だけ +$1+VP（同コスト・どちらか空は無し）。

### 各カードの実装要点
- **forum**：+3カード+1アクション→手札2枚捨て（FORUM_DISCARD）。on-buyでなく**on-gain**で+1購入。
- **sacrifice**：手札1枚廃棄→**種別ごとに全適用**（アクション=+2カ+2ア／財宝=+$2／勝利点=VPトークン2）。空手札なら pending 無し。
- **groundskeeper**：+1カ+1ア。**場にある物理枚数ぶん**、勝利点獲得毎にVPトークン（triggerOnGainの隠し財産と同型・自手番のみ）。
- **chariot_race**：+1アクション。自分の山札上を公開して手札へ、左隣は公開のみ（山札に残す）。上記の厳密比較で +$1+VP。
- **villa**：プレイ＝+2アクション+1購入+1コイン（手札に戻らない）。**獲得時**（triggerOnGain）＝手札に加え+1アクション、**自分の購入フェイズ中の獲得ならアクションフェイズに戻る**（`state.turn.phase='action'`）。engineer等での獲得やBUYどちらも同じ入口。
- **charm**：財宝の二択（`charm_mode`）。A=+1購入+$2／B=`t.charmNextGain` を積み、このターン**次の獲得**でその獲得札と**同コスト（$・負債・ポーション一致）で名前の異なる**カード1枚を獲得（`charm_gain`・複数チャームは積む・無効な選択は拒否してpending維持）。
- **legionary**：+$3。金貨公開の選択（`legionary_reveal`）→公開したら各相手が手札2枚まで捨て**その後1枚引く**。**`discard_down` に `drawAfter` を追加**（捨て後に+1引き・免疫でない全相手が対象＝手札≤2でも引く）。アタック（堀/灯台で全無効）。
- **enchantress**：即時効果なしのアタック持続。免疫でない各相手に `p.enchanted` を立て、**その手番で最初にプレイするアクションを +1カード+1アクションに置換**（PLAY_ACTIONで消費・記載効果=アタックも抑止・チャンピオン/教師トークン等ライン下能力は先に適用済みで機能）。次の自分の手番開始時 +2カード（DURATION_RESOLVERS）。`enchanted` は相手の cleanup で消える。置換された持続は予約を張らずそのターン捨て。堀react（ENCHANTRESS_REACT/MOAT_REVEAL）。
- **archive**：+1アクション。山札上3枚を裏で脇へ＝新ゾーン **`p.archives = [{id, cards}]`**（所有者のみ可視＝**maskStateForで相手にback**・**allCards/invariants tally/empires tally に加算**・cleanupは `p.archives.length` ぶん物理カードを保持）。今回と次の2手番開始時に1枚ずつ手札へ（`archive_pick`。手番開始ぶんは resolveDurationStartEffects→startQueue）。玉座で2脇（各id独立）。

### 敵対レビュー（多エージェント6次元→node再現検証）＝確定バグ3種→修正2件＋据え置き1件
- **[修正] 生贄×玉座/王の宮廷/行進でCPU無限ループ**：`sacrifice` を2回プレイし1回目で最後の非生贄札を廃棄→2回目が手札=['sacrifice']で開くと、CPU `decidePending` が生贄を除外して `card:null` を返し続ける（engineは手札>0で廃棄必須＝null拒否でpending不閉）。**全プールfuzz（玉座+生贄）／闇市場経由（出荷promoで到達可）**。→ CPU sacrifice に `|| p.hand[0]` フォールバック（手札があれば必ず非null＝最後は生贄自身を廃棄＝アクション種別で+2カ+2ア）。回帰テスト2件。
- **[修正] 闇市場でvilla/forum等を獲得→on-gain効果が飛ぶ**：`BLACK_MARKET_BUY` はサプライ外札のため gain() 非経由で discard 直挿し＝**`triggerOnGain` を呼んでいなかった**（villaの手札化/+1ア/フェイズ復帰・forumの+1購入・庭師VP・御守り次獲得が消失）。→ BLACK_MARKET_BUY 末尾に `triggerOnGain(state, pd.player, card, 'discard')` を追加（**従来から闇市場に在った on-gain 欠落＝キャッシュ/大使館/国境の村等も併せて解消**・負債は従来どおり手動付与で二重にならない）。回帰テスト2件。
- **[据え置き＝許容簡略化] enchantress×追加ターン**：outpost（前哨地）/possession（支配）で相手が追加ターンを取ると、`enchanted` が再付与されず追加ターンの最初のアクションが置換されない（過小適用）。**出荷帝国セットに追加ターン源は無く到達不能**（全プールfuzzでのみ seaside/alchemy と同居し得るが、保存則/非ループ/クラッシュは無し＝fuzz緑を維持）。champion/教師トークンの「玉座再演では発火しない」と同種の意図的簡略化。

### 検証
- **狙い撃ちテスト（各カード）＝合計140件超 全緑**（実行後に一時ファイル削除）。`test/empires.test.js` を **全79件**に拡張（E1=39＋E2＝カード裁定＋敵対レビュー回帰5件＋E2 CPUソーク24戦[2〜3人・E2重点王国3種・膠着0/例外0/保存則0]）。
- **invariants**：tally/hasBack に `archives` ゾーンを追加（脇置きも保存則に数える）。全プール混成fuzz緑（exit0）。
- **npm test 全32スイート緑（exit 0・整合性3134不変）**。

### 【次にやること】Batch E3（集合＝サプライ山上のVPトークン）
- `docs/research/empires_rules.md` §1-2 の集合機構＝新スカラー `state.pileVP = {[pileId]:個数}`（公開・非カード）＋temple/farmers_market/wild_hunt。§3 の個別裁定を着手時に再確認。以降 E4分割山5組（sauna/avanto流用）→E5城8（knights混合山流用）→E6命令(overlord/crown)→E7=CARD_SET昇格。

---

## 0-10. 段階2＝帝国（Empires）実プレイ化 **着手・Batch E1＝負債経済の基盤 完了**（2026-07-08・WIP・未push）

**帝国段階2に着手**。段階1（画像/カタログ/GAIN_ORDER）は §0-6 で完了済み。設計＝**`docs/research/empires_rules.md`**（公式ルール裏取り＋6機構＋バッチ計画）を必読。新機構6系統＝負債Debt／集合=山上VPトークン／命令overlord・crown／分割山5組／城8混合山／villa。横型ランドスケープ（イベント/ランドマーク）は対象外（縦枠パイプライン未対応）。

### バッチ計画（安全順・`docs/research/empires_rules.md` §2）
- **E1＝負債経済の基盤＋純負債4枚**（✅完了）／E2＝既存VPトークン＆単独カード（sacrifice/chariot_race/groundskeeper/forum/legionary/enchantress/archive/charm/villa）／E3＝集合（山上VP＝farmers_market/temple/wild_hunt）／E4＝分割山5組（sauna/avanto流用）／E5＝城8（knights混合山流用）／E6＝命令（overlord/crown）／E7＝Phase E＝CARD_SET昇格。

### Batch E1 完了（負債(Debt)経済の基盤＋engineer/city_quarter/royal_blacksmith/capital）
- **負債スカラー `p.debt`**（公開・VP無関係・ターンを跨いで残る＝freshTurn非対象・maskStateForで公開）。**負債コストのカードを購入/獲得すると `gain()` 末尾でその数だけ負債を負う**（購入も効果獲得も gain() 一元入口を通る）。**負債>0 の間は BUY を拒否**（購入ブロック）。返済＝新 action **`REPAY_DEBT`**（購入フェイズ・$1=負債1個・購入権消費なし・amount未指定で可能な限り）。
- **カード効果**：engineer（≤$4を1枚獲得[強制]→自己廃棄してもう1枚[強制]。多段pending gain1/maytrash/gain2＋4点セット。玉座2回目は removeOne 失敗で自己廃棄不発＝保存則OK）／city_quarter（+2アクション＋手札のアクション枚数ぶん+カード）／royal_blacksmith（+5カード＋手札の銅貨を全捨て）／capital（財宝 coin:6＋1購入。**cleanupで場から捨てるとき負債6＋残コインで即返済**＝コイン使い切れば負債6残・使わなければ0）。
- **重要ルール（裏取り済・`docs/research/empires_rules.md`）**：負債は「購入」だけをブロック（獲得は可・終了時減点なし）。**「コストN以下/ちょうどN の獲得」は負債コストのカードを取れない**（負債は追加コスト）＝engineer/messenger の canGain と CPU `bestGain`/`bestGainExact`・UIモーダルに負債除外を追加。
- **CPU**：decide の購入フェイズで財宝後に debt>0 なら REPAY_DEBT（コイン0なら END_TURN＝非ループ）。chooseAction に city_quarter/royal_blacksmith/engineer。decidePending に engineer 全段。**UI**：負債バッジ（オレンジ）＋返済ボタン＋購入ボタンの負債>0無効化（buyableId/onPileTap 両方）＋viewPendingModal の engineer 3段＋黒市モーダルの負債ガード。`sw.js` v36→**v37**。
- **多エージェント敵対レビュー（5次元→各自 node で再現検証）＝確定バグ1クラスタ（闇市場×負債）→修正**：
  - **[MED] 闇市場(BLACK_MARKET_BUY)が負債を完全に無視**＝(1)負債>0でも購入可・(2)`discard.push` 直挿し（gain()非経由）で負債カードを買っても負債が付かない。黒市デッキ母集団は全POOLS（empires含む）＝promo-pack等で到達可。E1で負債カードが実効果を持ったため『働くカードを負債ゼロで不正入手』に。→ BLACK_MARKET_BUY に負債>0拒否＋負債カード購入時の負債付与、CPU黒市ハンドラに負債>0でSKIP＋負債カード除外、UI黒市に負債ガード。
- **意図的な据え置き（非到達＝ポーション費用と同型）**：workshop/ironworks/armory/feast/altar/remodel等の**汎用「≤$N獲得」reducer の canGain は負債カードを除外していない**（engineer/messengerのみ除外）。**どの出荷 CARD_SET でも負債カードとこれらの汎用gainerは同居しない**（empires固定/random-empiresは empires のみ・mix-allセットは無し）＋**fuzzはCPU駆動で `bestGain` が負債カードを提案しない**ため到達不能。将来 mix-all モードを足すときに共通ヘルパ `costUpTo` へ集約して**ポーション費用問題と一緒に**対応する方針（§6）。
- **検証**：狙い撃ち/回帰 `test/empires.test.js`（39件・package.json登録＝**32スイート目**）＝負債の購入ブロック/返済/gain付与・capitalのon-discardと即返済・engineerの多段と玉座耐性と負債除外・闇市場×負債の回帰・CPUソーク16戦（膠着/例外/保存則違反0）。`node test/invariants.test.js` 緑（全プール混成fuzzで帝国カードを引いても保存則OK）／**npm test 全32スイート緑（exit 0）**。

### 【次にやること】Batch E2（既存VPトークン＆単独カード）
- `docs/research/empires_rules.md` §2 の E2＝sacrifice（廃棄→種別別ボーナス・VPも）/chariot_race（コスト比較→VP）/groundskeeper（場にある間 勝利点獲得毎VP）/forum（+3カード-2捨て・on-buy+1購入）/legionary（アタック）/enchantress（持続アタック＝相手の最初のアクションを+1c+1aに置換）/archive（3手番持続・脇3枚→1枚ずつ）/charm（財宝の二択＝獲得コピー）/villa（獲得で手札＋アクションフェイズ復帰）。プレイヤーVPトークン `p.vpTokens` は既存（繁栄）。個別裁定は §3 を着手時に再確認。

---

## 0-9. 段階2＝冒険（Adventures）実プレイ化 **全38枚＋Phase E昇格 完了・push待ち**（2026-07-08・WIP・未push）

**冒険は実プレイ可能（CARD_SET昇格済）＝残りは push（ユーザー確認）のみ**。着手前に `docs/adding-cards.md` と本節を必読。**未pushコミット（Batch1a〜6＋各レビュー修正＋Phase E）**。push すれば本番に反映。

### Phase E 完了（2026-07-08・CARD_SET昇格）＝`5fe688a`＋`09193bc`(UI誤購入修正)
- **配線**（js/cards.js）：`DOM.KINGDOM_ADVENTURES` 固定10種（page/peasant/guide/ranger/amulet/caravan_guard/haunted_woods/lost_city/artificer/hireling＝自作showcase）／`POOLS.adventures` から成長先8種を `POOLS.travellers` に分離（賞品prizesと同型＝random抽選から外す・page/peasant はサプライで adventures に残す）／`DOM.CARD_SETS` に `adventures`＋`random-adventures` の2行。成長山は page/peasant があるとき initSupply が各5枚設置（既存）。
- **UI**（js/ui.js）：非サプライの数値キー山（成長先/賞品/戦利品）を盤面に表示（`nonSupplyPile` セクション＝残枚数可視化・購入不可）。**Phase E自己検出のUXバグ修正**＝非サプライ山タップで誤購入ボタンが出た（`onPileTap`/`buyableId` が `canBuyCard` 未参照）→両方に `canBuyCard` 追加（高級市場/分割山下段の誤ハイライトも解消）。カード一覧に成長先グループ追加。
- **テスト新設**：`adventures.test.js`（44件）＋`adventures-ui.test.js`（40件）を package.json 登録。invariants の出荷セット検証に adventures/random-adventures 追加。
  - **回帰テスト（永続）**：玉座/王の宮廷/行進×Reserve 保存則／page/peasant×upgrade で$4が成長先のみ→獲得なし終了（デッドロック回帰）／swindler×成長先の贈与拒否／呪いの森×農地で詰まない・沼の妖婆×名品(過払い)で呪い発動・玉座×沼の妖婆の免疫が反応順独立／玉座×語り部×水晶玉で基本+1×2／使者の初回配布。
- **webp**：storyteller 再生成（2022エラッタ＝+1カード基本・目視確認済）。`sw.js` v35→**v36**。
- **多エージェント敵対レビュー（3次元＝配線/UI盤面/CPU出荷セット→敵対検証）＝確定バグ0件**（カード効果は Batch5/5c/6 で既にレビュー済み）。
- **検証**：npm test 全31緑（整合性3134・冒険44・冒険UI40）／Phase E CPUソーク（adventures/random-adventures・2〜4人・全難易度）**240戦=完走240・deadlock0・例外0・保存則0**（champion保持28P）／emptyPileCount が成長先の空を3山終了に数えないことを確認。

### 【次にやること】push（ユーザー確認）→ その後 帝国（Empires）段階2
- **push**：冒険を push すると本番 Pages/Render に「冒険セット」「冒険から」が出る（sw.js v36）。**ユーザー確認の上で** `git push`。
- **その後＝帝国（Empires）段階2**（別の大仕事・段階1[画像/カタログ]は §0-6 で完了済み）：負債Debt経済／集合=VPトークン山（農民の市場/神殿/野生の狩り）／命令(overlord/crown)／分割山5組（サウナ/アヴァント機構流用）／城8（騎士の混合山流用・勝利点）／villa(手札に獲得しアクションフェイズに戻る)。横型ランドスケープ（イベント/ランドマーク）は縦枠パイプライン未対応で段階1すら未着手。

### Batch6 完了（2026-07-08・複雑系4枚）＝`0b9c9d7`＋`92bb666`(レビュー修正)＝**これで冒険 全38枚 実装完了**
- **倒壊raze**($2)：+1アクション。これ（場のraze）か手札1枚を廃棄（強制・空手札でもraze自身を廃棄可）→廃棄カードの**現在コイン費用**分だけ山札の上を見て1枚を手札・残りを捨てる（`deck.shift` 直読み＝**-1カードトークンの影響を受けない**[公式ruling]）。玉座2回目で raze が場に無く手札も空なら pending を立てない。
- **工匠artificer**($5)：+1カード+1アクション+$1。手札を好きな枚数捨て→捨てた枚数**ちょうどの現在コスト**のカード1枚を**山札の上に**獲得してよい（任意・0枚→$0可・`!NON_SUPPLY.has(id)` を両側[anyGainable/canGain]で除外＝Batch5の教訓）。
- **語り部storyteller**($5・**2022エラッタ**)：+1アクション。手札から最大3枚の財宝をプレイ→**+1カード（基本）**＋所持コイン$1につき+1カード（既存＋財宝のコインを全て使い切る＝coins=0）。財宝が pending を立てる（遺物/法貨/掘出物/混成の水晶玉等）と中断→**reduce末尾の安全網 `t.storytellerResume` が解決後に残り財宝→変換を再開**。カタログ文を2022エラッタ（+1カード基本）に更新＝**Phase E で storyteller の webp 再生成が必要**（このPCのみ）。
- **使者messenger**($4)：+1購入+$2＋山札を捨て札にしてよい（プレイ効果・`triggerOnDiscard` を通さない＝Tunnel不発[公式]）。**そのターン最初の購入（`t.buysMade===1`）で買った**とき$4以下1枚を獲得し他の各Pが手番順にコピーを獲得（在庫がある限り）。購入以外の獲得では発動しない。
- 公式ルール研究（RGGルールブック逐語＋2022エラッタ）で裏取り＝storyteller の欠けていた基本+1ドロー／raze のコスト基準・玉座耐性・-1カードトークン無効を修正。新pending7種＋4点セット。UI: `modalMultiHand` に filter 追加（語り部の財宝限定選択）＋`t.buysMade` を freshTurn に追加。
- **多エージェント敵対レビュー（4次元→敵対検証・空試験で再現）＝確定バグ1件（偽陽性0）→修正（`92bb666`）**：
  - **[MED] 玉座/王の宮廷×語り部×割り込み財宝**：reduce末尾で runReplays（玉座の再演）が storytellerResume の再開より先に走り、1回目の語り部の財宝が pending を立てると、解決後に runReplays が2回目を先に発火→1回目の再開スキップ＋storytellerResume 上書きで**1回目の基本+1カード（王の宮廷なら最大2枚）＋残り財宝のコイン変換が失われる**（引き枚数誤り・保存則は保つ・CPU未到達＝人間操作限定）。→ **語り部の中断再開を runReplays より前で処理**（順次玉座＝1回目が完全解決してから2回目）。
- **許容簡略化**：champion/teacher/caravan_guard のボーナスは玉座/王の宮廷/門下生の再演では発火しない（PLAY_ACTION のみ）。
- **検証**：狙い撃ち32/32／invariants に「倒壊/工匠/語り部×遺物/使者×玉座/王の宮廷」の敵対王国追加＝全4緑／npm test 全29緑（整合性3132不変）／Batch6ソーク210戦=完走210・deadlock0・例外0・保存則0／throne×storyteller×水晶玉の回帰（基本+1×2＝正しいドロー）確認。

### Batch5c 完了（2026-07-08・純持続/アタック3枚）＝`b6424f6`＋`edbd8a4`(レビュー修正)
- **新機構＝「相手の購入をフックする持続」`applyLingerOnBuy`**：各Pの `delayedEffects` に発動元の予約（type＝swamp_hag/haunted_woods, immune[], **一意 rid**）を張り、`BUY`／`BLACK_MARKET_BUY` 末尾で「購入者≠発動元」の有効予約を発火。**無条件発動**（購入した以上フックは必ず効く）。予約は次の自分の手番開始時に `DURATION_RESOLVERS` で消費され窓が閉じる。物理カードは cleanup の持続保持で durationCards に残り予約消費後に捨て札へ。
- **沼の妖婆swamp_hag**（アタック持続）：即効果なし。次の自分の手番まで他Pの購入毎に呪い1枚（**予約1つにつき1枚**＝玉座で2枚）。次手番+$3。
- **呪いの森haunted_woods**（アタック持続）：即効果なし。次の自分の手番まで他Pの購入で手札を全て山札の上へ（複数予約でも1回）。次手番+3カード。
- **隊商の護衛caravan_guard**（持続＋リアクション）：+1カード+1アクション・次手番+$1。他Pのアタック時に手札から先にプレイしてよい（`hasReaction`＋`CARAVAN_GUARD_REACT`＝番犬/馬商人型・免疫にはならない）。**リアクションプレイ時の+1アクションは相手の手番なので加算しない**（相手に手番を与えない＝+1カードのみ効く）。相手の手番にプレイした caravan_guard は反応者の inPlay に残り、反応者の次の手番開始で+$1発火→その後の反応者の cleanup で捨て札へ（保存則OK）。
- **免疫**：プレイ時に確定＝champion/灯台の受動免疫(`attackImmune`)と堀公開者を予約の immune[] に記録（`markLingerImmune` は**その予約[rid]1つだけ**＝封鎖の gained と同型）。購入フックが immune の購入者を飛ばす。
- ATTACKS に haunted_woods/swamp_hag（onMoat＝免疫記録＋続行）＋新pending react（`LINGER_REACT`）。DURATION_RESOLVERS 3種。4点セット（CARAVAN_GUARD_REACT/LINGER_REACT＋PLAYER_ACTIONS＋CPU＋UI[reactOptions＋embedded窓3＋react モーダル]）。
- **多エージェント敵対レビュー（4次元→敵対検証・全件 空試験で再現）＝確定バグ2件（偽陽性0）→修正（`edbd8a4`）**：
  - **[HIGH] 呪いの森×農地のデッドロック＋素朴ガードの空振り**：購入フックが農地の廃棄pending成立後に手札を空にし FARMLAND_TRASH が解決不能→CPU無限ループ/人間詰み。素朴な `!state.pending` ガードは逆に「過払い/農地/高貴な山賊 購入時にフックを丸ごと免れる」空振りを生む。→ 根治：**FARMLAND_TRASH を手札空で終端**（公式：手札無しなら農地は何も廃棄しない）＋**applyLingerOnBuy を無条件化**＋UI農地に手札空スキップ。呪いの森×農地は農地が空振りするが詰まない（fuzz限定・許容簡略化）。
  - **[LOW] 玉座/王の宮廷×沼の妖婆/呪いの森の免疫過剰付与**：markLingerImmune が同型の予約を全走査し、後から堀を公開すると『既に受けた予約』まで遡って免疫化＝反応順で結果が0/1に割れた。→ 予約に**一意 rid**を付与し、堀公開はその予約1つだけを免疫に（per-window モデル維持＝受け→受けなら呪い2枚）。
- **許容簡略化**：champion/teacher/caravan_guard のボーナスは玉座/王の宮廷/門下生の再演では発火しない（PLAY_ACTION のみ）。呪いの森×農地の同時購入時は農地が空振り（fuzz限定＝出荷セットで両者は同居しない）。
- **検証**：狙い撃ち28/28＋レビュー回帰17/17／invariants に「呪いの森/沼の妖婆/隊商の護衛×玉座/王の宮廷」の敵対王国追加＝全4緑／npm test 全29緑（整合性3132不変）／持続中心ソーク240戦＋混成ソーク180戦（呪いの森/沼の妖婆×農地/名品/watchtower/闇市場）＝完走・deadlock0・例外0・保存則0。

### Batch5 完了（2026-07-07・トラベラー全10枚）＝`7c790b9`(5a)＋`86ab97c`(5b)＋`7cda556`(レビュー修正)
- **Batch5a（page/peasant＋成長先7枚）**：
  - **非サプライ山**：成長先8種（treasure_hunter/warrior/hero/champion/soldier/fugitive/disciple/teacher）を `NON_SUPPLY`／cpu `NON_SUPPLY_SET` に追加。`initSupply` が page あれば treasure_hunter/warrior/hero/champion を各5枚、peasant あれば soldier/fugitive/disciple/teacher を各5枚 supply に追加（**page/peasant のみサプライ・購入可**）。
  - **トラベラー交換窓**：`endBuyTail` を `endBuyTailSchemeOrCleanup` に分割し、その手前に `traveller_exchange` pending を挟む。交換＝場のトラベラーを supply へ返し次の成長先を supply から取り**捨て札へ**（`TRAVELLER_NEXT` 系列。**獲得でも廃棄でもない**＝on-gain/on-trash不発・treasure_hunter に数えない・次の山が空なら不可）。`TRAVELLER_EXCHANGE_RESOLVE`。
  - **champion＝永続持続**（hireling/prince型＝`p.champions` を cleanup の `cnt.champion` に加算し durationCards に残す）＋`attackImmune` に champion 条件追加＋`PLAY_ACTION` で**アクション使用毎に+1アクション**（自身のプレイは除外）。
  - **カード効果7枚**：page(+1c+1a)／peasant(+1購入+$1)／treasure_hunter(+1a+$1・右隣[pi-1]の直前手番の獲得数だけ銀貨・多重度カウント)／warrior(+2c・場のトラベラー数[自身含む]だけ各相手の山札上を捨て**ちょうど$3/$4[非ポーション]なら廃棄**・アタック)／hero(+$2・財宝1枚を強制獲得)／soldier(+$2・場の他アタック[inPlay+durationCards・自身除く]毎+$1・**手札4枚以上の各相手が1枚捨て**・アタック)／fugitive(+2c+1a・1枚捨て)／disciple(手札のアクション1枚を2度使い＋そのコピーを獲得・**非サプライ札はコピー獲得せず**)。
  - **新pending7種＋4点セット**：warrior/soldier react・soldier/fugitive discard・hero_gain・disciple_play・traveller_exchange。CPU decidePending 全分岐（交換は常に実施＝終端保証）＋UI viewPendingModal 全分岐。
- **Batch5b（teacher＝Reserve＋山トークン）**：
  - teacher プレイ→酒場マットへ。`TAVERN_START_CALLS` に teacher 追加＋呼び出し窓の開閉を `anyTavernStartCallable` に集約（teacher は置き先がある時のみ開く）。
  - **山トークン新機構 `p.pileTokens = {card|action|buy|coin: 山id}`**（各種別1つ・各山1つまで・**公開情報＝非カードで保存則に無関係**・maskStateFor の Object.assign で残る）。呼び出し＝`teacher_call`（stage token→pile）でトークン1つを「自分のトークンが無いアクションのサプライ山」へ移動。**`validTeacherPiles(state,pi)`＝置き先候補**（非サプライ/騎士/分割山下段除外・**山が空でも可**＝公式）＝engine/CPU/UI で共有（公開API）。
  - **ボーナス `applyPileTokens`**：その山のカードをプレイしたとき、**効果解決より前に** +1カード/アクション/購入/コイン（`PLAY_ACTION` 内）。UI：サプライ山に教師トークンのバッジ（`pileTokenBadge`＋`.pile-tokens` CSS）。
- **公式ルール研究（wiki/RGG裏取り）で事前修正2点**：disciple のコピー獲得は非サプライ札を除外／warrior の廃棄は非ポーション$3/$4のみ。
- **多エージェント敵対レビュー（6次元→敵対検証・全件 空試験で再現）＝確定バグ4件（偽陽性0）→全修正（`7cda556`）**：
  - **[HIGH] exact-cost 強制獲得のCPUデッドロック**＝upgrade/remake/governor_remodel/forge が anyGainable/canGain で NON_SUPPLY を除外せず、ちょうど$Nの受け皿が成長先(warrior/fugitive=$4等)しか無いと CPU の bestGainExact が card:null を返し続け pending が閉じない無限ループ（$4は基本カードに受け皿が無く確実に嵌る）＋人間の不正獲得。両側述語に `!NON_SUPPLY.has(id)` 追加（farmland と同型）。
  - **[MED] 詐欺師(swindler) の贈与**＝pickSwindlerGift＋swindlerTrash の anyGainable＋SWINDLER_GAIN の canGain が NON_SUPPLY 未除外で同コストの成長先(champion/teacher等)を被害者に贈与できた（**賞品Prizes にも波及する潜在バグ**）。3箇所そろえて除外。
  - **[LOW] soldier の「+$1/他アタック」が durationCards を無視**＝inPlay＋durationCards の両方から数える。
  - **[LOW] validTeacherPiles が空アクション山を不当に除外**＝公式は空の山にも置ける。残枚数条件を外す。
- **許容簡略化（意図的・研究で公式挙動を確認済だが未対応）**：**champion の+1アクション／teacher の山トークンのボーナスは、玉座/王の宮廷/門下生の再演では発火しない**（`PLAY_ACTION` のみ・再演は `applyEffect` 経由）。公式は各再演で発火するが、冒険の固定出荷セットに玉座/王の宮廷は入らない見込み＝shipping影響ゼロ・保存則影響なし。
- **検証**：狙い撃ち 5a=54/54・5b=15/15・レビュー修正 12/12／invariants に「page/peasant×玉座/王の宮廷」「page/peasant×upgrade/remake/forge/swindler」の敵対王国2つ追加＝全4緑／npm test 全29緑（整合性3132不変）／トラベラー中心ソーク 5a=250戦・5b=220戦（完走・stuck0・例外0・保存則0）。

### 残り＝push（ユーザー確認）→ 帝国段階2　※Phase E は完了済み（上の「Phase E 完了」節が詳細）
冒険は**全38枚＋CARD_SET昇格まで完了**＝実プレイ可能。残るは push（本番反映・ユーザー確認）のみ。その後は帝国（Empires）段階2。

### Batch3 完了（2026-07-07・トークン基盤＋トークン系4枚）
- **トークン基盤3種**（すべてスカラー公開情報・`maskStateFor` の `Object.assign` でそのまま残る・JSONセーフ・旧スナップショット後方互換）：
  - **旅トークン `p.journeyDown`**（false=表向き。山守/巨人が**共有**・プレイ毎に裏返す＝per-player）。
  - **-1カードトークン `p.minusCard`**（遺物。単一boolean・非スタック）＝**`draw()` 冒頭にフック**して「次に1枚以上引くドロー」を1枚減らして返す（cleanup先引き限定でなく開始時持続ドロー等どの引きにも効く＝公式忠実。研究エージェントの指摘で cleanup限定案から変更）。
  - **-$1トークン `p.minusCoin`**（橋の下のトロル。単一boolean・非スタック）＝`END_ACTION_PHASE` で `t.coinPenalty` に変換し、`applyCoinPenalty(state)` が**購入フェイズで最初に得る$1に食い込む**（財宝＝`playTreasureCard` 末尾／財源＝`COFFERS_SPEND`／行動フェイズ稼ぎ＝END_ACTION_PHASE時に相殺）。**$0未満にならない**（`max(0,...)` 相当）。素朴な「buy開始時に coins-1」は財宝ぶんに効かず**トークンを空振り消化する誤り**＝回避済み（研究＆敵対レビューで確認）。`freshTurn` に `coinPenalty:0`。
- **4枚**（各 engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal＋新pendingの終端保証）：
  - **山守ranger**($4)：+1購入・旅トークンを裏返してから判定（**flip-then-check**＝初回は+5なし・2回目に+5カード・以後交互）。
  - **巨人giant**($5・アタック)：旅トークンを裏返す。裏で+$1／表で+$5＋アタック（各相手の山札上を公開＝$3-6は `trashCard` で廃棄[城塞/ネズミ等on-trash発火]・他は捨てて呪い。**山札空でも呪いを獲得**）。marauder型＝ATTACKS登録＋堀react窓（GIANT_REACT）。
  - **遺物relic**($5・財宝アタック・`coin:2`)：+$2＋各相手に-1カードトークン。marauder型（RELIC_REACT）。`playTreasureCard` で発火＝PLAY_ALL_TREASURES は pending で停止し残り財宝は再発行で継続（charlatan と同型）。
  - **橋の下のトロルbridge_troll**($5・持続アタック)：各相手に-$1トークン＋今と次+1購入＋**このターンと次ターン全カード$1安い**（`t.costReduction`。**コスト軽減はスタック**・-$1トークンは非スタック）。`DURATION_RESOLVERS.bridge_troll` が次手番開始時に +1購入＋costReduction を再付与（アタックは初回のみ）。玉座×トロルも seaside 持続の armDuration+cnt 機構でそのまま動く。
- **CPU**：chooseAction に bridge_troll/giant/ranger（冒険ターミナル群）。decidePending に relic/giant/bridge_troll の react（堀→MOAT_REVEAL／無ければ X_REACT・効果は自動）。relic は財宝＝chooseAction不要。GAIN_ORDER は既に全id網羅。
- **UI**：viewPendingModal に relic/giant/bridge_troll の react モーダル（reactOptions）。盤面に冒険トークンのバッジ表示（旅=裏向き時のみ・-1カード・-$1）。
- **検証**：狙い撃ち60/60（flip-then-check・空山呪い・-$1が財宝/財源に食い込む・堀防御・持続の持ち越しとコスト軽減継続・旅トークン共有）／invariants全プール混成fuzz緑（exit0）／npm test 全29緑（整合性3132不変）／冒険中心120戦ソーク=膠着0・保存則0・例外0・トークン異常0。
- **敵対レビュー（多エージェント5次元→敵対検証）＝確定バグ1件（偽陽性0）→修正済**：**-$1トークン×財源**＝財宝を出さず財源(Coffers)だけで賄うターンで `COFFERS_SPEND` が `applyCoinPenalty` を呼ばず、トークンが空振り（保存則は保つが ruling#3 逸脱・全プールfuzzで到達可）→ COFFERS_SPEND に `applyCoinPenalty(state)` を追加＋回帰テスト。
- **許容簡略化**：-1カードトークンの「相手ターン中のリアクションドロー（隊商の護衛等）で先に消化される」厳密タイミングは未対応（隊商の護衛は未実装・研究でも relic への反応ドローはトークン付与前に解決＝影響なしと確認）。書庫の `p.deck.shift()` 直接ドローは draw() を通らないので -1カードが効かない（書庫は冒険外・稀）。

### Batch4 完了（2026-07-07・酒場マット/Reserve 全9枚）＝`34aa8c4`(4a)＋`98e483b`(4b)＋レビュー修正
- **酒場マット `p.tavern[]` ゾーン基盤**（公開＝islandMat型）：player初期化・`allCards`（VP/庭園/公爵に数える）・**invariants の ZONES に `'tavern'` 追加**・maskStateFor は Object.assign で公開のまま。cleanupAndAdvance はマットに触れない（呼び出しまで残る）。`putOnTavern()`＝Reserve をプレイ直後に場→マットへ移す共通入口（**island/祝宴と同じ自己移動ガード必須**＝下記バグ参照）。
- **Batch4a（6枚・自己完結トリガー）**：守銭奴miser(手札の銅貨をマットへ／マットの銅貨1枚+$1・非Reserve)／遠隔地distant_lands(マット上で4VP＝`vpOf`専用clause・固定vpは持たせない)／鼠取りratcatcher・案内人guide・変容transmogrify(ターン開始コール＝`resolveDurationStartEffects`で`tavern_start`をstartQueueへ→`TAVERN_START_CALL`で1枚ずつ→効果→`offerTavernStart`で再オファー)／ワイン商wine_merchant(購入フェイズ終了コール＝END_TURNの後処理を`endBuyTail()`に抽出しその前に窓を挟む)。
- **Batch4b（3枚・外部トリガー）**：法貨coin_of_the_realm(財宝・`coin:1`)／御料車royal_carriage(アクション再演＝`state.replay`にlabel:'royal_carriage')＝**アクション解決直後フック**（PLAY_ACTIONで`t.afterActionCard`を記録→reduce()末尾ネットが`after_action`窓→`AFTER_ACTION_CALL`が保持したまま再オファー・辞退/候補ゼロ/END_ACTION_PHASEでクリア）。複製duplicate＝**獲得時フック**（triggerOnGainの対話ゲート末尾・$6以下・自手番・トップレベル→`DUPLICATE_CALL`はpending保持でコピーgain＝入れ子オファー抑止→別の複製で再オファー）。
- **新pending8種**（miser/tavern_start/ratcatcher_trash/transmogrify_trash/transmogrify_gain/wine_merchant/after_action/duplicate）＝すべて engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal＋終端保証（呼び出しでReserveが場へ移り消費＝有限）。
- **敵対レビュー（多エージェント6次元→敵対検証）＝確定バグ1件[HIGH]（偽陽性0・空試験で再現確認）→修正済**：**`putOnTavern` が `removeOne(inPlay)` の成否を無視して push**＝玉座/王の宮廷/行進でReserveを複製プレイすると2回目の`applyEffect`で「場に無いのにマットへ push」→**マットに幻のカードが増殖（保存則違反）**。→ island/祝宴/宝の地図と同じ `if(removeOne(p.inPlay,cardId)){...push...}` ガードに修正。行進×Reserve は「マットへ移動＝lose track で廃棄不発・格上げは獲得」で公式どおり。invariants に玉座/王の宮廷/行進×Reserve の敵対王国を追加（CPU駆動＝ベストエフォート）。**Phase E の adventures.test.js で throne/KC/procession×Reserve の強制保存則テストを必ず入れること**（CPUが必ずしもReserveを玉座対象に選ばないため）。
- **許容簡略化（意図的）**：(1)1獲得につき on-gain 対話は1つだけ（複製の窓は他の on-gain 対話が立たない獲得でのみ）(2)複製のコピー gain は「コピー自身の on-gain 対話」を抑止（pending保持のため。border_village等をコピーしても格下げ獲得の対話は出ない・自動on-gainは発火）(3)アクション解決直後の窓は**トップレベルの PLAY_ACTION のみ**（呼び出したReserveや玉座サブプレイでは開かない）。いずれも保存則OK・非ループ。
- **検証**：狙い撃ち 4a=40/40・4b=34/34・回帰(玉座/KC/行進×Reserve)=17/17／invariants全プール混成fuzz緑／npm test 全29緑（整合性3132不変）／酒場マット中心ソーク 4a=135戦・4b=135戦・全機構混成200戦（膠着0/保存則0/例外0/tavern異常0）。

### この着手順の理由（重要）
- **冒険/帝国は fuzz（invariants.test.js §B「全プール混成」）が引く**＝1枚実装した瞬間から「完全な4点セット（engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal）＋新pendingの終端保証＋新ゾーンの保存則tally配線」が必須。段階1の無効果カードのうち一部だけ効果化すると fuzz が壊れる。
- そこで**新ゾーン不要・既存機構だけで足せるカードから**着手中（安全順）：単純→純粋な持続→（次）トークン→（次）酒場マット/Reserve→（次）トラベラー→複雑。

### 完了（各バッチ＝fuzz緑＋狙い撃ちテスト＋全29スイート緑でコミット済み）
- **Batch1a**（`9d60c22`）単純4枚：**港町port**(+1c+2act・on-buyでもう1枚＝BUY内)／**失われし都市lost_city**(+2c+2act・on-gainで他全P+1カード)／**カササギmagpie**(公開→財宝は手札・アクション/勝利点でカササギ獲得)／**掘出物treasure_trove**(財宝・`coin:2`追加・金貨+銅貨獲得)。
- **Batch2**（`71d93f5`）純粋な持続3枚：**雇人hireling**(永続持続＝princes同型。`p.hirelings`稼働数を cleanup cnt に加算し durationCards に残す／resolveDurationStartEffects で各手番開始時+1カード)／**地下牢dungeon**(+1act・今と次の手番に+2カード→2枚捨て・次ぶんは startQueue)／**道具gear**(+2c・手札最大2枚を脇→次手番に戻す＝haven型)。
- **Batch2b**（`5b094a3`）**魔除けamulet**(持続・今と次の手番にそれぞれ +$1／手札1枚廃棄／銀貨獲得 の3択。次ぶん startQueue=viaStart・trashは amulet_trash サブpending)。

### 重要な設計事実（次セッションが知らないと事故る）
- **このエンジンの手番タイミング**＝`cleanupAndAdvance` は「自分の手番終了時に**自分の次の手札を先引き**（draw 5）」してから次プレイヤーへ回す。**持続の「手番開始時」効果（`resolveDurationStartEffects`）は手番が戻ってきた時に発火**し、先引き済みの手札に加算される（例：雇人は5枚先引き＋開始時+1＝6枚）。テストで「手番開始効果」を確認するときは END_TURN 1回では足りない（相手を回して自分の手番へ戻す）。
- **-$1/-1カード/旅トークンは Batch3 で実装済**（実装詳細は上の「Batch3 完了」節）。-$1は coins を負にできない＝`applyCoinPenalty`（`t.coinPenalty` を「最初に得る$1」に食い込ませる。素朴な「buy開始時に coins-1」は財宝ぶんに効かず空振り＝**採用しないこと**）。-1カードは `draw()` 冒頭フック（cleanup先引き限定ではない）。旅=`p.journeyDown`（false=表向き・山守/巨人が共有・flip-then-check）。
- **財宝の実プレイ化には `coin:` が要る**（段階1カタログには無い）。`treasureCoins`=`C()[id].coin`。表示テキスト不変＝**webp再生成不要**。relic に `coin:2` 追加済（掘出物と同型）。

### バッチ進捗（着手順・完了状況）
1. ~~**Batch3＝トークン基盤＋トークン系カード**~~ ✅ **完了**＝ranger/giant/relic/bridge_troll＋旅/-1カード/-$1トークン。
2. ~~**Batch4＝酒場マット/Reserve 基盤＋Reserveカード9枚**~~ ✅ **完了**＝守銭奴/遠隔地/鼠取り/案内人/変容/ワイン商/法貨/御料車/複製。
3. ~~**Batch5＝トラベラー（page/peasant＋成長先8）**~~ ✅ **完了（2026-07-07）**＝上の「Batch5 完了」節が詳細。
4. ~~**Batch5c＝純持続/アタック3枚（隊商の護衛/呪いの森/沼の妖婆）**~~ ✅ **完了（2026-07-08）**＝上の「Batch5c 完了」節が詳細。
5. ~~**Batch6＝複雑4枚（倒壊/工匠/語り部/使者）**~~ ✅ **完了（2026-07-08）**＝上の「Batch6 完了」節が詳細。**これで冒険 全38枚のカード効果 実装完了**。
6. ~~**Phase E＝CARD_SET昇格**~~ ✅ **完了（2026-07-08）**＝上の「Phase E 完了」節が詳細。**冒険は実プレイ可能**。
7. **【次はここ】＝push（ユーザー確認）→ その後 帝国（Empires）段階2**。

---

## 0-8. 段階2＝暗黒時代56枚 実装 **完了**（2026-07-06・全56枚実プレイ化＋UI＋テスト＋CARD_SET昇格＋敵対レビュー修正）

### 完了サマリ（2026-07-06。**`8a6f430` まで push済＝本番 Pages/Render に反映済み（sw.js v35）**）
- **カード効果56/56枚 実装完了**。前回までの24枚に加え、今セッションで残り32枚を Group A〜E で実装（各 engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal の4点セット）:
  - **A（素直系）** junk_dealer/mystic/altar/bandit_camp/catacombs/hunting_grounds＋feodum VP。**on-trash対話キュー `state.onTrashQueue`** を新設（地下墓所/狩場/従者の獲得対話をアタック中の廃棄と競合させず reduce 末尾で1件ずつ消化）。
  - **B（trash-to-gain）** graverobber/rebuild/count。
  - **C（複雑）** band_of_misfits(命令)/death_cart(on-gain廃墟2)/hermit(狂人交換=buyPhaseGainedフラグ+END_TURN)/counterfeit(財宝2回)/procession(runReplaysにprocession2/procession_finishラベル)。
  - **D（アタック）** marauder/cultist(連鎖)/pillage/rogue/urchin(→傭兵)/mercenary＋汎用 `discard_down`（民兵型embedded・浮浪児4/傭兵3/sir_michael3で共用）。
  - **E（騎士10種）** 混合山アタック（上2枚公開→被害者が$3-6を選び廃棄→騎士なら攻撃騎士も相討ち=sourceCard）。dame_anna/natalie/sir_michael の前段対話・sir_martin($4)・sir_vander(相討ちで金貨)・dame_josephine(2VP)。
- **【実バグ修正2件（副産物）】** (1)`reshuffleDeck` が山札を**置換**しており、山札の上N枚を見る系（旅の楽団/生存者/地下墓所）が残り<Nの非空山札で札を消失させる保存則バグ→**append方式**に修正（全プール混成fuzzで検出）。(2)CPUの `discard_down` に堀チェック漏れ→既存の汎用アタック免疫テストが捕捉→修正。
- **UI**：全pending（既存24＋新32）のモーダル＋混合山（騎士＝一番上表示・廃墟＝獲得専用の山表示）。
- **テスト新設**：`darkages.test.js`（70件・経路別on-trash＝城塞×礼拝堂/狂信者×死の荷車/封土×騎士 ほか＋敵対レビュー回帰8件）＋`darkages-ui.test.js`（57件）。`package.json` 登録。**全29スイート緑（整合性3132）**。
- **CARD_SET昇格**：`DOM.KINGDOM_DARKAGES`＝公式「Grim Parade」（armory/band_of_misfits/catacombs/cultist/forager/fortress/knights/market_square/procession/hunting_grounds）＝`darkages` 固定セット＋`random-darkages`。**避難所は「王国が KINGDOM_DARKAGES と内容一致」で `createInitialState` が自動ON**（opts不要＝local/再戦/オンライン全経路で機能）。random系はOFF。invariants の出荷セット検証にも追加。
- **敵対レビュー（多エージェント8次元→敵対的検証・確定10件/偽陽性0）→全10件修正**（コミット `587e747`）:
  - **リアクション3種を新規実装**（§0-8決定4の既知TODO・出荷darkagesセットで到達）：青空市場(on-trash→金貨)・納屋(on-gain勝利点→廃棄)・物乞い(被弾時→銀貨2枚・免疫にはならない=horse_traders型)。
  - はみだし者の対象から騎士の混合山 'knights' を除外（無効果の死に選択肢だった。captainTargetsも）／BoM×死の荷車の自身廃棄を不発化(fromCommand)／偽造通貨×偽造通貨の2回目+1購入を追加／傭兵の1枚廃棄を許可(効果不発)／浮浪児の狂信者連鎖トリガー(maybeUrchinTrap)。
- **webp9枚再生成**（hermit/procession/pillage/death_cart/rats/counterfeit/marauder/cultist/band_of_misfits＝§0-8でカタログを現行エラッタに更新した分）→ `sw.js` v34→**v35**。death_cart/band_of_misfits を目視確認。
- **CPUソーク**：出荷2セット（darkages/random-darkages・2〜4人・全難易度）**280戦 完走280/stuck0/保存則0/例外0**（納屋反応175回発火）。

### 残タスク・許容簡略化（詳細は下の「決定事項」「詰まり」参照）
- **push 済（`8a6f430`・2026-07-06）**＝本番 Pages/Render・sw.js v35 反映済み。**次の大仕事＝冒険/帝国の段階2（§5-2）**。
- **許容簡略化（意図的・再議論しない）**：浮浪児→傭兵の玉座/行進経由リプレイは未対応（狂信者連鎖のみ対応・低頻度＆任意得のみ）／on-trash対話キューと廃棄札の続きの獲得の順序は多少前後し得る／band_of_misfits・procession は持続カードを対象外（船長と同型）／納屋on-gainは自分の手番のトップレベル勝利点獲得のみ（相手ターン/入れ子獲得の稀な反応は未対応）。

### （以下は実装中の設計メモ＝リファレンスとして保持。現在は上の「完了サマリ」が最新）
### 旧・現在地（実装当時のメモ）
- **完了①＝公式ルール研究**（成果物＝**`docs/research/darkages_rules.json`**＝55枚の現行英文/エラッタ/裁定/山構成、**`docs/research/darkages_catalog_diff.md`**＝差分レポート）。**実装前に該当カードを必ず読む**。
- **完了②＝カタログ現行化**（cards.js/carddata.js/integrity.test.js。エラッタ6枚＋looter/command種別。整合性3130）。
- **完了③＝triggerOnTrash 自動系6枚**（城塞=手札へ戻り戻り値false／ネズミ・草茂る屋敷=+1カード／封土=銀貨3／サー・ヴァンダー=金貨1／狂信者=+3カード。誰の廃棄でも持ち主に発動）。
- **完了④＝trashCard 統一関数＋on-trash第2層配線（今セッション）**：`trashOwn`→`trashCard(state,owner,card)`にリネーム＋**戻り値=trashに残ったか**（城塞=false）。本人任意廃棄（礼拝堂/司教/改築/拡張/溶鉱炉/仮面舞踏会/引揚水夫/金貸し/取壊し/身代わり/総督/鉱山/改良/衛兵/見張り/物見やぐら/水晶玉/remake/forge/sentry/salvager/bishop等）とアタック廃棄（詐欺師/破壊工作員/山賊/盗賊thief/私掠船corsair＝owner=被害者）を寄せた。明示除外（自己廃棄札=投資/祝宴/宝の地図/鉱山の村/豊穣の角・lurkerのサプライ廃棄）は据え置き。
- **完了⑤＝基盤機構（今セッション）**：**混合山**＝騎士は`supply.knights`（数値・王国枠・購入可）＋`state.knights`（実カード配列）／**廃墟は`state.ruins`配列のみ**（`'ruins'`はカタログ非在＝supplyに持つとCPU/UIのsupply走査が`C()['ruins']`で落ちるため）。gain冒頭で山先頭を実カードid（survivors/sir_martin）に解決しshift。`cardCost('knights')`=山の一番上の実コスト（sir_martin=$4）。`canBuyCard('ruins')`=false。emptyPileCountで`state.ruins.length===0`を明示加算。maskStateForで混合山は先頭1枚だけ公開。invariants tallyは混合山を実カード(state.ruins/knights)で数え supply数値のruins/knightsはskip。非サプライ＝NON_SUPPLYにspoils/madman/mercenary追加。避難所＝`opts.shelters`で開始デッキの屋敷3枚→納屋/共同墓地/草茂る屋敷。initSupply条件節＝rats20固定・looterで廃墟state・spoils15/madman10/mercenary10。**全プール混成fuzz緑**。
- **完了⑥＝カード効果24/56枚（今セッション）**：単純15（廃墟4種abandoned_mine/ruined_library/ruined_market/ruined_village・城塞・共同墓地・貧民街poor_house・放浪者vagrant・賢者sage・物乞いbeggar・狂人madman・青空市場market_square・戦利品spoils）＋対話9（生存者survivors・ネズミrats・武器庫armory・採集者forager・従者squire+on-trashアタック獲得・倉庫storeroom・清掃scavenger・鉄物商ironmonger・放浪楽師wandering_minstrel）。**engine reducer＋PLAYER_ACTIONS＋CPU decidePending の4点セット済み（UIは未実装）**。CPU側は`NON_SUPPLY_SET`（PRIZE_SET＋戦利品/狂人/傭兵）で汎用獲得の無限ループを防止。
- **未着手（残り）**：**カード効果32枚**＝中盤14（屑屋junk_dealer/秘術師mystic/墓暴きgraverobber/地下墓所catacombs/建て直しrebuild/伯爵count/はみだし者band_of_misfits(命令)/盗賊rogue/略奪pillage/山賊の宿営地bandit_camp/偽造counterfeit/狩場hunting_grounds/祭壇altar/隠遁者hermit）＋アタック（略奪者marauder/狂信者cultist/傭兵mercenary/浮浪児urchin）＋**騎士10種**＋death_cart(on-gain廃墟2枚)。／**UI**（viewPendingModal 新pending分岐＋混合山top表示）／**darkages.test.js・darkages-ui.test.js**／**CARD_SET昇格**（darkages固定10種＋random-darkages）／敵対レビュー＋CPUソーク／webp9枚再生成＋sw.js v35。

### 決定事項（ユーザー委任により確定。再議論しない）
1. **避難所＝固定 `darkages` セットのみON**（`createInitialState` の `opts.shelters`。CARD_SET昇格時に darkages だけ true を渡す）／random系はOFF。
2. **on-trash第2層は完了④で配線済み**。第1層＝新規暗黒時代カードは最初から `trashCard` を使う。第3層＝lurker/自己廃棄札は明示除外（据え置き）。
3. **経路別の明示テスト必須**（保存則fuzzは「城塞がtrashに残ったまま」を検知できない）：城塞×礼拝堂／狂信者×死の荷車／封土×騎士 等をdarkages.test.jsに。
4. 対話系on-trash（catacombs=安い獲得／hunting_grounds=公領or屋敷3／squire=アタック獲得）とリアクション（market_square/hovel/beggar）は pending/キューが別途必要。**squireのon-trashは`!state.pending`ガードの簡略実装済み**（複数on-trash競合は先着のみ＝許容簡略化）。catacombs/hunting_groundsのon-trash対話はまだ未実装。

### 実装設計の要点（下敷き＝§0-7の各機構＋docs/adding-cards.md。裁定の正本＝docs/research/darkages_rules.json）
- **混合山（実装後の確定形）**＝**騎士**は`supply.knights`＋`state.knights`（購入・獲得とも `gain(pi,'knights')` で山先頭を取る）。**廃墟**は`state.ruins`のみ（`gain(pi,'ruins')`で配布。購入不可）。`gain`は`isMixed`分岐で `state[cardId].shift()` し、supply.knightsがあればデクリメント。UIは混合山を「先頭1枚（maskで公開）」で描画する（Todo10）。
- **騎士アタック**＝thief/saboteur型の複合（山札上2枚公開→$3-6を**被害者が選んで**廃棄→残り捨て札→**騎士が廃棄されたら攻撃騎士も廃棄**）。dame_anna（先に自分の廃棄≤2）/sir_michael（先に手札3枚まで捨て=民兵型）/dame_natalie（≤$3任意獲得）は前段付き。cardCost('knights')は山の一番上で変動。
- **はみだし者(band_of_misfits・命令)**＝既存 captainTargets/CAPTAIN_PLAY の同型（上限=自身の現在コスト-1・非Command・サプライに残したまま使用）。
- **隠遁者交換(hermit)**＝END_TURN（cleanup前）で「購入フェイズ中にgainゼロ」判定→隠遁者を山へ戻し狂人を捨て札へ（**gainでなく交換＝獲得フック不発・狂人山空なら不成立**）。turn に「購入フェイズ中に獲得したか」の旗が要る。
- **death_cart**＝on-gain（triggerOnGain）で廃墟2枚を`gain(pi,'ruins')`。プレイ時は「これ or 手札のアクション1枚を廃棄（任意）→廃棄したら+$5」。
- **主要裁定**（rules.json rulings 参照）：行進×城塞=廃棄成立で獲得実行／浮浪児=「**先に**」廃棄→傭兵獲得→アタック解決／青空市場=相手ターンでも・1廃棄に複数枚反応可／偽造通貨×戦利品／伯爵count=独立2段階の三択。

### 次の一歩（この順で・各グループ末で `node test/invariants.test.js` 緑を確認しコミット）
1. **カード効果の残り32枚**：素直なpending（屑屋/秘術師/墓暴き/地下墓所/建て直し）→ アタック（略奪者/狂信者/略奪/傭兵/浮浪児/盗賊）→ **騎士10種**（混合山アタック）→ 複雑（伯爵count/はみだし者band_of_misfits命令/隠遁者hermit交換/death_cart on-gain/catacombs・hunting_groundsのon-trash対話/counterfeit/altar）。各カード engine reducer＋PLAYER_ACTIONS＋CPU decidePending の4点セット必須（UIは2.でまとめて）。
2. **UI**（ui.js `viewPendingModal` に新pending分岐＋混合山topの盤面表示）。
3. **darkages.test.js / darkages-ui.test.js 新設**（経路別on-trashテスト＝城塞×礼拝堂/狂信者×死の荷車/封土×騎士 は必須）。
4. **CARD_SET昇格**（DOM.CARD_SETS に darkages固定10種＋random-darkages。darkagesにだけ `opts.shelters=true` を渡す配線）→ **全27+スイート緑**（新スイート含む）。
5. 敵対レビューWorkflow→確定バグ修正→CPUソーク（240戦級）。
6. **webp9枚再生成**（hermit/procession/pillage/death_cart/rats/counterfeit/marauder/cultist/band_of_misfits＝文言/種別変更ぶん。`CARDS_ONLY=<ids> node tools/build-cards.js`・このPCのみ可）→ sw.js v34→v35 → PROGRESS更新 → コミット（**pushはユーザー確認**）。

---

## 0-7. 段階2＝新プロモ6枚（王子/船長/教会/サウナ/アヴァント/へそくり）を実プレイ化（2026-07-05 完了）

### 結論
- **新プロモの王国カード6種を段階2（実プレイ・現行エラッタ準拠）で実装完了**。`DOM.CARD_SETS` に `promo2-pack`（固定10種＝moat/village/militia/smithy/market＋stash/prince/captain/church/sauna）を追加し**出荷済み**。既存 `random-promo`（basic+intrigue+promo 抽選）でも 6種が実プレイになった。`sw.js` v33→**v34**。テスト **27スイート全緑**（`promo2.test.js` 141件＋`promo2-ui.test.js` 22件を新設・`package.json`登録・`invariants` に `promo2-pack` セット＋`princes` ゾーン追加）。整合性 3115→**3122**。CPU序列 100/64/95 維持。
- **公式ルールは多エージェント研究＋敵対検証Workflowで確定**（wiki.dominionstrategy.com/wikiwiki/RGG公式FAQ で裏取り）。**王子/船長は現行エラッタで種別が アクション-持続-命令（Action-Duration-Command）に変更**済み＝それを採用（王子=2022改訂・船長=2019改訂。「動かさずに使用」＝場に出さずプレイ）。webp（prince/captain）を新種別・新文言で再生成。carddata に `アクション・持続・命令` の複合ラベル追加。
- **新機構をすべて新設（簡略化なし）**：
  - **分割山（サウナ/アヴァント）**＝10枚1山（上5サウナ・下5アヴァント）。`supply.sauna/avanto` 各5＋「上が尽きるまで下は取れない」を **4系統ガード**（`gain()`冒頭・`canBuyCard`・`emptyPileCount`はペアで1山・CPU `splitBlocked`）で表現。抽選 `randomKingdom` は avanto→sauna に正規化（1山ぶんの枠）。`createInitialState` が sauna⇔avanto を相互補完。**`finishGain`/`SMUGGLERS_GAIN` は gain() の戻り値を検証**（拒否カードで pending を閉じない）。
  - **サウナ/アヴァント連鎖**（`sauna_chain` pending）＝アクション権を消費せず相方をプレイ。**サウナの銀貨トリガー**＝`t.saunaPlays`（このターンの使用回数）ぶん、銀貨を使うたび手札1枚を廃棄してよい（`sauna_trash` pending・+2コイン計上後・玉座で累積）。`playTreasureCard` の銀貨分岐＋TIARA_PLAY の2回目にも配線（§敵対レビューで後者の漏れを修正）。
  - **教会**（アクション-持続）＝手札最大3枚を伏せて脇（`church_setaside`）→次ターン開始時に手札へ戻し任意廃棄（`DURATION_RESOLVERS.church`→`church_trash` を startQueue へ・0枚でも廃棄機会）。脇置きは相手にマスク（setAside＋delayedEffects.stashed 両方伏せる）。
  - **船長**（アクション-持続-命令）＝現在と次ターン開始時、サプライの $4以下・非持続/命令アクションを**サプライに残したまま**使用（`captain` pending・`captainTargets` を engine/CPU/UI で共有）。自己移動（採掘村の廃棄等）は removeOne 失敗で**自然に不発**（+2コインも出ない＝公式）。次ターンぶんは `DURATION_RESOLVERS.captain`→startQueue。アタックもサプライから通常どおり機能（堀/リアクション窓OK）。
  - **王子**（アクション-持続-命令・現行）＝手札の $4以下・非持続/命令アクションを脇（`p.princes[]`）に置き（`prince_setaside`）、**毎ターン開始時に脇のまま強制プレイ**（`prince_play`・resolveDurationStartEffects が startQueue へ）。置いた王子は**持続としてゲーム終了まで場に残る**（cleanupAndAdvance が `cnt.prince += princes.length`）。脇のカードも所有カード（`allCards` に princes）。玉座×王子＝2枚脇置き（現行公式）。自己移動する対象（島/宝の地図）は複製しない＝removeOne ガード（§敵対レビューで island の欠落を修正）。
  - **へそくり**（財宝$5+2コイン）＝「シャッフル時に山札の好きな位置へ」。シャッフルは効果解決中に同期発生し対話を挟めない（業界最大手も未実装の難物）ため、**常設方針 `stashPlacement`（top既定/mix/bottom・`STASH_SETTING` で本人がいつでも変更）で自動配置**。全リシャッフルを共通入口 `reshuffleDeck(p)`（37箇所を一括置換）＋`placeStash(p)` に集約。裏面が異なる＝**山札/手札/脇のへそくり位置は公開情報**（maskStateFor が位置だけ晒す）。
  - **startQueue 安全網**（`reduce()` 末尾）＝王子/船長がターン開始時にアタック等を使うと連鎖の終端が pending=null で閉じるだけで後続の開始時効果を取り残す→「pending無し＆startQueue残」を検知して popStartQueue する一括救済。
- **新pendingは全て4点セット**（engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal）＝8種（PRINCE_SETASIDE/PRINCE_PLAY/CAPTAIN_PLAY/CHURCH_SETASIDE/CHURCH_TRASH/SAUNA_CHAIN/SAUNA_TRASH/STASH_SETTING）。CPU＝chooseActionに6枚＋`bestPrinceTarget`（PRINCE_AVOID=島/宝の地図）＋chooseBuyのstash分岐＋GAIN_ORDER実強度順再配置。UI＝へそくり配置トグル・王子の脇チップ（👑）表示。

### 敵対的レビュー（多エージェント8次元Workflow）＝確定バグ2件（両方修正・回帰テスト済）＋自己検証6次元クリーン
- **【中】王子×島の複製**（保存則違反・VP無限増殖）：王子で島を「動かさず使用」すると `case 'island'` が inPlay に島が無いのに `islandMat.push` し、幻の島が毎ターン増殖。→ `removeOne` 成功時のみ push（treasure_map/祝宴と同型ガード）。**黒市場経由で島を入手＋王子＝到達可**（CPUは PRINCE_AVOID で回避するため CPU戦では出ない）。
- **【低】ティアラ×サウナの銀貨トリガー漏れ**：ティアラの2回目は `playTreasureCard` を通らず、銀貨の2回目でサウナの廃棄機会が立たなかった（remaining が1のまま）。→ TIARA_PLAY の2回目副次効果に silver 分岐を配線（1回目の sauna_trash に合算）。黒市場経由の稀ケース。
- **自己検証（Workflowがセッション上限で8/10中断→残りをmain側で直接再現検証）**：船長で各種$4以下アタック（民兵/役人/追いはぎ）をサプライからプレイ→リアクション/堀/保存則OK・王子で獲得系（工房）を脇置き→毎ターン獲得pending→startQueue安全網OK・分割山の全獲得経路（BUY/闇市場/改築/密輸人/物見やぐら型on-gain）でavantoガードOK・anyGainable×avanto の極端局面でも無限ループ無し・教会のマスク漏れ無し。**CPUソーク約240戦（promo2-pack/random-promo 2-4人）で stuck/例外/保存則違反ゼロ**。

### 次（未着手）＝段階2の残り拡張（§5-1）
- **着手順＝ ~~収穫祭~~✅ → ~~ギルド~~✅ → ~~異郷~~✅ → ~~新プロモ~~✅(§0-7) → 暗黒時代を全56に完成**。暗黒時代＝廃墟/騎士の混合山・避難所・戦利品/狂人/傭兵・要塞等のon-trash等（特殊山は§6「4系統除外チェックリスト」必須）。設計図＝`docs/adding-cards.md`（分割山/「動かさず使用」/永続持続/startQueue安全網/シャッフル介入 の手順を §0-7 で追記済み）。
- **冒険/帝国の段階2（実プレイ化）は別の大仕事**（Reserve/酒場マット/トラベラー交換/旅トークン/負債経済/分割山/城/命令/勝利点トークン/集合）＝暗黒時代の後。

---

## 0-6. 段階1＝冒険（Adventures）＋帝国（Empires）74枚を画像化・カタログ追加（2026-07-05 完了・`fcfae02`）

### 結論
- **冒険＋帝国の縦カード全74枚を段階1で追加**（＝画像は出るがゲーム未参加。CARD_SETS 未参照）。内訳＝**冒険 王国30＋トラベラー成長先8＝38**／**帝国 非分割18＋分割両面10＋城8＝36**。`DOM.CARDS`＋`POOLS.adventures/empires`＋`GAIN_ORDER`（74id）＋ui.js一覧グループを追加。`npm test` 25スイート全緑（整合性 2417→**3115**）。`sw.js` v32→**v33**。**webp74枚生成済（`asset/cards/*.webp`）**。
- **カード定義（公式和名/コスト/負債/種別/効果テキスト）は多エージェント研究＋校閲Workflowで確定**（wikiwiki/fandom/dominionstrategy.com で裏取り・全74枚 confirmed）。研究データは scratchpad に保存（`adv_emp_carddata.json`）。
- **新種別の表示対応**を carddata.js に追加：reserve(リザーブ)/traveller(トラベラー)/castle(城)/command(命令)＋action+treasure(冠)＋duration+reaction(隊商の護衛)の複合ラベル。integrity の JP/EN マップにも登録（種別ラベル網羅テストが緑）。frameType は base type に落ちるので新スキン不要。
- **帝国の負債(Debt)コスト**を build-cards.js に**オレンジ六角トークン**で新規描画。負債のみ札（技術者/市街/大君主/王室の鍛冶屋＝cost:0,debt:8等）はコイン位置に大トークン、大金（cost:8+debt:8）はコイン＋小トークン。carddata に `debt` 透過。
- **画像回収**＝Downloads の74枚を生成時刻でバッチ（A1〜E4）割当→ステージング→**多エージェント視覚判別**（各画像を Read で実見し id↔絵を一意確定）。**A1の最初2枚が生成逆順**（page↔coin_of_the_realm）だったのを訂正、他73枚は指示順どおり・全て high 信頼。→`asset/art/<id>.png` 回収→`CARDS_ONLY` で新74枚のみ webp生成（既存を再エンコードしない）。モンタージュで全74枚 目視OK（負債トークン・城の序列 粗末→王城 も正しい）。

### 完全性（作った74枚 vs 公式全カード）＝**縦カードは冒険/帝国とも完全網羅・抜けなし**
- **✅冒険38枚**＝王国30（法貨/トラベラー起点 page/peasant 含む）＋トラベラー成長先8（treasure_hunter/warrior/hero/champion/soldier/fugitive/disciple/teacher）。**縦カードの抜けゼロ**。
- **✅帝国36枚**＝非分割18＋分割両面10（陣地/鹵獲品・パトリキ/エンポリウム・開拓者/騒がしい村・投石機/石・剣闘士/大金）＋城8（粗末→王城）。**縦カードの抜けゼロ**。
- **❌未作成＝横長ランドスケープのみ**（現行の縦型枠768×1152では非対応のため意図的に除外）：**冒険 イベント20種**（Alms/Borrow/Quest/Save/Scouting Party/Travelling Fair/Bonfire/Expedition/Ferry/Plan/Mission/Pilgrimage/Ball/Raid/Seaway/Trade/Lost Arts/Training/Inheritance/Pathfinding）＋**帝国 イベント13種＋ランドマーク20種**。＝計約53枚。これらは横長枠パイプラインを作る時に別途。
- **拡張パックの抜け**：発売順で冒険/帝国の次＝**夜想曲/ルネサンス/移動動物園/同盟/略奪/旭日** は未着手（画像・カタログとも無し）。

### 次（この段階1の続き）
- **冒険/帝国を段階2（実プレイ化）するのは大仕事**：酒場マット(Reserve)・トラベラー交換・旅トークン・持続多数・負債コスト経済・分割山・城の混合山・命令(overlord/crown)・勝利点トークン・集合(Gathering)等の新機構が必要（`docs/adding-cards.md` に追記してから着手）。段階2の着手順（§5-1）では **新プロモ→暗黒時代完成 が先**。冒険/帝国段階2はその後。

---

## 0-5. 段階2＝異郷（Hinterlands）35枚を実プレイ化（2026-07-05 完了）

### 結論
- **異郷の王国カード35種を段階2（実プレイ・完全忠実）で実装完了**。`DOM.CARD_SETS` に `hinterlands`（固定10種 `KINGDOM_HINTERLANDS`）＋`random-hinterlands` を追加し**出荷済み**。`sw.js` v31→**v32**。テスト **25スイート全緑**（`hinterlands.test.js` 83件＋`hinterlands-ui.test.js` 44件を新設・`package.json`登録・`invariants` に hinterlands/random-hinterlands も追加）。整合性 2415→**2417**。CPU序列 100/64/95 維持。
- **固定10種 `DOM.KINGDOM_HINTERLANDS`**＝岐路/愚者の黄金/開発/オアシス/トンネル/何でも屋/絹の道/値切り屋/辺境伯/国境の村（on-gainトリガー・可変VP・on-discardリアクション・on-buy・アタック・財宝リアクションを味わえる showcase）。**公式の異郷専用10種は存在しない**＝常に混成なので自作。異郷は**特殊山・非サプライ・持続カードが無い**＝「4系統除外チェックリスト」不要でシンプルな部類。
- **新機構をすべて新設（簡略化なし）**：
  - **on-gainトリガー**（triggerOnGain 拡張）：自動＝キャッシュ(銅貨2)/大使館(他者銀貨)/不正利得(他者呪い・非アタック＝堀不可)/遊牧民の野営地(山札上)/遊牧民(+2コイン)/役人(場の財宝を山札上)。対話＝国境の村(安いカード獲得)/宿屋(捨て札アクションを山札へ混ぜる)/スーク(手札2枚廃棄)/公爵夫人(公領獲得で公爵夫人)/狂戦士(獲得時プレイ)。獲得時対話ゲート `_gainDepth===1 && !pending` の else-if 連鎖（1獲得=1対話）。
  - **on-discardフック**（`triggerOnDiscard`）：トンネル(金貨自動)/小道(自動プレイ)/織工(獲得選択・noPromptで銀貨自動)。**異郷は基本/他拡張と混成しない**ので、フックは異郷の捨て札リデューサ（オアシス/地図職人/何でも屋/大使館/宿屋/公爵夫人/神託/辺境伯/狂戦士/魔女の小屋/車大工）にのみ配線。
  - **on-trashフック**（`triggerOnTrash`＝trashOwn経由）：遊牧民(+2コイン)。
  - **on-buyフック**（BUY内）：値切り屋(購入毎に格下げ獲得・while in play)/農地(廃棄→+2コスト獲得)/高貴な山賊(プレイ/購入の両方でアタック)。
  - **獲得置換リアクション**（交易商人 trader_react）：自分の手番の獲得を銀貨に置換（サプライへ戻す）。**active本人・銀貨以外・pending無しのみ**（相手ターンの呪い獲得置換は非対応＝§6の既知簡略化）。
  - **番犬**（guard_dog）：`hasReaction` 入り＝攻撃反応窓で先にプレイ（+2〜4カード・免疫にはならない・馬商人型）。
  - **アタック6種**（辺境伯/神託/高貴な山賊/狂戦士/魔女の小屋/大釜）＝witch型 EnterVictim/Apply/REACT ＋ ATTACKS登録＋堀/灯台免疫。大釜＝このターン3回目のアクション獲得で呪い配布（actionsGainedThisTurn カウンタ）。
  - **可変VP**（silk_road＝所持勝利点カード/4・vpOf/vpOfPlayer両方）／**コスト軽減**（highway＝場の枚数ぶん-1）／**策謀のクリンナップ**（END_TURN→scheme_cleanup で場の非持続アクションを山札上へ→cleanupAndAdvance）／**愚者の黄金**（1枚目$1/2枚目$4・他者の属州獲得で金貨化リアクション）。
- **新pendingは全て4点セット**（engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal）。CPU＝chooseActionに35枚・decidePendingに全pending（終端保証＝必須獲得は候補あれば必ず非null）。evaluateKingdom は**異郷を MONEY 既定のまま**（§0の「半端エンジンは負け」を踏襲＝CPUは異郷で純ビッグマネー。CPU序列は無悪化を確認）。

### 自作スモーク＋敵対レビュー（多エージェント6次元→独立検証）で バグ5件検出→修正
- **自作スモーク（CPU 60戦＋3-4人48戦）で無限ループ3件**：神託(ORACLE_DECIDE)・辺境伯(MARGRAVE_DISCARD)・狂戦士(BERSERKER_DISCARD) の捨て札解決が **pending を null にせず前進**し、CPUが同じ捨てを反復→修正（`state.pending=null` を前進前に追加）。＝**新reducerで「捨て→triggerOnDiscard→次へ」型は null 忘れが定番の罠**。
- **敵対レビューで2件（出荷到達は稀だが実在の潜在バグ）を追加修正**：(1)**値切り屋CPUのフォールバック欠落**（`bestGain(noVictory) || bestGain(...)` に揃える＝呪いしか無い局面で card:null→engine必須獲得と噛み合い無限ループ。兄弟border_village/weaver/berserker/farmlandは持っていた書き漏れ）。(2)**神託/公爵夫人の自分対象でトンネル捨て→金貨獲得が trader_react 等を立て、攻撃キュー（残り被害者＋使用者+2カード）を潰す**＝triggerOnDiscard中は pending を保持して獲得時対話を抑止するよう修正（回帰テスト2件追加）。
- **偽陽性1件**（狂戦士×交易商人の pending 上書き）＝BERSERKER_GAIN は gain前に pending を保持＝trader ゲートで抑止済＝バグでない、を検証で確認。

### 次（未着手）＝段階2の残り拡張（§5-1）
- **着手順＝ ~~収穫祭~~✅ → ~~ギルド~~✅ → ~~異郷~~✅ → 新プロモ6 → 暗黒時代を全56に完成**。暗黒時代＝廃墟/騎士の混合山・避難所・戦利品/狂人/傭兵・要塞等のon-trash等（特殊山は§6の「4系統除外チェックリスト」必須）。設計図＝`docs/adding-cards.md`。

---

## 0-4. 段階2＝ギルド（Guilds）13枚を実プレイ化（2026-07-04 完了）

### 結論
- **ギルドの王国カード13種を段階2（実プレイ・完全忠実）で実装完了**。`DOM.CARD_SETS` に `guilds`（固定10種 `KINGDOM_GUILDS`）＋`random-guilds` を追加し**出荷済み**。`sw.js` v30→**v31**。テスト **23スイート全緑**（`guilds.test.js` 81件＋`guilds-ui.test.js` 25件を新設・`package.json`登録・`invariants` に guilds/random-guilds も追加）。整合性 2413→**2415**。CPU序列 100/64/95 維持。
- **固定10種 `DOM.KINGDOM_GUILDS`**＝蝋燭職人/石工/医者/助言者/収税吏/伝令官/パン屋/肉屋/商人ギルド/予言者（財源・過払い・アタック2種・公開・trash-to-gain・on-buy・setup を全て味わえる構成）。**公式のギルド専用10種は存在しない**（Guildsは13枚のみ＝常に基本/陰謀と混成。研究Workflowで確認）ので showcase 用の自作10種。
- **新機構をすべて新設（簡略化なし）**：
  - **財源 Coffers（＝日本語名「財源」）**＝per-player数値 `coffers`（createInitialStateで初期化・**公開＝マスク不要・VPに数えない**）。付与＝蝋燭職人/パン屋+1・肉屋+2・広場（財宝捨てで+1）・商人ギルド（購入毎）。消費＝`COFFERS_SPEND`（購入フェイズに1枚=+1コイン）。UI＝金色バッジ＋「💰財源を使う」ボタン＋数量ステッパー `modalAmount`。**パン屋のセットアップ**＝王国にbakerがあれば開始時 全員+1財源。
  - **商人ギルド**＝`t.merchantGuildPlays`（このターンの使用回数）を購入毎に財源へ。**公式2E＝プレイ回数で累積**（玉座で2回使えば購入毎+2）＝場の枚数ではない。出荷セットでは玉座系と同居しないため差は出ないが忠実性のためプレイ回数で実装。
  - **過払い overpay**＝`OVERPAY_CARDS`(石工/医者/名品/伝令官)。BUY後（残コインがあれば）`maybeStartOverpay`→`overpay` pending→`OVERPAY_RESOLVE`(額確定)→カード別 `applyOverpayEffect`。名品=銀貨/枚・石工=ちょうど同コストのアクション2枚(`stonemason_overpay`)・医者=1枚ずつ山札上を廃棄/捨て/戻す(`doctor_overpay`・私的なので**maskで伏せる**)・伝令官=捨て札から山札上へ(`herald_overpay`)。**闇市場購入でも過払いを提供**（promo-pack/random-promoで黒市デッキにギルド札が入るため到達可＝敵対レビューで確定・修正済）。
  - **アタック2種**＝収税吏 taxman（財宝廃棄→+$3までの財宝を山札上に獲得→他の各自[手札5枚以上]が同名を捨てる。廃棄しなければ無効果）／予言者 soothsayer（金貨獲得→他の各自が呪い獲得→**引いたら+1カード**。呪い枯渇なら引かない）。`ATTACKS` 登録＋`*EnterVictim`＋堀/灯台免疫。
  - **trash-to-gain**＝石工（廃棄→それより安い2枚）／肉屋（+2財源→廃棄→財源を払い(廃棄コスト+財源)以下を獲得）。**公開系**＝助言者（上3枚→**左隣**が1枚捨てさせ残りは使用者の手札へ・pending.player=左席）／熟練工（指定以外が3枚出るまで公開→手札）／伝令官（山札上を公開しアクションならプレイ）。
- **新pendingは全て4点セット**（engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal）。CPU＝`chooseAction`に13枚・`decidePending`に全pending・**`coffersToSpend`**（財源を最小枚数だけ使って買いを底上げ・終端保証）・`evaluateKingdom`に`inPool('guilds')`追加（guilds→ENGINE＝CPUがエンジンを組む）・GAIN_ORDERに13枚を強度順再配置。

### 敵対的レビュー（多エージェントWorkflow・7次元→再現検証）＝確定バグ2件（両方修正済）
- **【低】闇市場で過払い対象カードを買っても過払いが飛ばされていた**（「出荷セットで到達不能」コメントが誤り＝promo-pack/random-promoで黒市デッキに全POOLS[ギルド含む]が入る）。→ `BLACK_MARKET_BUY` に `maybeStartOverpay` を追加＋回帰テスト。
- **【低】過払い数量ステッパーが連続購入で前回値を持ち越す**（同一 pending キー`overpay`でリセットされず、意図しない過払いの恐れ）。→ `modalAmount` の確定時に `UI.amount=null`（card識別だけでは名品2連続で不足＝インスタンス毎リセットが正解）。
- **自分の事前精査で1件修正**：必須獲得(収税吏/肉屋)で獲得先が皆無（銅貨/銀貨/名品 枯渇）のとき card=null を拒否し続けCPU無限ループ/人間詰みの恐れ→ engine で「候補ゼロなら獲得せず解決（収税吏はアタックは実行）」＋UIに skip フォールバック＋回帰テスト。
- 他5次元（coffers/attacks/baseeffects/cpu/conservation-integrity）は**クリーン**（偽陽性0・nit0）。

### 次（未着手）＝段階2の残り拡張（§5-1）
- **着手順（新機構の少ない順）＝ ~~収穫祭~~✅ → ~~ギルド~~✅ → 異郷35 → 新プロモ6 → 暗黒時代を全56に完成**。異郷=on-gainトリガー/可変VP(silk_road/feodum)等、暗黒時代=廃墟/騎士の混合山・避難所・戦利品/狂人/傭兵・要塞等のon-trash等。設計図＝`docs/adding-cards.md`。

---

## 0-3. 段階1＝ギルド13＋暗黒時代残り36 を画像化・カタログ追加（2026-07-04 完了・`651e3f6`）

### 結論
- **残り未カタログの全49枚を段階1で追加**（＝画像は出るがゲーム未参加。CARD_SETS 未参照）。内訳＝**ギルド13**／**暗黒時代 王国14＋騎士の山(knights)＋騎士10種／廃墟5／避難所3／非サプライ3（戦利品/狂人/傭兵）**。`npm test` 21スイート全緑（整合性 1947→**2413件**）。`sw.js` v29→**v30**。**これで Downloads の絵は全処理済（desktop.ini のみ残置）**。
- **公式カードデータは多エージェント＋WebSearch で敵対的に確定**。和名は推測でなく公式採用：taxman=**収税吏**／herald=**伝令官**／soothsayer=**予言者**／junk_dealer=**屑屋**／mystic=**秘術師**／rogue=**盗賊**／catacombs=**地下墓所**／band_of_misfits=**はみだし者**／candlestick_maker=**蝋燭職人**／rebuild=**建て直し**／counterfeit=**偽造通貨**。**Coffers＝「財源」**（段階2で使う訳語を確定）。hovel=納屋/necropolis=共同墓地/overgrown_estate=草茂る屋敷。
- **spoils の名前は「戦利品」を採用**（公式は「略奪品」だが、既存 marauder/新規 bandit_camp/pillage が「戦利品置き場」と参照＝プロジェクト内一貫性を優先。将来 Plunder/Loot を入れる時に再考）。
- **新種別 knight/ruins/shelter を追加**：carddata の typeLabel/typeLabelEn（複合語を先に決めて全typeを落とさない）＋ integrity の JP/EN マップに登録。frameType は base type で既存スキンに落ちるので変更不要。dame_josephine は `['action','attack','knight','victory'] vp:2`。
- **孤立プール** `guilds/knights/ruins/shelters/darkages_np` を新設＋`darkages` に15種合流（20→35）。GAIN_ORDER に49 id追加（整合性=全カード網羅）。ui.js カード一覧にギルド/騎士/廃墟/避難所/非サプライのグループ追加。
- **画像回収**＝多エージェント識別（内容判別・Read で実見）＋敵対検証＋**カバレッジ整合で49画像↔49idを一意確定**（二重割当4件[catacombs/pillage/dame_natalie/masterpiece]を欠落4件[necropolis/ruined_village/dame_sylvia/journeyman]へ再割当）。**騎士10種は絵での個体判別不能＝性別(Dame/Sir)一致で割当（コスメのみ・ゲーム無影響）**。→`asset/art/<id>.png` 回収→`CARDS_ONLY` フィルタ（build-cards.js 新設）で新49枚のみ webp生成（既存222を再エンコードしない）。

### 次（未着手）＝段階2: ギルド13枚を実プレイ化（§5-1）
- 新機構＝**コイントークン Coffers(=財源)**（per-player数値＋消費action の4点セット）／**overpay 過払い**（BUY拡張：stonemason/doctor/masterpiece/herald）／**アタック2種**（収税吏 taxman・予言者 soothsayer）／trash-to-gain（stonemason/butcher/graverobber系）／merchant_guild の購入毎on-buyトリガー／baker のセットアップ（開始時全員+1財源）／advisor/journeyman/mystic の公開・宣言／plaza/candlestick_maker の財源。**賞品Prizes山(§0-2)と収穫祭の各機構が良いコピー元**。

## 0-2. 段階2＝収穫祭13＋褒賞5 を実プレイ化（2026-07-04 完了）

### 結論
- **収穫祭(Cornucopia)の王国カード13種＋賞品Prizes5種＝計18枚を段階2（実プレイ・完全忠実）で実装完了**。`DOM.CARD_SETS` に `cornucopia`（固定10種 `KINGDOM_CORNUCOPIA`）＋ `random-cornucopia` を追加し**出荷済み**（＝プレイ可能）。`sw.js` v28→**v29**。テスト **21スイート全緑**（`cornucopia.test.js` 107件＋`cornucopia-ui.test.js` 21件を新設・`package.json`登録・`invariants` にも cornucopia/random-cornucopia を追加）。CPU序列 100/64/95 維持。
- **新機構をすべて新設（簡略化なし）**：
  - **賞品Prizes山**＝`supply` の数値キー(各1枚)。`NON_SUPPLY` set で `emptyPileCount`(3山終了)・`canBuyCard`(購入)・`blackMarket`母集団・汎用獲得(`bestGain`/`bestGainExact`/`horn_of_plenty`)から除外。獲得は馬上槍試合のみ。
  - **災いカードBane**（若き魔女）＝`createInitialState` が `$2-3` の王国カードを1つ選び `state.baneCard` に格納し `kingdom` に push（11山目・通常の購入可能サプライ・`pickBane`）。攻撃時は所持者に反応窓、公開で免除（手札に残す）。
  - **可変VP品評会**＝`vpOf`（engine）＋`vpOfPlayer`（cpu）に `2×floor(異名数/5)×枚数`。
  - **王女コスト-2**＝`cardCost` に active の場の princess 枚数ぶん減算。
  - **馬商人リアクション**＝`hasReaction` に horse_traders 追加。反応窓（stage 'react' ＋ embedded民兵/拷問人）で脇置き→免疫にはならず攻撃は受ける→次手番開始で `DURATION_RESOLVERS.horse_traders` が +1カードして手札に戻す。CPU は decidePending 冒頭で先に脇置き（無限ループしない）。
  - **アタック4種**（占い師/道化師/家臣団/若き魔女）＝witch型 EnterVictim/Apply/REACT ＋ `ATTACKS` 登録。**馬上槍試合**＝属州公開→賞品/公領を山札上、相手が公開しなければ +1カード+1コイン（属州も上置き→ボーナスで即引くのは公式挙動）。**豊穣の角**＝場の異名数コストまで獲得＋勝利点なら自身廃棄。
- **新pendingは全て4点セット**（engine reducer＋PLAYER_ACTIONS＋CPU decidePending＋UI viewPendingModal）を満たす。

### 敵対的レビュー（多エージェントWorkflow）で 8件の実バグを検出→全修正・回帰テスト済み
- **【高】闇市場に賞品が漏れる**：`blackMarket` 母集団が全 `POOLS`（賞品含む）から作られ、tournament 不在の出荷セット（promo-pack 等）で賞品が $0 購入可能だった。→ `NON_SUPPLY` を母集団から除外。
- **【高】豊穣の角で賞品を獲得できた**：`HORN_OF_PLENTY_GAIN` が `NON_SUPPLY` 未チェック＋CPU `bestGain` が賞品を提案。→ reducer に `NON_SUPPLY` ガード、CPU `bestGain`/`bestGainExact` から賞品(`PRIZE_SET`)を除外（reducer単独だとCPU無限ループ＝両方必須）。
- **【中】CPU `vpOfPlayer` が品評会を未計上**（hard CPUの終局読み誤差）。→ engine.vpOf と同じ品評会項を追加。
- **【低】頼もしい乗騎の選択順**：クリック順で解決し「銀貨→山札捨て」を先に処理し得た。→ カード記載順(`valid.filter`)で解決＝+2カードを先に。
- **【低→実は到達可】馬商人リアクションが embedded型(民兵/拷問人)で欠落**（闇市場経由で到達）。→ CPU guard を民兵/拷問人へ拡張＋`modalMilitia`/`modalTorturer` に脇置きボタン。
- 2つの高危険 exploit（闇市場・細工 HORN_OF_PLENTY_GAIN）は node で **閉鎖確認**、promo-pack/cornucopia/random-cornucopia の CPU 24戦が stuck/例外ゼロで完走。

---

## 0. B案＝王国評価型CPU購入AI 実装完了（2026-07-03）

### 結論
- **B案を実装・採用**（`js/cpu.js` `evaluateKingdom`）。ゲーム開始時に王国を評価し **ENGINE→エンジン構築買い（現行 `bestEngineBuy`）／ MONEY→純ビッグマネー（`bestEngineBuy` を呼ばない）** に切り替える。**購入ロジックのみの変更・新pendingなし**（decidePending/UI 不変）。王国は対局中不変なので内容キーで1回だけ評価しキャッシュ。
- **最終分類ロジック**：`ENGINE = 海辺プール or 繁栄プールを含む or (礼拝堂あり & +2カード級ドローあり & 庭園なし)`。それ以外は MONEY。
- **自己対戦A/B（NEW=本実装 vs OLD=常時エンジン, 全22出荷セット, N=200, 席交換ペア校正）で採用条件を完全達成**：**総合 hard 71.5% / normal 71.3%**（>52%）、**全セット ≥48.5%**（最小=固定alchemy 48.5%、random-seaside=50.0%＝§0重点も無悪化）。`npm test` 19スイート全緑・**難易度序列 強vs弱100%／強vs普通64%／普通vs弱95%**（従来95/55/87から改善・閾値60/45/55クリア）。

### 実測で判明した重要事項（当初の叩き台からの修正）
- **§0当初データ（「random-seaside は純BM 43%＝エンジンが勝つ」）は現HEAD＋均衡ペア計測では再現せず、random-seaside は互角（50%）だった**。ただし**固定セットの海辺/繁栄はエンジンが圧勝**（BM は 海辺15%・繁栄23%）。＝§0の「seasideはエンジン有利」という結論自体は固定セットで強く正しく、randomがマイルドだっただけ。→ 拡張シグナル（海辺 or 繁栄プール）を ENGINE にすれば random は互角（損無し）・固定は取りこぼさない。
- **第一仮説「村とドローが両方ある→ENGINE」は不採用**：それだと basic/intrigue の random 王国もエンジン化して負ける（BM が 55〜96% 勝つ）。逆に村/ドロー等の一般特徴量では**固定繁栄エンジン（kings_court等）を取りこぼす**（villages/draws は random-basic と区別不能）。→ 拡張レベルのシグナルが正解。
- **例外1件＝推奨「ビッグマネー」固定セット**：名前と裏腹に chapel(圧縮)+laboratory(ドロー) の軽量エンジンで、BM だと 43%（<45%違反）。→ **`礼拝堂+ドロー` を ENGINE に追加**して 50% に修正。ただし **庭園(gardens)があれば“庭園ラッシュ”＝BM有利**（size-distortion は BM 99%勝ち）なので庭園を除外し、両立させた。
- **失敗済みの中間案（§0記載、再試行しないこと）を再確認**：村/ドロー・キャントリップ厳選系は random で改善しても固定エンジンや BM-rush を壊す。**半端エンジンは無エンジンより弱い**の教訓は正しい。

### 検証手法（再現用メモ・スクリプトは規約により削除済み）
- vm sandbox に cards/engine を1回読み、**NEW=作業ツリー cpu.js／OLD=`git show HEAD:js/cpu.js`** を同一 context に順に runInContext して `DOM.cpu` を2つ捕捉。1ゲーム内で `E.actor(s)===newSeat ? newCpu.decide : oldCpu.decide` と席で差し替え。
- **校正**：同一(kingdom)を席0/席1の両方で対戦＋**試行ごとにシードをリセットして初期シャッフルを一致**させる強ペア設計。ミラー（NEW=OLD）で**全セット厳密に50.0%**・`isEngine=true`（常時エンジン）でも 50.0% を確認してから本計測。進行判定は `pending` を含む状態フィンガープリント（`pending.stage` を落とすと誤stuck多発）。
- 分類候補は `_abx.tmp.js` で ENGINE_CLASSIFIER ブロックを文字列差替えして一括比較（H8=拡張のみ・H8f=拡張+礼拝堂エンジン 等）。**H8f（採用）が TOTAL 71.4%・MIN 49.3% で最良**。

---

## 1. ゴール
- スマホ向けドミニオン対戦Webアプリ（CPU戦・2〜4人ローカル・オンライン対戦）。回答/UIは日本語。
- **公式の全拡張を「完全忠実に」実プレイ可能にする**（簡略化より忠実性を優先。やむを得ず簡略化したら
  PROGRESS に「許容簡略化」と理由を必ず書く）。
- **2026-08-13 時点＝全676枚（縦型505＋横型171）が実プレイ可能で、全部に絵が入っている**。
  実装済み拡張＝基本・陰謀（第二版）／プロモ・新プロモ／海辺／錬金術（支配含む）／繁栄／収穫祭／ギルド／
  異郷／暗黒時代／冒険（＋イベント20）／帝国（＋ランドマーク21・イベント13）／ルネサンス（＋プロジェクト20・
  アーティファクト5）／移動動物園（＋習性20・イベント20）／夜想曲（＋祝福12・呪詛12・状態5）／
  **同盟（＋同盟カード23）**。さらに **mix-all**（15拡張＋プロモを自由に混ぜる）。
- **2026-08-13 に同盟（Allies・72種）が実プレイ可能になり、絵も入った**（§0-29 の A5）。
- **残るは 略奪 → 旭日 の2拡張だけ**（§4 のユーザー決定C＝収益化せず全拡張制覇）。
  **略奪（Plunder・85種）は 2026-08-15 に段階0（研究）＋段階1（カタログ85種＋webp）まで完了**
  （§0-30。正本＝`docs/research/plunder_rules.md`）＝**`CARD_SETS` 未参照なのでまだ実プレイには出ない**
  （`DOM.CARDS` 560／`DOM.LANDSCAPES` 201＝計761枚。**絵は未回収＝枠＋文字だけ**）。**旭日は未着手**。
- カードは「金属枠＋AI生成の絵＋コード描画の文字」を合成した完成画像（`asset/cards/<id>.webp`・金トリム方式）。
- 拡張を「壊さず」増やせる単一ソース設計と、テストで守られた堅牢性を維持する。

## 2. アーキテクチャ（カードを増やす/触るとき必読）
- **表示データの正本＝`js/cards.js` の `DOM.CARDS`**（id/name/cost/types/text、+持続や `potion` 等）。`js/carddata.js` が名前/コスト/種別ラベル/枠色/画像パスを自動導出。`cards.html`（一覧プレビュー）も `tools/build-cards.js`（画像生成）も `DOM.CARDS` を見る。
- **完成画像の生成**：`node tools/build-cards.js`（プロジェクト直下がcwd）。masterフレームを種別スキンにrecolor→枠＋絵(`asset/art/<id>.png`)＋文字をcanvas合成→768×1152 WebP（全143枚）。`CARDS_OUT` で出力先変更可。入力 `images/`・`asset/art/` は `.gitignore`＝**このPCのみ**（再生成はこのPCでしかできない）。
- **エンジン**：`js/engine.js`。`reduce(state, action)` の純関数。`applyEffect` の per-card switch、選択は `state.pending`＋`*_RESOLVE` reducer。攻撃は `ATTACKS` 登録表＋`*EnterVictim`。`PLAYER_ACTIONS`(Set) が送信可能actionの唯一の許可リスト（サーバも参照）。
- **CPU**：`js/cpu.js`。`chooseAction`／`decidePending`（**新pendingには必ず分岐を足す。無いとCPU無限ループ**）／`GAIN_ORDER`（購入優先＝**全カード網羅必須**）／`chooseBuy`(easy/normal/hard)＋`bestEngineBuy`。
- **UI**：`js/ui.js`。`viewBoard`／`viewPendingModal`（**新pendingには分岐必須＝無いと人間が詰む**）／`modal*`ヘルパ。オンラインも同じ ui.js（NetStore.dispatch。クライアントは reduce しない＝サーバ権威）。
- **整合性テスト** `test/integrity.test.js`：reduce case↔PLAYER_ACTIONS一致／GAIN_ORDER=全カード／POOL所属／固定セット10種／react攻撃はATTACKS登録／表示データ一致／種別ラベルが全typeを含む。**抜けはCIで即赤**。
- **テスト全体**：`npm test`＝19スイート（integrity／invariants=**カード保存則ほかプロパティベースfuzz**／engine／各拡張／cpu／attacks-multiplayer／UI各種(jsdom)／server／online／stress）。手動＝`verify:e2e`・`verify:visual`・`test/verify-online.js`（これのみ要サーバ起動）。
- **デプロイ**：main に push → `.github/workflows/deploy.yml` が Pages 公開、サーバは Render 自動再デプロイ。**新しい配信フォルダは deploy.yml に追加**（忘れると本番404）。**client資産（js/css/webp等）を変えたら `sw.js` の VERSION を上げる**（現在 v26）。コミット者設定済み（Naoki Inoue）。

## 3. 完了したこと（サマリ。詳細は各コミットメッセージ＝git log が正）
### 2026-07-10 E8＝命令(Command)の忠実化「プレイした札は動かない」（§0-17・未push）
- **前セッションの E8 計画（＝命令カードが身代わりに動く）は廃止済みの2016年ルールだった**。研究WF＋敵対検証で 2019エラッタ／RGG Dark Ages(2022) PDF を逐語確認し方針を反転。カタログ文も既に現行側だった（大君主「サプライに残す」）。
- 実バグ3種を修正：**船長/王子で使った鉱山の村・倒壊・死の荷車・陣地が、場にある同名の本物のコピーを誤って廃棄/脇置きしていた**（`random-promo` で到達）／命令経由の倒壊に engine が拒否する死に選択肢が出ていた／`farmers_market` の山上VP取得・`island` の手札1枚・`feast` の獲得 などの「移動に条件づかない効果」の扱いを明確化。
- 敵対レビュー確定1クラスタ（2件）を修正：**`pending.self` 新設によるオンライン永続化スナップショットの後方互換バグ**（空手札の倒壊 pending が永久 livelock）→ `DOM.engine.pendingSelf` にフォールバックを内包。
- `sw.js` v43→v44・`overlord.webp` 再生成。全33スイート緑（帝国269/暗黒時代87/新プロモ165/冒険59・整合性3146不変・CPU序列 100/64/95 維持）。

### 2026-07-10 帝国 Batch E6＝命令(overlord/crown)＋財宝再演の根治（§0-15・未push）
- 帝国の新機構6系統すべて実装完了（残るは E7＝CARD_SET昇格のみ）。`sw.js` v41→v42。
- 敵対レビュー確定9件を全修正。うち**出荷済み拡張の実バグ3件**＝ティアラ/偽造通貨の「2回目のアタックが飛ぶ」（繁栄・暗黒時代）／はみだし者×行進の「再演で選び直せる」（暗黒時代）／CPU `pickSwindlerGift` の分割山ロック無限ループ。
- テスト：empires 197件・darkages 79件・prosperity 69件・promo2 142件。invariants の敵対王国+4種。追加ソーク120戦クリーン。**全32スイート緑（整合性3144不変・CPU序列 100/64/95 維持）**。

### 2026-07-04 段階2の実装設計図を作成（`docs/adding-cards.md`）
- 全カード実プレイ化（段階2）に向け、エンジンの全機構（効果/pending・on-gain等トリガー/リアクション・特殊山/セットアップ/トークン/CARD_SET昇格・持続/command/可変VP・テスト雛形）を多エージェント調査し、**file:line＋コピー元コード＋落とし穴**を `docs/adding-cards.md` に恒久化。段階2の作業計画は §5-1。

### 2026-07-04 新拡張カードの画像化・カタログ追加（段階1・79枚）
- **収穫祭13＋褒賞5＋異郷35（初版26＋第二版新規9）＋暗黒時代20＋新プロモ6＝79枚を段階1で追加**（`DOM.CARDS`＋孤立プール `POOLS.cornucopia/hinterlands/darkages`＋promo合流＋`GAIN_ORDER`＋ui.js一覧グループ）。**画像は出るがゲーム未参加**（CARD_SETSから孤立プールを参照しない＝実サプライに出ない）。完成webp79枚生成済み（`asset/cards/*.webp`）。整合性テスト緑（fools_gold=財宝+リアクション／tunnel=勝利点+リアクションの新種別ラベルを carddata に追加）。`sw.js` v27→v28。
- 絵はユーザーがChatGPTで生成→私がDL画像を判別し `asset/art/<id>.png` に回収（→ChatGPT運用ルールは記憶 `chatgpt-card-art-workflow` 参照）。カード定義・画像判別は多エージェントworkflowで起草/照合。
- **未完了＝段階2（実プレイ化）**：engine効果／CPU decidePending・chooseAction・chooseBuy／UI viewPendingModal／CARD_SETへの昇格が未実装。暗黒時代は20/56のみ（残36＋廃墟/避難所/騎士/特殊は未着手）。§5参照。

### 2026-07-03 CPU購入AI強化（B案）
- **B案＝王国評価型 CPU購入AI 実装完了**（`js/cpu.js` `evaluateKingdom`）。ENGINE/MONEY 切替で総合勝率 hard 71.5%/normal 71.3%・全22出荷セット≥48.5%。詳細・A/B結果・実測の修正点は **§0**。`sw.js` v26→v27。テスト19スイート全緑・難易度序列 100/64/95%。

### 2026-07-01〜03 堅牢化マラソン（多エージェント監査①〜⑦＋fuzz＋実測。実バグ約20件修正）
- `8235b32` 海辺の簡略化2点を本格実装（封鎖の堀免疫窓＝`immune[]`／海賊の財宝獲得リアクション＝`pirate_react`）＋混成王国CPU購入バランス＋cpu.test決定論化（固定シード）
- `0cb288c` 監査①: 泥棒王国の無限ループ(高)＝CPU経済フォールバック＋`isGameOver`150手番安全網／会計士2枚目消失／ティアラ×ペテン師2回目／水晶玉の財宝プレイ委譲／封鎖×2
- `43c3e5a` 監査②オンライン: 水晶玉看破の漏洩／自席山札の順序透視→ソート配信／DoS対策(`MAX_ROOMS`＋無人ロビー即破棄)。**anti-cheatはexploit無しを確認**
- `d6f5e45` 監査③表示: ペテン師「財宝・アタック」/会計士3種ラベル修正＋**webp2枚再生成**＋ラベル網羅テスト
- `30cf5ba` 監査④UI/UX: 支配中の看破マスクでrender例外(高)＝`secretSeer`＋cardEl未知id防御／自動スキップ・コーチの被支配者ルーティング／闇市場サプライ外カードの手札描画／おすすめ買いにcolony/platinum
- `54d3018` 監査⑤: 混成王国の潜在バグ2件（支配×外部self-trash・コイン獲得札×ポーション費用）は**全プール/全セット走査で到達不能を証明**＝意図的に未修正（§6参照）
- `c94e84f` `d089a47` `b91bb2d` 監査⑥⑦: **カード保存則fuzz新設**(`test/invariants.test.js`)→闇市場の公開カード消失・宝の地図複製（玉座2回目）を修正＋支配強制12戦＋負リソース/手番/終局検査＋ログ長≤250ガード
- `baee9dc` DoS即破棄が復元(restoreRoom)ロビーを壊す自己回帰を修正（`allowImmediate`引数）
- `d105b68` CPU強化: `throneValue` による玉座/王の宮廷の対象選択＋衛兵の銅貨圧縮（A/Bニュートラル＝質改善）＋**§0の戦略的発見を記録**
- `1a69139` 実ブラウザE2Eスモーク新設（`test/verify-e2e.js`・9/9・自己完結）
- `49149de` `verify-visual.js` 刷新＝320/360/390/414/768px×主要画面で横はみ出しゼロ確認
- `628ae15` a11y: カード/山に role=button＋aria-label＋Enter/Space（`a11yBtn`）
- `7cf27f4` 性能監査: reduce 0.04ms/手・render p95 22ms(4xスロットル)＝良好・バグ無し
- `d339b95` 多人数アタック検証: 監査0件＋シナリオ22件新設(`test/attacks-multiplayer.test.js`)＝**クリーン**（呪い枯渇は手番順先着・堀は公開者のみ免疫・玉座×魔女=2枚 等すべて正）
- `2df9f83` ドキュメント総点検（PROGRESS/CLAUDE/README を現状同期）

### 過去セッション（〜2026-07-01）
- カード完成画像 全143枚（金トリム方式）／海辺27種の実プレイ化（持続機構・マット・前哨地・灯台免疫）／錬金術13種（ポーション経済・支配=actorルーティング/gain・trashOwn精算/追加ターン）／繁栄27種（VPトークン・プラチナ/植民地・コスト軽減・王の宮廷）。広い経緯は `docs/handover.md`。

## 4. 決定事項とその理由
- **【2026-08-15・ユーザーが判断を委任（「任せる」）→ 私が決定】略奪の「要ユーザー確認」4件を確定した**：
  1. **`spoils`（暗黒時代）＝公式訳「略奪品」への改名を採用**（`ea0c091` を維持）。
     略奪の `Loot` の公式和名が**「戦利品」**と確定し、同じ文字列が2つの別物を指す状態になるため。
     **PROGRESS §0-3 が「将来 Plunder/Loot を入れる時に再考」と先送りしていた宿題**で、公式は一貫して Spoils＝略奪品
     ＝**出荷済みの忠実性バグの修正でもある**。mix-all で暗黒時代と略奪が同居すると「戦利品置き場」が2つ出る実害があった。
  2. **`journey`（旅行）＝2023年9月の Extra turn errata 側を採用**
     （`You don't discard cards from play in Clean-up this turn. Take an extra turn after this one
     (but not a 3rd turn in a row).`）。**`ONCE_PER_TURN_EVENTS` に入れるのは `launch` だけ**。
     「3ターン連続不可」は**島民・航海と同じ既存の共通機構**（§0-29 A5）に乗せる。
     - **根拠1＝本アプリは同じ2023エラッタの `mission`（使節団）を既に採用済み**
       （`js/engine.js` の `ONCE_PER_TURN_EVENTS` に `mission` が無い＝§0-26）。
       **旅行は使節団の完全な同型**（イベント／`Once per turn:` が消えた／追加ターン）＝
       ここだけ旧版にすると**同じエラッタの中で挙動が割れる**。
     - **根拠2＝決定3（日本語文面は Dominion Online 訳で統一）と噛み合わない**。
       日本語wiki（＝DO訳）の `旅行` は**エラッタ側の文面**なので、2022版を採ると
       **「表示は新版・挙動は旧版」**になり、カードがプレイヤーに嘘をつく。
     - **根拠3＝ユーザーは日本語版カードの現物を持っていない**（メモリ `no-physical-dominion-cards`）＝
       印刷物と一致させる実利が無く、**実際に触れる公式デジタル（＝エラッタ側）に合わせるほうがよい**。
     - ⚠ **反対材料も残す**＝英語wiki の `Versions` 表でエラッタ行の Print 欄は `Not printed yet`（未印刷）で、
       wiki は当該FAQ節を `Unofficial FAQ (Post-2023 Errata)` と呼ぶ。形式だけ見れば
       **§0-29 A4 の royal_galley（未印刷のアナウンスは採らない）と逆の判断**。
       ただし royal_galley は *announce* のみで**公式エラッタ文書にも `Versions` 表にも行が無かった**のに対し、
       旅行には**正式なエラッタ行がある**（`Announced = September 2023`）。
       **反転は1行＋カタログ文＋webp 1枚**（`ONCE_PER_TURN_EVENTS` に journey を足して text を旧版へ戻す）。
  3. **日本語のカード文面＝Dominion Online 訳で統一**（日本語wiki が出典として明記しているもの）。
     **印刷版（ホビージャパン2023年3月）の逐語を85種ぶん揃える手段が無い**（英語wiki の Japanese 行は4枚だけ・
     低解像度・誤字あり）。**カード名はどちらの版でも同じ**なので機能・表示とも実害なし＝**許容簡略化として記録**。
     ※夜想曲・同盟では「印刷版が正本」だったが、略奪はその前提が成り立たない。
     **【2026-08-15・ユーザーに確認済み】ユーザーは日本語版の現物を持っていない**＝印刷版と照合する手段は
     このプロジェクトには無い。**この件は決着済みなので蒸し返さないこと**（旭日でも同じ扱いでよい）。
  4. **細部2件**：**港の村(Harbor Village)は習性(Way)由来の +$ ではボーナスを出さない**
     （英語wiki の Official FAQ は「出す」だが、日本語wiki が挙げる**2025年2月の移動動物園エラッタ＋
     2025年3月の Donald X. の回答**のほうが新しく具体的＝新しい裁定を採る。mix-all 限定の到達）／
     **旗艦(Flagship)が持続を再演したら旗艦も場に残る**（RGG ルールブック逐語＋現行wiki の一般則。
     **同盟の専門家(Specialist)と同じ実装**）。はみだし者経由だけ残さないという脚注は**2025-09 に wiki から削除済み**
     ＝現行資料に例外が無いので**例外を設けない**。
- **【2026-08-15・ユーザー決定】このアプリは「趣味として全拡張制覇」まで作り切る（収益化はしない）**。
  ドミニオンは Rio Grande Games / Donald X. Vaccarino の著作物で、カード名・効果テキスト・カード構成を
  そのまま使う本アプリを**広告や課金で収益化すると権利上危ない**（公式のオンライン版 dominion.games がある）。
  → 選択肢A（無料公開のままにしてエンジンをオリジナル作品に載せ替えて収益化）／B（自作テーマの類似ゲーム）／
  **C（収益化はせずこのまま全拡張を完成させる）** を提示し、**ユーザーはCを選択**。
  ＝**残るは略奪（Plunder）と旭日（Rising Sun）の2拡張**。収益化の話は当面持ち出さない。
- **CPU購入はB案（王国評価で ENGINE/MONEY 切替）を実装・採用済み（2026-07-03完了）**：`evaluateKingdom`。詳細・A/B結果・実測の修正点は §0。A案（BM寄せ）は seaside悪化＋エンジンレスUXのため不採用。総合勝率 hard 71.5%/normal 71.3%・全セット≥48.5%。
- **枠は画像（金属枠）方式**：コード描画SVGでは基準カードの絵画的な金に届かなかった（5回差し戻し。詳細 `docs/handover.md`）。
- **画像だけ先・ゲームロジックは別タスク**：新拡張はまず `DOM.CARDS` カタログ＋孤立プール＋`GAIN_ORDER` で「画像は出るがゲームに入らない」状態にし整合性テストを緑に保つ→後で実ゲーム化（海辺/錬金術/繁栄はこの方式で完了）。
- **海辺の簡略化2点は本格実装済み**：封鎖の堀免疫窓・海賊の財宝獲得リアクション。on-gain対話は `!pending && _gainDepth===1` ゲートで安全側。

## 5. 未完了タスク（優先順。次セッションは 1. から）

1. ~~**【最優先】出荷済みバグ候補6件を潰す**~~ → ✅**完了（§0-32・2026-08-17）**＝6件全部が実バグと確定し修正済み
   （＋敵対レビューの確定5件も修正。回帰テスト +117 assert・全件バグ注入で感度確認済み）。

2. **旭日（Rising Sun）＝段階1＝カタログ50種＋webp**。正本＝**`docs/research/risingsun_rules.md`**
   （**着手前に冒頭の「実装前に必読18項目」と「決定事項 D1〜D5」を必ず読む**）。
   - `DOM.CARDS` 560→**585**（王国25）／`DOM.LANDSCAPES` 201→**226**（イベント10＋予言15）＝合計 761→**811枚**。
   - **新種別 `omen`（前兆）／`shadow`（影）**＝縦型（前例＝同盟の `liaison`／略奪の `loot`）。
     表示ラベルを **`js/carddata.js` / `js/ui.js` の TYPE_JP / `test/integrity.test.js` の JP/EN マップ**に登録する
     （登録漏れは §A2 で入れた恒久検査が捕まえる）。
   - **横型の新 kind `prophecy`（予言）**＝`tools/build-landscape.js` にスキンを新設。
     **iris blue（菖蒲色）**・**コスト欄なし**（前例＝同盟の `ally`／略奪の `trait`）。
   - ⚠ **`DOM.STAGE1_POOLS` に必ず入れる**（今は `[]`。入れないと闇市場に「買っても何も起きない死に札」が $0 で並ぶ）。
   - ⚠ **影カードは裏面が5種とも違う**＝カード画像（webp）で裏を作るかを決める。
   - ⚠ **id 衝突**を既存761枚と機械照合する（`artist`↔`artisan` などに注意）。

3. **旭日＝段階2＝engine/CPU/UI**（正本の **R1〜R7**）→ 多エージェント敵対レビュー → 絵50枚 → **ユーザー確認の上で** push。
   R1＝前兆/予言/Sunトークンの基盤 → R2＝影の基盤 → R3＝素直な王国 → R4＝予言15種（神風は最後）→
   R5＝イベント10種 → R6＝残りの王国 → R7＝CARD_SET 昇格（mix-all 16→17拡張）。
   **新しい pending は4点セット必須**（engine reducer＋`PLAYER_ACTIONS`＋CPU `decidePending`＋UI `viewPendingModal`）。
   着手前に **`docs/adding-cards.md`** も読むこと。

4. **第17拡張 Arcana**（2026年予定・500枚・王国37山・機構＝Study/Cart/Project）＝
   **英語wiki にカード一覧・追加ルール・ギャラリーが1件も無い**（カード名すら未公開）＝**現時点では段階0すら着手不能**。
   旭日を作り切ったあと、データが出ていれば着手する。

5. **錬金術アートの△3枚最終確認（任意）**：変成/薬草商/薬剤師。差し替えは `asset/art/<id>.png` →
   `CARDS_ONLY=<ids> node tools/build-cards.js` →該当webpデプロイ。

6. （任意・CPU購入の残課題）B案は「拡張＋礼拝堂エンジン」で ENGINE/MONEY を切替済み。さらに踏み込むなら
   **MONEY王国での BM+呪いアタック** や **王国個別のエンジン成立度スコアリング**が候補（優先度低）。

7. （任意・過去メモ）絵文字→game-icons.net SVG 化、vanilla効果DSL 等。

## 6. 詰まり・注意点・保留中の判断

### 【2026-08-17 追加】研究・調査の型（旭日の段階0で確立。次の拡張でもこのまま使う）
- **3ワークフロー方式**＝①8群を分担収集＋各群を**別エージェントが一次資料で敵対検証** →
  ②訂正を反映した確定版＋**完全性の批評**（「何が足りないか」だけを言わせる）→ ③批評の [must] を反映。
  **③では「鵜呑みにせず、疑わしければ自分で grep／wiki で確かめ、間違っていたら理由つきで不採用にせよ」と
  必ず指示すること**（実際に誤った指摘が1件棄却された）。
- **一次資料の取り方**：英語wiki＝`node tools/wikidirect.js "<Page>" [...]`（**301 追従あり**・スペースは `_`・
  生HTMLは `RAW_DIR=`）。RGG ルールブックPDF＝wiki の `Official Rulebook PDF` リンクから curl → `pdftotext -layout`。
  日本語wiki＝`python tools/jpwiki.py`（**6秒間隔＋429 バックオフ**）。
- ⚠ **日本語wiki は並列で叩くと必ず全滅する（429）＝エージェントには触らせず、自分で逐次に取る**。
  **個別カードページは英語原文と日本語訳（Dominion Online 訳）を同じ表に並記している**ので、
  1ページから「対応・両言語のカード文・区切り線（`--------------------`）」が同時に取れる。
- ⚠ **`Errata` の要約ページだけを見ると取りこぼす**（`Capital` の2025エラッタは個別カードの `Versions` 表にしか無い）。
  **年次ページ（`2024 Errata` / `2025 Errata` / `2026 Errata`）と各カードの `Versions` 表の両方を見ること。**
- ⚠ **回帰テストは必ずバグ注入で感度を確かめる**（修正を外して落ちるか）。旭日の段階0で、
  **屋敷($2)のせいで区別できておらずバグを戻しても緑のままのテスト**を1件書いてしまった。
- ⚠ **バックグラウンドのエージェントは「出力0バイト＋長時間無更新」なら死んでいる**＝待たずに投げ直す。

### 【2026-08-17 追加】engine を触るときの新しい鉄則
- **永続持続（ゲーム終了まで場に残る）を足すときは `permanentDurationCounts(p)` の1箇所だけに足す**。
  以前は `cleanupAndAdvance`（持続の仕分け）と `stayingCounts`（増築の対象除外）に**同じ一覧が二重にあり**、
  操舵手が片方だけ漏れて**増築が「場に残り続ける操舵手」を廃棄できた**。
  `flagship_linger`（旗艦が持続を再演したら旗艦も残す）の解除判定も同じ集合を見る。
- **「手札を捨てさせる」reducer を書いたら必ず `triggerOnDiscard` を呼ぶ**（坑道／小道／織工）。
  相手のアタックによる捨て札は `noPrompt=true`（拷問人・軍団兵と同じ慣行＝攻撃キューを壊さない）。
  自分のターンの自分の捨て札は `noPrompt` なし（村有緑地・忠犬の窓も開く）。
  **民兵・軍団兵・公共広場がこれを呼んでおらず、`promo-pack` で今日まで坑道が空振りしていた。**
- **負債(Debt)は「購入」でだけ負う**（`takeDebt` は `BUY` / `BUY_EVENT` / `BLACK_MARKET_BUY` からのみ呼ぶ。
  **`gain()` から呼んではいけない**）。**返済はターン中いつでもできる**（`REPAY_DEBT` にフェイズ判定を入れない）。
- **得点計算でコストを見るときは `scoringCost(state, id)`**（`cardCost` を使うと最終ターンの橋/街道まで拾う）。
  公式＝**「場にある間」型の軽減は得点計算に効かない／略奪の「安価な(Cheap)」と旭日の「盛大な取引」は効く**。

- **新カードを `DOM.CARDS` に足すと整合性テストが赤くなる**（GAIN_ORDER網羅＋POOL所属を要求）→ 孤立プール＋GAIN_ORDER追加で回避。実ゲーム化時は ATTACKS/PLAYER_ACTIONS/CPU decidePending/UI viewPendingModal も忘れず（抜けはCIで赤 or CPU無限ループ/人間詰み）。
- **デプロイ**：サーバ変更時は Pages と Render の反映タイミング差で一時的に空振りし得る。`sw.js` VERSION更新を忘れない。
- **【2026-08-06 に踏んだ】GitHub Pages の公開キューが混むと `deployment_queued` のまま待たされ、
  `actions/deploy-pages@v4` の既定タイムアウト(10分)で `Timeout reached, aborting!` になり失敗する**（3回連続で失敗。
  同日に成功した回も 10分42秒 かかっていた）。**`timeout` 入力を上げても効かない**＝`deploy-pages` の上限が 600000ms で、
  超える値は `timeout value is greater than the allowed maximum` の警告付きで丸められる（実測）。**待つしかない＝失敗したら入れ直す**。
  **失敗した run を `gh run rerun` すると「Multiple artifacts named github-pages」で必ず失敗する**（前回のアーティファクトと二重になる）。
  **やり直しは「新しいコミットを積んで push」しかない**＝Pages のデプロイIDは**コミットSHAそのもの**なので、
  一度キャンセルされた SHA で `gh workflow run`（新規実行）しても **5秒で `Deployment cancelled.`** になる（実測）。
  中身を変える必要が無ければ `git commit --allow-empty` でSHAを進めて push する。
  同時に2つ走らせると片方が `Deployment cancelled.` になるので、前の run が completed になってから投げること。
- **【最重要・2026-08-06 に7回連続で失敗した真因】タイムアウトした Pages デプロイが GitHub 側で
  `deployment_in_progress` のまま“ゾンビ化”し、以後のデプロイを全部 400 で弾く**：
  `Deployment request failed for <新SHA> due to in progress deployment. Please cancel <旧SHA> first`。
  ワークフローがタイムアウト時に出す `Canceling Pages deployment...` は**実際には解除できていない**。
  → **API で明示的にキャンセルすると即座に直る**：
  `gh api -X POST repos/ankake-web/dominion/pages/deployments/<旧SHA>/cancel`
  （状態確認は `gh api repos/ankake-web/dominion/pages/deployments/<SHA>` の `status`）。
  キャンセル後に新しいコミットを push すれば通る。**デプロイが失敗し続けたらまずこれを疑うこと。**
- **一時スクリプト規約**：使い捨ては**プロジェクト直下に `_*.tmp.js`** で作り実行後**必ず削除**。スクショ等は scratchpad へ。シェルcwdがずれることがあるので実行前に `Set-Location 'C:\Users\b1242\claude\game\dominion'`。
- ~~**支配（Possession）の廃棄カード返却の簡略化＝到達不能（監査⑤）**~~ → **§0-23（mix-all 硬化）で解消済み**。
  自己廃棄5枚（投資/祝宴/宝の地図/鉱山の村/豊穣の角）は `trashCard` 経由＝支配中は possessionTrash へ退避して返却される。
  **同型の「ポーション費用/負債コストは同居しないので未修正」も全て解消**（`costUpTo`/`costExact` が成分別に弾く）。
  **サプライからの廃棄（塩まき/待ち伏せ/剣闘士）は `trashFromSupplyPile`**＝支配中でも退避しない（属州が被支配者に湧かない）。
- **支配のCPU簡略化**：CPUは支配を自動購入しない（`bestPotionBuy` で除外）。人間が使うぶんは支配者がCPUでも動作する。
- **非サプライ数値キー山（賞品Prizes・将来の戦利品/狂人/傭兵）を足すときの必須チェックリスト**（§0-2のレビューで実際に踏んだ罠）：`NON_SUPPLY` set に登録し、**(1) `emptyPileCount`(3山終了) (2) `canBuyCard`(購入) (3) `blackMarket` 母集団（`createInitialState` の universe フィルタ） (4) 汎用獲得（engine の `*_GAIN` reducer と CPU `bestGain`/`bestGainExact`）** の4系統すべてから除外すること。特に「reducer だけガードして CPU 側を放置すると、CPU が拒否される獲得を出し続けて無限ループ」する（豊穣の角で実際に発生）＝**engine拒否とCPU非提案は必ずセット**。汎用獲得を持つ札（`horn_of_plenty` 等）は特に漏れやすい。
- **段階1(§0-3)＋ギルド段階2(§0-4)は push済（`6d1d69c`・2026-07-04・ユーザー確認の上で本番デプロイ）**。以後の段階2作業も 完成→CARD_SET昇格→全テスト緑→**都度確認の上で** push。
- **`5150017` まで push済（2026-07-05・本番デプロイ済）＝異郷段階2＋冒険/帝国段階1＋新プロモ段階2(§0-7)**。以後の段階2作業も 完成→CARD_SET昇格→全テスト緑→**都度ユーザー確認の上で** push。`sw.js` は v34。
- **冒険/帝国段階1の画像回収メモ**：Downloads の ChatGPT画像はファイル名不定→生成時刻でバッチ割当→視覚判別で id確定（chatgpt-card-art-workflow 記憶＋今回は多エージェント判別）。`asset/art/*.png`・`images/` は gitignore＝このPCのみ。webp再生成は `CARDS_ONLY=<id,...> node tools/build-cards.js`。研究データは scratchpad `adv_emp_carddata.json`。
- **異郷の許容簡略化（到達が稀 or 忠実性のみ・敵対レビューで重大でないと確定）＝意図的に未実装**：(1)**交易商人の獲得置換は自分の手番の獲得のみ**（相手ターンの魔女等の呪い獲得を銀貨に置換する反応は非対応＝獲得時対話ゲートが active限定・相手ターンだと pending 競合で潰れるため。呪いはそのまま受ける＝安全側）。(2)**値切り屋/農地/高貴な山賊の on-buy は「1購入=1 pending」**＝farmland/noble_brigand を買うと同ターン場の値切り屋の強制獲得がスキップされ得る（複数 on-buy を並べる汎用キューが無いため。カード保存則は保持・ループ/クラッシュ無し）。(3)**develop 等の獲得で入れ子の獲得時対話（border_village等）は `!pending` ゲートでスキップ**。いずれも「on-buy/on-gain の汎用 pending キュー」を導入する時にまとめて対応する方針（現状は保存則・非ループを敵対レビューで確認済）。
- ~~**【既存】闇市場デッキに「段階1のみ（engine未実装）のプール」が漏れる**~~ → **解消済み**：13拡張すべてが段階2（実プレイ可能）に
  なったため、闇市場デッキに入る札はすべて動く。混合山の中身（騎士/廃墟/城）は元から除外済み。（以下は当時の記録）
- **【旧記録】闇市場デッキに「段階1のみ（＝engineロジック未実装）のプール」が漏れる**：`createInitialState` の黒市universeは全 `Object.values(DOM.POOLS)` を平坦化するため、promo-pack/random-promo で黒市デッキに hinterlands/darkages/knights/ruins/shelters/darkages_np（＋spoils/madman/mercenary）が混入する。これらは段階1（applyEffect未実装＝買って使っても何も起きない死に札）。**ギルドの段階2化で guilds プールは playable になった**ので問題なし。残りは各拡張が段階2化される都度 自動解消。**根治するなら黒市universeを「CARD_SETSが参照する playable プールのみ」に絞る**（＝段階2化の順に自然消化。急がば注意：正しく除外しないと変種が減る）。敵対レビューが指摘（元からの挙動＝ギルド作業とは独立）。
- **段階1で追加した暗黒時代の非サプライ札（戦利品/狂人/傭兵/騎士10種/廃墟5/避難所3）を段階2で実プレイ化する時は、上の「4系統除外チェックリスト」を必ず通す**。特殊山（廃墟＝混合順序山→top-level配列・invariants tally追加／騎士＝混合山／避難所＝開始デッキ置換）は `docs/adding-cards.md` §C に実装手順あり。新種別 knight/ruins/shelter は表示ラベルのみ実装済（engineロジックは段階2で新設）。
- **暗黒時代 段階2 は WIP（§0-8 が正）**：カタログのみ現行エラッタに修正済み＝**カード一覧の文言と webp 画像の文字が9枚で不一致**（hermit/procession/pillage/death_cart/rats/counterfeit＋種別変更の marauder/cultist/band_of_misfits）。**CARD_SET昇格前に webp 再生成必須**（このPCで `CARDS_ONLY=<ids> node tools/build-cards.js`）。sw.js VERSION は完成時に v34→v35。
- **Read ツール出力の汚染を観測（2026-07-05）**：実在しないコード/コメントが Read 結果に混入して見え、「基盤実装済み」と誤認しかけた（git diff / grep の生バイト確認で否定して復旧）。以後この作業では、実装状態を断定する前に **Grep・`Get-Content`・`git show` での裏取り**を併用すること。
- **【E6で新設・以後の必読】「財宝を2回使う」は必ず `state.replay` の `'treasure_replay'` を使う**（§0-15）。`playTreasureCard` ＝移動＋`applyTreasureEffect`／`applyTreasureEffect` ＝**カードを動かさず効果だけ**。2回目を「コインだけ足す」で済ませると、pending を立てる財宝（御守り/水晶玉/金床/不正利得/豊穣の角）や +購入/+VP を持つ財宝（元手/大金/鹵獲品/収集/偽造通貨）の2回目が丸ごと消える＝**旧 `treasureReplayCoins` の轍**。**新しい財宝を足すときは `applyTreasureEffect` に書けば冠/ティアラ/偽造通貨の2回目が自動で正しくなる**。自己移動する財宝（投資/戦利品/法貨/私掠船の廃棄）は `removeOne` ガードで2回目に自然不発（lose track）＝**新規財宝で自己移動させるなら必ず `if (removeOne(...))` で包む**。
- **【E6で新設】命令（Command）の再演は選び直さない**（公式）：`state.turn.commandAs[命令id]` に1回目の選択を記憶し、`runReplays` が立てる `state._replaying` を見て `replayCommandAs` が再利用する。**`_replaying` はゴーレムの2枚目では立てない**（別カードの新しいプレイ＝選び直せる）。新しい命令カードを足すときは `case` の先頭に `if (replayCommandAs(state, pi, '<id>')) break;` を、選択の reducer に `rememberCommandAs` を入れる。**船長（captain）は持続で「次のターンの開始時」が別のプレイ＝毎ターン選び直す**ので commandAs を使わない（意図的）。
- **【E8で確定・重要】命令（Command）＝「プレイした札は動かない」（2019エラッタ・現行）**。命令カード（大君主/はみだし者/船長/王子）はカードを**サプライ／王子の脇に残したままプレイする**ので、そのカードの「これ(this)を廃棄／脇に置く／山へ戻す／マットに置く」は**必ず失敗する。命令カード自身も身代わりに動かない**。**移動そのもの（と「移動できたなら」で条件づいたボーナス）だけが失われ、残りの効果は普通に解決する**（祝宴の獲得／島の「手札から1枚」／死の荷車の「手札のアクション」／農家の市場の山上VP取得 は起きる。鉱山の村の+$2・略奪の戦利品・宝の地図の金貨・狂人のドロー は起きない）。実装＝`state._cmd`＋`playedByCommand`／`takeSelf`／`playAsCommand`（§0-17）。
  - **「そのカードの名前/種別/コストを得て身代わりに動く」のは 2016年初版ルール＝廃止済み**。RGG が旧 Empires PDF を今もホストしているので**そちらを読むと必ず間違える**。正本は RGG **Dark Ages(2022)** ルールブックPDF の Band of Misfits 項＋Donald X. の 2019エラッタ。
  - **`empires` 固定セットが大君主と自己移動札（農家の市場/陣地）を同居させていない理由「永久VPループ」は旧ルール前提の誤診**。現行では合法・非ループなので、将来セットを組み替えても構わない。
- **【E8で新設】`pending.self`（倒壊/死の荷車の「これを廃棄できるか」）は必ず `DOM.engine.pendingSelf(state, pd, cardId)` で読む**。engine拒否・CPU非提案・UI表示の3面が同じ述語を見ること（片側だけずれると即 CPU 無限ループ）。**pending に新フィールドを足したら、オンライン永続化スナップショット（`server/gameServer.js` が `state.pending` ごと保存・無変換復元）に無い場合のフォールバックを書くこと**＝E8 の敵対レビューで実際に踏んだ（旧 pending の `self` 欠落→falsy→空手札の倒壊が永久 livelock）。
- **【E8で新設】命令の代理プレイは `applyEffect` の内側で「別の本物のカード」をプレイする経路と区別する**（`_cmd.as` で識別）。伝令官/家臣/水晶玉/狂戦士(on-gain) は applyEffect の内側で本物を場に出してプレイするので、そちらの「これ」は本物を指す。
- **【E6の意図的な簡略化】玉座×大君主のネスト**で、玉座の2回目が「先に」走り対象不在で空振りすることがある（`state.replay` が単一FIFOのため＝玉座×玉座の既存挙動と同型）。**保存則・非ループ・クラッシュ無しを敵対レビューとfuzzで確認済み**＝再修正しなくてよい。
- **【E7で新設】「コスト$N以下」の判定は `costIsPlainCoin(id)` を必ず併用する**（engine.js）。公式のコスト比較は coin/負債/ポーションを**成分ごと**に比べるので、負債コストやポーション費用を持つカードは「コスト$N以下」に含まれない（`$0+負債4` は "up to $5" ではない）。`princeEligible`/`captainTargets`/`overlordTargets`/`bandOfMisfitsTargets` と CPU の `bestGain`/`bestGainExact`/`bestPrinceTarget` は同じ除外を持つこと（片側だけだとCPU無限ループ）。
- **【E7で新設】CPUで山のコストを見るときは必ず engine の `cardCost`（実コスト）を使う**。混合山（`knights`/`castles`）のプレースホルダは静的コストが実物とずれる（castles=$3だが一番上は最大$10）。`cpu.js` の `mixedTop(state,id)` が「一番上の実カード」を返す。
- **【E7で判明】新しい CARD_SET を足したら UI の picker にも出す**（`kind:'standard'` は「拡張」タイル）。`test/ui.test.js` の「全 CARD_SETS がセット選択画面から選べる」が守る。**新しいプレースホルダ card を `DOM.CARDS` に足したら webp も生成する**（`verify:e2e` の webp 404 検査が守る）。

## 2026-08-25 バッチ3：Dominion UI画像素材

- ホーム背景、カード裏面、リザルト背景、紋章、アプリアイコン3サイズ、装飾区切り線を新規生成・実装。
- 追加先：`asset/ui/`、`asset/cards/back.webp`、`icon-180.png`、`icon-192.png`、`icon-512.png`。
- `js/ui.js` と `css/style.css` に組み込み、ホーム／対戦画面／リザルト画面で使用。`sw.js` は `v91` に更新。
- 全テスト成功。レスポンシブ画像検証は 320 / 360 / 390 / 414 / 768px の全画面で横はみ出し 0。
