node_modules/
.firebase/
*.log
.DS_Store
pictier/
asset/*.png
asset/260313_assets/
# カード画像の重い原本はコミットしない（配信は asset/cards/*.webp のみ）
images/
asset/art/
asset/cards/*.png
# 使い捨ての一時スクリプト（狙い撃ちテスト・ソーク・レビューエージェントの検証用）は追跡しない。
# ⚠ 規約は `_*.tmp.js` だが、レビューエージェントは `_soak_out_A.json` のような別名も作る。
#    `git add -A` で巻き込んで誤コミットしたことがあるので、**ルート直下の `_` 始まりは全部**除外する
#    （追跡中の `_` 始まりファイルは1つも無いことを確認済み）。
_*
