<!-- /handoff が自動生成（2026-08-12）。新セッションはこのファイルの指示に従う。手編集不要 -->

**ウルトラコード（最大エフォート）で進めてください。**

スマホ向けドミニオン対戦Webアプリの続きです。
作業ディレクトリ＝`C:\Users\b1242\claude\game\dominion`／branch＝`main`（最新は `git log` で確認）。回答は日本語で。

## 最初にやること
1. `npm test` を実行し **全40スイート緑（exit 0・整合性 4763・不変条件 12・同盟 310件・同盟UI 99件）** を確認する。
2. `PROGRESS.md` の **§0-29（同盟＝いま作業中。特に「段階2 A2」「A2b」「A3」の3節と末尾の注意節）／
   §1（ゴール）／§5（未完了タスク）／§6（詰まり・注意点）** を読む。
3. **`docs/research/allies_rules.md` 冒頭の「実装前に必読」9項目**を読む（同盟の正本・554KB。
   巨大なので全部読まず、Grep で必要な節だけ読むこと。王国カードの逐語は `g02`〜`g08` の各節）。

## いまどこ
新拡張 **同盟（Allies）**。**作業ツリーはクリーン／未pushなし**（`sw.js` v66・本番反映を実機確認済み）。
- ✅ 公式ルール研究（多エージェント22体）
- ✅ **段階1**＝カタログ72種（王国49＋同盟カード23）＋分割山プレースホルダ6＋webp78枚（**絵は未回収＝枠＋文字だけ**）
- ✅ **A1**＝好意(Favor)トークン `p.favors` と 同盟(Ally)カードの選定 `state.ally`
- ✅ **A2**＝分割山6組（混合山モデル）＋循環(Rotate)
- ✅ **A2b**＝「サプライから獲得/廃棄するカードの種別」を**山の一番上**で判定する横断修正
- ✅ **A3**＝**同盟(Ally)カード23種**（盤面表示＋好意の使用窓＋全効果＋敵対レビュー確定7件の修正）
- ⛔ **CARD_SETS 未参照＝同盟は実プレイには出ない**（闇市場にも出ないよう `DOM.STAGE1_POOLS` で塞いである）

## 次に取り組むタスク（優先順1位）＝**A4＝王国カード49種**
非分割25種＋分割山6組×4種24種。**素直な系 → 持続 → アタック → 複雑** の順に、
バッチごとに `npm test` 緑を確認してコミットする（A2/A3 と同じ進め方）。

**要点（詳細は PROGRESS §0-29 の各注意節と `docs/research/allies_rules.md`）**
- **連携(Liaison)9種が好意をくれるのは A4**（今は開始時の1個／輸入者の5個しか無い）。
  道化棒/ごますり/輸入者/**生徒（魔法使いの分割山の中）**/下役/仲買人/契約書/密使/ギルドマスター。
- **循環(Rotate) を配線するのは A4**（`rotatePile` はまだどの reducer からも呼ばれていない）。
  薬草集め/古地図/天幕/触れ役/生徒/戦闘計画。**循環の位置はカードごとに違う**（生徒だけ「循環→その後に強制廃棄」）。
  **戦闘計画だけが「任意のサプライ山」**（`rotatableSupplyPiles`）。他5枚は自分の山を名指し。循環は**任意**＝「回さない」ボタン必須。
- **新しく「カードを使用する」経路を足したら `noteAllyPlay(state, pi, card)` を呼ぶ**
  （魔女の輪／小売店主連盟／写本士の仲間たちが誘発する。A3 の最大の取りこぼしがこれだった）。
- **要塞(Stronghold)は4種別**（アクション・勝利点・持続・城砦）。**蛮族(Barbarian)の「種別を共有する」に連携も分割山種別も入る**。
- **商人の野営地(Merchant Camp)は「場に出さずに使用」経路では山札の上に置けない**（`takeSelf`/`playedByCommand` を通す＝保存則）。
- **歩哨(Sentinel)が見る5枚は deck から抜いて脇に持つ**／**ごますり(Sycophant)の3枚は同時に捨てる**。
- **専門家(Specialist)で持続を2回使うと専門家自身も場に残る**（§0-25 の「玉座×持続」の既存簡略化と衝突）。
- **航海(Voyage)/島民は「3ターン連続不可」**＝`t.chain`（A3 で新設）と §0-26 の追加ターン優先順位に組み込む。
  **航海の「手札から3枚まで」は Ally の能力（市場の町・都市国家）も止める**。
- **リッチ(Lich)のターンスキップ**は「飛ばしたターンも取っていたのと同じようにタイブレークに数える」＝`tieTurns`/`freeTurns` に直撃。

その後は **A5＝CARD_SET 昇格**（`js/cards.js` の `DOM.STAGE1_POOLS` から `'allies'` を外す）→ 多エージェント敵対レビュー →
CPUソーク・`verify:e2e`/`verify:visual` → **絵（webp）72枚の回収** → push。

## 守るべき進め方・流儀（このプロジェクト固有）
- **簡略化より忠実性**。やむを得ず簡略化したら PROGRESS に「許容簡略化」と理由を必ず書く。
- **新しい pending は4点セット必須**＝engine reducer ＋ `PLAYER_ACTIONS` 登録 ＋ CPU `decidePending` 分岐 ＋
  UI `viewPendingModal` 分岐（＋終端保証）。欠けると **CPU 無限ループ／人間が詰む**。
- **CPU の `decidePending` は絶対に `null` を返さない**（オンラインで `reduce(state,null)` が TypeError → 部屋が固まる）。
- **engine・CPU・UI は必ず同一コミットで直す**。述語を1つ直したら、その述語を見ている3面すべてを grep で確認する。
- **獲得可否・コスト比較は engine の述語**（`gainableBase`/`costUpTo`/`costUnder`/`costExact`/`sameCost`）。
  **カードの種別は `isTypeSupply(state, id, ty)`**（混合山＝一番上）。ただし**「山のコスト・種別」は randomizer**。
  **非サプライ札を候補にしたい効果では `gainableBase` 系を使わない**（候補ゼロ→livelock）。
- **`t.actions += n` / `t.coins += n` を直接書かない**（`addActions`/`addCoins`）。財宝の効果は `applyTreasureEffect`／
  公開は `reveal()`／廃棄は `trashCard()`／獲得は `gain()`／サプライからの廃棄は `trashFromSupplyPile()`／
  山へ戻すのは `returnToPile()`／シャッフルは `reshuffleDeck()`。
- **選択系モーダルの確定は必ず `takeSelection()` を通す**（`UI.selection` を持ち越さない＝A3 の [high] バグ）。
- **多エージェント敵対レビューを必ず行う**（観点ごとに finder → 別エージェントが node で再現して確定。偽陽性は棄却）。
  A2/A2b/A3 で確定23件のうち **13件が「同盟と無関係な既存バグ」**だった＝毎回やる価値がある。
- **push は毎回ユーザー確認を取る**（コミットは随時してよい）。
  **既存バグの修正が入ったら拡張の完成を待たずに push を提案する**（2026-08-12 のユーザー決定）。
- **client 資産（js/css/webp 等）を変えたら `sw.js` の VERSION を上げる**（現在 **v66**）。
- 使い捨てスクリプトは**プロジェクト直下に `_*.tmp.js`**（gitignore 済み）で作り、実行後に必ず削除。
  一時ファイルは scratchpad か `c:\tmp` へ。実行前に `Set-Location 'C:\Users\b1242\claude\game\dominion'`。

## 次セッションが知らないと事故る事項
- **`js/engine.js` は CRLF・`js/ui.js` は LF**。スクリプトで一括置換するときは改行コードを合わせること
  （`\n` の複数行パターンが engine.js だけマッチしない）。
- **混合山の正本は `DOM.engine.MIXED_PILE_KEYS`**（廃墟/騎士/城＋同盟の分割山6組＝9山）。新しい混合山はここに足すだけでよい。
  **「山キー」はプレースホルダ＝実在する1枚ではない**（プレイヤーのゾーン・廃棄置き場・闇市場デッキ・相続の脇・
  命令の対象・宣言モーダルの候補 に入れない）。`id !== 'knights'` のような1つだけのリテラルを書かない＝`isMixedPileKey()`。
- **山の上に載せる効果（山上VP・負債・山トークン・好意トークン）の READ は必ず `pileKeyOf` を通す**（分割山で孤児化する）。
- **`reshuffleDeck(p, state)` は「メイソン団が捨て札に札を残したか」を返す**。新しく「山札が足りなければシャッフル」の
  ループを書くときは戻り値を見て**同じアクセスで2度目のシャッフルをしない**こと。
- **開始時 Ally の窓は `createInitialState` の末尾でも開く**（ゲームのターン1）。`allyStartOfTurn` に窓を足したら
  ターン1で開いてよいかを確認する。
- **Ally が起こす攻撃（魔女の輪／すり師団）は `ATTACKS` に登録しない**（堀で防げてしまう）。
- **`test/edition2.test.js` の「堀は全アタックを無効化」が 220件**あるのは同盟のアタック7種が自動で加わったぶん
  （いまは効果ゼロで通っている＝**A4 でアタックを実装したとき MOAT 配線漏れを自動検出してくれる**）。
- **絵（webp）は同盟の78枚だけ枠＋文字**（47〜67KB）。他の598枚には絵が入っている。回収は A5 の後
  （手順はメモリ `chatgpt-card-art-workflow`。**ChatGPT の生成順は指示順と一致しないので必ず全枚を実見**）。
  分割山のプレースホルダ6枚は最安カードの絵を流用してよい（城 `castles` と同じやり方）。
- **英語wiki は Wayback 経由＝`python tools/wikifetch.py <Page>`**。**出力の `snapshot=` の年を必ず見る**
  （2022 より古いと同盟の発売前のページ）。**2025年12月以降のキャプチャは bot 検知画面**。
- **GitHub Pages のデプロイが失敗し続けたら「ゾンビ化した進行中デプロイ」を疑う**＝
  `gh api -X POST repos/ankake-web/dominion/pages/deployments/<旧SHA>/cancel`（PROGRESS §6 に詳細）。
  失敗した run の `rerun` は必ず失敗する＝**空コミットを積んで push し直す**。
- **保存則ソークを自作するときの落とし穴**：① 王国に**横型のid（イベント等）を混ぜない**（`C()[id].potion` で落ちる）。
  ② tally の比較に `JSON.stringify` を使わない（**キーの挿入順が変わるだけで差分が出る**）＝
  キーをソートするか `test/invariants.test.js` の `diffTally` を真似る。
