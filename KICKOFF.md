<!-- /handoff が自動生成（2026-08-17）。新セッションはこのファイルの指示に従う。手編集不要 -->

**ウルトラコード（最大エフォート）で進めてください。**

スマホ向けドミニオン対戦Webアプリの続き。作業ディレクトリ＝`c:\Users\b1242\claude\game\dominion`（branch `main`）。回答は日本語で、フランクに短く。

## まず最初にやること
1. `npm test` を実行して **全44スイート緑（exit 0・整合性5307・不変条件12）** を確認する。
2. **`PROGRESS.md` の §0-31（旭日の段階0）／§5（未完了タスク）／§6（注意点）を読む。**
3. 設計の正本＝**`docs/research/risingsun_rules.md`（847KB）**。
   **着手前に冒頭の「実装前に必読18項目」と「決定事項 D1〜D5」を必ず読むこと**（記憶で書かない）。

## いまの状態（1行）
16拡張＋プロモ＝761枚が実プレイ可能・全部に絵あり。**次の拡張＝旭日（Rising Sun・実装対象50種）の段階0（公式ルール研究）が完了**したところ。`DOM.CARDS`/`LANDSCAPES` はまだ不変＝旭日は実プレイに何も出ない。

## 次にやる具体的タスク（優先順1位）
**PROGRESS §0-31 の「残っているバグ候補」6件を検証して潰す。**（engine を触る段階1の前に片づける）
最有力＝**冠(Crown)／首謀者(Mastermind)／門下生(Disciple) が `canPlayHandCard` を通らず `inPlay.push` している**
＝将軍(Warlord)に止められず、航海(Voyage)の3枚制限にも数えない。**`random-allies` で到達**（3枚とも同盟の非分割25種）。
他5件（元手×支配／`gainFromOutside` の `lastGainedAny`／ヴィラ等の購入フェイズ終了時効果／回避×`state` ガード／
`TAX_PILE` の空山／旅行のコメント矛盾）は §0-31 の表を見ること。

そのあと **2位＝旭日の段階1（カタログ50種＋webp）→ 3位＝段階2（正本の R1〜R7）**。

## 守るべき進め方
- **多エージェント＋敵対的検証**でやる（研究・レビューとも）。型は §6「研究・調査の型」に書いてある。
- **バグは必ず node で再現してから直す**（エージェントの指摘を鵜呑みにしない。実際に誤指摘が出ている）。
- **回帰テストはバグ注入で感度を確かめる**（修正を外して落ちるか）。今回、感度の無いテストを1件書いてしまった。
- **engine を締めたら CPU と UI も同じコミットで直す**（窓を開く条件・受理・CPU候補・UIフィルタの4面）。
  新しい pending は**4点セット必須**（engine reducer＋`PLAYER_ACTIONS`＋CPU `decidePending`＋UI `viewPendingModal`）。
- **client 資産（js/css/webp/sw）を変えたら `sw.js` の VERSION を上げる**（今 v76）。
- 使い捨てスクリプトは**プロジェクト直下に `_*.tmp.js`** で作り**実行後必ず削除**。
- **push はユーザー確認**（コミットは自由）。
- **日本語wiki（wikiwiki.jp）は並列で叩くと 429 で全滅する**＝エージェントに触らせず自分で逐次に取る。

## 知らないと事故ること
- **未pushが10コミットある**（`git log origin/main..HEAD` で確認）。中身は
  **旭日の段階0の正本**＋**出荷済みバグ7件の修正**（負債の2024エラッタ2件・宝物庫・旗艦×永続持続・
  高原の羊飼い×安価な・**民兵ほかで坑道が金貨を出さない**・増築×操舵手）。**本番にはまだ出ていない。**
- **旧い誤った挙動を固定していたテストを2件、公式へ直してある**（御守り×市街の負債 16→8／木工ギルド 4→0）。
  期待値が変だと思っても戻さないこと。
- **拡張の日本語名は「旭日（きょくじつ）」**。「日の出づる国」は誤りで、全部訂正済み。
- **旭日は最後の拡張ではない**＝第17拡張 **Arcana**（2026年予定）がある。ただし**カード名すら未公開で着手不能**。
- **`permanentDurationCounts(p)` が永続持続の唯一の正本**（王子/雇人/尽きぬ杯/チャンピオン/資料庫/操舵手）。
  新しい永続持続はここだけに足す（二重管理に戻さない）。
- **`c:\tmp` には本業の `LiS_AF_資料一式` があるので一括削除しない**。
  旭日の一次資料は `C:/tmp/risingsun_research/`（ルールブックPDF・wiki 出力・8群の doc）に置いてある。
