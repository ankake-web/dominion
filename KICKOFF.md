<!-- /handoff が自動生成（2026-08-17）。新セッションはこのファイルの指示に従う。手編集不要 -->

**ウルトラコード（最大エフォート）で進めてください。**

スマホ向けドミニオン対戦Webアプリの続き。作業ディレクトリ＝`c:\Users\b1242\claude\game\dominion`（branch `main`）。回答は日本語で、フランクに短く。

## まず最初にやること
1. `npm test` を実行して **全44スイート緑（exit 0・整合性5307・不変条件12）** を確認する。
2. **`PROGRESS.md` の §0-32（今回のバグ修正13件）／§0-31（旭日の段階0）／§5（未完了タスク）／§6（注意点）を読む。**
3. 設計の正本＝**`docs/research/risingsun_rules.md`（847KB）**。
   **着手前に冒頭の「実装前に必読18項目」と「決定事項 D1〜D5」を必ず読むこと**（記憶で書かない）。
4. **未pushが11コミットある**（`git log origin/main..HEAD`）。中身＝旭日の段階0の正本＋**出荷済みバグ修正13件**
   （§0-31 の7件＋§0-32 の6件相当。うち [high]＝首謀者モーダルで人間が詰む）。
   メモリの方針（push-bugfixes-early）どおり、**セッション冒頭にユーザーへ push を提案するとよい**（push は必ず確認を取る）。

## いまの状態（1行）
16拡張＋プロモ＝761枚が実プレイ可能・全部に絵あり。旭日（Rising Sun・50種）の**段階0（研究）完了＋段階1前の宿題
（出荷済みバグ候補6件）も §0-32 で完済**。`DOM.CARDS`/`LANDSCAPES` はまだ不変＝旭日は実プレイに何も出ない。

## 次にやる具体的タスク（優先順1位）＝**旭日の段階1（カタログ50種＋webp）**
- `DOM.CARDS` 560→**585**（王国25）／`DOM.LANDSCAPES` 201→**226**（イベント10＋予言15）。
- **新種別 `omen`（前兆）／`shadow`（影）** のラベルを `js/carddata.js`・`js/ui.js` の TYPE_JP・
  `test/integrity.test.js` の JP/EN マップ（計4〜5箇所）に登録（漏れは恒久検査が捕まえる）。
- **横型の新 kind `prophecy`** のスキンを `tools/build-landscape.js` に新設（**iris blue（菖蒲色）・コスト欄なし**。
  前例＝同盟の `ally`／略奪の `trait`）。
- ⚠ **`DOM.STAGE1_POOLS` に必ず入れる**（今は `[]`。入れないと闇市場に死に札が $0 で並ぶ）。
- ⚠ **id 衝突を既存761枚と機械照合**／**影カードは裏面が5種とも別の絵**＝webp の扱いを決める。
- 日本語名・英日対応50/50・両言語のカード文・区切り線は取得済み＝`C:/tmp/risingsun_research/g0_jp_pairs.md`。
- 進め方は略奪の段階1（PROGRESS §0-30b）と同じ型＝**多エージェントで群を起草 → 別エージェントが研究docと逐語照合**。
そのあと **2位＝段階2（正本の R1〜R7 の順。最大の難所＝神風）→ 敵対レビュー → 絵50枚 → push（ユーザー確認）**。

## 守るべき進め方
- **多エージェント＋敵対的検証**でやる（研究・カタログ・レビューとも）。型は §6「研究・調査の型」。
- **バグは必ず node で再現してから直す**／**回帰テストはバグ注入で感度を確かめる**（§0-32 は全件この方式で通した）。
- **engine を締めたら CPU と UI も同じコミットで直す**（窓・受理・CPU候補・UIフィルタの4面）。
  新しい pending は**4点セット必須**（engine reducer＋`PLAYER_ACTIONS`＋CPU `decidePending`＋UI `viewPendingModal`）。
- **client 資産（js/css/webp/sw）を変えたら `sw.js` の VERSION を上げる**（今 **v77**）。
- 使い捨てスクリプトは**プロジェクト直下に `_*.tmp.js`** で作り**実行後必ず削除**。
- **push はユーザー確認**（コミットは自由）。
- **日本語wiki（wikiwiki.jp）は並列で叩くと 429 で全滅する**＝エージェントに触らせず自分で逐次に取る。

## 知らないと事故ること（§0-32 で増えた鉄則）
- **`modalSingleHand` の skip は `{label,on}` オブジェクト必須**（boolean `true` は押せない死にボタン＝人間が詰む）。
- **新しい「購入フェイズ終了時」効果は `endBuyTail` 系と `midBuyPhaseEnd`（ヴィラ/騎兵隊のフェイズ復帰）の両方に足す**。
  mid 窓の解決 reducer は `pd.mid` を見て片付けへ進まないこと。
- **`reshuffleDeck` は state を渡さなくても `_reduceState` で救済される**。ただし `shuffle()` 直呼びの
  「山札に混ぜる」経路（併合/宿屋/飢饉）は回避/運命の/占星術師団の対象外＝許容簡略化として記録済み。
- **allowEmpty の獲得モーダルは `DOM.engine.isNonSupplyPile` で非サプライを弾く**。
- **`permanentDurationCounts(p)` が永続持続の唯一の正本**（新しい永続持続はここだけに足す）。
- **予言(Prophecy)は「カード」ではない**（保存則 tally・横型2枚制限に入れない）等、旭日固有の罠は
  §0-31「注意（旭日固有）」と研究doc 必読18項目が正本。
- **`c:\tmp` には本業の `LiS_AF_資料一式` があるので一括削除しない**。
  旭日の一次資料は `C:/tmp/risingsun_research/`（ルールブックPDF・wiki 出力・8群の doc・g0_jp_pairs.md）。
